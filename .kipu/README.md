# Working a kipu store

The agent's page for this store lives in CLAUDE.md, "Working the kipu
store". The scaffold's version named verbs this build of kipu does not
ship (`kipu context`, `kipu pour`); CLAUDE.md names only the ones it does.
