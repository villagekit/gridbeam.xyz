---
title: Open findings from the old task tree
---
Carried forward from the deleted task tree so the ledger can check them rather than rediscover them. None is trusted: each is re-derived by the `parity` skill and filed as a difference only if it still holds.

Copy rewrites never presented to the operator (August 2026 list): the home "How does it work?" heading and paragraph; "Have fun assembling your imagination."; the stories blurb; the community paragraph (lost "solarpunk"); the typing-section paragraph "Perfect for creators, inventors, and other innovative folk"; the `StoryEditorialNote` device and its "originally on gridkit.nz" byline; three `####` headings promoted to `###` in how-to-furniture-bolts; the cutting-planner labels ("Enter your desired beam lengths here.", "Infeasible beams", "Unused beams", "Plan it!") and its default rows and stock; the catalogue empty-state text; the subscribe page's "no audience pressure" clause.

Code and behavior findings still open in August 2026: Cloudinary video and media transforms via the ui media components; `ImageCarousel` accessibility and its dead half; a dead-code sweep; metadata and config drift; self-hosting the detect-gpu benchmarks (drop the unpkg.com request, in `../gridkit`); sandbox controls invisible under Chakra v3 (`../gridkit`); `LinkCard`'s overlay anchor has no accessible name (`../ui`); `@villagekit/ui` claims Next is optional but requires it (`../ui`); `utility-workbench` emits 0 gu grid beams (`../products`); the `CutBeamSvg` to `CutGridBeamSvg` swap, done in `../gridkit` at `915085f`, `d4b3e7e`, `e58d700` and blocked on a publish.

The May 2026 screenshot audit's per-page findings are superseded by the ledger; its cross-cutting observation that the site reads greyer than legacy (pink and accent colors lost) belongs to the `shell` ledger.
