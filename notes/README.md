# notes

The `note` collection, declared in [.kipu/collections/note.toml](../.kipu/collections/note.toml):
prose that carries no state. A note holds what a plan's Log would hold
when it belongs to no single plan: a carried-forward list, a research
finding, an observation for a future reader. Cite a note by its prefix.
