---
title: Operator wants accessibility taken further than parity
---

While sanctioning `6c0e17bf75ca` (footer landmark labels) in the copy grilling (`e2805adefd47`), the operator said they cared about accessibility on the legacy site and want at least the same care here, ideally better, not just parity with legacy's own accessibility level.

Nothing to act on yet: the parity ledger only files what differs from legacy, so it won't surface an accessibility gap legacy also had. Worth a dedicated accessibility pass after M2 (an axe/Lighthouse sweep, keyboard-nav check, screen-reader spot check), scoped as its own plan rather than folded into route parity.

## Log
