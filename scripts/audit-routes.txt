# scripts/audit-routes.txt
# Routes captured by `pnpm audit:pages` and `pnpm audit:dom` (the parity ledger).
# One route per line. Lines starting with `#` are comments. Blank lines are skipped.
# Each route is captured against BOTH the legacy base and the current base, unless the line
# carries a side marker after the path: `legacy-only` or `current-only`. A marked route is
# still screenshotted on both sides (the 404 capture is a useful signal), but its missing
# side is not a failure for `pnpm audit:dom`.
#
# Designs catalog: a representative sample of slugs that exist on both sides.

# --- top-level pages ---
/
/about
/faq
/contact
/subscribe
/tools-and-resources

# --- tools ---
/tools/cutting-planner

# --- legal ---
/legal
/legal/privacy-policy
# Legacy-only (current site has /legal as a single page):
/legal/cookie-policy legacy-only
/legal/return-policy legacy-only

# --- stories ---
/stories
/stories/whats-a-grid-unit
/stories/how-to-cut-grid-beams
/stories/how-to-furniture-bolts
/stories/building-with-grid-kit
/stories/2021-winter-newsletter
/stories/2022-newsletter

# --- designs ---
# Index, plus a small representative sample of slugs that exist in both legacy and current:
/designs
/designs/bed-frame
/designs/shelf-tower
/designs/5-12-13-triangle-desk

# --- suppliers (current) vs store (legacy) ---
# These compare structurally, not as direct route mirrors:
/suppliers current-only
/store legacy-only
