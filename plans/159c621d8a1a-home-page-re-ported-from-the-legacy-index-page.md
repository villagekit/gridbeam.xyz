---
title: Home page re-ported from the legacy index page
status: done
parent: fd9a92bd8abd
derived_from: fd9a92bd8abd
blocked_by:
  - 4faefea81c76
  - 2de4b709bb36
  - 6f90e7e24ca6
  - 0bb6cc8ab1ad
  - e332105c3b52
  - c92c235205f5
  - 60cca8519469
  - a7bf623f885c
tags:
  - "worker:fable"
priority: medium
---
The home page is legacy's `pages/index.tsx` again, translated for the app router: a server `app/page.tsx` holding the metadata and the design index, and a client `app/HomePage.tsx` holding legacy's page body and its private helpers at the bottom (`TypingDesignSection`, `LandingSection` with `forwardRef`, `LandingRow` on `useSectionIndex`, `LandingColumn` on the ui `Column`, `landingMediaSx`, `LandingImage` and `LandingVideo` on the ui media components), the breakpoint hooks driving `buttonSize`, `textSize`, directions and gaps, the seven sections' markup line for line, the list inline with `List.Indicator`, the two fixed story cards from their MDX metadata, and the `app/_components/landing` module deleted. Copy is as the copy slice left it; this slice changes no visitor-facing text. The one shape the app router forces, the server and client files, is filed as a difference for the rules. A re-port that decides every translation, so Fable. Thirty-three items on `/`, one moved to `upstream`, the landing video's site half of a shell item. Record `fd9a92bd8abd`; decisions `ee86d68a`, `2032533f`, `ca677697`, `5dfd824923c9`, `28c1a536`.

## Work

- Legacy: `../node-modules/apps/gridkit/pages/index.tsx` at `fce357d` (551 lines), `packages/ui-page/src/components/{Section,Row,Column}.tsx` (the `Section`, `Row`, `Column` and `useSectionIndex` the ui `1.2.0` exports, `dist/index.d.ts`). Current: `app/page.tsx`, `app/_components/landing/{LandingSection,LandingPhoto,LandingVideo,TypingDesignSection,index}.{tsx,ts}`.
- The files: `app/page.tsx` (server) keeps `metadata` (`title: { absolute: 'Grid Beam' }`, the removals slice's) and `const designs = await getDesignIndex()` (`ceb59dadc691`, sanctioned; the label sort, the generated module and the nullable image stay with [[272613135119]], left for the designs record, so the `image !== null` filter stays) and renders `<HomePage designs={designs} />`; `app/HomePage.tsx` (`'use client'`, colocated in the route's directory, `app/`, as `app/stories/StoriesBrowser.tsx` is in its route's) exports `HomePage` and holds everything from legacy's `HomePage` and the functions below it. Legacy's `MainLayout` wrapper is the shell's layout (`a7bf623f885c`), and `getStaticProps` the server page. The `metadata` imports of the two stories (`index.tsx:48-49`; here `@/content/stories/whats-a-grid-unit.mdx` and `building-with-grid-kit.mdx`, each cast `as StoryMetadata` since `app/_lib/mdx.d.ts` types `metadata` as `Record<string, unknown>`, the cast legacy made too) live in `page.tsx` and pass down as a prop: an MDX import from the client module would pull both stories' content into the home's client bundle, which legacy's pages router did not. The split into two files is forced by the app router (`metadata` is exported from a server component only; `useIsMobile` needs a client one): file one difference on `/` (code, `changed`, `open`, for rule 4) as `531b810f2dbd` filed `1eddda919812`, never sanctioned here.
- The hooks and sizes, [[5bb93411f90e]], [[6e96991035bf]], [[58b08f1b7b4c]]: `useIsMobile()` and `useBreakpointValue({ base: false, lg: true }, { fallback: 'lg' })` from `@villagekit/ui` (`index.tsx:59-70`), `buttonSize = isMobile ? 'md' : 'lg'` on every `LinkButton`, `textSize = isWideScreen ? 'xl' : 'lg'` on every paragraph and the list, no `lineHeight` ([[67825713ec5d]]); the outer `<VStack gap={isMobile ? 8 : 12}>` around the sections ([[53403c466838]]).
- The helpers, [[d4c3f4f49287]], [[d48f1e1356c5]], [[fd4d31160efe]], [[9979421930d7]], [[bade326f7b5e]], [[4a8b2ea0ca74]], [[6c892b44b69b]], [[3c9eb2ae7027]], [[7e5772c7a092]], [[a3d8186cc50c]]: `LandingSection = forwardRef` rendering `<Section index colorPalette={isEven ? undefined : 'gray'} ref maxW="1500px">` (`colorScheme` to `colorPalette`, the `1.2.0` prop) around `<LandingRow index={0}>`; `LandingRow` reading `useSectionIndex()`, `direction` `column-reverse` below `lg` else `row` or `row-reverse` by parity, `alignItems="center"`, `gap={[8, null, 12]}`, `css={{ paddingX: isMobile ? 2 : 16, paddingY: isMobile ? 8 : 16, width: '100%' }}`; `LandingColumn` on `Column` with `alignItems="flex-start"`, `gap={[4, null, 8]}`, `css={{ flex: 1 }}`; `landingMediaSx(isMobile)` (`borderRadius: 'xl'`, `boxShadow: 'md'`, `height: isMobile ? 200 : 350`, `objectFit: 'cover'`) spread into `LandingImage` (the ui `Image`, `css`) and `LandingVideo` (the ui `Video`, `css`); the `landing` directory deleted whole (its other files having moved in the testimonial, carousel and design carousel slices). The `useAssertChildIndexes` that `Section` and `Row` run in development wants `vk-row` and `vk-column` children with indexes, which `LandingRow index={0}` and `LandingColumn index={0}` satisfy as legacy's did.
- The hero, [[41d41423af09]], [[aae96ab27be2]], [[29896b70afa7]], [[f0e807981b22]], [[fde554c6891c]]: `<Heading size="2xl">` with no `as` (an `h2`, legacy `index.tsx:87`), two `<Text fontSize={textSize}>` paragraphs, the span `color="primary.400"` (`Span`, `8eca60c60e82`), the one `LinkButton` at `size={buttonSize}`, the carousel in `<Flex justifyContent="flex-end" css={{ flex: 1 }}>` with the slides inline in the JSX as legacy's, the testimonials `<Stack direction={isWideScreen ? 'row' : 'column'} gap={isWideScreen ? '16' : '8'} css={{ width: '100%' }}>`.
- The typing section, [[878e1bbba4a9]], [[ce0ba1ecc47a]]: a private `TypingDesignSection` (`index.tsx:388-459`) with `useRef`, `useInView` from `motion/react` (`83b6c8ceb5d0`), the three-tuple from `useDesignTypingEffect`, `pluralize.isPlural` on the label's last word, the `<Heading>` with no `size` holding the `VisuallyHidden aria-atomic aria-live="polite"` announcement inside it and the visible `<Box as="span" aria-hidden>` beside it, the `Span color="accentA.500"`, `DesignCarousel` in `<Box css={{ flex: 1, overflow: 'hidden' }}>`, the hidden preload `Image`; `LandingSection`'s `ref` carries `useInView`'s ref. Legacy's `textTransform: 'captitalize'` (`index.tsx:424`) is a typo that sets nothing; carried or dropped, the Outcome says which and why.
- The kit section, [[2bf90b2cd251]]: `<Heading>` with no `size` on this and the later sections; `<LandingVideo src posterSrc title sizes={{ base: '356px', md: '622px' }} />` (`index.tsx:199-209`) on the ui `Video`, which `1.2.0` renders with the mounted provider: the landing video's site half of the shell item `b397b0deb1cc` (`upstream`), so a note on it and on `99f2fe62c62f` that its clause (2) is overtaken, `getCloudinaryVideoUrl` in `app/_lib/cloudinary.ts` staying for `StoryVideo`.
- The list, [[2fe1d4706b82]], [[324782e45004]], [[2bf081b27df4]], [[b1eebd887847]]: `List.Root` with `gap="8"` and `css={{ alignSelf: 'center', fontSize: textSize, width: { base: 'full', lg: '2xl' } }}`, each step a `List.Item` holding `<List.Indicator asChild><TfiThought /></List.Indicator>` (the v3 shape `3d21d3543886` names) and its text inline, no `Step` helper, no `color` on the indicator, `BsFillBox2HeartFill` on the supplier step (`react-icons/bs`), the six steps' copy as the copy slice left them and the `Buy` step's sanctioned text (`2ac105aa8073`) with no image (`6daba9f3b020`); the `LinkButton` under the list at `size={buttonSize}` with `css={{ alignSelf: 'center' }}`.
- The stories section, [[a7c1c7e34597]], [[01bf9692dcea]], [[774792ad37d6]], [[92f76a8c5316]]: `<Heading css={{ alignSelf: 'center' }}>`, `<Text fontSize={textSize} css={{ alignSelf: 'center' }}>`, `<Stack justifyContent="center" direction={isWideScreen ? 'row' : 'column'} gap={[4, null, 8]} css={{ width: '100%' }}>` of `<Item metadata={whatsAGridUnit} showDate={false} />` then `<Item metadata={buildingWithGridKit} showDate={false} />` (`index.tsx:296-304`; `Item` from the story card slice), no filter, no empty guard; the button `variant="secondary" size={buttonSize}` centered.
- The two photo sections: `LandingImage` with `type="cloudinary"`, legacy's `src`, `width`, `height`, the alts as the copy slice left them, `sizes={{ base: '100%', md: ['1500px', 2] }}` (the record's Log rule; add the two call sites to the `sizes` item the carousel slice filed and to its bump note, and close [[1501ee97ed24]], every static `sizes` string gone with the last call site; its `unoptimized` half is the item the design carousel slice filed).
- The community section, [[0e32d2acb530]], [[58252b32e362]]: `<LinkButton variant="secondary" href="https://discuss.villagekit.com" size={buttonSize}>` with no `isExternal` (same tab, `index.tsx:354-357`); the two inline anchors (`wider vision`, `with the community!`) keep `target="_blank" rel="noopener noreferrer"` for now, since `1.2.0`'s `Link` takes no `isExternal` (`dist/components/Link.js`) and the sibling's does (ui commit `540e9c3`, the recipes slice `45d6f5634a11`): move [[58252b32e362]] to `upstream` with a note citing that commit and a note on `99f2fe62c62f` naming the two lines to become `<Link variant="paragraph" href isExternal>` at the bump.
- Chakra v3 translations throughout, each a sanctioned or upgrade-forced shape: `spacing` to `gap`, `sx` to `css` or a style prop, `Box as="span"` and `Text as="span"` to `Span`, `colorScheme` to `colorPalette`, `NextSeo` to `metadata`, `framer-motion` to `motion/react`; a translation with more than one fitting form is named in the Outcome with the form chosen.
- Interfaces: consumes `Testimonial`, `ImageCarousel`, `DesignCarousel`, `useDesignTypingEffect`, `Item` and the mounted `MediaProvider` from the sibling slices, the shell's layout (`a7bf623f885c`) and theme; produces nothing another slice consumes.
- Verify first: `wc -l app/page.tsx` after the earlier slices; `grep -n 'useSectionIndex\|useIsMobile\|Column\b\|Row\b' node_modules/@villagekit/ui/dist/index.d.ts` prints the four exports; `grep -rn "from './_components/landing'" app` prints the one importer.
- Docs: none; CLAUDE.md's Structure already names the route and the shared components. The diff is the largest of the record's slices, over the five hundred lines the layout slice `a7bf623f885c` held itself to, because a page re-port replaces the file whole and deletes the drifted module; the record's Log states it.
- Not this slice: the designs lib's sort and nullable image; the story pages' `StoryVideo` and `getCloudinaryVideoUrl`; the two anchors' `isExternal` (the bump).

## Seams under test

None pure; the proof is the DOM pair's copy diff and aria tree, the screenshot pairs and the eye on `pnpm dev`.

## Done when

- `pnpm audit:dom --routes <a file naming />` against a running `pnpm dev`: `diff audit/_root/dom/legacy.txt audit/_root/dom/current.txt` shows only lines a `sanctioned` item on `/` names, a shell item in the footer, or the typing heading, whose typed label differs on every run (the Outcome lists each residual line and its item); `current.aria.yaml` has every heading at `level=2` including the typing heading, three `region "Testimonial"`, two story `region`s, and the hero's `list`
- `pnpm audit:pages --routes <the same file>` at 375, 768 and 1280 looked at against legacy: the 1500px section width, the 200 and 350px media heights, the `2xl` list width at 1280, `md` buttons at 375 and `lg` at 1280, the text one step smaller at 768 than at 1280; a residual difference is an `upstream` item on `shell` or `/` (the heading recipe's sizes among them) or is filed
- `ls app/_components/landing` fails; `grep -c "useIsMobile\|forwardRef" app/HomePage.tsx` prints at least 2
- The thirty-three items are `fixed`, `58252b32e362` is `upstream` with its notes, the two-file difference is filed `open`, the notes on `b397b0deb1cc`, the `sizes` item and `99f2fe62c62f` are written, checked after the moves
- `timeout 900 just check` is green

## Outcome

Shipped: the home page is legacy's `pages/index.tsx` at `fce357d` again, in the two files the app router forces. `app/page.tsx` (25 lines) is the server component: `metadata` (`title: { absolute: 'Grid Beam' }`, the removals slice's), `await getDesignIndex()` with the `image !== null` filter (`272613135119`, the designs lib's), the two stories' `metadata` imported from `@/content/stories/whats-a-grid-unit.mdx` and `building-with-grid-kit.mdx` and cast `as unknown as StoryMetadata` (the direct cast legacy wrote fails under `strict` because `Record<string, unknown>` and `StoryMetadata` do not overlap; the double cast is the form `app/_lib/stories.ts` already uses), and `<HomePage designs whatsAGridUnit buildingWithGridKit />`. `app/HomePage.tsx` (`'use client'`, 548 lines, colocated in the route's directory as `app/stories/StoriesBrowser.tsx` is) is legacy's page body line for line: `useIsMobile` and `useBreakpointValue({ base: false, lg: true }, { fallback: 'lg' })` driving `buttonSize` (`md`/`lg`), `textSize` (`lg`/`xl`), the stack directions and gaps; the outer `<VStack gap={isMobile ? 8 : 12}>`; the hero with `<Heading size="2xl">` in its `VStack`, the two paragraphs, the `primary.400` bold `Span`, the one `LinkButton`, the carousel's four slides inline in the JSX inside `<Flex justifyContent="flex-end" css={{ flex: 1 }}>` (the first slide's `src` stays `gridbeam.xyz/home/record-shelf-hero`, `bea4a6b2871b`), the testimonials at `gap={isWideScreen ? '16' : '8'}`; the private `TypingDesignSection` with `useInView` from `motion/react`, `pluralize.isPlural` on the label's last word (`currentDesignIsPlural` null before the shuffle, so the server HTML reads `Build a ` again, `0903eceefbaf`), the `<Heading>` holding the `VisuallyHidden aria-atomic aria-live="polite"` announcement and the visible `<Box as="span" aria-hidden>` with the `accentA.500` `Span`, the `DesignCarousel` behind the `hasImage` guard and the hidden preload `Image` as the design carousel slice left them; the kit section's `LandingVideo` on the ui `Video` with legacy's `sizes` and comments; the list as `List.Root gap="8" listStyleType="none" css={{ alignSelf: 'center', fontSize: textSize, width: { base: 'full', lg: '2xl' } }}` of six `List.Item`s, each `<List.Indicator asChild><Icon as={...} /></List.Indicator>` and its text inline, `BsFillBox2HeartFill` on the supplier step, the three list links without a `variant` as legacy wrote them; the stories section's `Heading`, `Text` and the two fixed `StoryItem` cards in `<Stack justifyContent="center" direction gap={[4, null, 8]}>`; the two `LandingImage` photos on the ui `Image`; the community `LinkButton` with no `isExternal`; and at the bottom `LandingSection = forwardRef` on `Section colorPalette maxW="1500px"`, `LandingRow` on `useSectionIndex()` with legacy's direction, gap and padding, `LandingColumn` on `Column`, `landingMediaCss(isMobile)` (`borderRadius: 'xl'`, `boxShadow: 'md'`, `height: isMobile ? 200 : 350`, `objectFit: 'cover'`) spread into `LandingImage` and `LandingVideo`. `app/_components/landing/` is deleted whole (`LandingSection.tsx`, `LandingPhoto.tsx`, `LandingVideo.tsx`, `TypingDesignSection.tsx`, `index.ts`). Copy is as the copy slice left it: no visitor-facing string was written or changed. No dependency added.

The Chakra v3 and app-router translations, each named here as the plan asks: `spacing` to `gap`; `sx` to `css`, in the array form `css={[{ ... }, props.css]}` where legacy spread `...props.sx` into its object (`LandingRow`, `LandingColumn`, `LandingImage`), since v3's `css` prop may itself be an array (the form the ui's own `Section.js` uses); `landingMediaSx` to `landingMediaCss`, typed `SystemStyleObject`; `Box as="span"` and `Text as="span"` with `sx` to `Span` at the three sites `8eca60c60e82` names, and the bare `<Box as="span" aria-hidden>` kept as `Box`; `colorScheme` to `colorPalette`; `StackDirection` (not exported by 1.2.0) to `StackProps['direction']`; `List`, `ListItem`, `ListIcon` to `List.Root`, `List.Item`, `List.Indicator asChild` around `Icon as={...}` (`3d21d3543886`), the `Icon` there because a bare react-icons `svg` under `asChild` is exposed as an unnamed `img` in the accessibility tree where legacy's `ListIcon` carried `role="presentation"`; `listStyleType="none"` on the root and no `variant`, because v3's default `marker` variant reverts the marker to the browser's disc where v2's `List` defaulted to none, and the `plain` variant makes each item `inline-flex`, which collapses the spaces around the inline links (noted on `3d21d3543886`); `NextSeo` to the server `metadata`; `getStaticProps` to the async server page; `framer-motion` to `motion/react`; `Link isExternal` to `target="_blank" rel="noopener noreferrer"` at the two inline anchors until the bump (`58252b32e362`, `upstream`); `sizes={{ base: 'full' }}` to `base: '100%'` at five call sites (`152511f71ef4`, `upstream`). Legacy's `textTransform: 'captitalize'` (`index.tsx:424`) is dropped: the value is not CSS and sets nothing, the computed `text-transform` reads `none` on both sides, and carrying it would write an invalid declaration into the stylesheet. Legacy's `TypingDesignSection` read `currentDesign.image` from a never-null design; here the design carousel slice's `hasImage` guard and `nextDesign.image !== null` check stay until `272613135119` closes.

Deviations from the plan's letter: none in what the visitor sees. Two the plan did not foresee, both toward legacy: the three list links had `variant="paragraph"` on the current page (underlined, `accentA.800`) where legacy's `pages/index.tsx:228,240,270` wrote no variant (the ui `Link`'s default `primary`, no underline, a probe of the live site reading `rgb(0, 163, 196)` with no underline against `rgb(19, 65, 82)` underlined here); the re-port writes them as legacy did, and the M1 ledger had no item on it, so none is closed by name. And the `Icon` wrapper inside `List.Indicator asChild`, above. Not this slice's, as the plan says: the designs lib's sort and nullable image (`272613135119`, `regression`, a note on it from the split names the verdicts plan), `StoryVideo` and `getCloudinaryVideoUrl`, and the two anchors' `isExternal`.

Proof, against a running `pnpm dev` with the published `@villagekit/ui@1.2.0` (no override, no `transpilePackages`): `pnpm audit:dom` on `/`, then `diff audit/_root/dom/legacy.txt audit/_root/dom/current.txt`: every residual line is the shell's (the skip link, the wordmark, the nav items `Tools` and `About` for `Store`, `Find a supplier` for the cart, the free-shipping banner and cookie notice, the footer's columns), a copy verdict on `/` (`Grid Kit` to `Grid Beam` in the testimonials and CTAs, `grid beam` in the paragraphs, `catalog` for `catalogue`, the sustainability paragraph, the community paragraph's stray space, the Buy step to `Find a supplier, or make your own.` with its image gone and the cut step's asterisk and footnote gone), or the typing heading's label, which differs on every run; the hero's two paragraphs and the story cards' lines diff empty. `current.aria.yaml` has every heading at `level=2`, the typing heading among them (`heading "Build a Coffee Table" [level=2]` in the last capture), three `region "Testimonial"`, the two story regions `What's a Grid Unit` and `Building with Grid Kit` in that order, the hero's bare `list` of six items, and the steps list identical to legacy's block in shape (each `listitem` its text and link only, no exposed icon). A Playwright probe of both sides at 375, 768 and 1280 (the scratchpad's `probe.mjs`): the `Section` container at `max-width: 1500px`; the row padding 8px/32px at 375 and 64px/64px above, the row gap 32px then 48px, `column-reverse` below `lg` and `row` at 1280; the outer stack gap 32px then 48px; the hero heading 36px then 48px, the section headings 30px then 36px, the paragraphs 18px at 375 and 768 and 20px at 1280; the video and both photos 200px tall at 375 and 350px at 768 and 1280, `border-radius: 16px`, `object-fit: cover`; the list at `font-size` 18px then 20px, 672px wide (`2xl`) at 1280, its icons 1em with `margin-right: 8px`; the story stack `column` at 32px then `row` at 64px; the community button with no `target`; the typing heading's visible span reading `Build a ` before the label on both sides. `pnpm audit:pages` at 375, 768 and 1280 under `audit/_root/`, looked at: the seven sections in legacy's order and rhythm, the media above the copy below `lg` and alternating sides at 1280, the `md` buttons at 375 and `lg` above, the two cards centered. The residuals on the pairs and the probe, each an item: the band tint and the missing dashed rules (the new shell item below), the buttons' 40px/14px and 44px/16px against legacy's 40px/16px and 48px/18px (the new shell item below), the media 16px narrower at 768 and 1280 from Chakra v3's container padding (`5c1af396cc2e`, shell, `regression`), the story card headings at 18px (`77cd1426ac65`, `upstream`), the palette and shadow literals (`72b776cb0d3f`, `f45538f480af`, `upstream`), the 768 capture 928px wide from the footer's overflow (`c9ea22823a23`, shell). One reading that is not a difference: at 768 the steps column is 548px wide against legacy's 608px, because both rows center their children and the column shrinks to its longest step, which the sanctioned `Find a supplier` text made shorter than legacy's `Buy from our store` line. The console on `/` holds the pre-existing `HoverCardContainer` kebab-case error only (`7877c267268c`, shell). The Done-when greps: `ls app/_components/landing` fails, `grep -c "useIsMobile\|forwardRef" app/HomePage.tsx` prints 7, no importer of the landing module remains, `grep -rn "base: '100%'" app` prints the five call sites for the bump's check. The typing section's random design differs between captures by design.

The ledger: thirty-four items on `/` fixed with outcome `plan 159c621d8a1a`: the plan's thirty-three (`5bb93411f90e`, `6e96991035bf`, `58b08f1b7b4c`, `67825713ec5d`, `53403c466838`, `d4c3f4f49287`, `d48f1e1356c5`, `fd4d31160efe`, `9979421930d7`, `bade326f7b5e`, `4a8b2ea0ca74`, `6c892b44b69b`, `3c9eb2ae7027`, `7e5772c7a092`, `a3d8186cc50c`, `41d41423af09`, `aae96ab27be2`, `29896b70afa7`, `f0e807981b22`, `fde554c6891c`, `878e1bbba4a9`, `ce0ba1ecc47a`, `2bf90b2cd251`, `2fe1d4706b82`, `324782e45004`, `2bf081b27df4`, `b1eebd887847`, `a7c1c7e34597`, `01bf9692dcea`, `774792ad37d6`, `92f76a8c5316`, `1501ee97ed24`, `0e32d2acb530`) and `0903eceefbaf` from the Log. `58252b32e362` moved `upstream` with a note citing ui commit `540e9c3`. Notes on `152511f71ef4` (the five call sites in `app/HomePage.tsx`), `99f2fe62c62f` (the swap back to `'full'` at those lines with the grep, the two anchors' `isExternal`, and clause (2) of the mdx and media note overtaken), `b397b0deb1cc` (the landing video's site half done) and `3d21d3543886` (the list's shape). Filed: `091a47cb93e7` on `/` (code, `changed`, `open`), the two-file page, for rule 4 as `1eddda919812` was; `1ea1f9eda079` on `/` (accessibility, `changed`, `open`), the list icons' `aria-hidden` for `role="presentation"`, the same mechanism as `89301ca8a1fc`; `df46087faa2d` on `/` (visual, `changed`, `open`), the v3 indicator's `inline-block` at `middle` for v2's `inline` at `text-bottom`; `9746fd6890c2` on `shell` (visual, `changed`, `regression`), the 1.2.0 `Section` band with no dashed rules; `ebdb3183c0dd` on `shell` (visual, `changed`, `regression`), the v3 button sizes; `12a2882546e6` on `shell` (visual, `changed`, `regression`), Chakra v3's link recipe making every inline link `inline-flex` so it no longer breaks across lines, found by the Parity review in the published recipe and pre-existing on the page. No verdict written. Left on `/` after this slice: `open`, `a704be5b8765`, `f9729ac7552a`, `091a47cb93e7`, `1ea1f9eda079`, `df46087faa2d`; `regression`, `272613135119`; `upstream`, `152511f71ef4`, `58252b32e362`. The record's exit demo does not hold on its own terms (six items `open` or `regression`), which the record's Log foresaw: the `open` items and `272613135119` go on the home's verdicts plan at the finish (decision `40abdb2f222a`).

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity), none critical in the code. Standards found one critical citation slip, legacy's `NextSeo` at `index.tsx:74` cited as `:75` in `091a47cb93e7` (fixed), and three line-range drifts (`Button.js:11-89` in `ebdb3183c0dd`, `HomePage.tsx:464-477` in `9746fd6890c2`, legacy `:545-551` in the `b397b0deb1cc` note), fixed; its judgment calls dropped: the `css` array form, the double cast and the absent TSDoc on `HomePage` (the route-local pattern, as `StoriesBrowser` and legacy), and the pre-existing comment at `app/HomePage.tsx:454-455` citing the difference id `272613135119`, left as the design carousel slice wrote it since code cites the ledger by id here. Spec found every Work bullet and Done-when line met and every upstream claim verified, with the same `:74` slip and two more ranges (`list.js:21-27` in `df46087faa2d`, `Section.js:31-33` and the `yborder-bg` branch `:65-73` in `9746fd6890c2`), fixed, and the typing label quoted above corrected to the last capture's. Parity found the route at parity at 375, 768 and 1280 apart from the recorded items, every claimed item genuinely closed, the copy verbatim, nothing silently dropped, and one unrecorded difference, pre-existing and the ui's: Chakra v3's link recipe renders every inline link `inline-flex`, so `with the community!` sits alone on its own line at 375 where legacy's wraps inside the link; filed `12a2882546e6` on `shell`, `regression`. Dropped: nothing else.


Gate: `timeout 900 just check` green after the dev server was stopped; `kipu verify --warnings-as-errors` green.

## Log

- 2026-09-26: From the design carousel slice (plan [[60cca8519469]]): one more item on the section's heading for this re-port, [[0903eceefbaf]] (the article before the shuffle, legacy's currentDesignIsPlural null falling through to 'a '). The DesignCarousel call site (app/_components/landing/TypingDesignSection.tsx:69-75, with the hasImage type guard the nullable image needs until [[272613135119]] closes) and the hidden next-design Image (:79-90) move into app/page.tsx as they are; useDesignTypingEffect lives at app/_lib/useDesignTypingEffect.ts.
