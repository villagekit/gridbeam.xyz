---
title: "ui Section: the yborder-bg band's 2px dashed rules re-ported from the 0.9.0 section"
status: done
parent: 337e35d86920
derived_from: a78b167170b8
tags:
  - "worker:fable"
priority: medium
---
A tinted `Section` in its default `yborder-bg` mode renders legacy's band again: the palette's `50` fill between a `2px dashed` top and bottom rule in its `200` shade, as `packages/ui-page/src/components/Section.tsx:65-73` at `fce357d` wrote it; on the home page the odd sections read `rgb(247, 250, 252)` between dashed `gray.200` rules, as on the live legacy site. Closes [[9746fd6890c2]] on `shell` for the shell record `a78b167170b8`, finished before the item was found (decision `40abdb2f222a`, a slice beside the record). A fix in `../ui`, so it follows decision `28c1a536`: committed in the sibling by pathspec, never pushed from here; seen on this site through the uncommitted `pnpm.overrides["@villagekit/ui"] = "file:../ui"` (`pnpm install` after each sibling edit) beside the uncommitted `transpilePackages: ['@villagekit/ui']` in `next.config.ts` that the `src/` copy needs (CLAUDE.md's ui row), both reverted by path before the commit; the item moved to `upstream` with a note citing the sibling commit; the bump plan `99f2fe62c62f` is `blocked_by` this slice (the edge written at the mint).

## Work

In `../ui/src/components/layouts/Section.tsx:52-76` the `yborder-bg` branch of `paletteCss` writes `backgroundColor: 'colorPalette.50'` alone, under a comment that keeps the band quiet on purpose and makes the `yborder` and `roundborder` modes `1px solid` so they do not compete with the dashed header and footer chrome; legacy's three branches (`Section.tsx:65-90` at `fce357d`) were all `2px dashed` in `colors[colorScheme][200]`: `yborder-bg` the `50` fill with top and bottom rules, `yborder` the rules alone, `roundborder` the fill, rules on all four sides and `borderRadius: 'xl'`. Write legacy's `yborder-bg` branch (`borderTopWidth: 2`, `borderBottomWidth: 2`, `borderStyle: 'dashed'`, `borderColor: 'colorPalette.200'` beside the fill), the item's fix, and replace the comment with the constraint (the branch is the 0.9.0 band, the legacy author's). The `yborder` and `roundborder` branches are 1.2.0's own drift from the same file: the port rule (`ee86d68a`) makes legacy's branches the default, so the worker re-ports all three unless a consumer on this site or in the package renders one of those modes (`grep -rn "mode=" ../ui/src app`), in which case the visible change on that route is filed as a difference first; the Outcome states which. The fill's token value (`gray.50` reading `#F7FAFC`) is the palette slice's ([[72b776cb0d3f]], `upstream`) and needs nothing here.
Verify first: on `pnpm dev` with the published 1.2.0 at 1280 on `/` the odd `section.vk-section` elements read `border-top-width: 0px`, and on the live legacy site `2px dashed rgb(226, 232, 240)`; `grep -rn "mode=" ../ui/src app` lists every consumer that sets a mode.
Docs: `../ui/CHANGELOG.md`, an entry under Fixed.
Not this slice: the band's fill value (the palette's, `upstream`); the home sections' padding and rhythm (the page re-port's, fixed); the section container's own padding ([[5c1af396cc2e]], the recipes slice minted beside this one).

## Seams under test

None pure; the proof is a Playwright probe of the computed borders on `pnpm dev` under the override, against the live legacy site, and the screenshot pairs.

## Done when

- A Playwright probe at 1280 and 375 on `/` under the override reads, on each tinted section (`section.vk-section` with a `colorPalette`), `border-top: 2px dashed rgb(226, 232, 240)`, the same at the bottom, `0px` left and right, and `background-color: rgb(247, 250, 252)`, the readings of the live legacy site's same sections; an untinted section reads no rule on both sides
- `pnpm audit:pages --routes scripts/audit-routes.txt` at 375, 768 and 1280 under the override (a tinted `Section` renders on `/`, `/about`, `/contact`, `/legal`, `/subscribe`, `/tools-and-resources`, the design pages and the story pages' `StorySection`), looked at: the bands' rules match legacy's and every remaining difference is ledgered
- In `../ui`: `pnpm lint`, `pnpm types`, `pnpm build:pkg` and `pnpm build:storybook` green; the change committed by pathspec on its `main`, not pushed
- [[9746fd6890c2]] is `upstream` with a note citing the sibling commit; `99f2fe62c62f` is `blocked_by` this slice (checked at the finish) and carries a note that the bump makes no site edit for it
- The override and the `transpilePackages` entry reverted by path (`git restore -- package.json pnpm-lock.yaml next.config.ts`, `pnpm install --frozen-lockfile`); `timeout 900 just check` is green

## Outcome

Shipped in `../ui` as commit 16bb376 on its `main` over `6603102` (twelve ahead of origin, not pushed; the push goes with the operator's publish, decision `28c1a536`), by pathspec: `src/components/layouts/Section.tsx` and `CHANGELOG.md` (one Fixed entry under Unreleased, beside the recipes slice's four). The package version stays 1.2.0. This repo's commit carries kipu writes only: no site code changes.

What shipped: the three `mode` branches of `paletteCss` are the legacy `ui-page` Section's again (`packages/ui-page/src/components/Section.tsx:65-90` at `fce357d`, translated to Chakra v3's `colorPalette.50` and `colorPalette.200` tokens as 1.2.0's file already did): `yborder-bg` the `50` fill between `borderTopWidth: 2`, `borderBottomWidth: 2`, `borderStyle: 'dashed'` and `borderColor: 'colorPalette.200'`; `yborder` the rules alone; `roundborder` the fill, the rules on all four sides and `borderRadius: 'xl'`. The diff is the exact inverse of the ui repo's own quieting commit `6f0454e`, which the fold `c836fda` had carried in from the legacy file. All three branches re-ported: `grep -rn "mode" ../ui/src app` finds no consumer that sets a mode on the site or in the package source, so `yborder` and `roundborder` change nothing any route renders; the one consumer of those two modes is the package's own Storybook story `../ui/stories/layouts.stories.tsx:44-59` (`SectionPaletteVariants`, outside `src/`, which the plan's grep missed), whose `yborder` and `roundborder` panels now draw dashed 2px rules; a story is not a route, so no difference is owed, and the storybook build is green. The comment above the branches states the constraint (the three modes are the legacy author's, a new visual takes a new mode); the site's `LandingSection`, `StorySection` and the routes' tinted sections take the band with no edit.

Verified. Before, on `pnpm dev` with the published 1.2.0 against the live legacy site (the scratchpad's `section-probe.mjs`, `section-before.json` and `section-legacy.json`): on `/` at 1280 and 375 the tinted sections (data-index 1, 3, 5) read `background-color: rgb(250, 250, 250)` with `0px solid` borders against legacy's `rgb(247, 250, 252)` between `2px dashed rgb(226, 232, 240)` top and bottom rules. After, under the `file:../ui` override (`section-after.json`): every tinted section on `/` at 1280 and 375 reads `rgb(247, 250, 252)`, `2px dashed rgb(226, 232, 240)` top and bottom, `0px` left and right, `0px` radius, and every untinted section `0px` on both sides, legacy's readings field for field; the section heights match legacy's except the design carousel section (723 against 1148 at 1280, the home's own items). `/stories/whats-a-grid-unit` at 1280 (`section-routes-after.json` against `section-routes-legacy.json`): seven sections, the even ones tinted, every reading legacy's. The screenshot pairs over every route in `scripts/audit-routes.txt` at 375, 768 and 1280 under `audit/`, captured under the override and looked at: on `/` six 2px dashed rule rows on each side at every width (three bands, top and bottom), the same dash pattern and fill on both sides in the crops; the story pages the same rule count on both sides; the cutting planner's controls band at parity with legacy's; `/about`, `/tools-and-resources`, `/legal` and `/subscribe` are 500 under the override (the `LinkCard` icon boundary in the bump plan's note), each with a page-shape regression item covering its tinted section; the doubled footer credit is the brand footer's bump note. On `/contact`, the design pages and `/suppliers` the rules now sit on tinted sections legacy does not render at all (legacy's contact page is a `CardsLayout`, `packages/applet-contact/src/pages/contact.tsx:28`; `section-routes-legacy.json` finds no `vk-section` on `/contact` or `/designs/shelf-tower`), each already ledgered as a regression of the route's own shape: [[fdfb21259558]], [[7e28a0685fba]] and [[cf729c827453]]; every remaining difference on the pairs is ledgered (the `upstream` items, `c9ea22823a23` at 768, the routes' own items, the stories index width [[1d5c23180ebf]]). In `../ui`: `pnpm lint`, `pnpm types`, `pnpm build:pkg` and `pnpm build:storybook` green (lint, types and the package build again after the review's comment edit). The override and the `transpilePackages` entry reverted by `git restore -- package.json pnpm-lock.yaml next.config.ts` and `pnpm install --frozen-lockfile`; `timeout 900 just check` green against the published 1.2.0.

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity) before the commit, none critical or major. Standards, no hard violation; two judgment calls taken: the comment's last sentence no longer brings the consumer's parity process into the package or half-describes the reversal, and the sibling commit message cites the legacy file as its SHA-pinned GitHub URL; its third, that `roundborder` writes four side widths where one `borderWidth` would do, is not taken, the legacy author's shape winning. Spec, two findings taken into this Outcome: the Storybook story above, which the plan's `src`-scoped grep missed; and the plan's Done when naming `/contact` and the design pages among routes whose bands match legacy's, where legacy has no band on either, the rules there sitting on ledgered added sections as stated above. Parity: the closure holds on the probes and the pairs, no copy in the diff, nothing legacy did silently dropped, no unrecorded difference; it named `/tools/cutting-planner`'s controls band, left out of the plan's route list, at parity, and `/suppliers`' band as [[cf729c827453]]. Findings dropped: none.

Items: [[9746fd6890c2]] moved to `upstream` with the ui commit in its note; `99f2fe62c62f` `blocked_by` this slice (the edge from the mint, checked) and carrying the note that the bump makes no site edit for it, what it checks, and the three added-section bands expected on its pairs. No visitor-facing copy in the change. For the HoverCardContainer slice `8235bd4bea81`, which commits on top of 16bb376: the pairs now show every tinted band with its rules, so a `LinkCard` inside a tinted section sits between dashed rules on both sides.

## Log
