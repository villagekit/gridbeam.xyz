---
title: "ui Title: the heading's container at breakpoint-md, legacy's container.md width"
status: done
parent: 337e35d86920
derived_from: a78b167170b8
tags:
  - "worker:fable"
priority: medium
---
The ui `Title` wraps its heading at legacy's width again: its `Container` is `maxW="breakpoint-md"` (768px, Chakra v3's size token for the `md` breakpoint), where `@villagekit/ui@1.2.0` and the sibling write `2xl` (672px) and legacy's `ui-page` `Title` wrote `container.md`. Closes [[318456ddabc6]] on `shell` for the shell record `a78b167170b8`, filed at the about split after the record was finished, its Log naming this slice for the about's finish (decision `40abdb2f222a`, a slice beside the record). A fix in `../ui`, so it follows decision `28c1a536`: committed in the sibling by pathspec, never pushed from here; seen on this site through the uncommitted `pnpm.overrides["@villagekit/ui"] = "file:../ui"` (`pnpm install` after each sibling edit) beside the uncommitted `transpilePackages: ['@villagekit/ui']` in `next.config.ts` that the `src/` copy needs (CLAUDE.md's ui row), both reverted by path before the commit; the item moved to `upstream` with a note citing the sibling commit; the bump plan `99f2fe62c62f` is `blocked_by` this slice (the edge written at the mint).

## Work

In `../ui/src/components/layouts/Title.tsx:19` `Title` writes `<Container maxW="2xl">` around its `AnchorHeading` (the published `@villagekit/ui@1.2.0 dist/components/layouts/Title.js:10` the same), where legacy's `packages/ui-page/src/components/Title.tsx:20` at `fce357d` wrote `<Container maxW="container.md">`, Chakra v2's 768px token. Chakra v3 has no `container.*` size: `expandBreakpoints` registers `sizes.breakpoint-md` from the `md` breakpoint (`node_modules/@chakra-ui/react/dist/esm/styled-system/token-dictionary.js:21-29`), 768px, and `sizes.2xl` is 42rem, 672px (`node_modules/@chakra-ui/react/dist/esm/theme/tokens/sizes.js:13`). Write `maxW="breakpoint-md"`, the name the LinkCard slice's ui commit `a4ef8ed` wrote on `CardsLayout`'s container for the same rename ([[8a3babf21c3c]]) and the about re-port wrote on the page's own container, with the constraint in a comment; nothing else in `Title` changes (the `VStack`'s `my="8" gap="12"`, the `paddingTop` breakpoint value, `Description`'s own `3xl` container). Every route that renders `Title` takes it: on this site `/about`, `/faq`, `/contact`, `/legal`, `/legal/privacy-policy`, `/tools-and-resources`, `/tools/cutting-planner`, `/stories`, `/stories/[slug]`, `/subscribe` and `/suppliers` (`grep -rln '<Title' app`, eleven files), several of them drifted routes their records re-port later; on legacy `/about`, `/faq`, `/stories`, `/tools/cutting-planner`, the story pages (`apps/gridkit/components/layouts/stories.tsx:56` at `fce357d`), `/subscribe` (`packages/applet-subscribe/src/page.tsx:32`) and the three `CardsLayout` routes. A heading narrower than 672px does not move; a wider one (a long story title, if any) wraps at 768px again, as on legacy. The reading is the container's computed `max-width`, on every route.
Verify first: on `pnpm dev` with the published 1.2.0, a probe at 1280 on `/about` reads the h1's parent `max-width` as `672px`; the live legacy site's reads `768px` (Chakra v2 emits `var(--chakra-sizes-container-md)`).
Docs: `../ui/CHANGELOG.md`, an entry under Fixed.
Not this slice: the heading recipe's sizes ([[0369f14e4df4]], [[77cd1426ac65]], `upstream`); `Description`'s container; the container recipe's padding ([[5c1af396cc2e]], `upstream` by [[2bd0169a6dda]]); the routes' own `Title` props (the added descriptions, the `as="h2"` section titles), each route record's.

## Seams under test

None pure; the proof is a Playwright probe of the computed style on `pnpm dev` under the override, against the live legacy site, and the screenshot pairs.

## Done when

- A Playwright probe at 1280 on `pnpm dev` under the override reads the h1's `Container` `max-width` as `768px` on `/about`, `/faq`, `/tools/cutting-planner`, `/stories` and one story page (the one with the longest title), the live legacy site's reading on the same routes; at 375 and 768 the same element reads `768px` on both sides too (the reading is the declared maximum, which does not change with the viewport), saved beside the probe
- `pnpm audit:pages --routes <a file naming the eleven routes that render Title>` at 375, 768 and 1280 under the override, looked at: no heading narrower than 672px moves, a wider one wraps as legacy's does, and every remaining difference on the pairs is ledgered (the `upstream` items' residuals, the shell's `c9ea22823a23` at 768, the routes' own open and regression items)
- In `../ui`: `pnpm lint`, `pnpm types`, `pnpm build:pkg` and `pnpm build:storybook` green; the change committed by pathspec on its `main`, not pushed
- [[318456ddabc6]] is `upstream` with a note citing the sibling commit; `99f2fe62c62f` is `blocked_by` this slice (checked at the finish) and carries a note that the bump makes no site edit for it and that the bump repeats the probe on the published package
- The override and the `transpilePackages` entry reverted by path (`git restore -- package.json pnpm-lock.yaml next.config.ts`, `pnpm install --frozen-lockfile`); `timeout 900 just check` is green

## Outcome

Shipped in `../ui` as commit 5c5cd1e on its `main` over `2e2d68c` (fourteen ahead of origin, not pushed; the push goes with the operator's publish, decision `28c1a536`), by pathspec: `src/components/layouts/Title.tsx` and `CHANGELOG.md` (one Fixed entry under Unreleased, beside the six the earlier ui slices wrote). The package version stays 1.2.0. This repo's commit carries kipu writes and the run sheet only: no site code changes.

What shipped: `Title`'s heading `Container` is `maxW="breakpoint-md"`, the size token Chakra v3 generates from the `md` breakpoint (`@chakra-ui/react@3.35.0 styled-system/token-dictionary.js:26`, `theme/breakpoints.js:4`, 768px), the width legacy's `ui-page` `Title` wrote as Chakra v2's `container.md` (`packages/ui-page/src/components/Title.tsx:20` at `fce357d`) and the name `CardsLayout`'s container took at ui commit `a4ef8ed`; 1.2.0 and the sibling wrote `2xl` (`theme/tokens/sizes.js:13`, 42rem, 672px). Nothing else in `Title` changes: the `VStack`'s `my="8" gap="12"`, the heading's breakpoint `paddingTop` and `Description`'s own `3xl` container. The constraint went into a one-paragraph TSDoc on `Title` (the review's call, the shape `CardsLayout` has) with a line on `TitleProps.description`, in place of the inline comment the plan asked for.

Verified. Before, on `pnpm dev` with the published 1.2.0 against the live legacy site (the scratchpad's `title-probe.mjs`, `title-before.json` and `title-legacy.json`): on `/about`, `/faq`, `/tools/cutting-planner`, `/stories` and `/stories/how-to-furniture-bolts` (the longest story title) at 1280, 768 and 375 the `h1`'s parent read `max-width: 672px` against legacy's `768px`, with the parent padded 32px at 1280 and 24px at 768 (the container recipe, `5c1af396cc2e`) against legacy's 16px. After, under the `file:../ui` override (`title-after.json`): every route at every width reads `768px`, legacy's reading; the parent is padded 16px at every width, the recipes slice's fix (`2bd0169a6dda`, `upstream`) rendering with this one, so the bump's repeat probe needs both in the publish; on `/about` at 1280 the container is 768 wide and the `h1` 736, legacy's numbers exactly; every `h1` height equals legacy's on every route and width (80, 75, 118 at 1280; 128 for the story title at 375), so no heading moved and the long title wraps after `(Joint` on both sides (`crops/stories__how-to-furniture-bolts-{1280,768}-{legacy,current}.png`). Where the rendered parent is 736 against legacy's 768 (`/faq`, `/stories`, `/tools/cutting-planner` at 768; the story page at 1280) the cause is each route's own outer `Section` or `Container` padding, ledgered on the route: `e253200bf486`, `1d5c23180ebf`, `69bc376ee08f` and `6ec723376c9e`, `69e3e2447813`; the declared maximum, the reading the plan names, is 768px there too. The screenshot pairs for the eleven routes that render `Title` at 375, 768 and 1280 under `audit/` (66 of 66 captures), captured under the override and looked at: no heading narrower than 672px moves; `/legal`, `/tools-and-resources` and `/subscribe` are 500 under the override (the `LinkCard` icon boundary in the bump plan's note), `/suppliers` is current-only; every remaining difference on the pairs is ledgered (the `upstream` items, `c9ea22823a23` and the wrapping wordmark `1d2562d6b3bc` at 768, the routes' own items). In `../ui`: `pnpm lint`, `pnpm types`, `pnpm build:pkg` and `pnpm build:storybook` green, twice (again after the review's TSDoc edit, which changes no emitted CSS). The override and the `transpilePackages` entry reverted by `git restore -- package.json pnpm-lock.yaml next.config.ts` and `pnpm install --frozen-lockfile`, the dev server stopped by pid first; `timeout 900 just check` green against the published 1.2.0 (78 tests, exit 0).

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity) before the commit, none critical or major. Standards, no hard violation; one judgment call taken: the constraint moved from an inline JSX comment into a TSDoc on `Title` with a line on the `description` prop, the gap `CardsLayout`'s commit closed the same way. Spec, three minor notes taken into this Outcome: the comment's "wraps at 768px" overstated it (the heading is bounded at 768px and wraps at 736 inside the padding; the TSDoc says bounded); the 16px padding in the after readings is the recipes slice's fix rendering with this one; the 736 parents are the routes' own wrappers. Every Done when line held on the evidence files and every upstream citation was verified in `token-dictionary.js`, `breakpoints.js`, `sizes.js` and the legacy file. Parity: the closure holds in the code and on screen on every probed route and width, no copy in the diff, nothing legacy did silently dropped; one unrecorded difference beside the change, in the same component: `Description`'s container is `3xl` (768px) where legacy's `ui-page` `Description.tsx:23` wrote `container.lg` (1024px), visible at 1280 on `/stories/how-to-furniture-bolts` where the description runs to two lines (the review's probe, `desc.mjs`: 704 by 54px against legacy's 992 by 27px). The plan scopes it out ("Not this slice: `Description`'s container") but no shell item named it, so it is filed as `c325dd0181d9` (`shell`, code, `regression`) and handed to a ui slice minted beside the shell record in this slice's shape, `c06d8381c4b5` (`--parent` M2, `derived_from` `a78b167170b8`, `worker:fable`, `priority: medium`, blocking the bump plan from the mint), one line in `../ui/src/components/layouts/Description.tsx` to `breakpoint-lg`; the shell record and the bump plan carry a note naming it, and `plans/README.md`'s Milestones paragraph names it. Findings dropped: none.

Items: [[318456ddabc6]] moved to `upstream` with the ui commit in its note; `99f2fe62c62f` `blocked_by` this slice (the edge from the mint, checked) and carrying the note that the bump makes no site edit for it, what it checks, and that a fourth blocker joined it. No visitor-facing copy in the change. Beyond the ask: the new item, the new slice and the README line above.

## Log
