---
title: "Route: home"
status: done
parent: 337e35d86920
blocked_by: a78b167170b8
---

## Goal

The route `/` is at parity with the legacy site: every difference on it is `fixed` or `sanctioned`, and the operator reviews it at the parity gate `f7a700a3e482`. Decision `ee86d68a`.

## Scope

The differences on `/` in the ledger, listed here by id when this record is sliced (`kipu list --collection difference --filter route=/ --json`). The port rule of decision `ee86d68a` decides whether each slice re-ports from the legacy source or fixes in place.

## Seams under test

Named when sliced; none pure unless the route carries logic.

## Exit demo

`kipu list --collection difference --filter route=/ --json` shows no `open` or `regression` item, and `/finish-epic` finishes this record; the operator reviews the route at the parity gate `f7a700a3e482`.

## Out of scope

Other routes; the shell, except where a difference on this route is closed by a shell change, which then belongs to the shell record.

## Outcome

Finished unattended by `/finish-epic` on 2026-09-26 (decisions `b9af27e0df50` and `40abdb2f222a`), the eight children of the split `done`. The home's ledger at the finish: 125 items on `/`, 106 `fixed`, 11 `sanctioned`, none `dismissed`, 2 `upstream` (`152511f71ef4` the carousel `sizes` value and `58252b32e362` the two inline anchors' `rel`, both fixed in `../ui` and waiting on the operator's publish and the bump plan `99f2fe62c62f`, decision `28c1a536`), 5 `open` and 1 `regression`, the last six handed on below.

The exit demo, clause by clause. First, `kipu list --collection difference --filter route=/ --status open` prints five items, `091a47cb93e7`, `1ea1f9eda079`, `a704be5b8765`, `df46087faa2d` and `f9729ac7552a`, and `--status regression` prints one, `272613135119`. By `40abdb2f222a` the clause is met when each is `upstream`, named by the one attended verdicts plan beside the record, or named by a slice beside the record for a fix an agent can make: all six are on the verdicts plan `8bb4a4380264`, each carrying a note naming it, and none was judged here (no state changed, no verdict written); no home item needed a slice, the one `regression` being the designs library's to fix with its twin `abb539b3555e` on `/designs`, as the split's Log foresaw. Second, `/finish-epic` finishes this record: this finish, `kipu ready --json` having listed it as the one completable plan. Third, the operator reviews the route at the parity gate `f7a700a3e482`: the gate is `blocked_by` `8bb4a4380264` (written here), by the shell's verdicts plan, by the bump plan and by M2, so the operator's verdicts on the home and the publish come before the review.

The items handed on. To the operator, on `8bb4a4380264` (attended, `--parent` M2, `derived_from` this record, a Wants line for the verdicts, one bullet per item with its state and the question with the alternatives the item names): `091a47cb93e7` the two-file page under rule 4; `1ea1f9eda079` Chakra v3's `aria-hidden` for `role="presentation"` on the step icons; `df46087faa2d` the v3 indicator's alignment against v2's `text-bottom`; `a704be5b8765` the `unoptimized` design images under the global Cloudinary loader; `f9729ac7552a` no `fetchpriority="high"` under Next 15; `272613135119` the home's use of the designs library (the generated module, the label sort, the nullable image), judged beside `abb539b3555e`. Six leftovers the home's slices filed on `shell`, finished before they were found, are the ui's to fix and went to three slices minted beside the shell record, not under it (`--parent` M2, `derived_from` `a78b167170b8`, `worker:fable`, `priority: medium`, each following `28c1a536` and blocking the bump plan from the mint), grouped by recipe and component as the shell's own ui slices were: `2bd0169a6dda` the link, badge, container and button-size recipes (`12a2882546e6` inline links `inline-flex`, `2363cb52f9d8` the badge box, `5c1af396cc2e` the container padding, `ebdb3183c0dd` the button sizes); `8235bd4bea81` the `HoverCardContainer` nested selectors (`7877c267268c`, the console error behind Next's dev `1 Issue` badge); `2de775cb197b` the `Section` band's dashed rules (`9746fd6890c2`). Each shell item carries a note naming its slice, and the shell record a note naming the three. Two `open` shell items filed after the shell's verdicts plan was written, `c14d1a8d177f` (the header action's focus on press) and `53f8a05a03fa` (the hover styles behind `@media (hover: hover)`), are now named on that plan `77cf83a1285a` by a note, as the MediaProvider slice named `afe50ab5c7aa` and `0efe45924dbe`. Every `open` and `regression` item on `shell` is now named by a verdicts plan or by a slice.

The children, one line each with the site commit:
- `4faefea81c76` the added sections, the second hero button and the per-page metadata removed: `627eb3b`.
- `2de4b709bb36` the copy verdicts applied on the page and the typing section: `23981f0`.
- `6f90e7e24ca6` `MediaProvider` mounted in `SiteProvider`: `47b8f9b`.
- `c92c235205f5` `ImageCarousel` re-ported onto `react-responsive-carousel`: `84f1bfa`.
- `0bb6cc8ab1ad` `Testimonial` fixed in place: `4c6e725`.
- `60cca8519469` `DesignCarousel` and `useDesignTypingEffect` re-ported, the next design preloaded: `9d86640`.
- `e332105c3b52` the story card re-ported from legacy's stories `Item`: `c5bb50c`.
- `159c621d8a1a` the page re-ported from `pages/index.tsx`: `5522dde` and `cb83ac3`, two commits for one slice, the first having run on a partial index after a chained `git add` aborted on a pathspec (its Log); the tree at the second is the one its gate ran on.

Deviations from Scope, each recorded in its child's Outcome. Two crossed the record's Out of scope on the split's own calls (its Log, calls 4 and 6): the MediaProvider slice changed the shell (`SiteProvider`), and the story card slice fixed three `/stories` items. The MediaProvider slice chose the provider's place (inside `ChakraProvider`, around `QueryParamProvider`) and filed the mount on `shell` as `afe50ab5c7aa`, superseding `0efe45924dbe`, which moved `sanctioned` to `open`, both on the shell's verdicts plan; the testimonial slice kept `'use client'` as upgrade-forced (a note on `1bbb3def5d97`); the story card slice wrote the external overlay in the `asChild` shape, since Chakra v3's `LinkOverlay` drops `rel` (noted on `ed16aad27638`), carries `'use client'` for the `Icon`, fixed the three `/stories` items the same change closes and left `2c38c9e6a5da` there with a note, and did not meet its console clause, the remaining line being the ui's `HoverCardContainer` (now `8235bd4bea81`); the image carousel slice added `react-responsive-carousel` at legacy's range, wrote `base: '100%'` for legacy's `'full'` against the published size hooks (`152511f71ef4`, `upstream`) and filed `f9729ac7552a`; the design carousel slice added `lodash-es` with its types, built the `getDesignImage` pair and the `hasImage` guard the site's nullable image needs until `272613135119` closes, and filed `a704be5b8765`; the page re-port is two files (`091a47cb93e7`), wrote the three step links without the `variant` the previous page had added (legacy's shape, with no item to close), wrapped each `List.Indicator asChild` around an `Icon` so no unnamed `img` reaches the tree, dropped legacy's misspelled `textTransform: 'captitalize'` (a declaration that set nothing on either side), and filed `1ea1f9eda079`, `df46087faa2d` and the shell's `9746fd6890c2`, `ebdb3183c0dd` and `12a2882546e6`. The removals and copy slices shipped as scoped. Every `regression` on `/` at the split was closed by exactly one slice, as the split's Log set out, except `272613135119`, which the Log sent to the verdicts plan; of the two filed during the run, `0903eceefbaf` was closed by the page re-port and `152511f71ef4` moved to `upstream` rather than closed.

Flags the children raised, together, for later iterations: the bump plan `99f2fe62c62f` carries the home's site edits for the publish (the five `sizes` call sites in `app/HomePage.tsx` back to `'full'`, the two inline anchors as `Link isExternal`, `imageComponent={NextImage}` on the provider, `unoptimized` staying on the local design images either way); `StoryVideo` and `getCloudinaryVideoUrl` are the stories records' half of `b397b0deb1cc`; `82762f6b27de` (the ISO date parsed as UTC midnight, a hydration text mismatch west of UTC) sits on the story page template for the story pages record `56e6eb197e6c`; `2c38c9e6a5da` (the external icon's exposure) is the stories index record's `ca353de8b645`, with the grid, filters and container the story card re-port left; the `hasImage` guard and the `getDesignImage` pair go when `272613135119` closes; React 19 keeps one image preload link per design shown and `next dev` refetches each design PNG at its swap (`f9729ac7552a`'s notes, not differences); the 768 captures are 928px wide from the footer's overflow (`c9ea22823a23`, the operator's); the dev overlay's `1 Issue` badge on `/`, `/stories` and `/designs` is `7877c267268c` until `8235bd4bea81` ships, which also restores the catalog cards' nested hover recolor on `/designs`, lost today and named in a note on the item.

Beyond the ask: `plans/README.md`'s Milestones paragraph names this finish, the verdicts plan and the three slices, and its shell sentence now counts three `../ui` slices, naming `8a869061bce7` (the press scale), which the shell's finish predated; three items the operator reads at the verdicts (`a704be5b8765`, `f9729ac7552a`, `272613135119`) carry a note with their Current lines after the page re-port, whose file moves made the cited ones stale; the three new slices were minted beside the shell record, not under it (`--parent` M2, as the decision, the skill and the shell's own ui slices have it), where the orchestrator's brief said to parent them on the finished record.

Reviewed once on a fresh Opus sub-agent before the commit, nothing critical, three major findings, all taken: the HoverCardContainer slice and its item claimed no consumer nests a `HoverCard` in a container, where the catalog's `ItemCard` does, so `/designs` loses the nested recolor today (the slice's Work and Done when corrected, a note on `7877c267268c`); the recipes slice and its item called Chakra v2's badge `inline`, where the v2 `Badge` component's own `__css` wrote `inline-block` (corrected, a note on `2363cb52f9d8`, the `display` read on the suppliers page's badges since a flex parent blockifies the story card's); and the two slices' screenshot lists were narrower than the components' reach (both now run every route in `scripts/audit-routes.txt`, the package's `Container` list corrected to `Title`, `Description` and `TableOfContents` and without `ContentLayout`, the footer's bare `Container` named). Minor, taken: the doubled footer credit flag dropped (a shell ui slice's finding, not a home child's); the Out of scope crossings named as such; the closed-by-one-slice claim reworded for the two items filed mid-run; the run sheet edit and the parent choice stated here; the verdicts bullet for `272613135119` made to agree with the plan's Work paragraph; the three stale Current cites noted. Dropped: a split of the container recipe out of the recipes slice (the shell's recipes slice carried more, and one `../ui` commit per recipe family is the grouping the model set); a code item on `shell` for the `yborder` and `roundborder` branches the Section slice re-ports beside the band, since no route renders those modes (`grep -rn "mode=" ../ui/src app` prints nothing), so there is no difference on any route to record, the slice's Outcome stating what it did.

## Log

- 2026-09-26: Split into eight slices (plan fd9a92bd8abd), reviewed once on two Opus sub-agents. Site-side against `@villagekit/ui@1.2.0`, in the order the edges give: `4faefea81c76` the added sections, the second hero button and the per-page metadata removed (Sonnet); `2de4b709bb36` the copy verdicts applied on the current page (Sonnet); `6f90e7e24ca6` `MediaProvider` mounted in the shell for the home's ui `Image` and `Video` (Fable); `0bb6cc8ab1ad` `Testimonial` fixed in place (Opus); `e332105c3b52` the story card re-ported from legacy's stories `Item` (Fable); `c92c235205f5` `ImageCarousel` re-ported onto `react-responsive-carousel` (Fable); `60cca8519469` `DesignCarousel` and `useDesignTypingEffect` re-ported (Fable); `159c621d8a1a` the page re-ported from `pages/index.tsx` (Fable), last, consuming the rest. Every `regression` on `/` is closed by exactly one slice except `272613135119`, whose fix is the designs lib's (`app/_lib/designs.ts`: the generated module, the label sort and the nullable image, the same mechanism as `abb539b3555e` on `/designs`) and not the home's to make: at the finish it goes on the home's verdicts plan (decision `40abdb2f222a`), the item carrying a note. No home item cites `app/_lib/url-state.ts`. Calls made at the split, for the operator to read: (1) the page is re-ported, not fixed in place, since 107 items span every axis and the port rule says in doubt re-port; its slice is the largest, over the five hundred lines the layout slice held itself to, because a re-port replaces the file whole and deletes the drifted module, and a split by section would leave every intermediate state a hybrid of two frames; (2) the copy lands first, on the current page, on Sonnet, so the re-port carries settled text and its review is structure only; (3) the published `1.2.0` size hooks throw on legacy's `sizes={{ base: 'full' }}` (`dist/components/media/hooks.js`, `Unexpected size value`), the name restored only in the unpublished sibling (`252edab16c7a`, `upstream`), and no slice waits on a publish (`28c1a536`), so the three slices that write a `sizes` object write `'100%'`, the value legacy's own `useSizeWidths` resolved `full` to, which renders the identical `sizes` attribute (`100.00vw`); the carousel slice files that deviation as one code item on `/`, moved to `upstream` citing ui commit `1c3e3e8`, with the bump note naming every call site to swap back to `'full'`; (4) the ui `Image` and `Video` need a cloud name at `1.2.0`, so a prefactor slice mounts `MediaProvider` in `SiteProvider` and files the mount on `shell` as an `open` addition superseding `0efe45924dbe`, which moves to `open` with a note; both go on the shell's verdicts plan `77cf83a1285a`; at the bump the provider takes `imageComponent={NextImage}`; (5) `1.2.0`'s `Link` takes no `isExternal`, so the two inline anchors' `rel` (`58252b32e362`) moves to `upstream` at the page slice with a bump note, the fix being the recipes slice's ui commit `540e9c3`; (6) the story card re-port fixes the `/stories` items the same change closes, the ledger being the current state, and leaves `2c38c9e6a5da` (the icon's exposure, whose Log asks the operator first) with a note; (7) at `1.2.0` a `local` ui `Image` has no loader of its own and falls through to the site's Cloudinary `loaderFile`, so the design images keep `unoptimized`, filed by the design carousel slice as an `open` code item on `/` for rule 4 (a consequence of `6c566c2715e0`); (8) the two-file page (server metadata, client body) is upgrade-forced and filed `open` by the page slice for rule 4, as the adapter slice filed `1eddda919812`. Items already the operator's: none `open` on `/` at the split. Expected stops: none for the slices; at the finish, the three `open` items on `/` the slices file (the `unoptimized` design images, the two-file page, and `272613135119`) go on the home's verdicts plan, so the record finishes on `40abdb2f222a`. Review findings taken: the leftover's owner named; the expected stops named; the testimonial's aria line rewritten to legacy's two paragraphs; the carousel and design carousel slices blocked by the copy slice, since they touch the hero's alts and the typing section's file; the DOM-diff lines in three slices name the typed label and the later slices' residuals; the media slice's baseline capture and its check timing; the testimonial slice's Interfaces line; the LinkCard bump note corrected by the removals slice; the story card's external overlay in the `asChild` shape, since Chakra v3's `LinkOverlay` drops `rel`; the `unoptimized` finding above; the MDX metadata imported in the server page and cast; five line citations. Dropped: a 200 check on each carousel clone's image URL at the bump (the bump plan's pairs cover it).
