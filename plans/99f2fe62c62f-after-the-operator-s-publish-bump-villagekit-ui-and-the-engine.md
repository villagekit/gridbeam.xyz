---
title: "After the operator's publish: bump @villagekit/ui and the engine packages, close the upstream items"
status: todo
tags:
  - attended
blocked_by:
  - 1c74a465996d
  - 1977c9af920c
  - 1cc03cfabcf2
  - 45d6f5634a11
  - b0f69896e764
  - bc0407533650
  - c14505b76b6a
  - target: 61a42a0adbc8
    note: a sibling fix the publish must carry
  - target: 9e54dca30d48
    note: a sibling fix the publish must carry
  - target: 8a869061bce7
    note: a sibling fix the publish must carry
  - target: 2bd0169a6dda
    note: a sibling fix the publish must carry
  - target: 8235bd4bea81
    note: a sibling fix the publish must carry
  - target: 2de775cb197b
    note: a sibling fix the publish must carry
---
Every difference fixed in `../ui` or `../gridkit` during M2 waits here in `upstream`, since the publishes are deferred to the end of the milestone (decision `28c1a536`). This plan is the operator's until the packages are on npm; then it bumps the site and turns every `upstream` item into `fixed`, or back into `regression` where the publish did not carry the fix.

wants: the operator's publish of `@villagekit/ui` and of every `@villagekit/*` engine package a `blocked_by` slice below changed, to npm.

## Work

- `pnpm update @villagekit/ui --latest` and the same for each engine package the blocking slices changed; commit `package.json` and `pnpm-lock.yaml`.
- The gate, then `pnpm audit:pages` and `pnpm audit:dom` over every route with an `upstream` item; look at the pairs.
- For each `upstream` item (`kipu list --collection difference --status upstream --json`): `kipu fix <id> --outcome "plan <this prefix>"` when the pairs show it closed, else `kipu move <id> regression --from upstream` with a note saying what the pairs still show, and a new slice under the item's route record for it.
- The parity gate `f7a700a3e482` is `blocked_by` this plan; it becomes ready when this finishes.

## Seams under test

None.

## Done when

- `kipu list --collection difference --status upstream` is empty, checked after the moves
- `package.json` pins the published versions and `pnpm-lock.yaml` matches
- `timeout 900 just check` is green

## Log

- 2026-09-25: From the shell split (plan a78b167170b8): the shell's ui slices move the package's API toward the legacy shape (the nav provider taking `usePathname` and a link component, `Footer` taking `socialLinks`, `Provider` taking a `system`, `LinkCard`'s `icon` as a component type, the tuple `FooterSections`, the media components defaulting to legacy's cloud name). Each such slice writes a note here naming the site edits (file, prop, value) this plan's bump commit makes before its gate, since a bump is the commit that adapts to what it buys; the site's video components moving onto the ui `Video` (`b397b0deb1cc`) and `SiteFooter` becoming legacy's thin `footer.tsx` (`ca69d5d2e67c`) are among them.

- 2026-09-26: From the ui framework boundary slice (plan 1c74a465996d, ui commit 4e11d57): the site edits this bump makes, verified by a probe against the sibling on 2026-09-26. (1) New file `app/_components/SiteNav.tsx`, a `'use client'` component: imports `NavContextProvider` and `type NavItemDescriptors` from `@villagekit/ui`, `NextLink` from `next/link`, `usePathname` from `next/navigation`; renders `<NavContextProvider items={items} usePathname={usePathname} linkComponent={NextLink}>{children}</NavContextProvider>` with props `{ items: NavItemDescriptors; children: ReactNode }`. A client wrapper is needed because `app/layout.tsx` is a server component and a hook or a component cannot cross to a client component as a prop. (2) `app/layout.tsx`: drop `NavContextProvider` from the `@villagekit/ui` import, import `SiteNav` from `./_components/SiteNav`, and replace `<NavContextProvider items={navItems}>` and its closing tag with `<SiteNav items={navItems}>` and `</SiteNav>`. With these two edits the nav, the footer's internal links, MDX prose links on story pages and every `LinkCard` route through `NextLink`, and the current route's nav item is selected, as at 1.2.0. Without them the bump ships plain anchors and no selected item. No other call site changes: `as={NextLink}` on `Link`, `LinkButton` and `LinkIconButton`, and `linkComponent={NextLink}` on `LinkCard`, keep working and may stay. The site does not render the ui `Image`, so `MediaProvider`'s new `imageComponent` needs no edit here; the mdx and media slice `bc0407533650` says whether its site half adds `<MediaProvider imageComponent={NextImage}>`.

- 2026-09-26: From the ui brand footer slice (plan 1977c9af920c, ui commit ae593d0): the site edit this bump makes, verified by a probe against the sibling on 2026-09-26 (the thin file is exactly legacy's apps/gridkit/components/footer.tsx shape). app/_components/SiteFooter.tsx: keep the file-top citation of footer.tsx only, the 'use client' directive, the dynamic ssr:false LogoGl import, footerSections and socialLinks as they are; change the import to `import { Footer, type FooterSections, type SocialLinkDescriptor } from '@villagekit/ui'` (dropping Container, HStack, Icon, IconProps, Link, LinkProps, StackProps, Text, VStack, NextLink, React and FaHeart); render `<Footer socialLinks={socialLinks} sections={footerSections}><LogoGl size="12" /></Footer>`; delete the site-local SocialLinkDescriptor, Social, SocialLink and FooterSlogan (the package's Social and credit replace them, with the two sanctioned aria-labels of 6c0e17bf75ca). FooterSections is a 4-tuple in the package, and the site's four sections type-check against it. The footer's internal links and the envelope go through the framework link component the SiteNav wrapper of the boundary note supplies; without it they render as plain anchors. Nothing else in the file changes; the icons and hrefs stay.

- 2026-09-26: From the ui LinkCard slice (plan 1cc03cfabcf2, ui commit a4ef8ed): the site edits this bump makes, verified by a probe against the sibling on 2026-09-26. `LinkCard`'s `icon` is a component type again and required (`icon: ComponentType`, rendered `<Icon as={IconComponent} />`), so the five call sites change from an element to the component: `app/page.tsx:353,360,367`, `app/about/page.tsx:182,189,196`, `app/legal/page.tsx:50,56`, `app/subscribe/page.tsx:61,67` (`icon={<FaCut />}` to `icon={FaCut}`) and `app/tools-and-resources/page.tsx:47-99` (`icon: <FaCut />,` to `icon: FaCut,`, with the entry type at line 38 `icon: ReactNode` to `icon: ComponentType` and the `react` type import to match). One more edit the split's Log did not foresee: all five pages are server components (each exports `metadata`), and a `react-icons/fa` icon is a plain function, not a client reference, so `icon={FaCut}` fails at the server-to-client boundary of the `'use client'` `LinkCard` (`Error: Functions cannot be passed directly to Client Components`, every route 500 on `pnpm dev`; `linkComponent={NextLink}` is fine because `next/link` is a client module). The probe that works: a new file `app/_lib/icons.ts` with `'use client'` on its first line and one named re-export, `export { FaBookOpen, FaCode, FaCubes, FaCut, FaEnvelope, FaGithub, FaInfoCircle, FaQuestionCircle, FaSeedling, FaShoppingBag, FaTools, FaUserShield, FaUsers } from 'react-icons/fa'`, and the five pages importing those names from `@/app/_lib/icons` instead of `react-icons/fa` (the home page's other icon imports stay); every route then serves 200 with the cards at 224 by 256. That module is a helper the legacy site did not have, forced by the app router (legacy's pages were client-rendered), so the bump files it as a code difference on `shell` for the rules to judge, unless the route records have re-ported the pages by then in a shape that carries the icons itself. With the fixed box, the current pages' longer card descriptions clip at the card's bottom edge (`overflow: hidden`): the routes' copy items (`c99d9cf3728c`, `816723482a44`, the added-card items on `/legal`, `/tools-and-resources` and `/subscribe`) own that copy, so the bump's pairs will show it until those land. `CardsLayout`'s container is `breakpoint-md` (768px); no route consumes `CardsLayout` yet, the routes' records do that.

- 2026-09-26: From the ui recipes and provider slice (plan 45d6f5634a11, ui commit 540e9c3): the site edit this bump makes, verified by a probe against the sibling on 2026-09-26. `app/_components/SiteProvider.tsx`: change the import to `import { Provider } from '@villagekit/ui'` and render `<Provider system={system}>{children}</Provider>` in place of `<ChakraProvider value={system}>{children}</ChakraProvider>`; `app/theme.ts` stays as it is (the system it builds is what `Provider` mounts). That edit is what mounts the toast regions on the site (`region "Notifications, bottom (alt+T)"` in the aria tree, one region where legacy's v2 provider mounted six; the bump reads that under rule 4 when it judges f93e37cf6ef4). Nothing else changes for the recipes: the `/designs` search input, the selects, the planner's controls, the footer headings and links and the `/faq` accordion take the new sizes, weights, transitions and focus rings from the package. Expected on the bump's pairs: the `/faq` trigger and panel padding stays the route's `paddingY="3"` (item 6c31193ec773) until the faq record removes it; the planner's top-up select stays `size="sm"` (df4fbe144cb3) until its record changes it.

- 2026-09-26: From the ui nav slice (plan b0f69896e764, ui commit 1cf88ed): no site edit. NavHeaderProps and MainLayoutProps are unchanged, the site renders the menu only through MainLayout, and the new dependencies react-focus-on and motion (^12.38.0, the range the site pins, so one copy) arrive with the publish. What the bump checks on pnpm dev at 375: the toggle Toggle menu opens the panel under the header with no backdrop, Escape closes it, and focus on open lands on the toggle rather than the panel under React StrictMode (the dev double-invocation of react-focus-lock's sidecar returns it); a production build, like the legacy site, leaves focus on the panel, so that is not a difference to file. The toggle's hover and press color is c440126f7064, filed regression by the nav slice, unaffected by this bump.

- 2026-09-26: From the ui mdx and media slice (plan bc0407533650, ui commit 1c3e3e8): the site edits this bump makes, verified by a probe against the sibling on 2026-09-26 (every rendered video and poster URL returned 200 from Cloudinary). (1) `app/_components/story/StoryVideo.tsx` becomes legacy's `apps/gridkit/components/story/story-video.tsx` on the mdx `Video`: `import { Video, type VideoProps } from '@villagekit/ui/mdx'`, props `VideoProps & { isInColumn?: boolean }`, rendered `<Video aspectRatio="standard" {...rest} css={{ ...css, borderRadius: 'xl', boxShadow: 'md', objectFit: 'cover' }} />` with `isInColumn` destructured and not read, exactly as legacy (which builds a `sizes` object from it and never spreads it, so the prop has no effect on a video: `8df7a085e421`'s note); the wrapping `Box`, the `controls`, `preload` and mp4-only source go (they are the stories record's items). Every video then takes the `MediaContainer` default `sizes` (448px at `base`, 512px from `md`, `w_448` and `w_512` URLs, what the live legacy site requests). (2) `app/_components/landing/LandingVideo.tsx` renders the ui `Video` (`import { Box, Video } from '@villagekit/ui'`) in place of the `<video>` element: `<Video src={src} posterSrc={posterSrc} title={title} sizes={{ base: '356px', md: '622px' }} css={{ width: '100%', height: '100%', objectFit: 'cover' }} />`, legacy's `sizes` from `pages/index.tsx:200-209`; the wrapping `Box` with its aspect ratio stays until the home record's items `a3d8186cc50c` and `7e5772c7a092` decide it, and the `width`/`height` props then go with it. (3) `app/_lib/cloudinary.ts`: `getCloudinaryVideoUrl`, its options interface and the header comment's paragraph about it leave; `CLOUDINARY_NAME` and `getCloudinaryUrl` stay for the image loader. No `MediaProvider` is mounted (`0efe45924dbe`): the package's default cloud name is `villagekit`. Expected on the bump's pairs: the story videos autoplay muted in a loop with no controls, as legacy's. (4) The site renders no ui `Image` (`StoryImage` is a `next/image` wrapper, `09c3946b1912`), so no `MediaProvider` and no `imageComponent` is added here; if the story pages record re-ports `StoryImage` onto the mdx `Image`, legacy's `next/image` rendering needs `<MediaProvider imageComponent={NextImage}>` in a client component, which supersedes `0efe45924dbe`'s verdict, and that record files it. (5) A correction to the framework boundary note above: `MdxLink` no longer reads the framework link component (the mdx and media slice re-ported legacy's link, every non-hash href external), so with the `SiteNav` wrapper the nav, the footer's internal links and every `LinkCard` route through `NextLink`, and MDX prose links do not. Then `kipu fix` `f9b9ec72d771`, `d750c0a47839`, `b397b0deb1cc` and `252edab16c7a`, and `63baa85427e0` and `d04a1827b838` once a story's internal link is seen opening in a new tab with `rel="noopener"`.

- 2026-09-26: From the palette slice (plan c14505b76b6a, ui commit 9642114): the site needs no edit at the bump for it. The published package then carries Chakra v2's gray, pink, cyan and yellow literals, fg on gray.800 and v2's shadow scale, so the bump moves 72b776cb0d3f, f45538f480af and 1f11773f8e2b to fixed once the screenshot pairs on /faq, /designs, /stories and a story page show the same tones as legacy (the probe: curl -s localhost:3000/faq | grep -o -- '--chakra-colors-gray-700:[^;]*' prints #2D3748, and the body, h1 and h2 computed color is rgb(26, 32, 44)). Still regression on shell after this slice, with no slice owning them: f40107b60034 (the radius scale, a rule 4 verdict per the recipes slice's note), f85e98a67092 (rel on five components, one line each in the ui, a rule 4 verdict or a ui slice the operator mints).

- 2026-09-26: From the ui toolbar toggle slice (plan 61a42a0adbc8, ui commit d736bfe): no site edit. The toolbar variant's rest color is the --toolbar-color variable (gray.700 unless set) and the package's own NavHeader sets it for the toggle; the site's one toolbar consumer, the catalog's Clear search button (app/_components/catalogue/Catalogue.tsx:408), sets no color and keeps gray.700 through the fallback, as legacy's search-bar.tsx did. What the bump checks on pnpm dev at 375 on /faq: the toggle's computed color is rgb(23, 25, 35) at rest and rgb(213, 63, 140) under the pointer and on press (the scratchpad's toggle-probe.mjs reads it), then kipu fix c440126f7064. Still regression on shell after this slice: d2f1007d7d37 (a pressed button keeps the hover scale 1.08), owned by the slice 8a869061bce7 minted beside the shell record, which this plan is blocked_by.

- 2026-09-26: Slice `8a869061bce7` (ui commit 6a7c036, the press transform): the bump makes no site edit for it. The recipe change is the package's alone; after the bump, a press on any ui button reads `matrix(1, 0, 0, 1, 0, 0)` while hovered, the check being the scratchpad's `press-probe.mjs` or a pointer on the mobile menu toggle at 375.

- 2026-09-26: From the ui links slice (plan [[9e54dca30d48]], ui commit 46eab3c): no site edit beyond the brand footer's swap already noted above (the site's own SiteFooter writes rel="noopener noreferrer" until the thin file replaces it). What the bump's DOM extraction should show: the footer's social links and villagekit.com credit, and every LinkButton with isExternal (/suppliers, /about, /), rendering target="_blank" rel="noopener"; a LinkCard overlay with isExternal still "noopener noreferrer", legacy's LinkOverlay output ([[ed16aad27638]]). The routes' own anchors that write "noopener noreferrer" in their JSX are the route records' ([[f85e98a67092]]'s note names them), not this publish's.

- 2026-09-26: From the removals slice (plan [[4faefea81c76]]): the LinkCard slice's note above names `app/page.tsx:353,360,367` among the call sites this bump's icon swap serves. That section (`For makers`) is deleted by the removals slice, so the home page renders no LinkCard after it; the bump's four remaining call sites, at their current lines, are `app/about/page.tsx:178,185,192`, `app/legal/page.tsx:46,52`, `app/subscribe/page.tsx:49,55` and `app/tools-and-resources/page.tsx:38-90` (the entry type at line 29), not `app/page.tsx`.

- 2026-09-26: From the home's MediaProvider slice (plan [[6f90e7e24ca6]]): the site edit this bump makes, superseding three sentences above that assumed the site renders no ui Image: the framework boundary note's last sentence (no imageComponent edit needed), and clauses (3) (no MediaProvider is mounted) and (4) of the mdx and media slice's note. The home's re-ports (the story card [[e332105c3b52]], the image carousel [[c92c235205f5]], the design carousel [[60cca8519469]] and the page [[159c621d8a1a]]) render the ui Image and Video, so SiteProvider mounts MediaProvider now (item [[afe50ab5c7aa]]). After the publish the provider takes imageComponent: in app/_components/SiteProvider.tsx, a client module, add import NextImage from 'next/image' and render <MediaProvider cloudinaryName={CLOUDINARY_NAME} imageComponent={NextImage}>; without it every ui Image renders a plain <img> on one URL at the numeric width or 1280 (the ui CHANGELOG's Unreleased entry for MediaProvider, ui commit 4e11d57). The component crosses no server boundary since SiteProvider is a client component. cloudinaryName may stay: the package's default is villagekit since ui commit 1c3e3e8, and the prop reads the same constant the loader file uses.

- 2026-09-26: From the image carousel slice (plan [[c92c235205f5]]): the site edit this bump makes once the publish carries ui commit 1c3e3e8 (full known to useSizeWidths again): in app/page.tsx the hero ImageCarousel's sizes={{ base: '100%', md: ['1500px', 2] }} becomes legacy's sizes={{ base: 'full', md: ['1500px', 2] }} (pages/index.tsx:109), then kipu fix [[152511f71ef4]]. The rendered sizes attribute is the same before and after ((min-width: 768px) 750.00px, 100.00vw), so the pairs cannot show it; the check is grep -n "base: '100%'" app/page.tsx printing nothing. The design carousel slice [[60cca8519469]] and the page re-port [[159c621d8a1a]] extend this note with the other call sites (pages/index.tsx:334,365,444,454) as they write them.

- 2026-09-26: From the design carousel slice (plan [[60cca8519469]]): two more site facts for this bump. (1) The sizes swap of the image carousel note above has two more call sites: app/_components/landing/TypingDesignSection.tsx:72,87, the DesignCarousel and the hidden next-design Image (legacy pages/index.tsx:444,454), each sizes={{ base: '100%', md: ['1500px', 2] }} to base: 'full'; the page re-port [[159c621d8a1a]] moves them into app/page.tsx, so the check is grep -rn "base: '100%'" app printing nothing after the swap, then kipu fix [[152511f71ef4]]. (2) unoptimized stays on both local design images under imageComponent={NextImage} too: the ui Image in the sibling (../ui/src/components/media/Image.tsx:166-167 at 46eab3c) passes a loader for type="cloudinary" only, as 1.2.0 does, so a local image still falls through to the site's Cloudinary loaderFile, which answers 404 for a /_next/static/media/ path. Removing unoptimized is [[a704be5b8765]]'s verdict (open, the operator's), not this bump's.

- 2026-09-26: From the page re-port (plan [[159c621d8a1a]]): three site facts for this bump. (1) The sizes swap of the two notes above now has five call sites in one file, app/HomePage.tsx:110 (the hero carousel), :333 and :369 (the forest and camp kitchen LandingImage), :433 (the DesignCarousel) and :446 (the hidden next-design Image), each base: '100%' to base: 'full' (legacy pages/index.tsx:109,334,365,444,454); the check is grep -rn "base: '100%'" app printing nothing, then kipu fix [[152511f71ef4]]. (2) The two inline anchors on the home page, app/HomePage.tsx:270 (with the community!) and :346-351 (wider vision), become <Link href={...} isExternal> and <Link variant="paragraph" href={...} isExternal> with their target and rel attributes dropped, legacy pages/index.tsx:270,347; the DOM extraction then reads rel="noopener" on both, and kipu fix [[58252b32e362]]. (3) Clause (2) of the mdx and media slice's note above is overtaken: app/_components/landing/LandingVideo.tsx is deleted, and the home's video is the ui Video already, rendered by the private LandingVideo helper in app/HomePage.tsx:544-548 with legacy's sizes and the shared landingMediaCss; the only site half left on [[b397b0deb1cc]] is clause (1), StoryVideo, and clause (3), getCloudinaryVideoUrl leaving app/_lib/cloudinary.ts, which StoryVideo still reads.
