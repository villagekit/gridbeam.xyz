---
title: "FAQ: the copy verdicts applied on the sections, the questions, the answers and the closing line"
status: done
parent: 7f0b60d948c5
derived_from: 7f0b60d948c5
blocked_by: b52b62e260f3
tags:
  - "worker:sonnet"
priority: medium
---
The FAQ page's copy is what the FAQ grilling settled: legacy's surviving categories in legacy's order (`Product`, `Sustainability`, and `Support` for what survives of `Returns & Support` and `Other`), fifteen questions in legacy's order with their verdict text, and the closing line, applied on the page as the removals slice leaves it, before the page is re-ported, so the re-port carries settled text and its review is structure only. Three answers are several paragraphs again, so legacy's array branch for an answer returns with them. Every string comes from the item's last Log line or the legacy line it names, and from nowhere else (`ca677697`); the diff is stated line by line below, so the work is Sonnet's. Nineteen items on `/faq`. Record `7f0b60d948c5`; decisions `ee86d68a`, `2032533f`, `ca677697`, `edad0df8`.

## Work

Read each item (`kipu show <id>`) and write exactly the text its last Log line gives. Where the line says `legacy verbatim` or `legacy with the swap`, or restores a legacy string with one named edit (`62ebc1fe6962`), the string is the legacy source's, `../node-modules/apps/gridkit/pages/faq.tsx` at `fce357d`, with legacy's characters: the typographic apostrophes of `can’t` (`faq.tsx:153`), `We’d` (`:321`), `isn’t` (`:375`) and `We’re` (`:299`), which the Log lines write straight only by their quoting, and the em dashes legacy wrote (`edad0df8`). Where the line gives the operator's words, the string is the line's, character for character. In `app/faq/page.tsx` as the removals slice leaves it (search by the current text, not by line). Links stay in the page's current form until the re-port: `<Link as={NextLink} variant="paragraph" href="...">` for a route, `<Link variant="paragraph" href="...">` for an external one, with `target` and `rel` only where the current element already carries them (the `variant` is `753e30b25966`'s, the re-port's). The `categories` array with its slugs stays the mechanism (`eb2304414513`, the re-port's); only its contents change.

- The three elements of `categories`, in order: `heading: 'Product', slug: 'product'`; `heading: 'Sustainability', slug: 'sustainability'`; `heading: 'Support', slug: 'support'` ([[8368a3c0a5cc]], [[cf09a6601985]]). No `Other` element remains: the verdict of [[cf09a6601985]] names the contact support and custom design questions as the two under `Support`, and `Other`'s only other question is the sanctioned removal `805d395bccce` (the split's call 3 in the record's Log).
- `Product`, nine entries in legacy's order (`faq.tsx:30-148` minus the sanctioned removals `ba753f7499b9` and `adce8828614b`):
  1. [[887f3d253094]]: question `What is grid beam?` as it is (`a1336cb4a3ef`); the answer is the Log line's operator's words, one paragraph ending in the link `Learn more.` to `/about` with the full stop inside the link text as `faq.tsx:41-43`; the em dash in the line is legacy's (`faq.tsx:40`); no link on Grid Beam (rule 1, the verdict).
  2. [[7416dab47f06]]: question `Do I need any special tools to assemble it?` as it is; answer `No special tools needed! Grid beam is designed for easy assembly, and you only need basic hand tools like a 4mm hex key. For help on assembly, see our guide ` then the link `"How To Install Furniture Bolts"` (the double quotes inside the link text, `faq.tsx:54`) to `/stories/how-to-furniture-bolts` then `.` outside the link.
  3. [[a344d9b33bca]]: `What materials are used?` as it is; answer `Locally sourced wood and fasteners.`, a plain string, no link.
  4. [[2a940fa3e6fd]]: `Are the materials durable?`; `Yes, the materials are both strong and long-lasting, so your builds stand the test of time.`
  5. [[3e2d549b5ada]]: `Is the wood treated?` as it is; `Depends on the supplier. Untreated wood keeps grid beam non-toxic and safe for indoor use.`
  6. [[086eab31135d]]: `Can you paint or stain the wood?`; `Absolutely! The untreated wood can be painted or stained to match your style. Customize it to make it truly your own.`
  7. [[96e9c9873dd1]]: `Is the wood suitable for outdoors?`; `Grid beam is designed primarily for indoor use. If you want to use it outdoors, consider treating the wood with a suitable finish for added protection.`
  8. [[39a41b3159df]]: `How do I use panels with grid beam?`; `Panels can be easily integrated into your grid beam structure to create surfaces, walls, or dividers. Feel free to experiment!`
  9. [[79a184bb6000]]: `What add-ons are compatible with grid beam?`; one paragraph: `For now, if you have a 3d printer, we have ` then the link `an open source repository of 3d printable add-on designs` to `https://github.com/villagekit/replicad-models` (the current element, its `target` and `rel` kept until the re-port closes `226f1042adb3`) then `.` (`faq.tsx:139-145`). Legacy's first paragraph stays out (rule 2, the verdict), so the answer is a single node, not a one-element array (the split's call 2).
- `Sustainability`, four entries in legacy's order (`faq.tsx:149-209`):
  1. [[8106f4775b6b]]: `What can I make with grid beam?`; an array of two strings: `faq.tsx:153` with `Grid Kit` to `Grid beam` (the sentence opens `More like, what can’t you make with it! Grid beam is designed to adapt to almost any structure...`), then `The original inventor of grid beam made bicycles, electrical vehicles, trailers, and more. Anything is possible.` (`faq.tsx:155-161` with the swap and no link, rule 1, the verdict).
  2. [[3e857451d7f2]]: `What do you mean by modular?`; an array of two strings: `faq.tsx:168` with `Grid Kit system` to `grid beam system` and its em dash kept, then `faq.tsx:169` verbatim (`Think of it like LEGO: ...`).
  3. [[b83540802426]]: `Can I recycle or repurpose grid beam?`; `Yes! Grid beam is built to be reused, adapted, or reassembled into new projects. When it reaches the end of its life, components can be repurposed or recycled, supporting a circular economy.`
  4. [[9da5502523b9]]: `What makes grid beam sustainable?`; an array of four: (a) the link `The U.S. EPA estimates` (the current element with its href, `target` and `rel`) then ` nearly 12 million U.S. tons of furniture waste is disposed of each year with 9.6m U.S. tons making it to landfills.` (`faq.tsx:185-193`: no comma after `year`, `9.6m`); (b) the link `In Australia on average` to `https://www.handkrafted.com/blog/landfill-australias-underground-furniture-movement/` (the verdict's href) with no `target` and no `rel`, since legacy's `faq.tsx:197-200` wrote none, then ` each household disposes of around 24kg of wooden furniture per year.`; (c) the string `Grid beam is designed to be repairable, reusable, recyclable, and it reduces furniture waste going to landfill every year.`; (d) the string `Once a piece of furniture out lives its purpose, it can be broken down into its base parts and reused to build something new.` (`faq.tsx:207`, `out lives` as legacy wrote it). Each `Link` inside an array answer carries legacy's `key` (`faq.tsx:187,198`); the fragments around them carry none.
- `Support`, two entries, the verdict of [[cf09a6601985]] naming both, in its order:
  1. [[62ebc1fe6962]]: `How do I contact support?`; `Find our contact details ` then the link `here` to `/contact` then `. We’re here to help!` (`faq.tsx:292-299`, legacy's space before the period dropped as the verdict says).
  2. [[68a65ec89524]]: the question as it is (legacy's `faq.tsx:318`, its em dash included); answer `We’d love to hear your ideas! ` then the link `Get in touch with us` to `/contact` then ` to discuss custom designs and how we can make your vision a reality.` (`faq.tsx:321-325`).
- The closing line, [[eceaf3fc05dd]]: `If your question isn’t answered here, please ` then the link `send us an email` to `/contact` then ` or ` then the link `ask on the community forum` to `https://discuss.villagekit.com` (the current element's `target` and `rel` kept), and no full stop (`faq.tsx:374-383`).
- [[58dc1b8f98fd]]: `FaqEntry.answer` is `ReactNode | Array<ReactNode>` (`faq.tsx:25`), and the body renders an array as legacy's `faq.tsx:357-366` does, in place of the one `<Text>{entry.answer}</Text>`: `{Array.isArray(entry.answer) ? (<VStack alignItems="flex-start">{entry.answer.map((a, i) => (<Text key={i}>{a}</Text>))}</VStack>) : (<Text>{entry.answer}</Text>)}`, legacy's `// biome-ignore lint/suspicious/noArrayIndexKey:` comment above the keyed `Text` (`faq.tsx:360`), a reason after the colon if `pnpm lint` asks for one. Chakra v3's `VStack` default gap (0.5rem) is v2's default spacing, so nothing else is written. The three arrays above use it.
- Verify first: `grep -c "slug: '" app/faq/page.tsx` prints 3 after the removals slice; `grep -cP '\x{2014}' app/faq/page.tsx` prints at least 6 before (the current answers' em dashes and the custom design question's); `grep -c 'Array.isArray' app/faq/page.tsx` prints 0 before; against a running `pnpm dev`, `curl -s localhost:3000/faq | grep -o 'blonder look.</p>' | wc -l` prints 1, so the collapsed panels are in the served markup and not only in the RSC payload, which repeats every string (if it prints 0, the answer checks below run on `app/faq/page.tsx` instead and the Outcome says so).
- Not this slice: the slugs' mechanism, the links' `variant`, the two `isExternal` links' `target` and `rel`, the repository link's tab, the trigger's label and every visual item (the page re-port, `FAQ page re-ported from the legacy faq page`).

## Seams under test

None pure; the proof is the copy diff of the DOM pair and the served HTML.

## Done when

- `pnpm audit:dom --routes <a file naming /faq>` against a running `pnpm dev`: between the nav and the footer, `audit/faq/dom/current.txt` reads, in order: `Frequently asked questions`; `Product`; `What is grid beam?`; `Do I need any special tools to assemble it?`; `What materials are used?`; `Are the materials durable?`; `Is the wood treated?`; `Can you paint or stain the wood?`; `Is the wood suitable for outdoors?`; `How do I use panels with grid beam?`; `What add-ons are compatible with grid beam?`; `Sustainability`; `What can I make with grid beam?`; `What do you mean by modular?`; `Can I recycle or repurpose grid beam?`; `What makes grid beam sustainable?`; `Support`; `How do I contact support?`; the custom design question; the closing line with no full stop: twenty lines, and every other line is the shell's; the Outcome lists the lines that differ from `legacy.txt` and the item or sanction each maps to
- `curl -s localhost:3000/faq | grep -o 'Absolutely! The untreated wood\|Feel free to experiment\|Think of it like LEGO\|In Australia on average\|Locally sourced wood and fasteners\|Find our contact details\|Depends on the supplier\|out lives its purpose' | sort -u | wc -l` prints 8 (the RSC payload repeats each string, so the distinct count is the check)
- `grep -cP '\x{2014}' app/faq/page.tsx` prints 3 (the What is grid beam answer, the modular answer and the custom design question, each legacy's) and `grep -cP '\x{2019}' app/faq/page.tsx` prints 4 (`can’t`, `We’d`, `isn’t`, `We’re`)
- `grep -c 'Array.isArray' app/faq/page.tsx` prints 1 and `grep -c 'handkrafted.com' app/faq/page.tsx` prints 1
- The nineteen items are `fixed` (`kipu fix <id> --outcome "plan <this prefix>"`), checked after the fixes
- `timeout 900 just check` is green

## Outcome

Applied on app/faq/page.tsx: three categories (Product, Sustainability, Support), fifteen questions in legacy's order with the verdict text, the closing line with no full stop, and legacy's array branch for answers (FaqEntry.answer is ReactNode | Array<ReactNode>). Typographic apostrophes as legacy wrote them; one straight apostrophe in "it's limited", as legacy. Proof: audit:dom current.txt reads the twenty lines the plan names between the nav and the footer; the served-HTML check printed 8; grep counts: 3 slugs, 3 em dashes, 4 typographic apostrophes, 1 Array.isArray, 1 handkrafted.com; timeout 900 just check exited 0. Lines that differ from legacy.txt map to the nineteen items. 58dc1b8f98fd had no verdict Log line, so it was closed by the default rule. The quoted link label is written as a JS string expression, a cosmetic form the re-port can replace with plain text. The biome-ignore comment carries a reason. An Opus review found no blocking findings and no unrecorded difference.

## Log
