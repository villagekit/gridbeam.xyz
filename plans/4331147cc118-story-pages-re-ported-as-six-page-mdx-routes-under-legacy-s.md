---
title: Story pages re-ported as six page.mdx routes under legacy's StoriesLayout, the [slug] route, the catalog lookup and content/stories gone
status: done
parent: 56e6eb197e6c
derived_from: 56e6eb197e6c
blocked_by:
  - 373320c95e55
  - eeeb3813939b
  - 52adacea5b2f
  - a7bf623f885c
tags:
  - "worker:fable"
---
The six stories are pages again, as legacy's `pages/stories/*.mdx` were: each at `app/stories/<slug>/page.mdx`, its body legacy's under `export default withStoriesLayout(story)`, the layout legacy's `components/layouts/stories.tsx` translated: one `article` around the `Title` (with the description and the anchor), the cover when `showImageInStory` in a `Center` with a bottom margin of 16, and the MDX body. The root layout's `ContentMainLayout` is legacy's `MainLayout` wrapper, so no `ContentMainTocLayout`: no table of contents, no second `main`, no doubled bottom margin, no `3xl` container with top padding over the header. The `[slug]` route with its dead `notFound` guard and stale comment, `STORY_SLUGS`, `getStory`, `hostedStories` and `content/stories/` go; the catalog imports each page's `story` export as legacy's `stories.ts` imported `metadata`. One name is forced by the app router and is the operator's: a page module's `metadata` export is what Next reads as the page's metadata (`node_modules/next/dist/lib/metadata/resolve-metadata.js:294-303`) and cannot stand beside `generateMetadata`, so the story's data export is `story`, and each page exports a `metadata` built from it by a helper carrying the sanctioned `NextSeo` translation ([[fcdf83c992a1]]: the title, `shortDescription || description`, the article Open Graph over the site's fields); the rename is filed as [[0e005a229aee]] (`open`, on the verdicts plan `Story pages: the operator's verdicts`), and this slice ships it as the shape the port rule gives (`ee86d68a`, in doubt re-port) unless the operator's verdict names another by the time it runs. Nine items on three story routes. A re-port that decides the route's shape, so Fable. Record `56e6eb197e6c`; decisions `ee86d68a`, `2032533f`, `ca677697`.

## Work

- Legacy at `fce357d`: `../node-modules/apps/gridkit/components/layouts/stories.tsx` (80 lines: `StoriesLayout` `:19-70`, `withStoriesLayout` `:72-80`), `pages/stories/*.mdx:1-25` (the imports, `metadata`, `export default withStoriesLayout(metadata)`), `stories.ts:3-8`, `next.config.mjs` (`pageExtensions`). Current: `app/stories/[slug]/page.tsx`, `content/stories/*.mdx` as the earlier slices leave them, `app/_lib/stories.ts` (the residue below `allStories`), `app/_lib/mdx.d.ts`, `app/page.tsx` (two metadata imports), `mdx-components.tsx` (the components slice's map), `next.config.ts` (`pageExtensions` lists `mdx` already), `.next/types/validator.ts` (generated).
- `git mv content/stories/<slug>.mdx app/stories/<slug>/page.mdx` for the six; in each, after the imports: `export const story = { ... }` (legacy's `metadata` object as the fields slice left it, renamed: [[0e005a229aee]]), `export const metadata = getStoryPageMetadata(story)`, `export default withStoriesLayout(story)` (legacy `:21`), then the body unchanged. MDX's default export is the layout wrapper (`@mdx-js/mdx`'s `recma-document` turns it into `MDXLayout`), the mechanism legacy used; `@next/mdx` 15.5 runs `@mdx-js/loader` under Turbopack.
- `app/_components/layouts/StoriesLayout.tsx` (legacy's `components/layouts/stories.tsx`; the file name the site's PascalCase, as `Item.tsx`), under its `// ported from` header: `StoriesLayout({ metadata, children })` rendering `<article><Title description={description} hasAnchor>{title}</Title>{showImageInStory && <Center css={{ marginBottom: 16 }}><StoryImage {...image} priority /></Center>}{children}</article>` ([[4ab3e21024a0]] the article around the whole story, [[69e3e2447813]] the header with no `3xl` container or top padding, the cover in a `Center`; the ui `Title` renders an `h1` by default, so no `as`); `withStoriesLayout(metadata)` returning the wrapper as legacy `:72-80`; and `getStoryPageMetadata(metadata): Metadata`, the `NextSeo` block's (`:35-53`) app-router form, the body the current `generateMetadata` has (`title`, `description: shortDescription || description`, `openGraph: { ...siteOpenGraph, type: 'article', publishedTime: publishedAt.toISOString(), modifiedTime: updatedAt.toISOString(), tags: [category], images: [{ url: getCloudinaryUrl({ src: image.src, width: 1200 }), alt: image.alt }] }`), beside the layout as `NextSeo` sat inside it. The `MainLayout` wrapper is the root layout's (`a7bf623f885c`): dropping `ContentMainTocLayout` closes [[756288b40fd0]] (one `main`), [[7584e6e05aea]] (one `ContentContainer` margin), [[e5faf0655462]] and [[b5aa272c70e7]] (no `On this page` nav ahead of the article), [[aece63987800]] (one `Contents` list on the three guides) and [[4030f9d739c4]] (no empty list on the 2021 newsletter); the `MDXProvider` is `mdx-components.tsx` (the components slice). No `'use client'` unless forced: the component calls no hook, and `Title`, `Center` and `StoryImage` are client modules it renders; if it is forced, `getStoryPageMetadata` moves to a server-safe module (`app/_lib/stories.ts`, beside the type it reads), since a client module's export cannot be called on the server, and the Outcome says why.
- `app/_lib/stories.ts`: the six imports become `import { story as whatsAGridUnit } from '@/app/stories/whats-a-grid-unit/page.mdx'` and so on (legacy `stories.ts:3-8`); the default imports, `hostedStories`, `STORY_SLUGS`, `getStory` and the residue comment go ([[9c48718ec6f8]]); `app/_lib/mdx.d.ts` declares `story: Record<string, unknown>` beside `metadata`; `app/page.tsx` imports `story as ...` from the two page modules. `app/stories/[slug]/` and `content/stories/` deleted (`content/suppliers.ts` stays, so `content/` does). The client modules that import the catalog (`app/_lib/context/stories.tsx` through `StoriesPage.tsx`, `app/_components/stories/Item.tsx`, `app/HomePage.tsx`) then import six page modules, as legacy's client pages imported `stories.ts` over its six MDX pages; `/` and `/stories` are checked below.
- [[0e005a229aee]] carries a note from this slice citing the six files and the helper by path; it stays `open` for the operator. If the operator's verdict names another shape by the time this runs (the `[slug]` route kept under rule 4, say), this slice takes it, re-ports the layout alone and says so.
- A note on [[fcdf83c992a1]] (it stays `sanctioned`): its Current text is now the per-page static `metadata` from the helper.
- Translations: `withStoriesLayout` kept as MDX's default-export layout; `NextSeo` to the static `metadata` built by the helper (rule 4, [[fcdf83c992a1]]); `MainLayout` to the root layout; `MDXProvider` to `mdx-components.tsx`; `sx` to `css`; `getImageUrl` to the site's `getCloudinaryUrl`; `@/pages/stories/...` to `@/app/stories/.../page.mdx`.
- Interfaces: consumes `StoryImage` and the mdx map (the components slice), legacy's `StoryMetadata` (the fields slice), the six MDX bodies as the inline slice leaves them, the root layout (`a7bf623f885c`); produces the six routes, `withStoriesLayout` and `getStoryPageMetadata`.
- Verify first: `grep -n pageExtensions next.config.ts` prints the line with `mdx`; `sed -n '294,303p' node_modules/next/dist/lib/metadata/resolve-metadata.js` reads `generateMetadata` when it is a function, else `mod.metadata`; `grep -rn 'STORY_SLUGS\|getStory\|hostedStories' app | wc -l` prints 9, the catalog's residue and the page's calls; `grep -rn 'content/stories' app mdx-components.tsx tsconfig.json | wc -l` prints 8, the catalog's six imports and the home's two. The build is `next build --turbopack` (`package.json`), whose type generator writes `.next/types/validator.ts` and validates `page.ts` and `page.tsx` files alone (`node_modules/next/dist/server/lib/router-utils/typegen.js:373-375`), so a `page.mdx` gets no generated check and `pnpm typecheck` reads the ambient `*.mdx` module (`app/_lib/mdx.d.ts`); one trap: `just check` runs `typecheck` before `build`, and the validator written by the last build still imports `app/stories/[slug]/page.js` (`.next/types/validator.ts:111-114`), so run `pnpm build` (or delete `.next/types`) once after the route is deleted, before the gate, or the first `typecheck` fails on a stale file.
- Docs: CLAUDE.md's Structure line on `app/` and `content/` in the same commit: the story pages live under `app/stories/<slug>/page.mdx`, `content/` holds the suppliers records.
- Not this slice: the components, the fields, the copy, the inline JSX; the shell's `upstream` items on the pairs (the Title and Description containers [[318456ddabc6]], [[c325dd0181d9]], the container padding [[5c1af396cc2e]], the palette, the headings, the Section band); at 768 the footer [[c9ea22823a23]].

## Seams under test

None pure; the proof is the build's route table, the DOM pairs, a probe of the landmarks and margins on both sides, and the screenshot pairs.

## Done when

- `next build`'s route table lists `/stories/whats-a-grid-unit`, `/stories/how-to-cut-grid-beams`, `/stories/how-to-furniture-bolts`, `/stories/building-with-grid-kit`, `/stories/2021-winter-newsletter` and `/stories/2022-newsletter` as six static pages and no `/stories/[slug]`; against a running `pnpm dev`, `curl -s -o /dev/null -w '%{http_code}' localhost:3000/stories/<slug>` prints 200 for each, `/stories/nope` prints 404 (Next's, the custom page `dc1a7a98373f`), and `/` and `/stories` print 200 with their story cards rendered and no hydration error in the browser console
- `pnpm audit:dom --routes <a file naming the six story routes>`: each `current.aria.yaml` holds one `main` (no `main` inside `main`), no `navigation` but the shell's, no `On this page` heading, and an `article` holding the `h1`, the description, the cover on 2022 and every heading and image of the body, as `legacy.aria.yaml:22-152` on whats-a-grid-unit; `current.txt` on how-to-cut, furniture-bolts and building holds its `Contents` list once; `diff legacy.txt current.txt` on every route, between the nav and the footer, shows no line beyond what this record's earlier slices left (named in the Outcome)
- `curl -s localhost:3000/stories/2022-newsletter` holds `<title>Grid Beam: 2022 Newsletter</title>`, a `<meta name="description"` reading `The slow and steady progress of Village Kit in 2022.`, `property="og:type" content="article"`, `article:published_time`, `article:modified_time`, `article:tag` with `newsletter`, and an `og:image` on the Cloudinary URL at width 1200, each as the current `generateMetadata` wrote them (a curl of each route's `<head>` taken before the change, saved in the scratchpad, diffed after)
- A Playwright probe on both sides at 1280, 768 and 375, saved beside the pairs: the `article` holds the body; the gap between the last `section.vk-section`'s bottom edge and the footer's top edge is one `ContentContainer` margin, 64px at 1280 and 32px at 375, legacy's reading; the title block's first element has no padding-top of its own beyond the `Title`'s (the `pt={{ base: 4, md: 8 }}` container gone); on 2022 the cover's `Center` reads `margin-bottom` 64px and the cover's `img` a `sizes` attribute equal to legacy's
- `pnpm audit:pages --routes <the same file>` at 375, 768 and 1280 looked at against legacy: no TOC column at 1280, the title block as legacy's, every residual a `shell` item in `upstream`, the operator's, or filed
- `ls 'app/stories/[slug]' content/stories` fails for both; `grep -rn 'STORY_SLUGS\|getStory\|hostedStories\|ContentMainTocLayout\|content/stories' app mdx-components.tsx | wc -l` prints 0; `head -1 app/_components/layouts/StoriesLayout.tsx` prints its `// ported from` line; `grep -c 'export default withStoriesLayout(story)' app/stories/*/page.mdx` prints 1 for each of the six
- The nine items are `fixed` (`kipu fix <id> --outcome "plan <this prefix>"`); [[0e005a229aee]] carries this slice's note and stays `open`; [[fcdf83c992a1]] carries its note; checked after the fixes
- `timeout 900 just check` is green

## Outcome

Shipped as scoped, the six stories pages again as legacy's `pages/stories/*.mdx` were. Each of `app/stories/whats-a-grid-unit/page.mdx`, `how-to-cut-grid-beams`, `how-to-furniture-bolts`, `building-with-grid-kit`, `2021-winter-newsletter` and `2022-newsletter` is the `content/stories/<slug>.mdx` file moved (`git mv`, the body untouched) with three lines added after the imports and the data object: `import { getStoryPageMetadata, withStoriesLayout } from '@/app/_components/layouts/StoriesLayout'` as the last import, where legacy's `withStoriesLayout` import sat in all six; `export const story = { ... }` (legacy's `metadata` object as the fields slice left it, renamed: [[0e005a229aee]], no verdict having landed, so the port rule's shape shipped and the item carries this slice's note and stays `open`); `export const metadata = getStoryPageMetadata(story)`; `export default withStoriesLayout(story)`. `app/_components/layouts/StoriesLayout.tsx` is legacy's `components/layouts/stories.tsx` under its `// ported from` header: `StoriesLayout` renders one `article` around the `Title` (`description`, `hasAnchor`, the ui `Title`'s default `h1`), the cover in a `Center css={{ marginBottom: 16 }}` when `showImageInStory`, and the body; `withStoriesLayout` is legacy's `:72-80`; `getStoryPageMetadata` is the `NextSeo` block's (`:35-53`) app-router form beside the layout as `NextSeo` sat inside it, with legacy's `shortDescription || description` (`:37`, where the deleted `generateMetadata` wrote `??`), `type: 'image/jpeg'` on the article image (`:47`, closing [[6401346644c2]]) and `image.src as string`, legacy's own cast (`ui-media/src/image.tsx:141`), since the union's `src` admits a `StaticImport`. No `'use client'` was forced: the module calls no hook and renders client components, so the helper stays beside the layout. `app/_lib/stories.ts` imports `story` from the six page modules as legacy's `stories.ts:3-8` imported `metadata`, the default imports, `hostedStories`, `STORY_SLUGS`, `getStory` and the residue comment gone ([[9c48718ec6f8]]); `app/page.tsx` imports `story` from the two page modules; `app/_lib/mdx.d.ts` declares `story` beside `metadata`. `app/stories/[slug]/` and `content/stories/` are deleted, `content/suppliers.ts` stays. CLAUDE.md's Structure lines on `app/` and `content/` say where the story pages live now. Translations as planned: `withStoriesLayout` kept as MDX's default-export layout (`@mdx-js/mdx` 3.1.1 `recma-document.js` turns it into `MDXLayout`), `NextSeo` to the static `metadata` (Next 15.5.18 `resolve-metadata.js:293-303` reads `mod.metadata` when no `generateMetadata` exists), `MainLayout` to the root layout, `MDXProvider` to `mdx-components.tsx`, `sx` to `css`, `getImageUrl` to `getCloudinaryUrl`.
Deviations from the plan's letter, none visible: ten items closed, not nine, since the fields slice's review filed [[6401346644c2]] for this helper; the plan's Done-when grep `getStory` also matches `getStoryPageMetadata`, the helper the plan asks for, so the literal command prints 13 and `grep -rn 'STORY_SLUGS\|getStory\b\|hostedStories\|ContentMainTocLayout\|content/stories' app mdx-components.tsx | wc -l` is the check that prints 0; the head check reads "as the current generateMetadata wrote them" and the six heads differ from before by exactly one added line each, `og:image:type image/jpeg`, legacy's.
Proof, against `pnpm dev` at the published ui 1.2.0. Verify-first: `pageExtensions` lists `mdx`; `resolve-metadata.js` as above; the residue grep printed 9 and the `content/stories` grep 8 before, 0 after. `next build`'s route table lists the six story routes as static pages and no `/stories/[slug]`; `.next/types` was deleted before the gate, as the plan's trap warns, and the regenerated validator names no `[slug]`. `curl` prints 200 on the six routes, `/` and `/stories`, 404 on `/stories/nope`; `/` holds the two story cards and `/stories` six story links. The six heads were saved before the change and diffed after (the scratchpad's `heads-before/` and `heads-after/`): one added line each, `<meta property="og:image:type" content="image/jpeg"/>`, `2022-newsletter` carrying `Grid Beam: 2022 Newsletter`, the shortDescription, `og:type article`, the article times, `article:tag newsletter` and the Cloudinary `og:image` at width 1200 as before. `pnpm audit:dom` over the six routes (12 of 12): each `current.aria.yaml` holds one `main` and one `navigation` (the shell's), no `On this page`, one `article` holding the `h1`, the description, the 2022 cover and every body heading and image; the three guides' `current.txt` holds `Contents` once, as legacy's; between the nav and the footer the text diffs hold only the copy slice's verdicts (Grid Kit to grid beam or Village Kit, the cutting jig link) and the aria diffs only those, the carousel indicators ([[94b21887102b]], [[fee183274ff6]]) and the re-hosted decision-tree id ([[01a2acd9e8af]]). The Playwright probe (the scratchpad's `probe.mjs`, its output beside the pairs as `audit/stories__<slug>/probe.txt`) on both sides at 1280, 768 and 375: one `main`, one `nav`, the `article` holding the h1 and the last `section.vk-section`, the gap from that section to the footer 64px at 1280 and 768 and 32px at 375 on both sides, the title block's first element at padding-top 0 with the h1's own 32px (8px at 375) on both sides, the article's parent `main` with no max-width or top padding, and on 2022 the cover's `Center` at margin-bottom 64px and its `img` `sizes` `(min-width: 768px) 512.00px, 448.00px` equal to legacy's; the cover's `fetchpriority` is [[f9729ac7552a]]'s (`open`, the operator's); no hydration message on any route, `/` and `/stories` probed too. The current side's development console lines are known: React's unknown-prop line for `isInColumn` on `/stories/whats-a-grid-unit`, legacy's own spread (`story-image.tsx:34` into `ui-media`'s `Image`, which declares no such prop either, so the same line in legacy's development build; the attribute reaches neither side's HTML), the phantom cell's missing `src` and `alt` on furniture-bolts ([[0e4002cba0a5]], the operator's) and the HoverCard line on `/` and `/stories` ([[7877c267268c]]). `pnpm audit:pages` at 375, 768 and 1280 looked at on all six routes: no TOC column, the title block and the 2022 cover as legacy's, the residuals the shell's `upstream` items (the container padding [[5c1af396cc2e]], the palette, the headings, the Title and Description containers [[318456ddabc6]], [[c325dd0181d9]]), the dev overlay's indicator, and at 768 the footer ([[c9ea22823a23]]). The Done-when file checks: `ls` fails on both deleted paths, the layout's first line is its `// ported from` URL, `export default withStoriesLayout(story)` once in each of the six. No Vitest test: the plan names no pure seam. Gate: `pnpm dev` stopped by pid and port 3000 confirmed free, `timeout 900 just check` exited 0 (Biome clean over 107 files, `tsc` clean, 78 tests, `next build` prerendering 61 pages, the generated designs data unchanged), run again after the review's TSDoc lines; `kipu verify --warnings-as-errors` green.
The ledger: [[4ab3e21024a0]], [[69e3e2447813]], [[756288b40fd0]], [[7584e6e05aea]], [[e5faf0655462]], [[b5aa272c70e7]], [[aece63987800]], [[4030f9d739c4]], [[9c48718ec6f8]] and [[6401346644c2]] are `fixed` with outcome `plan 4331147cc118`; [[0e005a229aee]] carries this slice's note and stays `open`; [[fcdf83c992a1]] carries its note and stays `sanctioned`; [[ecfa31cea5a0]], [[63baa85427e0]], [[d915091fafea]], [[f7645a164ced]] and [[45fca45340c8]] carry a note mapping their `content/stories` paths to the new files. No verdict written; no `../ui` or `../gridkit` change; nothing parked `upstream`. Left on the six routes after this slice: the `open` rename item, the `upstream` stand-in [[d915091fafea]] and the release milestone's [[45fca45340c8]]. This was the last open child of the record [[56e6eb197e6c]]: finishing it is the next iteration's, the verdicts plan [[14be8f377b34]] standing beside it.
Reviews on three fresh Opus sub-agents (Standards, Spec, Parity): nothing critical. Standards: no hard violation beyond the missing TSDoc on `StoriesLayout` and `withStoriesLayout`, taken (one paragraph each); judgment calls left as legacy's shape, the `children` union (`stories.tsx:16`), the `src` cast and the `||`, and the `metadata` line in `mdx.d.ts` kept as the plan wrote it, the runtime reader being Next. Spec: every upstream claim verified at the pinned versions (the `metadata` fallback, `MDXLayout`, the `Title`'s `h1` default, Chakra's token 16 at 4rem, the type generator's `page.ts` and `page.tsx` filter); taken, the grep and the count into this Outcome and the staging of the TSDoc lines. Parity: every claimed closure holds on all six routes at three widths, no new difference, no copy in the diff; taken, the recapture of the `/` and `/stories` DOM pairs against this tree (two and six story links, the card text identical to legacy's on lines 10 to 53) and the notes on the five items. Dropped: none.

## Log

- 2026-09-26: From the fields slice (plan 52adacea5b2f): with image typed as legacy's RasterImagePropsWithOptionalSizes, the expression getCloudinaryUrl({ src: image.src, width: 1200 }) this plan writes for getStoryPageMetadata fails tsc (TS2322, the local member's src admits a StaticImport). The transient [slug] page writes image.src as string, legacy's own cast on the cloudinary branch of ui-media/src/image.tsx:141, and the helper needs the same.

- 2026-09-26: From the Parity review of the fields slice (plan [[52adacea5b2f]]): legacy's NextSeo block also writes type: image/jpeg on the article image (layouts/stories.tsx:47), which the current generateMetadata never did; filed as [[6401346644c2]] (regression, code, removed), for getStoryPageMetadata to close with the line. Its sibling [[45fca45340c8]] (the article times' dependency on the build machine's timezone) is the release milestone's, not this plan's.
