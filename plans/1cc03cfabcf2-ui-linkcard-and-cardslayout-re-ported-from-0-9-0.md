---
title: ui LinkCard and CardsLayout re-ported from 0.9.0
status: done
parent: a78b167170b8
derived_from: a78b167170b8
blocked_by: 1c74a465996d
tags:
  - "worker:fable"
priority: medium
---
`LinkCard` is 0.9.0's again: a `3xs` by `64` box padded 4 by 8 with its content spread by `space-around`, an inherited-colour icon passed as a component type and exposed as legacy's tree exposes it, an `h2` heading, and the wrapper's `aria-label`; `CardsLayout`'s container is legacy's `container.md` width. Record `a78b167170b8`; decisions `ee86d68a`, `2032533f`, `28c1a536`, `bfa9a416`.

## Work

- Legacy source: `@villagekit/ui@0.9.0 src/components/LinkCard.tsx` (`npm pack` into the scratchpad) and `../node-modules/packages/ui-page/src/components/layouts/CardsLayout.tsx` at `fce357d`. Current: `../ui/src/components/{LinkCard,HoverCard}.tsx`, `layouts/CardsLayout.tsx`, `stories/LinkCard.stories.tsx` if present.
- `852324855146`, `7a92b233069a`: the `HoverCard` at `height: 64`, `paddingX: 4`, `paddingY: 8`, `width: '3xs'`, the `Stack` `justifyContent="space-around"` at full height; no `h="full"` stretch on the `LinkBox`.
- `a01b12fe36e0`: no `color` on the icon.
- `ca0c2398f054`: `icon: React.ComponentType`, required, rendered `<Icon as={IconComponent} w="8" h="8" />`. This site's call sites pass elements today (`grep -rn "icon={<\|icon: <" app`: `app/page.tsx`, `app/about/page.tsx`, `app/legal/page.tsx`, `app/subscribe/page.tsx`, and `app/tools-and-resources/page.tsx` in its data entries), so the bump adapts them: `kipu note 99f2fe62c62f` naming each file and the edit (`icon={<FaCut />}` to `icon={FaCut}`, `icon: <FaCut />` to `icon: FaCut`); with the override in place `pnpm typecheck` is red on those files for that reason alone, and the Outcome lists the errors and confirms nothing else breaks.
- `2c024e4d002a`: the icon exposed as legacy's live tree exposes it, an unnamed `img` before the heading (`audit/legal/dom/legacy.aria.yaml:24`); v3's `Icon` writes `aria-hidden`, so override it. The live tree is the visual and interaction truth (`bfa9a416`).
- `bd05a2d3642d`: `Heading size="md"` with no `as`; confirm v3's `Heading` renders `h2` by default (`node_modules/@chakra-ui/react/dist/esm/components/heading/index.js`) else set `as="h2"`. The routes that put an `h2` above the cards have their own outline items.
- `2cbb4f4b8497`: `aria-label={title}` on the `HoverCard` wrapper as 0.9.0 had; the overlay anchor stays unnamed as on both sides (naming it is the accessibility pass after M2, note `eeba2a65cee4`, not this port).
- `8a3babf21c3c`: `Container maxW` at the v3 token for 768 px (`breakpoint-md`, generated from the breakpoints in `node_modules/@chakra-ui/react/dist/esm/styled-system/token-dictionary.js`; confirm the rendered `--chakra-sizes-breakpoint-md` is `48rem`); the routes' consumption of `CardsLayout` is their records' (`/contact`, `/legal`, `/tools-and-resources`), so the item's note says the width half landed here.
- Sibling steps (decision `28c1a536`): the change lands in `../ui` (`src/...`, and `stories/` where a story renders the component), verified there by `pnpm lint`, `pnpm types` and `pnpm build:pkg`, committed there by pathspec (`git -C ../ui add <paths>` then `git -C ../ui commit -- <paths>`, the message citing the legacy source by its SHA-pinned URL where code is ported) and not pushed; seen on this site through an uncommitted override (`pnpm.overrides` `"@villagekit/ui": "link:../ui"` in `package.json`, then `pnpm install`; the sibling's top-level `exports` point at `src/`, so no build is needed for the override), reverted by path before the commit here (`git restore -- package.json pnpm-lock.yaml`, then `pnpm install --frozen-lockfile`) so the commit never carries it; each item this slice closes moves to `upstream` (`kipu move <id> upstream --from regression`) with a note citing the sibling commit; then `kipu relate 99f2fe62c62f blocked_by <this slice>`. The package version stays: the publish is the operator's.
- Verify on this site through the override on `/legal` and `/tools-and-resources` at 1280 against `audit/legal/1280/legacy.png` and `audit/tools-and-resources/1280/legacy.png` (the cards' size and spread; the routes' own grid items remain).
- Closes (to `upstream`) `852324855146`, `7a92b233069a`, `a01b12fe36e0`, `bd05a2d3642d`, `2c024e4d002a`, `ca0c2398f054`, `2cbb4f4b8497`, `8a3babf21c3c`.
- Not this slice: the routes' layouts around the cards.

## Seams under test

None pure.

## Done when

- with the override in place (and the call sites edited locally to pass component types for the check, reverted with the override): every card on `/legal` and `/tools-and-resources` measures 224 by 256 px with its icon near-black, the tree shows an unnamed `img` before each card's `h2`, and the wrapper carries `aria-label` equal to the title; the override then reverted and `git status --porcelain` clean of the probe
- in `../ui`: `pnpm lint`, `pnpm types`, `pnpm build:pkg` and `pnpm build:storybook` are green and the commit there is by pathspec, its hash in each item's note
- the eight items are `upstream`, `99f2fe62c62f` is `blocked_by` this slice and carries the bump note, checked after the moves
- `timeout 900 just check` is green on this site, without the override

## Outcome

Shipped in `../ui` as commit a4ef8ed on its `main` (not pushed; the push goes with the operator's publish, decision `28c1a536`), by pathspec: `src/components/LinkCard.tsx`, `src/components/layouts/CardsLayout.tsx`, `stories/LinkCard.stories.tsx`, `CHANGELOG.md`. The package version stays 1.2.0.

The card is 0.9.0's again, read line for line against the tarball's `src/components/LinkCard.tsx`: `LinkBox` with no `h="full"`, the `HoverCard` at `height="64" paddingX="4" paddingY="8" width="3xs"` with `aria-label={title}`, the `Stack` at `justifyContent="space-around" gap="4" height="100%"`, `icon: ComponentType` required and rendered `<Icon as={IconComponent} w="8" h="8" />` with no color, `Heading size="md"` with no `as` (Chakra v3's `Heading` is `withContext("h2")` in `components/heading/index.js`, so `h2` by default), `Text textAlign="center"`. Kept from 1.2.0: the `asChild` overlay anchor (so `rel` survives) and the boundary slice's framework link component for an internal href. `CardsLayout`'s `Container` is `maxW="breakpoint-md"`, the size token the v3 token dictionary generates from the `md` breakpoint; the rendered `--chakra-sizes-breakpoint-md` is `768px`, not the `48rem` the plan guessed, the same width at a 16px root. Chakra v3 translations: `sx` to style props, `spacing` to `gap`, `isExternal` to `target` and `rel`, and `aria-hidden={undefined}` on the `Icon`, since v3's `Icon` writes `aria-hidden="true"` before spreading its props (`components/icon/icon.js`), so the icon is exposed as legacy's live tree exposes it. The story passes icon components, keeps the 1.2.0 `WithLinkComponent` guard, drops the no-icon case (the prop is required) and the `3xs` decorator (the card sizes itself); `WithIcon` is named `External` now, a rename nobody asked for and harmless.

The fact the plan lacked: the plan's bump edit (`icon={<FaCut />}` to `icon={FaCut}`) alone makes every route 500 on this site. The five call sites (`app/page.tsx`, `app/about/page.tsx`, `app/legal/page.tsx`, `app/subscribe/page.tsx`, `app/tools-and-resources/page.tsx`) are server components, each exporting `metadata`, and a `react-icons/fa` icon is a plain function, not a client reference, so it cannot cross into the `'use client'` `LinkCard` as a prop (`Error: Functions cannot be passed directly to Client Components`, in the scratchpad's dev log); `linkComponent={NextLink}` crosses because `next/link` is a client module. Legacy's pages router had no such boundary, which is why 0.9.0's shape worked there. The package keeps 0.9.0's shape as the plan asks: the cost is one client module on the site. The probe that works, reverted before this commit: `app/_lib/icons.ts` with `'use client'` and one named re-export of the thirteen icons from `react-icons/fa`, and the five pages importing from it; every route then served 200. The bump note on `99f2fe62c62f` names it exactly and says the module is a helper the legacy site did not have, forced by the app router, for the bump to file as a code difference under the rules (not sanctioned by an agent), unless the route records have re-ported the pages by then. With the override in place and the call sites unedited, `pnpm typecheck` was red on exactly those five files (twelve `TS2322: Type 'Element' is not assignable to type 'ComponentType'`, ten on the element call sites and two on the `ReactNode` entries in `app/tools-and-resources/page.tsx:131,150`) plus the brand footer slice's known `SiteFooter.tsx(122,6)` `socialLinks` error; with the call sites edited, only that footer error remained, so nothing else breaks.

Verified: in `../ui`, `pnpm lint`, `pnpm types`, `pnpm build:pkg` and `pnpm build:storybook` green (the Storybook build's use-client warnings 1010, the baseline). On this site, through `pnpm.overrides["@villagekit/ui"] = "file:../ui"` and the probe: a Playwright measurement of every card on `/legal`, `/tools-and-resources` and `/subscribe` at 1280 on both sides (the scratchpad's `measure-cards.txt`): each current card 224 by 256 with padding `32px 16px`, a 32px icon with no `aria-hidden` in the inherited text color, an `H2` heading, `space-around`, the wrapper's `aria-label` equal to the title, the overlay anchor unnamed, `target` and `rel` on the external cards; legacy the same box, padding, exposure, `H2` and `space-around`. The DOM pairs (`audit/legal/dom/current.aria.yaml`, `audit/tools-and-resources/dom/current.aria.yaml`) show an unnamed `img` before each card's `h2` [level=2]. The screenshot pairs at 375, 768 and 1280 under `audit/{legal,tools-and-resources,subscribe}/` looked at against legacy: the cards are legacy's size and spread. The override reverted by `git restore -- package.json pnpm-lock.yaml` and the probe by path, `pnpm install --frozen-lockfile`, then `timeout 900 just check` green against the published 1.2.0 and `kipu verify --warnings-as-errors` green.

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity). Standards and Spec found the same two things that would mislead: the CHANGELOG's icon entry sent an app-router consumer into the 500 (it now says the icon comes from a client module) and the overlay comment implied the wrapper's `aria-label` named the link (it now says the anchor is kept unnamed for parity and the label on a div names nothing); plus `labelled` to `labeled`, TSDoc on `title`, `description` and `href`, the `CardsLayout` entry under Fixed, plain version numbers; all fixed and verified clean by a second-round Opus sub-agent. Spec also asked that the conflict with item `2cbb4f4b8497`'s earlier note (name the anchor, not the wrapper) be recorded: a note on the item says the plan chose the wrapper for parity and the anchor is the accessibility pass's (note `eeba2a65cee4`). Parity confirmed all eight closures on all five card routes at 375, 768 and 1280 with a Playwright probe (size, padding, spread, color, hover scale and fill, focus-within border, transition, heading level, label, icon exposure all matching legacy) and found one difference the change introduces that no item recorded: descriptions longer than legacy's now clip inside the fixed box (`Site licence` 286px of content in a 252px inner height on `/legal`, `Suppliers directory` and `Source on GitHub` on `/tools-and-resources`, `Email the maintainer` on `/subscribe`, `Suppliers` on `/`), filed as `2774d994ab92`, `e55422c77b65`, `2ea664bcbdd0` and `5ebdc4a4f50c`, `regression` on each route, the copy being the routes' own; and that item `2c024e4d002a`'s Legacy section wrongly said the legacy server HTML hides the card's icon (a note corrects it). Notes on `21916a693c01`, `26646ac53e88`, `6426bf317deb` and `f8654793adfe` say their Current sections describe the 1.2.0 card. Nothing dropped. No visitor-facing copy in the change.

Items: the eight moved to `upstream` with the ui commit in their notes; `99f2fe62c62f` `blocked_by` this slice and carrying the bump note. Not this slice: the routes' layouts around the cards and their consumption of `CardsLayout`.

## Log

- 2026-09-26: From the framework boundary slice (plan 1c74a465996d, ui commit 4e11d57): the mechanism to follow is the framework context in ../ui/src/framework.tsx, filled by NavContextProvider({ items, usePathname?, linkComponent? }) and read through useFramework() by every composite that renders its own anchors; the leaf link components keep the explicit `as`. The override that shows a sibling change on this site is pnpm.overrides["@villagekit/ui"] = "file:../ui" (link: fails under next dev --turbopack), with pnpm install after each sibling edit, reverted by `git restore -- package.json pnpm-lock.yaml` and `pnpm install --frozen-lockfile` before the commit.
