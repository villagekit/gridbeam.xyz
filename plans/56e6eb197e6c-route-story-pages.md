---
title: "Route: story pages"
status: doing
parent: 337e35d86920
blocked_by:
  - a78b167170b8
  - target: ca353de8b645
    strength: soft
    note: route order
---

## Goal

Every page of this record's route family is at parity with the legacy site: every difference on it is `fixed` or `sanctioned`, and the operator reviews it at the parity gate `f7a700a3e482`. Decision `ee86d68a`.

## Scope

The differences on the six story routes in the ledger (`kipu list --collection difference --filter route=<route> --json` for `/stories/whats-a-grid-unit`, `/stories/how-to-cut-grid-beams`, `/stories/how-to-furniture-bolts`, `/stories/building-with-grid-kit`, `/stories/2021-winter-newsletter`, `/stories/2022-newsletter`): sixty-two items at the split, fifty-six `regression` (found by M1 and the shell's slices, twenty-one of them `open` until the stories grilling moved them to `regression`), five `sanctioned` (`01a2acd9e8af`, `854f2a3b4b83`, `fcdf83c992a1`, `989f0154c2f4`, `9d9045a6cfc3`), one `open` (`0e005a229aee`, filed at the split), none `fixed` or `upstream`. The story page template's items are filed on `/stories/whats-a-grid-unit` where first met and hold on all six routes. The port rule of decision `ee86d68a` decides whether each slice re-ports from the legacy source or fixes in place; here the components, the metadata and the page are re-ported, the removals and the copy landing first on the current files.

The removals, nine items, every verdict the operator's from the stories grilling under `dcd8df79` (`9f174b0d4c72`): the editorial notes `d30c02d49d97`, `bf7e1a1934ac`, `ca028cdfb6b7`; the byline `d1e0a048c853`, `c3a9fa723309`, `99ca564d160a`; the component and field `c983ec56248e`, `5577373dbaad`, `6be26304713a`.

The copy verdicts, seven items (`edf6cccdfbf6`): `1ab305e446c7`, `40e89fd36d02`, `e0746ac4eebc`, `a80668016926`, `ef21d53da4cd`, `fc3776853af3`, `cbb90b8648bb`.

The components re-port, twenty-one items (`373320c95e55`): the template `09c3946b1912`, `53e0084c7579`, `6462fcd59f39`, `85c8280822dd`, `16a68e71f5cd`, `363d4781e17c`, `381bb8498503`, `e4646669b7de`, `337dd8db6f52`; the tip `9affc679c88c`; the decision tree `7f10efea3a6e`, `0931cd538b55`; the 2021 newsletter `8df7a085e421`, `d090ce55d1bc`, `e872925977e9`, `17627227d88c`; the 2022 newsletter `67939d89aef4`, `e9c81a1a366f`, `2bb459b9f5db`, `dba160e62bcc`, `edf3beef7bdf`; and, by default, the operator's `0e4002cba0a5`, `e834d73bbbd5`, `5d81b89c4799`.

The inline MDX, five items (`eeeb3813939b`): the install video `d1fb9b4c176c`, `fe647a9b88d8`, `89d04ca61c84`; the creations intro and the closing line `ffe337ac3773`, `8661edb0bbc8`; and, by default, the operator's `8a6daf7fad72`.

The fields, one item (`52adacea5b2f`): `82762f6b27de`, whose notes extend it to the four linked stories.

The page re-port, nine items (`4331147cc118`): the route `9c48718ec6f8`; the landmarks and layout `4ab3e21024a0`, `756288b40fd0`, `7584e6e05aea`, `69e3e2447813`; the table of contents `e5faf0655462`, `b5aa272c70e7`, `aece63987800`, `4030f9d739c4`.

The operator's, on the attended verdicts plan beside this record (`14be8f377b34`): `0e005a229aee` (`open`, the name the app router forces on the six pages), and the four `regression` items `0e4002cba0a5`, `e834d73bbbd5`, `5d81b89c4799`, `8a6daf7fad72`, each also named by the slice that ships legacy's form for it if no verdict lands first.

Sanctioned, not a slice's: `01a2acd9e8af` the re-hosted ids, `854f2a3b4b83` the Chakra v3 renames, `fcdf83c992a1` the app-router metadata (the page re-port notes its new Current), `989f0154c2f4` and `9d9045a6cfc3` the rule 1 swaps.

## Seams under test

None pure: the story components, the metadata and the pages are markup and data; the proof is the DOM pairs, the probes and the screenshot pairs each slice names.

## Exit demo

`kipu list --collection difference --filter route=<route> --not-status sanctioned --not-status fixed --not-status dismissed` prints nothing for each of the six story routes, and `/finish-epic` finishes this record; the operator reviews the route at the parity gate `f7a700a3e482`.

## Out of scope

Other routes; the shell, except where a difference on this route is closed by a shell change, which then belongs to the shell record.

## Outcome

## Log

- 2026-09-26: From the split of the stories index record [[ca353de8b645]] (its Log, call 3): the catalog slice [[48c8cbdb206a]] re-ports app/_lib/stories.ts to legacy's stories.ts in structure (one allStories array, linkedStories inline, no wrapper or accessors) and leaves this record's three items where they are: the field names and date types on StoryMetadata, the six MDX exports and the four linked stories ([[82762f6b27de]], noted there), the originallyPublishedOn field ([[c983ec56248e]]) and the [slug] route's lookup, kept as STORY_SLUGS and getStory below legacy's exports with a comment naming [[9c48718ec6f8]]. This record's split removes that lookup with the route if it re-ports the six pages as page.mdx files, as that item's Log reads.

- 2026-09-26: Split into six slices (plan 56e6eb197e6c), reviewed once on one Opus sub-agent. Against `@villagekit/ui@1.2.0`, in the order the edges give: `9f174b0d4c72` the editorial notes, the byline, the publish date and their component and field removed (Sonnet, nine items, every verdict the operator's from the stories grilling, P2 and P3, under `dcd8df79`); `edf6cccdfbf6` the copy verdicts on the guides (Sonnet, seven items, P4 and P5), after the removals; `373320c95e55` the story components re-ported with legacy's mdx overrides, the Tip and every call site (Fable, twenty-one items and three of the operator's by default), consuming both, the home's `ImageCarousel` (`c92c235205f5`) and the mounted `MediaProvider` (`6f90e7e24ca6`); `eeeb3813939b` the install video, the two Descriptions and the unused imports in the MDX (Fable, five items and one of the operator's by default), consuming the components; `52adacea5b2f` the metadata fields re-ported to legacy's (Fable, one item), consuming the removals, the components and the card (`e332105c3b52`), after the inline MDX; `4331147cc118` the six pages re-ported as `page.mdx` routes under legacy's `StoriesLayout` (Fable, nine items), last, consuming the components, the inline MDX, the fields and the shell's layout slice (`a7bf623f885c`). Every `regression` on the six routes, fifty-six, is closed by exactly one slice, fifty-two of them outright and four (`0e4002cba0a5`, `e834d73bbbd5`, `5d81b89c4799`, `8a6daf7fad72`) named both by a slice's default and by the verdicts plan `14be8f377b34` beside this record, where the plan holds the question and the slice the form legacy's port gives if no verdict lands first; one `open` item filed at the split, `0e005a229aee`, is the plan's too; the five `sanctioned` items need no slice. Calls made at the split, for the operator to read: (1) the components and the page are re-ported, not fixed in place: the current template has a `next/image` wrapper, a `SimpleGrid` in a named region, static grids for the carousels, a controlled video, a TOC layout, a `3xl` container and a dynamic `[slug]` route over `content/`, where legacy has the `ui-mdx` media in its column context, nested stacks, `react-responsive-carousel`, a silent loop, one `article` in the main layout and six MDX pages; the port rule says in doubt re-port, and the removals and the copy land first on the current files, on Sonnet, so the re-ports carry settled text; (2) six slices, cut where a reviewer could reject one while approving its neighbor: the removals (a component, a field, a byline), the copy (strings), the components with their call sites (one directory and every MDX line that calls it, the newsletters' carousels included, since the grid's legacy API would otherwise force a transient rewrite of their calls), the inline JSX (a library under React 19 and two `Description`s), the fields (one module and its five readers), the page (the route's shape); (3) the mdx media exports and the `full` and `container.lg` size names are in the sibling only (`d750c0a47839`, `252edab16c7a`, `upstream` at 1c3e3e8) and no slice consumes a publish (`28c1a536`), so the components slice stands them in with a site-local media module and pixel literals, the about's mechanism (`36dc54eb5955`) widened from a literal to a module of three components, filing one item moved to `upstream` with a bump note; `StoryVideo` then renders the ui `Video` of 1.2.0 now, so the bump plan's clauses (1) and (3) from the mdx and media slice are superseded by that note and `getCloudinaryVideoUrl` leaves the site with it; (4) the `[slug]` route: six `page.mdx` files are legacy's shape and the app router renders them (MDX's default export as the layout, `@next/mdx` under Turbopack), but Next reads a page module's `metadata` export as the page's metadata and refuses `generateMetadata` beside it (`resolve-metadata.js:294-303`, the SWC check), so the story's data export takes another name (`story`) with a `metadata` built from it, or the article Open Graph the sanctioned `fcdf83c992a1` keeps is lost; the page re-port ships the six pages with the rename and closes `9c48718ec6f8` outright (its M1 note asks nothing of the operator, and keeping the route under rule 4 would be the stretch `2032533f` warns against), and the rename is filed now as `0e005a229aee` (`open`, code, a rule 4 candidate) on the verdicts plan, the stories index split's shape for `e3a2d4d66691`, so a verdict before the re-port runs shapes it; (5) four `regression` items carry M1 notes asking the operator before legacy's form is restored (rule 5): the phantom sixth cell (`0e4002cba0a5`; Next 15.5 sets `unoptimized` for a missing `src` and does not throw), the carousels' library-named controls and cloned slides (`e834d73bbbd5`, `5d81b89c4799`, which return with the carousel the interaction items need), and the unused imports (`8a6daf7fad72`, restored where they resolve, the one line naming an export `ui-mdx` never had, `VideoWrapper`, left out and noted); all four go on the verdicts plan, and the slices ship legacy's form if no verdict lands (the faq split's call 6); the accessibility pass after M2 (note `eeba2a65cee4`) is where the carousel's names would be bettered, as for `337dd8db6f52` and `89d04ca61c84` (P5); (6) `@u-wave/react-youtube` is a class component with no `'use client'`, and the MDX modules are evaluated in the server layer, where React's server build exports no `Component`, so the inline slice imports it through a one-line `'use client'` re-export module, the LinkCard bump note's boundary for the icons, a rule 4 translation and not a workaround; (7) legacy's `Tip` override of the blockquote (`9affc679c88c`) is site-side on 1.2.0's `BlockSection` and needs no publish; it bypasses the package's blockquote, so the bump's check for `f9b9ec72d771` reads the sibling's Storybook, not a story page (the components slice's note); (8) three items' Legacy text is corrected by a note as the slices meet them: `09c3946b1912` (legacy's call sites do pass `isInColumn` and `aspectRatio={null}` on eleven images), `363d4781e17c` (the live site's grid tiles sit 8px apart, Chakra's default stack gap on both sides, not touching) and `ef21d53da4cd` (the three h4s sit under `What Sizes To Use` and before the h3, not under it); (9) the file names: legacy's lowercase `story-*.tsx` become the site's PascalCase `Story*.tsx`, the index re-port's translation (`Item.tsx` for `item.tsx`), and `mdx.tsx` keeps its name; the stand-in is `media.tsx`; the layout is `app/_components/layouts/StoriesLayout.tsx`; the client boundary is `app/_components/YouTube.tsx`; (10) the translations: `sx` to `css`, `spacing` to `gap`, `colorScheme` to `colorPalette`, `container.lg` to `breakpoint-lg` on the `Section` and to a pixel literal inside a `sizes`, `MDXProvider` to `useMDXComponents`, `NextSeo` to a static `metadata` from a helper, `MainLayout` to the root layout, `withStoriesLayout` kept as MDX's default export. Expected on the pairs until the bump, the shell's `upstream` items: the container padding (`5c1af396cc2e`), the Section band (`9746fd6890c2`), the palette and shadows (`72b776cb0d3f`, `f45538f480af`), the headings (`77cd1426ac65`, `7124898563eb`), the inline links (`12a2882546e6`), the Title and Description containers (`318456ddabc6`, `c325dd0181d9`), the MDX link's tab and `rel` (`63baa85427e0`, `d04a1827b838`); at 768 the footer (`c9ea22823a23`, the operator's). Items already the operator's: the five on the verdicts plan. Expected stops: none for the slices; at the finish the record finishes on `40abdb2f222a` with the verdicts plan standing and the stand-in item `upstream`, waiting on the bump plan. Review findings taken, all twenty-two: the react-youtube class component and its client boundary; legacy's `isInColumn` and `aspectRatio={null}` call sites (twenty-one lines, eleven images) and the item's wrong Legacy text; the cover call's missing `type` between the components and the fields slices (the components slice writes it); the grid's 8px gap on the live site and the item's wrong Legacy text; `9c48718ec6f8` off the verdicts plan and the rename item filed now; the page re-port's six unnamed items named; the removals' grep scoped away from the card's date; the h4s' place before the h3 and the item's note; the two carousel items onto the verdicts plan; the 2022 Description named as the creations intro; the Turbopack type generator and the stale validator trap; the components slice's count; the closing link's href slash, legacy's with a note; the in-column `sizes` example (`100.00vw`); the furniture-bolts grid's missing `aspectRatio`; `/` and `/stories` checked by the page re-port and the helper's home if `'use client'` is forced; the import-block diff's `withStoriesLayout` line; the stand-in named as widened from the about's; the `published_time` wording; the worker-tag reasons on the inline and fields slices; six line-number cites and the blockquote's `{...props}`; a soft edge from the fields slice to the inline MDX. Dropped: none.
