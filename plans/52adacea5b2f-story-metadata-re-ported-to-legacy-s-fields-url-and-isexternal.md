---
title: "Story metadata re-ported to legacy's fields: url and isExternal, image.type and Date values, on the six exports, the linked stories and their readers"
status: done
parent: 56e6eb197e6c
derived_from: 56e6eb197e6c
blocked_by:
  - 9f174b0d4c72
  - 373320c95e55
  - e332105c3b52
  - target: eeeb3813939b
    strength: soft
    note: "the same MDX files: the inline JSX first"
tags:
  - "worker:fable"
---
The story metadata is legacy's shape again on all ten stories: `url` (the path under `/stories`, or the external address) and `isExternal` in place of `slug` and `external`, `image.type: 'cloudinary'` on every cover, and `publishedAt` and `updatedAt` as `Date` values built with legacy's slash form (`new Date('2024/10/10')`, parsed as local time, so a card's date reads the same day in every timezone and the hydration text mismatch west of UTC goes). `StoryMetadata` in `app/_lib/stories.ts` takes legacy's fields, the four linked stories legacy's `url` and `isExternal: true`, and every reader takes legacy's lines: the card's href, icon and date, the stories context's sort, the sitemap, and the story page's metadata and static params until the page re-port replaces them. One item, [[82762f6b27de]], whose notes extend it to the linked stories; a shape decision at a module three routes and the sitemap read, whose `Date` values cross a server boundary, so Fable. Record `56e6eb197e6c`; decisions `ee86d68a`, `2032533f`.

## Work

- Legacy at `fce357d`: `../node-modules/apps/gridkit/stories.ts:1,10-25,34-99` (the type, `image: RasterImagePropsWithOptionalSizes`, `linkedStories`), the six `pages/stories/*.mdx` metadata blocks (`whats-a-grid-unit.mdx:5-19` and the other five, `type: 'cloudinary'` before `src`, `publishedAt: new Date('2024/10/10')`), `components/stories/item.tsx:30,49,90-94,98,101-105`, `context/stories.tsx:32`, `components/layouts/stories.tsx:40-41,62`. Current: `app/_lib/stories.ts` (the catalog re-port `48c8cbdb206a` kept the site's names for this record), `content/stories/*.mdx` as the earlier slices leave them, `app/_components/stories/Item.tsx`, `app/_lib/context/stories.tsx`, `app/sitemap.ts`, `app/stories/[slug]/page.tsx`, `app/page.tsx`, `app/HomePage.tsx`.
- `app/_lib/stories.ts`: `StoryMetadata` as legacy `:14-25`, `url: string`, `image: RasterImagePropsWithOptionalSizes` (from `@villagekit/ui`, which 1.2.0 exports), `isExternal?: boolean`, `publishedAt: Date`, `updatedAt: Date`, the field order legacy's; `linkedStories` with `url` (the external address) and `isExternal: true` in legacy's per-story field order (`:35-98`), `type: 'cloudinary'` on each `image`, `new Date('2017/09/17')` and the other dates as legacy wrote them; the made-up slugs and the `external` objects go. The residue below `allStories` (`hostedStories`, `STORY_SLUGS`, `getStory`) takes `url.slice(1)` as the slug until the page re-port removes it.
- The six MDX `metadata` blocks: `url: '/<slug>'` where `slug` stood, `type: 'cloudinary'` as the first key of `image`, `publishedAt` and `updatedAt` as `new Date('YYYY/MM/DD')` with legacy's values; the ids stay re-hosted ([[01a2acd9e8af]]).
- The readers, each legacy's line: `app/_components/stories/Item.tsx` destructures `url, isExternal` (`:30`), spreads `{...image}` on the ui `Image` with the explicit `type="cloudinary"` gone (`:49`), prints `publishedAt.toLocaleDateString(...)` (`:90-94`), shows the icon on `isExternal` (`:98`), and renders the overlays as `isExternal ? ... href={url} ... : <LinkOverlay as={NextLink} href={`/stories${url}`} ...>` (`:101-105`; the external overlay keeps the `asChild` shape the card slice wrote, `ed16aad27638`); `app/_lib/context/stories.tsx` sorts on `b.publishedAt.getTime() - a.publishedAt.getTime()` (`:32`); `app/sitemap.ts` filters on `!story.isExternal`, writes `${SITE_URL}/stories${story.url}` and `lastModified: story.updatedAt`; `app/stories/[slug]/page.tsx` writes `publishedTime: publishedAt.toISOString()` and `modifiedTime: updatedAt.toISOString()` (legacy `layouts/stories.tsx:40-41`) and renders the cover as legacy's `<StoryImage {...image} priority />` (`:62`), the `type="cloudinary"` the components slice wrote on the call now coming from the spread; `app/page.tsx` and `app/HomePage.tsx` change nothing but what compiles (the two `as unknown as StoryMetadata` casts stay; `mdx.d.ts` is untouched). The `Date` values cross the server-to-client boundary as props of `HomePage`, which React 19's Flight serializes; see `/` render its two cards on `pnpm dev`.
- Interfaces: consumes `StoryImage` reading `type` from its props (the components slice) and `Item` (`e332105c3b52`); produces legacy's `StoryMetadata` for the page re-port (`publishedAt.toISOString()`, `{...image}` with `type`, `url`).
- Verify first: `grep -rln 'slug\|external' app/_lib/stories.ts app/_components/stories/Item.tsx app/sitemap.ts 'app/stories/[slug]/page.tsx' content/stories` prints those files (the three modules, the page and the six MDX files) and no other; `grep -c 'Date.parse' app/_lib/context/stories.tsx` prints 1; `grep -rn "slug: '\|external: {\|publishedAt: '" content/stories app/_lib/stories.ts | wc -l` prints 24; `grep -n 'RasterImagePropsWithOptionalSizes' node_modules/@villagekit/ui/dist/index.d.ts` prints the export.
- Docs: none.
- Not this slice: the route and the page (the page re-port, which consumes this); `originallyPublishedOn` (gone with the removals slice); the covers' ids.

## Seams under test

None pure: the module is data and the readers are one line each; the proof is the DOM pair of `/stories`, the sitemap and a timezone probe.

## Done when

- Against a running `pnpm dev`, `pnpm audit:dom --routes <a file naming /stories and />`: every card's date on `/stories` reads as `legacy.txt` has it (`29/11/2024` first, `11/03/2003` last), the four linked cards' overlays carry legacy's external `href`s and the six hosted ones `/stories/<slug>`, and `/` renders its two story cards; a Playwright probe with the browser west of UTC (`timezoneId: 'America/Los_Angeles'`) reads the same ten dates on `/stories` and no hydration mismatch in the console, where the ISO strings printed the previous day (the note on the item)
- `curl -s localhost:3000/sitemap.xml | grep -c '<loc>[^<]*/stories/'` prints 6; each of the six story routes and `/` serve 200; `curl -s localhost:3000/stories/whats-a-grid-unit | grep -o 'article:published_time" content="[^"]*"'` prints an ISO instant (local midnight of the build machine as UTC, legacy's `toISOString()` reading; before the change it printed the bare date string `2024-10-10`)
- `grep -rn "slug: '\|external: {\|publishedAt: '" content/stories app/_lib/stories.ts | wc -l` prints 0; `grep -c "type: 'cloudinary'" app/_lib/stories.ts` prints 4 and each of the six MDX files 1 in its metadata; `grep -c 'Date.parse' app/_lib/context/stories.tsx` prints 0; `grep -c 'type="cloudinary"' app/_components/stories/Item.tsx` prints 0
- [[82762f6b27de]] is `fixed` (`kipu fix 82762f6b27de --outcome "plan <this prefix>"`), its outcome naming the linked stories' fields and the readers changed; checked after the fix
- `timeout 900 just check` is green

## Outcome

Shipped as scoped: the story metadata is legacy's shape on all ten stories. `app/_lib/stories.ts` types `StoryMetadata` as legacy's `stories.ts:14-25` (`url: string`, `image: RasterImagePropsWithOptionalSizes` from `@villagekit/ui` 1.2.0, `isExternal?: boolean`, `publishedAt: Date`, `updatedAt: Date`, in legacy's order) and writes the four linked stories as legacy's `:35-98` key for key (`type: 'cloudinary'` between `src` and `width`, `isExternal: true`, `new Date('2017/09/17')` and the other dates in the slash form, the external address as `url`), the made-up slugs and the `external` objects gone; the residue below `allStories` (`hostedStories`, `STORY_SLUGS`, `getStory`) reads `url.slice(1)` as the slug until the page re-port removes it. The six MDX `metadata` blocks carry `url: '/<slug>'` where `slug` stood, `type: 'cloudinary'` as the first key of `image`, and `publishedAt` and `updatedAt` as `new Date('YYYY/MM/DD')` with legacy's values; the re-hosted ids stay ([[01a2acd9e8af]]) and no visible string changed. The readers take legacy's lines: `Item.tsx` destructures `url, isExternal` (`item.tsx:30`), spreads `{...image}` with the explicit `type="cloudinary"` gone (`:49`), prints `publishedAt.toLocaleDateString(...)` (`:90-94`), shows the icon on `isExternal` (`:98`) and picks the overlay on `isExternal` with `href={url}` (the `asChild` `chakra.a` shape of the card slice kept) or `` href={`/stories${url}`} `` (`:101-105`); the context sorts on `getTime()` (`context/stories.tsx:32`); the sitemap filters on `!story.isExternal` and writes `/stories${story.url}` and `story.updatedAt`; the `[slug]` page writes `publishedAt.toISOString()` and `updatedAt.toISOString()` (`layouts/stories.tsx:40-41`) and renders the cover as `<StoryImage {...image} priority />` (`:62`), the `type` now coming from the spread. `app/page.tsx`, `HomePage.tsx` and `mdx.d.ts` are untouched, the two casts stay, and `metadata` is not renamed (`0e005a229aee`, the page re-port's).

One line outside the plan's letter, from the type: with `image` typed as legacy's union, `getCloudinaryUrl({ src: image.src, width: 1200 })` on the `[slug]` page fails `tsc` (TS2322: the local member's `src` admits a `StaticImport`, `@villagekit/ui/dist/components/media/Image.d.ts:20-33`, and `getCloudinaryUrl` takes `src: string`), so the page writes `image.src as string`, legacy's own cast on the cloudinary branch of `ui-media/src/image.tsx:141`; a note on the page re-port plan [[4331147cc118]] says its helper, which writes the same expression, needs the same. One reading for the record: the slash form keeps a card's date the same in every timezone where the catalog module runs again in the browser, which `/stories` does (the context and `StoriesPage.tsx` are client modules); a `Date` that crosses Flight from a server component arrives as the server's local midnight as a UTC instant (`react-server-dom-turbopack-server.node.development.js:3231-3236` writes `$D` and the client reads `new Date(Date.parse(...))`), which is `/`'s case, where both cards render `showDate={false}` as legacy's `index.tsx:302-303` did, so nothing prints a date that crossed the boundary.

Proof, against `pnpm dev` at the published 1.2.0. Verify-first: the ten files, 1, 24 and the export at `dist/index.d.ts:53`. `pnpm audit:dom` over `/stories` and `/` (4 of 4): `audit/stories/dom/current.txt` lines 10 to 53 are identical to `legacy.txt`'s (`How to Mark and Cut Grid Beams` and `29/11/2024` first, `Shelter` and `11/03/2003` last), the remaining diff the shell's nav and footer, the cookie banner and the copy verdict; `audit/_root/dom/current.txt` holds the two story cards. The Playwright probe (`scratchpad/probe.mjs`, its output beside it) with `timezoneId` `America/Los_Angeles` and again `Pacific/Auckland`: the same ten dates on `/stories` in both zones, the ten overlays with the four external `href`s and `target="_blank"` and the six `/stories/<slug>`, `/` with its two cards, and no console message matching hydration on either route in either zone. `curl`: the sitemap prints 6 story `<loc>`s, none linked; the six story routes, `/stories` and `/` serve 200 and a linked slug 404; `/stories/whats-a-grid-unit` prints `article:published_time" content="2024-10-09T11:00:00.000Z"` (local midnight of the build machine, on NZST, as UTC), where it printed the bare `2024-10-10` before. The Done-when greps: 0; 4 and 1 in each of the six MDX files; 0; 0. `pnpm audit:pages` at 375, 768 and 1280 looked at for `/stories`, `/` and `/stories/2022-newsletter`: the ten cards in legacy's order with legacy's dates and the external icon on the four linked cards only, the home's two cards, the 2022 cover under its title; the residuals the shell's `upstream` items (the container padding `5c1af396cc2e`, the palette and headings, the Title and Description containers), the copy verdict, the TOC sidebar (the page re-port's) and at 768 the footer (`c9ea22823a23`). No Vitest test: the module is data and each reader is one line; the plan names no pure seam, and both reviewers agreed.

The ledger: [[82762f6b27de]] is `fixed` with outcome `plan 52adacea5b2f`, naming the linked stories' fields and the readers changed. No verdict written; no `../ui` or `../gridkit` change; nothing parked `upstream`. Gate: the dev server stopped by pid and port 3000 confirmed free, `timeout 900 just check` exited 0 (Biome clean over 107 files, `tsc` clean, 78 tests, `next build` prerendering the six story pages, the generated designs data unchanged); `kipu verify --warnings-as-errors` green. Not the last open child: the page re-port `4331147cc118` follows, the verdicts plan standing beside the record.

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity): nothing critical. Standards: no hard violation, no invariant decision to check; three judgment calls, the twice-written `url.slice(1)` in the residue (left: the residue is transient and the page re-port deletes it), the `src` cast (legacy's, recorded above) and the engine-defined slash parse and the build-machine timezone on the article times (legacy's behavior, asked by the plan). Spec: every load-bearing claim verified at source (the union type, legacy's cast, V8's local parse of the slash form, Flight's `$D` branch); taken, the cast and the Flight reading into this Outcome. Parity: every claimed closure holds on `/stories` at 1280 and 375, `/stories/2022-newsletter` at 1280 and the `/` DOM pair (the ten overlays with legacy's hrefs and the external ones' `target` and `rel`, the linked covers' `src` and `sizes` identical to live legacy, the card dates plain text with no `time` as legacy's, the cover spread passing the same five props and the same `sizes` and preload), no copy in the diff, the residuals the shell's `upstream` items and the dev overlay; one critical finding taken as a filing, since the fix is not this slice's call: the article Open Graph times now depend on the build machine's timezone, where the live legacy page serves UTC midnight (`2024-10-10T00:00:00.000Z`, Vercel building in UTC) and a build on the operator's NZST machine serves the previous day (`2024-10-09T11:00:00.000Z`), the same shift on the sitemap's `lastmod`, verified on both sides by curl and filed as [[45fca45340c8]] (`regression`, code, `/stories/whats-a-grid-unit`) with a note on the release milestone [[a4df2bf79395]], whose Cloudflare build is where a `TZ=UTC` pin or the operator's verdict lands; and one pre-existing difference no item recorded, legacy's `og:image:type` `image/jpeg` (`layouts/stories.tsx:47`) absent on the six story heads, filed as [[6401346644c2]] (`regression`, code, `removed`) with a note on the page re-port [[4331147cc118]], whose helper closes it. The cover's missing `fetchpriority` is [[f9729ac7552a]]'s (`open`, the operator's); the re-hosted `og:image` id is [[01a2acd9e8af]]'s. No code changed after the reviews, so the gate stands. Dropped: none.

## Log
