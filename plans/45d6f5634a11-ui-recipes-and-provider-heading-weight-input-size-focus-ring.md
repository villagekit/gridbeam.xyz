---
title: "ui recipes and provider: heading weight, input size, focus ring, field focus colour, accordion, Link isExternal, the Toaster"
status: done
parent: a78b167170b8
derived_from: a78b167170b8
tags:
  - "worker:fable"
priority: medium
---
Eight small fixes to `@villagekit/ui`'s recipes and wrappers restore what its 0.9.0 theme did under Chakra v2: the normal heading weight, the 48 px `lg` input, one cyan focus ring with no gray outline over it, the focus border colour on `Select` and `NumberInput`, the accordion's hover fill and uniform dashed separators, `Link`'s `isExternal`, and the toast regions mounted by the provider. Record `a78b167170b8`; decisions `ee86d68a`, `2032533f`, `28c1a536`.

## Work

- Legacy source: `@villagekit/ui@0.9.0` (`npm pack` into the scratchpad) `src/components/{Heading,Button,Select,NumberInput,Accordion,Link}.tsx`, and Chakra v2's input sizes and accordion theme in `@chakra-ui/theme@3.3.1`. Current: `../ui/src/components/{Heading,Input,Select,NumberInput,Accordion.recipe,Link,Button}.tsx`, `src/theme/index.ts`, `src/Provider.tsx`.
- `7124898563eb`: `headingRecipe.base.fontWeight: 'normal'`; the rendered weight stays the 600 face, the only one loaded.
- `6b5488415a06`: an `input` recipe (shared by the native select and the number input where v3 shares it) whose `lg` matches v2's: height 12 (48 px), font size lg, padding x 4, radius md; measured on `/designs`' search bar.
- `6a18067acb3b`: the global `:focus-visible` outline Chakra v3 adds is neutralised for the ui's controls, so buttons, icon buttons, selects and inputs show only the `outline` shadow (`rgba(0,163,196,0.5) 0 0 0 2px` on buttons), measured by Tab-focusing each control on `/tools/cutting-planner`.
- `dc60b9b9c1bf`: `Select` and `NumberInput` set the focused field's border to the theme's `outlineColor` as 0.9.0's `focusBorderColor` did (v3: the field's focus ring colour).
- `cb528cbc05e6`, `cb9dce13acba`: `itemTrigger._hover.bg: 'blackAlpha.50'` with the common transition; the item's border as one declaration, v3's `outline` variant neutralised so the 2 px dashed top rule and the last item's bottom rule are the only borders (`audit/faq/1280/legacy.png` against the current capture).
- `ecfa31cea5a0`: `LinkProps.isExternal` back on `Link`, rendering `target="_blank" rel="noopener noreferrer"` (the ui `MdxLink` computes its own; no story MDX passes the prop today, so a Link story or a scratch render is the check).
- `f93e37cf6ef4`: `Provider` mounts a `Toaster` (Chakra v3's `createToaster`) and exports the toaster so a route can create toasts; the regions appear in the tree as legacy's did. `Provider` also takes an optional `system` prop (default the package's), legacy's shape where the site passed its extended theme to the ui's `ChakraProvider`: the site theme slice mounts a bare `ChakraProvider value={system}` today because 1.2.0's `Provider` takes none, so `kipu note 99f2fe62c62f` that the bump swaps `app/layout.tsx`'s `ChakraProvider value={system}` for `Provider system={system}`, which is what brings the toast regions back on the site.
- Sibling steps (decision `28c1a536`): the change lands in `../ui` (`src/...`, and `stories/` where a story renders the component), verified there by `pnpm lint`, `pnpm types` and `pnpm build:pkg`, committed there by pathspec (`git -C ../ui add <paths>` then `git -C ../ui commit -- <paths>`, the message citing the legacy source by its SHA-pinned URL where code is ported) and not pushed; seen on this site through an uncommitted override (`pnpm.overrides` `"@villagekit/ui": "link:../ui"` in `package.json`, then `pnpm install`; the sibling's top-level `exports` point at `src/`, so no build is needed for the override), reverted by path before the commit here (`git restore -- package.json pnpm-lock.yaml`, then `pnpm install --frozen-lockfile`) so the commit never carries it; each item this slice closes moves to `upstream` (`kipu move <id> upstream --from regression`) with a note citing the sibling commit; then `kipu relate 99f2fe62c62f blocked_by <this slice>`. The package version stays: the publish is the operator's.
- Verify on this site through the override: the `/faq` accordion at 1280, `/designs`' input height, `/tools/cutting-planner`'s focus rings, and the notification regions in `audit/_root/dom/current.aria.yaml` (with `app/layout.tsx` probed to `Provider system={system}` if the site theme slice has shipped, the probe reverted with the override).
- Closes (to `upstream`) `7124898563eb`, `6b5488415a06`, `6a18067acb3b`, `dc60b9b9c1bf`, `cb528cbc05e6`, `cb9dce13acba`, `ecfa31cea5a0`, `f93e37cf6ef4`.
- Not this slice: the palette literals (its own ui slice); the mobile menu (the ui nav slice).

## Seams under test

None pure.

## Done when

- with the override in place: the `/designs` search input is 48 px tall; a Tab-focused button on `/tools/cutting-planner` has `outline` none and the cyan `box-shadow`, and its select and number inputs show the cyan focus border; a `Link isExternal` rendered in `../ui`'s storybook or a scratch page carries `target="_blank" rel="noopener noreferrer"`; the accordion rows on `/faq` show one 2 px dashed rule between items and a `blackAlpha.50` fill on hover; `audit/_root/dom/current.aria.yaml` lists the notification regions; the override then reverted
- in `../ui`: `pnpm lint`, `pnpm types` and `pnpm build:pkg` are green and the commit there is by pathspec, its hash in each item's note
- the eight items are `upstream`, `99f2fe62c62f` is `blocked_by` this slice and carries the bump note, checked after the moves
- `timeout 900 just check` is green on this site, without the override

## Outcome

Shipped in `../ui` as commit 540e9c3 on its `main` (not pushed; the push goes with the operator's publish, decision `28c1a536`), by pathspec: `CHANGELOG.md`, `src/Provider.tsx`, `src/Toaster.tsx` (new), `src/index.ts`, `src/theme/index.ts`, `src/components/{Heading,Input,Select,NumberInput,Button,Link}.tsx`, `src/components/Accordion.recipe.ts`, `stories/{Input,Link}.stories.tsx`. The package version stays 1.2.0.

What changed, each against the 0.9.0 source in the tarball and Chakra v2's theme (`@chakra-ui/theme@3.3.1`, `npm pack` into the scratchpad) and read back from the merged system (`createSystem(defaultConfig, config)` in `../ui`, bundled with the store's esbuild and printed with `system.css`): `headingRecipe` weighs `normal` and its `md` is `xl` on a 1.2 line, `lg` `2xl` to `3xl` at `md`, v2's; an `inputRecipe` with v2's four sizes (`lg` 48px at font `lg`, `md` 40px, `sm` 32px, `xs` 24px, padding 4/4/3/2, radius `md` and `xs`) and `--focus-color` set to the theme's `outlineColor`, the outline variant's v3 focus ring replaced by v2's focused border and one-pixel shadow; a `numberInputRecipe` mirroring it, since Chakra v3 copies its own input recipe into the number input's when the package loads, before the merge, and a `nativeSelectRecipe` with its own sizes and the same focus; `buttonRecipe` `focusVisibleRing: 'none'` so the `outline` shadow is the only ring (icon buttons share the recipe); `linkRecipe` with v2's `common` 150ms transition on v2's ease-out curve, written literally since v3's `ease-out` token is `cubic-bezier(0, 0, 0.58, 1)` where v2's was `(0, 0, 0.2, 1)`, `focusRing: 'none'` and v2's `_focusVisible` shadow; `Link` takes `isExternal` again, rendering `rel="noopener"`, what Chakra v2's `Link` rendered (the item's Legacy line said `noopener noreferrer`, corrected by a note); the accordion slot recipe defaults to `plain` (no 1px bottom rule), fills the trigger `blackAlpha.50` on hover with the `common` transition, squares it (`--accordion-radius: 0`), shows the `outline` shadow on keyboard focus, inherits its weight, sets the `md` size's `--accordion-padding-y` to `4` since v3's size variant overrides a base `paddingY` (the 1.2.0 recipe's `paddingY: 4` never landed: measured 8px under it), and pads `itemBody` 4 in place of `itemContent`, the box v3 animates; `Provider` takes `system` (default the package's) and mounts a `Toaster` (Chakra's toaster snippet, `createToaster({ placement: 'bottom' })`, v2's default position), `toaster` exported. The fonts comment in `src/theme/index.ts` now says the two ways an app supplies its families; the tokens keep the `var(--font-body, Bitter)` form, since dropping the variables would be a breaking change for a consumer this site cannot see, and the site overrides the tokens anyway.

Verified: in `../ui`, `pnpm lint`, `pnpm types`, `pnpm build:pkg` and `pnpm build:storybook` green (the Storybook build's `"use client"` directive warnings 1011, from 1009). On this site with the `file:../ui` override and `app/_components/SiteProvider.tsx` probed to `Provider system={system}` (both reverted before the commit), a Playwright probe (`measure-recipes.mjs` in the scratchpad) read the same properties on `pnpm dev` and on the live legacy site: the `/designs` search input 48px tall at 18px, radius 6px, on both; its focus `outline: none` with the cyan `0 0 0 1px` shadow and border on both; the `/designs` selects at 375 40px at 16px, radius 6px, on both; the `/tools/cutting-planner` `Add row` button Tab-focused with `outline: none` and `rgba(0,163,196,0.5) 0 0 0 2px` on both, its select and number input with the cyan border and one-pixel shadow on both; the `/faq` items 2px dashed above and 2px below the last only on both, the trigger hover `rgba(0,0,0,0.04)` on both with the 0.2s `common` transition, radius 0 on both; a footer link Tab-focused on `/faq` with `outline: none`, the cyan 2px shadow and the 150ms transition on `cubic-bezier(0, 0, 0.2, 1)` on both; the `/faq` trigger at weight 400 on both; the footer `Heading size="md"` 20px on a 24px line at weight 400 on both; the story page's first `h2` 30px on 36px at 1280 and 24px on 31.92px at 375 on both; `audit/faq/dom/current.aria.yaml` lists `region "Notifications, bottom (alt+T)"` where legacy's lists six `Notifications-<placement>` regions. The `/faq` trigger padding is 12px here against legacy's 16px (the route's `paddingY="3"` over the recipe's 4) and the panel 28px against 16px (the route's `paddingY="3"` on `ItemContent` stacked on the recipe's `itemBody` padding): the route's own props, item `6c31193ec773`, which carries a note. The screenshot pairs at 375, 768 and 1280 under `audit/{faq,designs,tools__cutting-planner,stories__whats-a-grid-unit}/`. The home page could not be measured under the override: it 500s on the LinkCard slice's `icon` type (`Element type is invalid`), the site-side edit the bump makes, so the footer and the regions were read on `/faq`.

Deviations and findings: the heading recipe's `lg` was one step below v2's too (the story pages' `h2`), found while porting `md`, filed as `0369f14e4df4` (regression, shell) and closed here; links showed Chakra v3's gray outline on any focus where legacy showed the `outline` shadow on keyboard focus, in the family of `6a18067acb3b`, filed as `dba0ac6ac25d` and closed here; the planner's top-up select is `size="sm"` where legacy's was the default `md`, the route's own, filed as `df4fbe144cb3` on `/tools/cutting-planner` for its record. The input recipe ports all four sizes, not `lg` alone, since v2's number input and select took the input theme's sizes and a half port would leave the planner's `sm` controls on v3's scale. The radius item `f40107b60034` is left `regression` with a note: this slice pins v2's radii on the inputs, selects and number inputs, which are the controls the site renders on v3 recipes, and the token scale itself (v3's `2xs`, `xs`, `4xl`, `l1` to `l3` and `sm` at 0.25rem in the inline CSS) is not the recipes' to fix, since rewriting `sm` would move every v3 recipe that reads `l2` further from v2, so it waits on a rule 4 verdict, the operator's. `da830eb83c42` was closed by the brand footer slice (commit ae593d0) and needed no provider default. The toast regions are one where legacy had six, Ark's one region per toaster, filed as `ee7575aea3ae` (regression, shell, accessibility) for the operator's rule 4 verdict; the story pages' MDX links still write `rel="noopener noreferrer"` themselves where legacy's `isExternal` gave `noopener`, filed as `d04a1827b838` for the mdx slice `bc0407533650`, with a note on that plan. The reinstall gotcha: `file:../ui` copies the sibling at install time, so a sibling edit after `pnpm install` is not on `pnpm dev` until the next install (the trigger weight read 500 from a stale copy until then).

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity), then a second round on a fourth. Spec found the `rel` claim wrong against Chakra v2's `Link` source and the live site (fixed to `noopener`), the Input docstring's mechanism wrong for the native select (reworded), the link curve a residue (written literally) and the toast regions a residue with no item (filed). Parity found the `/faq` panel padding stacking (noted on the route's item), the regions and the curve (as above), and the trigger weight claim false against a stale install (reinstalled and re-measured: 400 on both sides); it found `/faq`, `/designs`, `/tools/cutting-planner` and `/stories/whats-a-grid-unit` at parity at 375, 768 and 1280 beyond the items already recorded. Standards found no hard violation; its judgment calls taken: the theme comment no longer names this site, the input's focus block sits in the recipe base so every variant and the number input share one idiom (with comments on why the link and the accordion silence v3's ring their own way), `Toaster` is not exported (the provider mounts it; `toaster` is the API), the loading spinner takes the current color, the Input story's cast is gone, the exported recipes' headers are TSDoc; its call that the Chakra toaster snippet is the use of a normative reference, not a copy, stands with the docs URL in the file. SECOND.

Items: the eight of the Closes line and `1c6d12f592b3`, `77cd1426ac65`, `0369f14e4df4`, `dba0ac6ac25d` moved to `upstream` with the ui commit in their notes; `f40107b60034`, `ee7575aea3ae`, `d04a1827b838` and `df4fbe144cb3` left `regression` with notes naming who judges or owns them; `99f2fe62c62f` `blocked_by` this slice and carrying the bump note (`SiteProvider.tsx` to `Provider system={system}`). No visitor-facing copy in the change.

## Log

- 2026-09-26: From the framework boundary slice (plan 1c74a465996d, ui commit 4e11d57): the mechanism to follow is the framework context in ../ui/src/framework.tsx, filled by NavContextProvider({ items, usePathname?, linkComponent? }) and read through useFramework() by every composite that renders its own anchors; the leaf link components keep the explicit `as`. The override that shows a sibling change on this site is pnpm.overrides["@villagekit/ui"] = "file:../ui" (link: fails under next dev --turbopack), with pnpm install after each sibling edit, reverted by `git restore -- package.json pnpm-lock.yaml` and `pnpm install --frozen-lockfile` before the commit.

- 2026-09-26: The site theme slice [[39b1a28bb0cc]] filed [[f40107b60034]] (Chakra v3's radius scale where legacy kept v2's) for this slice's theme change.

- 2026-09-26: Found by the Spec review of the site theme slice [[39b1a28bb0cc]]: with the site's families written into the tokens, the ui's `var(--font-body, Bitter)` and `var(--font-heading, Fredoka)` tokens and the comment above them in `src/theme/index.ts` (consumers expose the fonts as CSS variables on html) are dead for this site; the package's theme is this slice's to reword.
