---
title: Story components re-ported from legacy's components/story, with the mdx overrides, the Tip and every story call site
status: done
parent: 56e6eb197e6c
derived_from: 56e6eb197e6c
blocked_by:
  - 9f174b0d4c72
  - edf6cccdfbf6
  - c92c235205f5
  - 6f90e7e24ca6
tags:
  - "worker:fable"
---
The story components are legacy's `components/story/` again, translated for Chakra v3 and the published `@villagekit/ui@1.2.0`: `StorySection` at legacy's `container.lg` width, `StoryRow` on `useIsMobile`, `StoryColumn` with no vertical centering, `StoryImage` wrapping the mdx `Image` with the column sizes from `useIsInColumn` and the `svg` branch, `StoryImageGrid` as nested stacks of bare `RasterImage`s over `numRows` and `numColumns`, `StoryImageCarousel` over the site's re-ported `ImageCarousel` with one 4:3 slide, the arrows, the dots and the loop, `StoryVideo` on the ui `Video` (a silent autoplaying loop, no controls), and legacy's two MDX overrides: the story `h2` centered, and the blockquote rendered as the `Tip` (a lightbulb `BlockSection` at `maxWidth: lg`), wired through `mdx-components.tsx` as legacy's `MDXProvider` was. Every story call site in the six MDX files takes the components' legacy props back (`type='cloudinary'`, `aspectRatio={null}` and `isInColumn` where legacy wrote them, the grids' `sizes`, the newsletters' twelve carousels, the decision tree's `RasterImage`). What the published package lacks until the operator's publish (the mdx `Image`, `Video` and `MediaContainer`, and the `full` and `container.lg` size names, all in the sibling at 1c3e3e8) is stood in by a site-local media module and pixel literals, filed as one item moved to `upstream` with a bump note, the about's mechanism widened from a literal to a module. Twenty-one items on five story routes, and three of the operator's on the verdicts plan ([[0e4002cba0a5]], [[e834d73bbbd5]], [[5d81b89c4799]]) whose legacy form ships by default unless judged by then. A re-port that decides every translation, so Fable. Record `56e6eb197e6c`; decisions `ee86d68a`, `2032533f`, `ca677697`, `28c1a536`.

## Work

- Legacy, all at `fce357d` under `../node-modules/apps/gridkit/`: `components/story/{index.ts,mdx.tsx,story-column.tsx,story-image.tsx,story-image-carousel.tsx,story-image-grid.tsx,story-row.tsx,story-section.tsx,story-video.tsx}`, `components/mdx/{index.tsx,blockquote.tsx}`, `components/tip.tsx`, and the six `pages/stories/*.mdx` bodies; for the stand-in, `../node-modules/packages/ui-mdx/src/{MediaContainer,Image,Video}.tsx`. Current: `app/_components/story/*` as the removals slice leaves it, `mdx-components.tsx`, `content/stories/*.mdx` as the copy slice leaves them, `app/_components/ImageCarousel.tsx` (the home's re-port `c92c235205f5`, consumed unchanged), `app/_lib/cloudinary.ts`, and one line of `app/stories/[slug]/page.tsx` (the cover call, below).
- The components at `app/_components/story/`, each under its `// ported from` header, the file names the site's PascalCase for legacy's lowercase (the index re-port's translation, `Item.tsx` for `item.tsx`): `StorySection.tsx` [[16a68e71f5cd]]: `maxW="breakpoint-lg"` (1024px, the size token Chakra v3 generates from the `lg` breakpoint, the name the ui Description slice `c06d8381c4b5` wrote for legacy's `container.lg`, rule 4), `colorPalette` and `gap={[8, null, 12]}` (the sanctioned [[854f2a3b4b83]]); `StoryRow.tsx` [[53e0084c7579]], [[e4646669b7de]]: `useIsMobile()` from `@villagekit/ui` picking `column`, `row` or `row-reverse`, `gap={[4, null, 12]}`, no `alignItems`; `StoryColumn.tsx` [[e4646669b7de]]: `alignItems="center" gap="4"` and `flex: 1`, no `justifyContent`; `StoryImage.tsx` [[09c3946b1912]]: legacy's `story-image.tsx` on the mdx `Image` (the stand-in below): the `aspectRatio` const `'standard'`, `baseSx` as `css` (radius `xl`, shadow `md`, `objectFit: cover`), `useIsInColumn()` from `@villagekit/ui` setting the column `sizes` with legacy's `NOTE (mw)` comment, the `svg` branch, no prop of its own for `isInColumn`, `aspectRatio` or `priority`: what a call passes arrives in `...rest` after the const and wins (`story-image.tsx:34`), which is how legacy's eleven `aspectRatio={null}` calls keep their native ratio (the item's Legacy text says the call sites never pass `isInColumn`, which holds for `how-to-cut-grid-beams.mdx` alone; `whats-a-grid-unit.mdx:34,58,72,100,118,218,232,245,259` pass it with `aspectRatio={null}`, and `how-to-cut-grid-beams.mdx:359,380` pass `aspectRatio={null}`: `kipu note` the item); `StoryImageGrid.tsx` [[6462fcd59f39]], [[363d4781e17c]], [[337dd8db6f52]]: props `images`, `sizes: { base: { numRows, numColumns }, md: { numRows, numColumns } }`, `aspectRatio`, `useBreakpointValue(sizes)` with legacy's throw, a `VStack` of `HStack`s of `RasterImage` (from `@villagekit/ui`) with `imageSizes` and `css={{ minWidth: 0, minHeight: 0, flex: '1 1 0' }}`, no `section`, no `ariaLabel` prop (legacy's grid declared none, so the label the MDX passes is dropped, as [[337dd8db6f52]]'s Legacy text says), no radius or shadow, and no `gap` written: legacy's stacks took Chakra v2's default spacing, 8px, and Chakra v3's `HStack` and `VStack` default to the same `0.5rem` (`node_modules/@chakra-ui/react/dist/esm/components/stack/stack.js:20`), so the line-for-line port matches the live legacy site's 8px between tiles ([[363d4781e17c]]'s Legacy text says the tiles touch, which the live site does not show: `kipu note` the item with the reading); `StoryImageCarousel.tsx` [[d090ce55d1bc]], [[2bb459b9f5db]], [[17627227d88c]], [[edf3beef7bdf]], [[e9c81a1a366f]], and the operator's [[e834d73bbbd5]] and [[5d81b89c4799]] (the library's control names and cloned slides return with the carousel unless a verdict landed): legacy's `story-image-carousel.tsx` over `ImageCarousel` from `app/_components/ImageCarousel.tsx`: `isInColumn`, `aspectRatio ?? '4 / 3'` as `slideCss`, the column sizes or `useMediaMaxWidthBreakpoints()`, inside `MediaContainer`, the rest of the props (`ariaLabel`, `slides`, `priority`) spread through; `StoryVideo.tsx` [[e872925977e9]], [[dba160e62bcc]], [[8df7a085e421]]: legacy's `story-video.tsx` on the mdx `Video` (the stand-in): `isInColumn` destructured into an `extraProps` block that is never spread, line for line with legacy's comment (the note on [[8df7a085e421]]: legacy builds the object and never spreads it, so the port keeps the dead block and the item closes on legacy's shape), `aspectRatio="standard"`, `css` with the radius, shadow and `objectFit`; the `controls`, `preload`, the wrapping `Box` and the mp4-only source go; `mdx.tsx`: legacy's `components/story/mdx.tsx`, the `h2` override writing `alignSelf: 'center'` under the caller's `css` over the site's base map [[381bb8498503]]; `index.ts`: legacy's barrel, `export *` of the seven. [[85c8280822dd]] closes with the set: the seven components and `mdx.tsx`, a carousel again, no `StoryEditorialNote` (the removals slice's).
- The site's mdx map, legacy's `components/mdx/` and `tip.tsx`: `app/_components/mdx/index.tsx` spreading `mdxComponents` from `@villagekit/ui/mdx` (1.2.0) with the `blockquote` override; `app/_components/mdx/blockquote.tsx` rendering `<Tip {...props} css={{ alignSelf: 'center', maxWidth: 'lg' }}>` with legacy's `|| null` child and its comment (`blockquote.tsx:11-16`); `app/_components/Tip.tsx` on the ui `BlockSection` (1.2.0 exports it, `css` for `sx`) with `FaRegLightbulb` from `react-icons/fa` [[9affc679c88c]]. `mdx-components.tsx` returns `{ ...components, ...storyMdxComponents }` from `app/_components/story/mdx`: the app router's `useMDXComponents` is where legacy's `<MDXProvider components={mdxComponents}>` (`layouts/stories.tsx:66`) mounts (rule 4, the mechanism [[fcdf83c992a1]] sanctions); the six story files are the only MDX on the site (`find . -name '*.mdx' -not -path './node_modules/*'` prints the six), so the story map is the site map. The package's `a` (1.2.0's `MdxLink`: the shell's [[63baa85427e0]] and [[d04a1827b838]], `upstream`) is spread unchanged, as legacy's `components/mdx/index.tsx` spread `ui-mdx`'s; the package's blockquote ([[f9b9ec72d771]], `upstream`) is overridden on this site as legacy's was, so the bump plan's check for it no longer reads through a story page: `kipu note 99f2fe62c62f` says so (the package's blockquote is then checked on the sibling's Storybook `mdx` story, not on this site).
- The stand-in, the about's mechanism ([[36dc54eb5955]] stood in a literal; this stands in three components too) since no slice consumes a publish (`28c1a536`): `app/_components/story/media.tsx`, a port of `packages/ui-mdx/src/{MediaContainer,Image,Video}.tsx` at `fce357d` onto `@villagekit/ui@1.2.0`'s media `Image` and `Video` (which 1.2.0 exports; the card and the home render them through the mounted `MediaProvider`, `6f90e7e24ca6`): `MediaContainer` (a `Center` at `maxW` by `useBreakpointValue` over `useConst`), `useMediaMaxWidthBreakpoints` (`{ base: 'md', md: 'lg' }`, names 1.2.0's `useSizeWidths` knows), and `Image` and `Video` wrapping the media components with those sizes as the default; its header comment names it a stand-in for `@villagekit/ui/mdx`'s exports (sibling commit 1c3e3e8, [[d750c0a47839]]) until the publish. The size names 1.2.0's hooks throw on ([[252edab16c7a]]: `useSizeWidths` names `3xs` to `8xl` only): write `'100%'` for `'full'` and `'1024px'` for `'container.lg'` wherever legacy wrote them in this slice's files (StoryImage's and StoryImageCarousel's column sizes, StoryImageGrid's `imageSizes`, the decision tree's `sizes` in furniture-bolts), each rendering the identical `sizes` attribute (the home's [[152511f71ef4]]: `useImageSizes` maps a `%` to `vw` and a pixel literal to itself, `dist/components/media/hooks.js`, so `{ base: '100%', md: ['1024px', 2] }` renders `(min-width: 768px) 512.00px, 100.00vw` as legacy's `{ base: 'full', md: ['container.lg', 2] }` does on the live site's in-column images). File one item on `/stories/whats-a-grid-unit` (code, `changed`, `regression` at the mint) with Legacy (the `ui-mdx` exports and the size names, cited) and Current (the stand-in module and the literals, each call site cited), move it to `upstream` (`kipu move <id> upstream --from regression`) with a note citing ui commit 1c3e3e8, and `kipu note 99f2fe62c62f` with the bump's site edits: delete `media.tsx`, import `Image`, `Video`, `MediaContainer` and `useMediaMaxWidthBreakpoints` from `@villagekit/ui/mdx` in `StoryImage`, `StoryVideo` and `StoryImageCarousel`, swap the literals back to `'full'` and `'container.lg'` (the check: `grep -rn "'100%'\|'1024px'" app/_components/story content/stories app/stories` printing nothing), then `kipu fix` the item. The same note supersedes two clauses of the mdx and media slice's note there: clause (1), since `StoryVideo` is on the ui `Video` from this slice and the bump's edit is the import path alone, and clause (3), since `getCloudinaryVideoUrl`, its options interface and the header paragraph about it leave `app/_lib/cloudinary.ts` here, `StoryVideo` being its only reader ([[b397b0deb1cc]]'s site half; `CLOUDINARY_NAME` and `getCloudinaryUrl` stay). The video ids `gridbeam.xyz/stories/...` render through the mounted provider as the bump probe read them (every video and poster URL 200); no `posterSrc` is passed, as legacy passed none.
- The six MDX bodies, each call site as legacy's line, the ids the sanctioned re-hosted ones ([[01a2acd9e8af]]: `gridbeam.xyz/...` stays where legacy wrote `v1/gridkit.nz/..._xxxx.jpg`) and the copy as the copy slice left it: `<StoryImage type='cloudinary' ...>` on every image, `isInColumn` and `aspectRatio={null}` written where legacy wrote them and nowhere else (legacy's `isInColumn` lines: nine `StoryImage` calls in `whats-a-grid-unit.mdx`, the two carousels in the 2021 newsletter and the ten in the 2022 newsletter, twenty-one in all; the current files pass it on fifty lines); the two grids as legacy `whats-a-grid-unit.mdx:131-134` (`sizes={{ base: { numRows: 3, numColumns: 2 }, md: { numRows: 2, numColumns: 3 } }} aspectRatio='1/1'`) and `how-to-furniture-bolts.mdx:117-119` (the same `sizes`, no `aspectRatio`), the `ariaLabel` still written; the newsletters' twelve galleries as `<StoryImageCarousel isInColumn ariaLabel slides={[...]}>` with legacy's slide objects (`type`, `src`, `alt`, `width`, `height`, the compost toilet's `orientation: 'portrait'`), `priority` on the 2022 opening gallery ([[e9c81a1a366f]]); the 2022 closing team-robot image as legacy `:577-585` ([[67939d89aef4]]: no `isInColumn`, the context sizing it); the three 2021 and the one 2022 `StoryVideo` calls as legacy's (`title`, `src`, `aspectRatio` where written, no `isInColumn`); the decision tree as legacy `how-to-furniture-bolts.mdx:308-310`: `<Link variant='paragraph' href={getCloudinaryUrl({ src, width: 1200, quality: 90 })} isExternal>` around `<RasterImage type='cloudinary' sizes={{ base: '100%', md: '1024px' }} quality={90} ...>` from `@villagekit/ui` ([[7f10efea3a6e]], [[0931cd538b55]]), `getCloudinaryUrl` from `@/app/_lib/cloudinary` as now. Imports: the story components from `@/app/_components/story` as now, `Link` from `@villagekit/ui`. The cover call in `app/stories/[slug]/page.tsx` takes `type="cloudinary"` beside its `src`, `alt`, `width` and `height`, since the mdx `Image` renders nothing without a `type` and the type requires it; the fields slice then spreads `{...image}` and the page re-port moves the call into the layout.
- The operator's three, on the verdicts plan `Story pages: the operator's verdicts`, each shipping legacy's form and closing with this slice unless the operator has sanctioned it by the time this runs (then the current form for that item alone, the Outcome citing the verdict): [[0e4002cba0a5]], legacy's grid renders `images[5]` on furniture-bolts, an undefined entry, as a `RasterImage` with no `src`, an unnamed `img` after the five, and Next 15.5's `getImgProps` sets `unoptimized` for a missing `src` without throwing (`node_modules/next/dist/shared/lib/get-img-props.js:276-280`) while the client logs `Image is missing required "src" property` in development (`node_modules/next/dist/client/image-component.js:150`); [[e834d73bbbd5]] and [[5d81b89c4799]], the carousel's `previous slide / item` and `next slide / item` buttons, the `slide item` indicators and the loop's cloned slides, the library's own. Say which happened for each.
- Translations, each with one fitting form: `sx` to `css`, `spacing` to `gap`, `colorScheme` to `colorPalette`, `container.lg` to `breakpoint-lg` on the Section and to the pixel literal inside a `sizes`, `StackDirection` to `StackProps['direction']`, `MDXProvider` to `useMDXComponents`, `'use client'` on a file that calls a hook; any other named in the Outcome with the form chosen.
- Interfaces: consumes `ImageCarousel` (`c92c235205f5`), the mounted `MediaProvider` (`6f90e7e24ca6`), and `BlockSection`, `Column`, `Row`, `Section`, `useIsInColumn`, `useIsMobile`, `useBreakpointValue`, `Image`, `RasterImage` and `Video` from 1.2.0; produces `StoryImage` (whose spread `{...image}` takes the `type` the fields slice adds) and the story map for the page re-port.
- Verify first: `grep -c 'MediaContainer' node_modules/@villagekit/ui/dist/mdx/index.d.ts` prints 0 and `grep -c 'BlockSection\|useIsInColumn\|useIsMobile\|RasterImage' node_modules/@villagekit/ui/dist/index.d.ts` prints at least 4; `grep -c "full\|container" node_modules/@villagekit/ui/dist/hooks/useSizeWidths.js` prints 0; `grep -c 'StoryImageGrid' content/stories/2022-newsletter.mdx` prints 11 (the import and ten galleries) and `grep -c 'react-responsive-carousel' package.json` prints 1; `grep -rn getCloudinaryVideoUrl app content` prints the helper and `StoryVideo` alone; `grep -c isInColumn ../node-modules/apps/gridkit/pages/stories/*.mdx` prints 9, 2 and 10 on whats-a-grid-unit and the two newsletters and 0 elsewhere.
- Docs: none; CLAUDE.md's Structure names `app/_components/` already.
- Not this slice: the YouTube embed and the two `Description` blocks (`Story pages: the install video on react-youtube, the creations intro and the closing line as Descriptions, and the unused legacy imports restored in the MDX`); the metadata fields and `image.type` on the six `metadata` exports (the fields slice); the page, its layout, the TOC and the cover's place (the page re-port); the unused legacy imports ([[8a6daf7fad72]], the operator's); the shell's `upstream` items expected on the pairs: the container padding [[5c1af396cc2e]], the Section band [[9746fd6890c2]] (the tinted sections' dashed rules), the palette and shadows [[72b776cb0d3f]], [[f45538f480af]], the headings [[77cd1426ac65]], [[7124898563eb]], the inline links [[12a2882546e6]], the Title and Description containers [[318456ddabc6]], [[c325dd0181d9]], the MDX link's tab and `rel` [[63baa85427e0]], [[d04a1827b838]]; at 768 the footer [[c9ea22823a23]].

## Seams under test

None pure; the proof is the DOM pairs (text and aria tree), a Playwright probe of the computed styles, the carousels and the videos on both sides, the screenshot pairs, and the eye on `pnpm dev`.

## Done when

- Against a running `pnpm dev`, `pnpm audit:dom --routes <a file naming the six story routes>`: on `/stories/whats-a-grid-unit` and `/stories/how-to-furniture-bolts` `current.aria.yaml` shows each grid as a run of `img`s with no `region`, as `legacy.aria.yaml:63-68` and `:66-70`, on furniture-bolts six `img`s after `What Sizes To Use` with the sixth unnamed as `legacy.aria.yaml:71` (or five, if the verdict landed); on the two newsletters each gallery as `legacy.aria.yaml` has it, a `list` of slides with the loop's clones, the `previous slide / item` and `next slide / item` buttons and the `slide item` indicators (`:65-84,89-105` on 2021, `:31-369` on 2022), no `region` (or as the verdicts say); on `/stories/how-to-cut-grid-beams` the tip exposed as a `paragraph` and no `blockquote` node; every `current.txt`, between the title block and the footer, matching `legacy.txt` line for line but for the lines other slices of this record own, named in the Outcome (the copy verdicts, the byline the removals slice took, the two `Description` sentences, the TOC)
- A Playwright probe on both sides at 1280 and 375, saved beside the pairs: on whats-a-grid-unit the story sections' container `max-width` 1024px; every `h2` of the body centered in its column (its box's center within 1px of the column's); the `Base Units` column's `justify-content` `normal` and its first text's top edge level with its image's top edge; a story image's `border-radius` 16px with the `md` shadow, the eleven `aspectRatio={null}` images at their native ratio, and each `img`'s `sizes` attribute equal to the live legacy site's on the same image (`(min-width: 768px) 512.00px, 100.00vw` in a column); the `Made With The Grid` tiles 8px apart with square corners and no shadow (the live site's reading: x 144, 477.33 and 810.67 at a width of 325.33 at 1280); on 2021 the first gallery one 4:3 slide in a rounded, shadowed frame, six list items for four slides, the `ul.slider` moving by `translate3d` after a click on `next slide / item`; on how-to-cut the tip's box centered in the column at legacy's width (the item's reading, 472px at 1280) with a lightbulb path; each story `video` carrying `autoplay`, `loop`, `muted` and `playsinline`, no `controls`, no `poster`, and webm, mp4 and ogv sources whose URLs answer 200
- `pnpm audit:pages --routes <the same file>` at 375, 768 and 1280 looked at against legacy: the centered section headings, the top-aligned rows, the square tiles, the carousels' frames, the videos mid-motion, the tip; every residual a `shell` item in `upstream`, the operator's `c9ea22823a23`, an item another slice of this record owns (named), or filed
- `head -1` of the seven component files, `mdx.tsx`, `app/_components/mdx/index.tsx`, `app/_components/mdx/blockquote.tsx` and `app/_components/Tip.tsx` prints each file's `// ported from` line; `head -1 app/_components/story/media.tsx` names the stand-in; `grep -rn 'controls\|getCloudinaryVideoUrl\|aria-label={ariaLabel}\|SimpleGrid\|NextImage' app/_components/story app/_lib/cloudinary.ts | wc -l` prints 0; `grep -c isInColumn content/stories/*.mdx` prints 9, 2 and 10 on whats-a-grid-unit and the two newsletters and 0 on the other three, legacy's counts; `grep -c 'aspectRatio={null}' content/stories/*.mdx` prints 9 on whats-a-grid-unit, 2 on how-to-cut and 0 elsewhere
- The twenty-one items are `fixed` (`kipu fix <id> --outcome "plan <this prefix>"`), and the operator's three `fixed` too or as their verdicts say; [[09c3946b1912]] and [[363d4781e17c]] carry their notes; the stand-in item filed and `upstream` with its notes on it and on the bump plan; checked after the moves
- `timeout 900 just check` is green

## Outcome

Shipped as scoped: the story components are legacy's `components/story/` again at `app/_components/story/`, each under its `// ported from` header and the site's PascalCase name: `StorySection.tsx` (`maxW="breakpoint-lg"`, `colorPalette`, `gap={[8, null, 12]}`), `StoryRow.tsx` (`useIsMobile` picking `column`, `row` or `row-reverse`, `gap={[4, null, 12]}`, no `alignItems`), `StoryColumn.tsx` (`alignItems="center" gap="4"`, `css={{ flex: 1 }}`, no `justifyContent`), `StoryImage.tsx` (legacy's `story-image.tsx` on the stand-in `Image`: the `'standard'` const, the radius, shadow and `objectFit`, `useIsInColumn` setting the column sizes under legacy's `NOTE (mw)`, the `svg` branch, no prop of its own for `isInColumn`, `aspectRatio` or `priority`), `StoryImageGrid.tsx` (`sizes` of `numRows` and `numColumns` per breakpoint, `useBreakpointValue` with legacy's throw, a `VStack` of `HStack`s of bare `RasterImage`s with `imageSizes` and `flex: '1 1 0'`, no `section`, no `ariaLabel`, no radius, shadow or `gap`), `StoryImageCarousel.tsx` (over the home's `ImageCarousel`, `slideCss={{ aspectRatio: aspectRatio ?? '4 / 3' }}`, the column sizes or `useMediaMaxWidthBreakpoints()`, inside `MediaContainer`), `StoryVideo.tsx` (on the stand-in `Video`, `isInColumn` destructured into the `extraProps` block that is never spread, `aspectRatio="standard"`, the radius, shadow and `objectFit`; the `Box`, `controls`, `preload` and mp4-only source gone), `mdx.tsx` (the `h2` centered over the site's base map) and `index.ts` (legacy's barrel). The site's mdx map: `app/_components/mdx/index.tsx` spreading `mdxComponents` from `@villagekit/ui/mdx` with the `blockquote` override, `app/_components/mdx/blockquote.tsx` rendering the `Tip` at `alignSelf: 'center', maxWidth: 'lg'` with legacy's `|| null` child and comment, `app/_components/Tip.tsx` on 1.2.0's `BlockSection` with `FaRegLightbulb`; `mdx-components.tsx` returns `{ ...components, ...storyMdxComponents }`, where legacy's `MDXProvider` mounted. The stand-in `app/_components/story/media.tsx` ports `packages/ui-mdx/src/{MediaContainer,Image,Video}.tsx` onto 1.2.0's media `Image` and `Video`, its header naming it a stand-in until the publish; `'100%'` and `'1024px'` stand in for `'full'` and `'container.lg'` at the five sites the plan names. `getCloudinaryVideoUrl`, its options interface and the header paragraph about it left `app/_lib/cloudinary.ts`. The cover call in `app/stories/[slug]/page.tsx` takes `type="cloudinary"`.

The six MDX bodies: every `StoryImage` call carries `type='cloudinary'` (3, 8, 7, 16, 8 and 9 per file, legacy's counts) and every slide and grid image `type: 'cloudinary'` (7, 37, 5 and 6, legacy's minus the metadata's); `isInColumn` on the nine `whats-a-grid-unit` images and the twelve newsletter carousels and nowhere else (9, 2, 10, 0, 0, 0; the current files had it on fifty lines); `aspectRatio={null}` on nine and two (whats-a-grid-unit, how-to-cut); the two grids with legacy's `sizes` (`aspectRatio='1/1'` on whats-a-grid-unit, none on furniture-bolts) and their `ariaLabel` still written; the twelve galleries as `StoryImageCarousel` with `isInColumn`, `ariaLabel` and legacy's `slides` (the compost toilet's `orientation: 'portrait'` back, `priority` on the 2022 opening gallery); the four videos and the 2022 closing image without `isInColumn`; the decision tree as `<Link variant='paragraph' href={getCloudinaryUrl({ ..., width: 1200, quality: 90 })} isExternal>` around `<RasterImage type='cloudinary' sizes={{ base: '100%', md: '1024px' }} quality={90} ...>`. A diff of each file against legacy with the src ids normalized leaves only the other slices' lines: the import blocks, the metadata (`slug`, ISO dates, `image.type`, the fields slice), the sanctioned re-hosted ids, the copy verdicts, the YouTube box (the inline slice's), the two `Description`s (the inline slice's; on 2021 the closing line and the team image sit in a `StoryRow`/`StoryColumn` the current file wraps them in, so that image takes column sizes until the inline slice restores the `Description`), and the dropped legacy imports (the operator's [[8a6daf7fad72]]). No visitor-facing text changed.

Translations, each with one fitting form: `sx` to `css`, and on `StoryImage` to the array form `css={[baseCss, css]}` (Chakra v3's `css` prop takes an array, the form the package's own `RasterImage` uses, since the caller's `css` may itself be an array); `spacing` to `gap`; `colorScheme` to `colorPalette`; `container.lg` to `breakpoint-lg` on the `Section` and to `'1024px'` inside a `sizes`; `StackDirection` to `StackProps['direction']`; `MDXProvider` to `useMDXComponents`; `'use client'` on `StoryRow`, `StoryImage`, `StoryImageGrid`, `StoryImageCarousel`, `media.tsx` (hooks) and `Tip.tsx` (the `FaRegLightbulb` component crosses from the server-evaluated MDX map into the client `BlockSection` as a prop, so the boundary sits on `Tip`, the LinkCard bump note's boundary for icons); `@ts-ignore` to `@ts-expect-error` on the `h2` override; `images[i]` to `images[i]!` in the grid, since `noUncheckedIndexedAccess` types the cell as possibly undefined and the assertion keeps legacy's line and its runtime (the phantom sixth cell). One deviation from the plan's letter, from the mechanism: the plan has the `h2` override writing `alignSelf: 'center'` under the caller's `css`, but Chakra v3 resolves the `css` prop before the style props (`node_modules/@chakra-ui/react/dist/esm/styled-system/use-resolved-props.js:49`, `css(cvaStyles, ...cssStyles, propStyles)`), so the base map's `alignSelf="flex-start"` would beat it; the override writes `alignSelf="center"` as a style prop before the caller's spread, which keeps legacy's precedence (the caller wins), and the probe reads every story `h2` at `align-self: center` and centered in its column on both sides.

The operator's three: no verdict had landed on [[0e4002cba0a5]], [[e834d73bbbd5]] or [[5d81b89c4799]] when this ran, so legacy's form ships and each closes: the furniture-bolts grid renders the sixth cell as an unnamed `img` with no `src` after the five (both `aria.yaml`s, and the served HTML's `<img decoding="async" data-nimg="1" ... />`); the twelve carousels expose `react-responsive-carousel`'s `previous slide / item` and `next slide / item` buttons and the loop's cloned first and last slides (six `listitem`s for four slides on 2021's first gallery, eleven for nine on 2022's GridBot Hex, both sides).

Proof, against `pnpm dev` at the published 1.2.0. The verify-first greps printed 0, 4, 0, 11, 1, the helper and `StoryVideo`, and 9, 2, 10. `pnpm audit:dom` over the six routes (12 of 12): each `current.txt` matches `legacy.txt` between the title block and the footer but for the copy verdicts, the TOC list (the page re-port's) and the shell's nav and footer; on whats-a-grid-unit and furniture-bolts the grids are a run of `img`s with no `region`, furniture-bolts' six after `What Sizes To Use` with the sixth unnamed as `legacy.aria.yaml:71`; on the newsletters each gallery is a `list` of slides with the clones between the two buttons; on how-to-cut the tip is a `paragraph` and no `blockquote` node exists on either side. A Playwright probe on both sides at 1280 and 375 (`audit/probe-373320c95e55/probe.json`): every story section's container `max-width` 1024px; every body `h2` `align-self: center` with its center on its column's (delta 0); every column `justify-content: normal` with its first child's top level with the row's image top; every image's `border-radius` 16px with `object-fit: cover`, and the `sizes` attribute identical on both sides for all 133 images across the six routes (`(min-width: 768px) 512.00px, 100.00vw` in a column, `341.33px, 33.33vw` on the grids, `1024.00px, 100.00vw` on the decision tree; the one exception the 2021 team image the inline slice's wrapper puts in a column); the eleven `aspectRatio={null}` images at their attribute ratio; the Made With The Grid tiles at x 144, 477.33 and 810.67 with width 325.33 on legacy and 32, 354.66 and 677.33 with width 314.66 here, 8px apart on both, `border-radius: 0px`, no shadow; on 2021 the first carousel six `.slide`s for four slides, `.carousel` at 16px radius with the `md` shadow, the first slide at `4 / 3`, and the `ul.slider` moving from `-472` to `-944` (legacy) and `-456` to `-912` (here) after a click on `next slide / item`; on how-to-cut the tip 472px wide in a 472px column on legacy and 456 in 456 here, `max-width: 512px`, centered in its column on both, with a lightbulb `path`; each of the four story `video`s `autoplay loop muted playsinline`, no `controls`, no `poster`, 448px wide at `4 / 3` or `16 / 9`, with webm, mp4 and ogv sources every one answering 200 (`w_512` at 1280, `w_448` at 375, the same transformations as legacy's). `pnpm audit:pages` at 375, 768 and 1280 looked at for the six routes: the centered section headings, the top-aligned rows, the square tiles, the carousels' frames, arrows and dots, the videos, the tips; the residuals the TOC sidebar (the page re-port's `4331147cc118`), the shell's `upstream` items (the container padding `5c1af396cc2e` narrowing every column by 16px, the Section band's dashed rules `9746fd6890c2`, the shadow values `f45538f480af`, the headings `77cd1426ac65` and `7124898563eb`), the dev overlay's issue badge, and at 768 the footer (`c9ea22823a23`). The Done-when greps: `head -1` prints the eleven headers and the stand-in's; the residue grep prints 0; `isInColumn` 9, 2, 10 and 0 elsewhere; `aspectRatio={null}` 9, 2, 0.

The ledger: the twenty-one items and the operator's three are `fixed` with outcome `plan 373320c9`; [[09c3946b1912]] and [[363d4781e17c]] carry their correction notes; [[d915091fafea]] filed on `/stories/whats-a-grid-unit` (code, `changed`) for the stand-in, moved `regression` then `upstream` with a note citing ui commit 1c3e3e8, and the bump plan [[99f2fe62c62f]] carries the site edits, the grep and the two superseded clauses. One item the DOM pairs found that the plan did not foresee: [[94b21887102b]] (`open`, accessibility, `changed`, `/stories/2021-winter-newsletter`), legacy's carousel indicators in the tree as `img "slide item"` where Chakra v3's `Icon` writes `aria-hidden="true"` on the same line of the home's `ImageCarousel` (the hero shows no indicators, so the carousel slice could not see it); the same mechanism as the shell's [[89301ca8a1fc]] and the home's [[1ea1f9eda079]], both `open` for the operator, so it is `open` and added to the verdicts plan [[14be8f377b34]] by a note, with its twin [[fee183274ff6]] on `/stories/2022-newsletter` (one item per route). No verdict written. No `../ui` or `../gridkit` change. No Vitest test: nothing pure was written; the proof is the DOM pairs, the probe and the screenshot pairs.

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity): nothing critical. Standards: no hard violation, no invariant decision to check; taken, the stand-in's header as three pinned `// ported from` lines under the stand-in notice; left as they are, the `images[i]!` assertion (documented, legacy's line) and the `h2` style prop (recorded above); its remark that the grid's comment goes stale if a verdict removes the phantom cell is the verdict's slice's to edit with the cell, so no note. Spec: every load-bearing upstream claim verified at source; taken, the `h2` override's comment reworded (a caller's `alignSelf` prop wins, a caller's `css` would not, which no caller writes) and three cites in [[d915091fafea]] corrected (`ui-mdx/src/index.tsx:25-27`, `useSizeWidths.js:5-18`, `ui-media/src/hooks.ts:116` for the `vw` step); its freshness note holds: after the pairs and the probe were captured only comments changed (the stand-in's header, the `h2` comment), so the evidence stands. Parity found every claimed closure holding at 375, 768 and 1280 on all six routes and the MDX bodies free of copy change; two findings taken: the indicators item needed its 2022 twin ([[fee183274ff6]], `open`), and the restored galleries show a difference no item recorded, the carousel arrows' chevron at 20px in a 14px button where legacy's is 16px at 16px, Chakra v3's button recipe `_icon` size, which the ui recipes slice `2bd0169a6dda` keeps through the deep merge (a probe of both sides at 1280 and 375: the `svg` 16 by 16 against 20 by 20 before the 1.5 scale, the button 40px on both); filed as [[e5f7c103b8bf]] and [[f2e4bf3755c7]] (`regression`, visual, one per newsletter) and named on the verdicts plan's note as a `../ui` recipe fix for the record's finish to mint as a ui slice beside the shell record, since no site-side line closes it without departing from legacy's. Its code note that `mdx-components.tsx` spreads the story map after the per-call `components` is legacy's precedence (`MDXProvider` merged the same way) and nothing passes any, so left. Dropped: none.

Gate: `timeout 900 just check` green twice, before the review and after its fixes, with the dev server stopped by pid each time (Biome clean, `tsc` clean, 78 tests, `next build` prerendering the six story pages, the generated designs data unchanged); `kipu verify --warnings-as-errors` green. Not the last open child: the inline MDX, the fields and the page re-port slices follow, the verdicts plan standing beside the record.

## Log
