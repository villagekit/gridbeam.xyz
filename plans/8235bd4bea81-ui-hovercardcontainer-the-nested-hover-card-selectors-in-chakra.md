---
title: "ui HoverCardContainer: the nested hover-card selectors in Chakra v3's form, the console clean"
status: done
parent: 337e35d86920
derived_from: a78b167170b8
tags:
  - "worker:fable"
priority: medium
---
`HoverCardContainer` styles a nested `HoverCard` on hover and focus-within again, and the browser console is clean on every route that renders one, as under `@villagekit/ui@0.9.0`: the two bare class keys under `_focusWithin` and `_hover` are written in the form Chakra v3's `css` reads as a nested selector. Closes [[7877c267268c]] on `shell` for the shell record `a78b167170b8`, finished before the item was found (decision `40abdb2f222a`, a slice beside the record). A fix in `../ui`, so it follows decision `28c1a536`: committed in the sibling by pathspec, never pushed from here; seen on this site through the uncommitted `pnpm.overrides["@villagekit/ui"] = "file:../ui"` (`pnpm install` after each sibling edit) beside the uncommitted `transpilePackages: ['@villagekit/ui']` in `next.config.ts` that the `src/` copy needs (CLAUDE.md's ui row), both reverted by path before the commit; the item moved to `upstream` with a note citing the sibling commit; the bump plan `99f2fe62c62f` is `blocked_by` this slice (the edge written at the mint).

## Work

In `../ui/src/components/HoverCard.tsx:74-79` `HoverCardContainer` writes `_focusWithin: { '.ui-hover-card': focusStyle }` and `_hover: { '.ui-hover-card': hoverStyle, ...containerHoverStyle }`, legacy's `@villagekit/ui@0.9.0 src/components/HoverCard.tsx:68-90` under Chakra v2's `sx`, which read a bare class key as a nested selector. Chakra v3's `css` treats only a key holding `&` or `@`, or starting with `_`, as a condition (`node_modules/@chakra-ui/react/dist/esm/styled-system/conditions.js:5,14`), so `.ui-hover-card` is flattened into a property and Emotion logs `Using kebab-case for css properties in objects is not supported. Did you mean .uiHoverCard?` once per page as a console error, which Next's dev overlay shows as `1 Issue`. Write the two keys as `'& .ui-hover-card'`, the form the site's re-ports use for a nested selector (`app/_components/stories/Item.tsx`'s `'& .stories-item-image'`), with the constraint in a comment; `HoverCard` itself and the container's own hover and focus-within styles stay as they are. One consumer on this site nests a `HoverCard` in a container, the catalog's `ItemCard` (`app/_components/catalogue/ItemCard.tsx:26,29`, legacy's `components/catalogue/item.tsx:33,48` at `fce357d`), so on `/designs` a hovered card's inner `.ui-hover-card` takes the `accentB.100` fill and `accentB.300` border again (`HoverCard.tsx:10-13`), lost today, and a focused one the `outlineColor` border; the story cards nest none, so on `/` and `/stories` the change is the console alone.
Verify first: on `pnpm dev` with the published 1.2.0 the browser console on `/`, `/stories` and `/designs` prints the kebab-case error once each; the emitted stylesheet holds no rule for `.ui-hover-card` under the container's hover.
Docs: `../ui/CHANGELOG.md`, an entry under Fixed.
Not this slice: any other console line on those routes (the design pages' `941bd045b043` and `505a856301b8` are their ledger's, the `next/image` LCP warning on `/stories` is nobody's item).

## Seams under test

None pure; the proof is the browser console and the emitted stylesheet on `pnpm dev` under the override.

## Done when

- On `pnpm dev` under the override, a Playwright console capture on `/`, `/stories` and `/designs` holds no `kebab-case` line, and Next's dev overlay shows no issue badge from it
- The emitted stylesheet holds a rule for `.ui-hover-card` nested under the container's hover selector and one under its focus-within selector, carrying `hoverStyle` and `focusStyle` (a stylesheet dump, saved beside the capture); the story cards on `/` and `/stories` and the design cards on `/designs` still scale on hover with the pointer cursor
- A Playwright probe at 1280 on `/designs` under the override reads, with the pointer on a card, the inner `.ui-hover-card`'s `background-color` and `border-color` as the `accentB.100` and `accentB.300` values, and with focus inside the card the `outlineColor` border, the readings of the live legacy site's same card
- In `../ui`: `pnpm lint`, `pnpm types`, `pnpm build:pkg` and `pnpm build:storybook` green; the change committed by pathspec on its `main`, not pushed
- [[7877c267268c]] is `upstream` with a note citing the sibling commit; `99f2fe62c62f` is `blocked_by` this slice (checked at the finish) and carries a note that the bump makes no site edit for it
- The override and the `transpilePackages` entry reverted by path (`git restore -- package.json pnpm-lock.yaml next.config.ts`, `pnpm install --frozen-lockfile`); `timeout 900 just check` is green

## Outcome

Shipped in `../ui` as commit 2e2d68c on its `main` over `16bb376` (thirteen ahead of origin, not pushed; the push goes with the operator's publish, decision `28c1a536`), by pathspec: `src/components/HoverCard.tsx` and `CHANGELOG.md` (one Fixed entry under Unreleased, beside the five the recipes and Section slices wrote). The package version stays 1.2.0. This repo's commit carries kipu writes only: no site code changes.

What shipped: `HoverCardContainer`'s two nested keys under `_focusWithin` and `_hover` read `'& .ui-hover-card'`, the legacy `@villagekit/ui@0.9.0 src/components/HoverCard.tsx:68-90` keys in the form Chakra v3's `css` reads as a selector (`@chakra-ui/react@3.35.0 styled-system/conditions.js:5,14`: a key holding `&`, starting with `@` or `_`, or naming a condition), with the constraint in a comment above them; `focusStyle`, `hoverStyle`, `containerHoverStyle`, `HoverCard` itself and the container's own hover and focus-within styles are unchanged. The mechanism, read in the emitted stylesheet on the published 1.2.0 (`hovercard-before.json` in the scratchpad): Chakra flattened each bare key into a property whose object value it read as conditions, emitting the empty rules `.css-x9jjzq bordercolor:is(:focus-within, [data-focus-within]) { }` and `@media (hover: hover) .css-x9jjzq backgroundcolor:is(:hover, ...) { }`, so the nested card styled nothing and Emotion logged the kebab-case error once per page.

Verified. Before, on `pnpm dev` with the published 1.2.0 (the scratchpad's `hovercard-probe.mjs`, `hovercard-before.json`): `/`, `/stories` and `/designs` at 1280 each log `Using kebab-case for css properties in objects is not supported. Did you mean .uiHoverCard?` once as a console error, Next's dev overlay reads `1 Issue`, the stylesheet holds no rule naming `.ui-hover-card`, and on `/designs` the first card's inner `.ui-hover-card` reads `rgb(250, 250, 250)` on `rgb(228, 228, 231)` at rest, under the pointer and with focus inside alike, while the container itself scales `1.05` with the pointer cursor. The live legacy site (`hovercard-legacy.json`, `WAIT=load`): no kebab-case line, the rules `.css-12wsqcg:hover .ui-hover-card { background-color: accentB-100; border-color: accentB-300 }` and `.css-12wsqcg:focus-within .ui-hover-card { border-color: outlineColor }`, and on `/designs` the inner card `rgb(254, 252, 191)` on `rgb(246, 224, 94)` under the pointer and `rgba(0, 163, 196, 0.5)` with focus inside, `rgb(247, 250, 252)` on `rgb(226, 232, 240)` at rest. After, under the `file:../ui` override (`hovercard-after.json`, `hovercard-after-root.json`): no kebab-case line on any of the three routes and no issue badge (`no badge`; what remains is the WebGL driver warnings on `/` and `/stories` and the `next/image` LCP warning, the plan's "not this slice"); the stylesheet holds `.css-1tjfjsx:is(:focus-within, [data-focus-within]) .ui-hover-card { border-color: var(--chakra-colors-outline-color) }` and, inside `@media (hover: hover)`, `.css-1tjfjsx:is(:hover, [data-hover]):not(:disabled, [data-disabled]) .ui-hover-card { background-color: var(--chakra-colors-accent-b-100); border-color: var(--chakra-colors-accent-b-300) }`; on `/designs` the inner card reads `rgb(254, 252, 191)` on `rgb(246, 224, 94)` under the pointer and `color(srgb 0 0.639216 0.768627 / 0.5)` with focus inside, which is `rgba(0, 163, 196, 0.5)` written the way Chromium serializes a color-mix result (0.639216 by 255 is 163, 0.768627 by 255 is 196), legacy's readings; the story cards on `/` and `/stories` and the design cards on `/designs` scale `1.05` with the pointer cursor (the first `/` reading in `hovercard-after.json` caught the 150ms transition at `1.04623`; the re-run `hovercard-after-root.json` reads `1.05`). The crops `hovercard-{current,legacy}-{hover,focus}.png` show the same yellow fill and dashed yellow border under the pointer and the same cyan dashed border with focus inside on both sides. The screenshot pairs for `/`, `/stories` and `/designs` at 375, 768 and 1280 under `audit/`, captured under the override and looked at: the change is a hover and focus state, so the pairs at rest show nothing new; every difference on them is ledgered (the `upstream` items, the routes' own regression items, `c9ea22823a23` at 768). In `../ui`: `pnpm lint`, `pnpm types`, `pnpm build:pkg` and `pnpm build:storybook` green (lint, types and the package build again after the review's comment edit, which changes no emitted CSS). The override and the `transpilePackages` entry reverted by `git restore -- package.json pnpm-lock.yaml next.config.ts` and `pnpm install --frozen-lockfile`; `timeout 900 just check` green against the published 1.2.0 (78 tests; the first run died in `next build` with workerd's `SQLITE_BUSY`, the CLAUDE.md gotcha, and the retry after `rm -rf .wrangler/state` was green).

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity) before the commit, none critical or major. Standards, no hard violation; one judgment call taken: the comment names the three key forms Chakra v3 reads as a selector exactly (`&` anywhere, `@` or `_` at the start, or a condition's name) and no longer restates the code below it; its other two remarks (the CHANGELOG entry's length matches its siblings; the `ui-hover-card` class coupling is the legacy author's) change nothing. Spec, two notes taken into this Outcome: the `color(srgb ...)` focus reading is legacy's color in another serialization, and the `1.04623` transform was a mid-transition read; every Done when line held on the evidence files and the upstream citation was verified in `conditions.js`. Parity: the closure holds in the code, the stylesheet and the browser (the first `/designs` card at rest, under the pointer and with focus inside reads legacy's colors), no copy in the diff, nothing legacy did silently dropped, no unrecorded difference: the new hover rule's `@media (hover: hover)` wrapper and `:not(:disabled, [data-disabled])` guard, absent in legacy's rule, are the shell's open item `53f8a05a03fa`; with the pointer over a focused card hover wins on both sides (current by specificity, legacy by order); the black outline on the card title link in the focus crop is the ledgered `dba0ac6ac25d`, not this change's. It looked at the pairs for `/`, `/stories` and `/designs` at 375, 768 and 1280, found nothing from this change on them, and probed `/designs` at 1280 only. Findings dropped: none.

Items: [[7877c267268c]] moved to `upstream` with the ui commit in its note; `99f2fe62c62f` `blocked_by` this slice (the edge from the mint, checked) and carrying the note that the bump makes no site edit for it and what it checks. No visitor-facing copy in the change. This was the last of the three ui slices the home record's finish minted beside the shell record; the bump plan's blockers are now all done, and it waits on the operator's publish (attended).

## Log
