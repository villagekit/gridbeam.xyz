---
title: Stories index re-ported from the legacy stories page, its context, filters, option and list
status: todo
parent: ca353de8b645
derived_from: ca353de8b645
blocked_by:
  - 48c8cbdb206a
  - 531b810f2dbd
  - a7bf623f885c
  - e332105c3b52
tags:
  - "worker:fable"
---
The stories index is legacy's `pages/stories.tsx` again, with the four modules it composed: the `constate` context over `use-query-params` (`context/stories.tsx`), the filter row (`components/stories/filters.tsx`), the generic `Option` chip on a `Badge` (`components/option.tsx`) and the animated list (`components/stories/list.tsx`), each translated for the app router, Chakra v3 and `motion`. The page is a `Title` with its description over one `Container maxW="8xl"`, the manual `menubar` box, and a `VStack` at the breakpoint hook's spacing holding `Filters` and `List`; the filter is the `filter` query parameter, each click a history entry; the chips are `Badge`s with `role="option"`, Enter-only keys, legacy's shades, sizes and focus ring, in legacy's order and palettes, left-aligned; the cards fade and scale in and out on a filter change in a two-column grid at gap 12. `StoriesBrowser`, `StoriesStatic` and the Suspense boundary go. Copy is as the copy slice left it; this slice changes no visitor-facing text. Eighteen items on `/stories`; one two-file translation the split filed for the operator. A re-port that decides every translation, so Fable. Record `ca353de8b645`; decisions `ee86d68a`, `2032533f`, `ca677697`, `28c1a536`.

## Work

- Legacy, all at `fce357d` under `../node-modules/apps/gridkit/`: `pages/stories.tsx` (42 lines), `context/stories.tsx` (69), `components/stories/filters.tsx` (41), `components/stories/list.tsx` (47), `components/stories/index.ts` (3), `components/option.tsx` (77). Current: `app/stories/page.tsx`, `app/stories/StoriesBrowser.tsx`, `app/stories/StoriesStatic.tsx` as the copy and catalog slices leave them; `app/_components/stories/Item.tsx` (the card, re-ported by `e332105c3b52`, unchanged here); `app/_lib/url-state.ts` (this route's consumer leaves it).
- Two files for the page, the home's mechanism (`091a47cb93e7`, [[e3a2d4d66691]]): `app/stories/page.tsx`, a server file holding `metadata` (`title: 'Stories'`, the copy slice's) and rendering `<StoriesPage />`; `app/stories/StoriesPage.tsx` under `'use client'` and the header `// ported from https://github.com/villagekit/node-modules/blob/fce357d/apps/gridkit/pages/stories.tsx`, exporting `StoriesPage` with legacy's body (`stories.tsx:10-36`): `const spacing = useBreakpointValue({ base: 8, md: 16 })` from `@villagekit/ui` ([[1e0284dbf88d]], [[d52710d109cb]]); `<StoriesContextProvider>` around `<Title description="Discover all things grid beam in our collection of articles, guides, and newsletters.">Stories</Title>` (the copy slice's string, on one line; the ui `Title` renders an `h1` by default, `node_modules/@villagekit/ui/dist/components/layouts/Title.js`, so no `as`), then `<Container maxW="8xl">` ([[1d5c23180ebf]]; Chakra v3's `sizes.8xl` is 90rem as v2's was) holding `<Box id="stories-menu" role="menubar" aria-owns="stories-menu-filters" />` with legacy's comment (`:25`), then `<VStack gap={spacing}>` of `<Filters />` and `<List />`. The `Section`, the inner `Container maxW="4xl"`, the `Suspense` and its fallback go. The client file exists because legacy's page calls `useBreakpointValue` and mounts a provider, which a server file cannot; `NextSeo` is the `metadata` (`65c21e08b3f1`, rule 4); `getLayout` is the root layout (`a7bf623f885c`). If the operator has judged [[e3a2d4d66691]] by the time this runs, the page takes the shape the verdict names.
- `app/_lib/context/stories.tsx`, legacy's `context/stories.tsx` line for line under its `// ported from` header, the first of legacy's `context/` modules ported (the designs catalog's `1fc20938eb70` follows it): `constate` (add it, `4.0.0`, MIT, peer React 19, with the caret range every other dependency carries, the lockfile pinning it with its integrity hash; read its license in `node_modules/constate/package.json` after the install); `filterOptions` in legacy's order `all`, `guide`, `inspiration`, `newsletter` ([[a1a6605dcec3]]); `createEnumParam` and `withDefault` from `use-query-params`; `useQueryParams({ filter: withDefault(filterEnumParam, 'all') })` under the shell's provider (`531b810f2dbd`, whose Outcome names this consumer: the query is empty until hydration, so the server renders every story as legacy's did, and no Suspense boundary is needed) ([[cbe45596b840]], [[35529c01ddaf]]); `setQuery({ filter: value })` with no `updateType`, so the default `pushIn` writes a history entry and Back undoes the filter ([[10bea19307bb]]); `sortedStories = allStories.sort(...)` at module scope (`:32`), the comparison on `Date.parse` of the ISO strings the catalog keeps until `82762f6b27de` (legacy's `getTime` on its `Date`s), named in the Outcome; `filteredStories` and the `constate(useStories)` export. `sortedStories` is a module const and not exported, as legacy's; the home reads its two stories by import as legacy's `index.tsx` did.
- `app/_components/stories/Filters.tsx`, legacy's `filters.tsx` under its header: `useIsMobile` from `@villagekit/ui` (`dist/hooks/useIsMobile.js`, `useBreakpointValue({ base: true, md: false }, { fallback: 'md' })`) ([[d52710d109cb]]); `FilterCategoryColors = { ...StoryCategoryColors, all: 'accentB' }`, so the Guides chip is `primary` ([[e524aaa90e2f]]); `<HStack id="stories-menu-filters" role="listbox" alignItems="center" gap={isMobile ? 2 : 4}>` with legacy's two comments, no `justifyContent` ([[33b40aded547]]) and no `aria-label` ([[ff07afa4ea2b]]); `map` from `lodash-es` over `filterOptions`; `<Option key label value isSelected onClick={setFilter} size={isMobile ? 'sm' : 'lg'} colorScheme={FilterCategoryColors[value]} />` ([[f3ff654e792d]]). `app/_components/stories/index.ts` is legacy's barrel of `Filters`, `Item` and `List`.
- `app/_components/Option.tsx`, legacy's `components/option.tsx` under its header, a shared component because legacy's catalog selector renders it too (`components/catalogue/selector.tsx`, the designs index record's to consume): the generic `Option<T>` on the ui `Badge` with `role="option" tabIndex={0}` and legacy's `biome-ignore` line, `onKeyDown` on Enter alone ([[73dd61e26135]], [[cc70e66936f8]]: no `aria-pressed`, no `type`, no `button`), legacy's `sx` as `css`: `_hover` on the `.100` background and `.300` border with `scale(1.05)`, `_active` at `scale(1)`, `_focus` (not `_focusVisible`, [[f7905b047359]]) with `boxShadow: 'outline'` (the token name for legacy's `theme.shadows.outline`, the chip's own `_focusVisible` line uses it today) and `outline: 'none'`, `backgroundColor` and `borderColor` transparent, `borderStyle` solid, `borderWidth: 2`, `cursor`, `fontSize: size`, `fontWeight: 'normal'`, `paddingX: 2` ([[f3ff654e792d]]), `transitionDuration: 'fast'`, `userSelect: 'none'`, `WebkitTapHighlightColor`, and the selected `.50` background with the `.200` border ([[92dd755da0a4]]). `colorScheme` stays the prop's name as legacy's (a string the template literals read, never Chakra's prop); `useTheme` goes with the token. At the published `1.2.0`, Chakra v3's badge recipe boxes the chip at `px 1.5`, `minH 5`, `inline-flex` where v2's was `px 1` inline, the shell's [[2363cb52f9d8]] (`upstream`): expected on the pairs until the bump, not this slice's.
- `app/_components/stories/List.tsx`, legacy's `list.tsx` under its header: `useBreakpointValue({ base: 1, md: 2 })` for the columns ([[d52710d109cb]]); `<SimpleGrid columns={columns} gap="12">` ([[54f14be9aedd]]); `AnimatePresence` and `motion` from `motion/react` (the package the site holds, `12.39.0`; the catalog component imports the same names), `itemVariants` as `:9-20`, the wrapper `const MotionBox = motion.create(Box)`: the call form `motion(Box)` warns `motion() is deprecated. Use motion.create() instead.` in development (`framer-motion/dist/es/render/components/create-proxy.mjs:19-24` under `node_modules/.pnpm`, `create` handled at `:32-33`), so `create` is the one translation; each card `<MotionBox key={story.title} layout variants={itemVariants} initial="hidden" animate="visible" exit="hidden">` around `<Item metadata={story} />` ([[226f8b8127e6]], [[c4edacc9eba5]]: keyed by `story.title` as `:34`). `useStoriesContext` from the context module.
- Deleted: `app/stories/StoriesBrowser.tsx` and `app/stories/StoriesStatic.tsx` (the chip row, the local `FilterChip`, the grid and the fallback, [[c4edacc9eba5]]). The Suspense boundary and its static fallback go with them, the sanctioned addition of [[9e10796f13c5]] (rule 4, `useSearchParams`) no longer forced once the consumer calls `useQueryParams` under the shell's adapter: the addition is gone, so the item moves to `fixed` with this plan named (`kipu fix 9e10796f13c5 --outcome "plan <this prefix>"`, the outcome appended below the sanction's verdict, which nobody edits), after a `kipu note` saying why the rule 4 force lapsed; if kipu refuses the move from `sanctioned`, the note alone records it and the Outcome says so. `app/_lib/url-state.ts` stays: three consumers remain (`app/_components/catalogue/Catalogue.tsx`, `app/_components/design/DesignViewer.tsx`, `app/tools/cutting-planner/CuttingPlanner.tsx`), and the last migrated deletes it (the note on [[cbe45596b840]]).
- The accessibility markup returns as legacy wrote it, the split's call 4 (the record's Log): the `menubar` box owning the `listbox` of `option` badges, each a tab stop with Enter alone, no `aria-pressed` and no group name. The stories grilling's I3 verdicts on [[cc70e66936f8]] and [[ff07afa4ea2b]] say so, and [[73dd61e26135]] is `regression` with no verdict, so legacy's form ships; the accessibility pass after M2 (note `eeba2a65cee4`) is where it is bettered. If any of the three is `sanctioned` when this runs, keep the current form for that item alone.
- Chakra v3 and app-router translations throughout, each sanctioned or upgrade-forced: `spacing` to `gap`, `sx` to `css`, `theme.shadows.outline` to the `outline` token, `useIsMobile` and `useBreakpointValue` from the ui package as legacy imported them, `framer-motion` to `motion/react`, `NextSeo` to `metadata`, `getLayout` to the root layout, the page's hook and provider to a client file; a translation with more than one fitting form is named in the Outcome with the form chosen.
- Interfaces: consumes `allStories` and `StoryCategoryColors` from the catalog slice (`Story catalog re-ported from the legacy stories module`), `Item` from `e332105c3b52`, the `QueryParamProvider` and adapter from `531b810f2dbd`, the layout from `a7bf623f885c`; produces `Option` at `app/_components/Option.tsx` for the designs index record's slices, and `app/_lib/context/stories.tsx` as the place legacy's `context/catalogue.ts` is ported beside.
- Verify first: `grep -c 'Section\|Suspense\|StoriesStatic' app/stories/page.tsx` prints at least 3; `grep -c 'useSearchParams\|aria-pressed\|replaceUrl' app/stories/StoriesBrowser.tsx` prints at least 3; `grep -c '<QueryParamProvider' app/_components/SiteProvider.tsx` prints 1; `grep -c '"constate"' package.json` prints 0; `grep -c 'export const allStories' app/_lib/stories.ts` prints 1; `kipu show 73dd61e26135`, `kipu show cc70e66936f8` and `kipu show ff07afa4ea2b` are `regression`.
- Docs: none; CLAUDE.md's Structure names the route, `app/_components/` and `app/_lib/` already.
- Not this slice: the card (`e332105c3b52`, done) and the icon's `aria-hidden` ([[2c38c9e6a5da]], the operator's on the verdicts plan `Stories index: the operator's verdicts`); the catalog's field names and the `[slug]` route (the story pages record); the container padding (`5c1af396cc2e`), the badge box (`2363cb52f9d8`), the heading's size and weight (`77cd1426ac65`, `7124898563eb`), the palette and shadow literals (`72b776cb0d3f`, `1f11773f8e2b`), the `Title` container (`318456ddabc6`) and the `HoverCardContainer` console line (`7877c267268c`), every one `shell` and `upstream`, each expected on the pairs; the shell's `c9ea22823a23` at 768.

## Seams under test

None pure; the proof is the DOM pair's copy diff and aria tree, a Playwright probe of the filter's URL, history and animation on both sides, the screenshot pairs with a computed-style probe, and the eye on `pnpm dev`.

## Done when

- `pnpm audit:dom --routes <a file naming /stories>` against a running `pnpm dev`: `audit/stories/dom/current.txt` between the nav and the footer reads `Stories`, the description line, then `All`, `Guides`, `Inspiration`, `Newsletters`, then the ten cards' lines in the copy slice's order, matching `legacy.txt` line for line but for the description's rule 1 swap; `current.aria.yaml` holds, as `legacy.aria.yaml:24-29` does, a `menubar` and a `listbox` with four `option`s in that order, none `[pressed]`, no named group, and every card block as the card slice left it
- `curl -s localhost:3000/stories` holds `id="stories-menu"`, `role="menubar"`, `aria-owns="stories-menu-filters"`, `id="stories-menu-filters"` with `role="listbox"`, four `role="option"` elements with `tabindex="0"`, and all ten cards' `aria-label`s (the unfiltered list in the static HTML, as legacy's SSR); `curl -s localhost:3000/stories | grep -o 'BAILOUT_TO_CLIENT_SIDE_RENDERING' | wc -l` prints 1, the footer cube's alone
- A Playwright probe against both sides, saved beside the pairs: on `/stories`, clicking `Guides` makes the URL `/stories?filter=guide` and lengthens `history` by one, the grid then holding the guide cards alone; Back returns to `/stories` with all ten; loading `/stories?filter=newsletter` directly renders the newsletter cards after hydration; the chip focused by a click shows the outline ring (`:focus`, both sides); the cards' wrappers carry inline `opacity` and `transform` styles during the 0.3s after a filter click (or the transition is seen by eye and said so)
- `pnpm audit:pages --routes <the same file>` at 375, 768 and 1280 looked at against legacy, with a computed-style probe on both sides saved beside it: the page's container `max-width` 1440px at 1280; the filter row starting at the container's left edge, its `column-gap` 16px at 1280 and 8px at 375; each chip `font-size` 18px at 1280 and 14px at 375, `padding-left` 8px, `border-width` 2px, the selected chip on the palette's `50` background with the `200` border, a hovered chip on the `100` with the `300`; the stack's `row-gap` 64px at 1280 and 32px at 375; the grid at two columns from 768 and `gap` 48px at every width; every residual difference is a `shell` item in `upstream`, the operator's `c9ea22823a23`, or filed
- `grep -rn 'StoriesBrowser\|StoriesStatic\|useSearchParams\|url-state\|aria-pressed\|Suspense' app/stories | wc -l` prints 0; `head -1` of `app/stories/StoriesPage.tsx`, `app/_lib/context/stories.tsx`, `app/_components/stories/Filters.tsx`, `app/_components/stories/List.tsx` and `app/_components/Option.tsx` prints each file's `// ported from` line; `grep -c '"constate"' package.json` prints 1
- The eighteen items are `fixed` (`kipu fix <id> --outcome "plan <this prefix>"`), `9e10796f13c5` is `fixed` with its note (or noted alone, as above), and `kipu note e3a2d4d66691` cites the two shipped files by path and line (or says the shape the operator's verdict gave instead), checked after the fixes
- `timeout 900 just check` is green

## Outcome

## Log
