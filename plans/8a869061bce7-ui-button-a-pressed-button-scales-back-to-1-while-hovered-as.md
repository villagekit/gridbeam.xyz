---
title: "ui Button: a pressed button scales back to 1 while hovered, as under Chakra v2"
status: done
parent: 337e35d86920
derived_from: a78b167170b8
tags:
  - "worker:fable"
priority: medium
---
Every ui button drops back to its size while pressed, as on the legacy site: the recipe base's `_active` transform `scale(1)` wins over the `_hover` transform `scale(1.08)` while the pointer is down and over the button. Closes [[d2f1007d7d37]] on `shell` for the shell record `a78b167170b8`, finished before the item was found (decision `40abdb2f222a`, a slice beside the record). A fix in `../ui`, so it follows decision `28c1a536`: committed in the sibling by pathspec, never pushed from here; seen on this site through the uncommitted `pnpm.overrides["@villagekit/ui"] = "file:../ui"` (`pnpm install` after each sibling edit), reverted by path before the commit; the item moved to `upstream` with a note citing the sibling commit; the bump plan `99f2fe62c62f` is `blocked_by` this slice (the edge written at the mint).

## Work

In `../ui/src/components/Button.tsx:29-35` the recipe base writes `'&:not(:disabled)': { _hover: { transform: 'scale(1.08)' }, _active: { transform: 'scale(1)' }, _focus: { boxShadow: 'outline' } }`, legacy's `@villagekit/ui@0.9.0 src/components/Button.tsx:41-48`. Chakra v3 emits the active rule before the hover rule at equal specificity (`(0,4,0)`, the hover rule under `@media (hover: hover)`), so the hover transform wins on press; Chakra v2 emitted the active rule after the hover rule. Make the active transform win while hovered with the smallest change that keeps every other button state as it renders today: the candidates the worker weighs against the sources (`node_modules/@chakra-ui/react/dist/esm/styled-system/`, the emitted CSS on `pnpm dev`) are the active state written with a selector the hover rule cannot beat (`_active` nested under `_hover` as well as beside it, or a compound `&:hover:active` selector), or the order of the two keys in the recipe if Chakra v3 emits conditions in object order (verify first). Read the legacy cascade before choosing; the color states of the variants stay as they are.
Verify first: on the live legacy site at 375 the mobile menu toggle's computed transform is `matrix(1.08, 0, 0, 1.08, 0, 0)` on hover and `matrix(1, 0, 0, 1, 0, 0)` on press; on `pnpm dev` under the override it is `matrix(1.08, 0, 0, 1.08, 0, 0)` on both (the scratchpad's `toggle-probe.mjs` from plan `61a42a0adbc8` reads both).
Docs: `../ui/CHANGELOG.md`, an entry under Fixed.
Not this slice: the toggle's colors ([[c440126f7064]], shipped by `61a42a0adbc8`).

## Seams under test

None pure; the proof is a Playwright probe of the computed transform on `pnpm dev` under the override, against the live legacy site.

## Done when

- A Playwright probe at 375 on `/faq` reads the toggle's computed `transform` as `matrix(1.08, 0, 0, 1.08, 0, 0)` on hover and `matrix(1, 0, 0, 1, 0, 0)` on press, on `pnpm dev` under the override and on the live legacy site alike, and the primary header action at 1280 the same
- The hover, press and focus colors of the four variants are unchanged by the change (read before and after)
- In `../ui`: `pnpm lint`, `pnpm types`, `pnpm build:pkg` and `pnpm build:storybook` green; the change committed by pathspec on its `main`, not pushed
- [[d2f1007d7d37]] is `upstream` with a note citing the sibling commit; `99f2fe62c62f` is `blocked_by` this slice (checked at the finish)
- The override reverted by path (`git restore -- package.json pnpm-lock.yaml`, `pnpm install --frozen-lockfile`); `timeout 900 just check` is green

## Outcome

Shipped in `../ui` at commit 6a7c036 on `main` over `d736bfe`, by pathspec (`src/components/Button.tsx`, `CHANGELOG.md`), not pushed: the recipe base's active transform sits nested under the hover state as well as beside it, `_hover: { transform: 'scale(1.08)', _active: { transform: 'scale(1)' } }`, with the constraint in a comment. The mechanism, read in `@chakra-ui/react@3.35.0`: `preset-base.js:67-71` defines `hover` as `["@media (hover: hover)", "&:is(:hover, [data-hover]):not(:disabled, [data-disabled])"]` and `active` as a plain selector, and `styled-system/css.js` ends `createCssFn` in `sortAtRules` (`sort-at-rules.js`), which at every nesting level writes the plain keys first and every `@media` key after them, so the hover rule always follows the active rule at equal specificity (0,4,0) and wins the press; `conditions.js`'s `sort` is stable and keeps object order among conditions, so reordering the two keys, the plan's third candidate, changes nothing (verified by resolution before choosing). Legacy's `@chakra-ui/styled-system@2.9.2` (packed into the scratchpad, `dist/index.mjs:1170,1174`) resolved `_hover` to `&:hover, &[data-hover]` and `_active` to `&:active, &[data-active]`, no media query, and its `getCss` (line 1793) walked the style object in key order, so the active rule came after the hover rule and won. The nested state resolves to `@media (hover: hover) { .x:not(:disabled):is(:hover, ...):not(...):is(:active, ...):not(...) { transform: scale(1) } }`, more specific than the hover rule inside the same media block; the plain active rule beside it still serves a pointer that cannot hover and a `data-active` element with no hover. Resolving the four variants through the package's own system (`press-resolve.mjs`, bundled with esbuild and run under node, `press-resolve-before.json` and `press-resolve-after.json`) shows the one difference to be that nested rule in each variant's hover block; every color declaration is as it was. The compound `&:hover:active` selector, the plan's other candidate, was not taken: it would drop the `data-hover` and `data-active` attribute forms and the `hover: hover` guard the v3 conditions carry.

Verified. In `../ui`: `pnpm lint`, `pnpm types`, `pnpm build:pkg` and `pnpm build:storybook` green. On this site through the `file:../ui` override on `pnpm dev`, a Playwright probe (`press-probe.mjs`) on `/faq`: the mobile menu toggle at 375 reads `transform` `none` at rest, `matrix(1.08, 0, 0, 1.08, 0, 0)` on hover and `matrix(1, 0, 0, 1, 0, 0)` on press, and the header action at 1280 (`Find a supplier`, the `primary` LinkButton) the same three, matching the live legacy site's toggle and Cart (`press-legacy.json`); before the edit, at the same sibling commit, the toolbar slice's `toggle-override-after.json` read the press as `matrix(1.08, 0, 0, 1.08, 0, 0)`. The colors read the same before and after: the toggle `rgb(23, 25, 35)` at rest and `rgb(213, 63, 140)` over the `primary.400` 10% fill on hover and press, the action white on `rgb(237, 100, 166)` at rest and on `rgb(213, 63, 140)` on hover and press. The press crops `press-toggle-{legacy,current}.png` show the toggle at its rest size with the outline ring on both sides. The screenshot pairs on `/faq` and `/stories` at 375, 768 and 1280 under `audit/`, captured under the override (a static capture cannot show a press): the doubled footer credit on the current captures is the brand footer's bump note. The override reverted by `git restore -- package.json pnpm-lock.yaml` and `pnpm install --frozen-lockfile` before the gate, which ran green against the published 1.2.0.

Found beside the scope and filed, not fixed: on legacy the header action anchor (the Cart) takes focus on mousedown and shows the `outline` ring while pressed; on this site `Find a supplier` does not (`document.activeElement` stays `body`), while the toggle, a `button`, focuses on press on both sides and a plain anchor on neither. Filed as [[c14d1a8d177f]] (`open`, `shell`, interaction, changed), its cause unread.

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity) before the commit, none critical or major. Standards: one minor taken, the recipe comment's last clause claimed the beside `_active` serves a pointer that cannot hover, where without hover no scale is applied and the rule sets `scale(1)` over `none`; the clause now says the line is the 0.9.0 recipe's own, kept as the legacy author wrote it. Its second minor, that Chakra v3's own recipes nest no condition under `_hover` while this file's `primary` variant already nests `_disabled` there, changes nothing. Spec: every clause of Work verified against the diff, both Chakra sources and the resolution diff, the probes re-run by the reviewer with the same readings on both sides; its minor that the browser proof of colors covers two variants (`toolbar`, `primary`) and the legacy action is a different variant from the current one is accepted, the resolution diff being the proof for `secondary` and `tertiary`. Parity: the closure holds on the crops and the probes, no copy in the diff, the `/faq` pairs at 1280 and 375 showing only ledgered differences; it found one difference older than this change that no item recorded, Chakra v3's `@media (hover: hover)` guard on every `_hover` style, under which a pointer that cannot hover never shows a button's hover fill, border or scale where legacy's unguarded `&:hover` showed them through the browser's sticky hover after a tap; filed as [[53f8a05a03fa]] (`open`, `shell`, interaction, changed), its verdict the rules' or the operator's, never this slice's. Its other observation, that v3's active selector excludes `[data-state=open]` where v2's did not, so a hovered and pressed button inside a future `Tooltip` would keep `1.08`, is noted here and not filed: no site button carries `data-state=open`. Findings dropped: none.

Items: `d2f1007d7d37` moved to `upstream` with the ui commit in its note; `99f2fe62c62f` `blocked_by` this slice (the edge from the mint, checked) and carrying the note that the bump makes no site edit for it. No visitor-facing copy in the change.

## Log
