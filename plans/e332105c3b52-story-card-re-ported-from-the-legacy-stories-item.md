---
title: Story card re-ported from the legacy stories Item
status: done
parent: fd9a92bd8abd
derived_from: fd9a92bd8abd
blocked_by: 6f90e7e24ca6
tags:
  - "worker:fable"
priority: medium
---
The story card is legacy's `components/stories/item.tsx` again, re-ported as `app/_components/stories/Item.tsx` exporting `Item`: a `HoverCardContainer` section named by the title, a `LinkBox` and a `Container maxW="md"`, the cover rendered by the ui media `Image` inline (the verdict of [[656dc6d730d7]]), a `Heading` beside an empty named `LinkOverlay`, the category badge on the palette's `100` shade with `capitalize`, a `showDate` prop, and the focus-within image shadow on a selector Chakra v3 accepts. Its three call sites (`app/page.tsx`, `app/stories/StoriesBrowser.tsx`, `app/stories/StoriesStatic.tsx`) render `Item`; the home passes `showDate={false}` as legacy's `index.tsx:302-303` did. A re-port that decides Chakra v3 shapes, so Fable. Nine items on `/`, filed there where first met and cited by the stories ledger. Record `fd9a92bd8abd`; decisions `ee86d68a`, `2032533f`, `ca677697`.

## Work

- Legacy: `../node-modules/apps/gridkit/components/stories/item.tsx` at `fce357d` (111 lines) and `apps/gridkit/stories.ts:27-32` (`StoryCategoryColors`). Current: `app/_components/StoryCard.tsx`, `app/_lib/stories.ts` (the `StoryMetadata` shape), `app/_components/story/StoryImage.tsx`.
- [[342f8aa75017]]: the file `app/_components/stories/Item.tsx`, `Item` with `metadata` and `showDate = true`; `app/_components/StoryCard.tsx` deleted; the site's `StoryMetadata` shape kept (`slug` and `external` in place of legacy's `url` and `isExternal`, `publishedAt` a string parsed with `new Date`: the catalog's shape is the stories index record's [[753b16d28130]]), so `href` is `/stories/${slug}` or `external.url`; `StoryCategoryColors` added to `app/_lib/stories.ts` as legacy's `stories.ts` had it (`guide: 'primary'`, `inspiration: 'purple'`, `newsletter: 'accentA'`) and the local `categoryLabels` and `categoryPalettes` maps gone.
- [[df00ae405803]]: `<HoverCardContainer as="section" aria-label={title}>` (exported by `1.2.0`). [[e24f5a2979c0]]: the hover and focus styles are the container's, no hand-written `_hover` or `_focusWithin` scale. [[7bb1c0ef2970]]: `<Container maxW="md">`. [[c66129279f12]]: `<Heading size="md">{title}</Heading>` and, after the content, `<LinkOverlay as={NextLink} href=... aria-label={title} />` empty, or for an external story `<LinkOverlay asChild><chakra.a href={external.url} target="_blank" rel="noopener noreferrer" aria-label={title} /></LinkOverlay>`, Chakra v2's `LinkOverlay isExternal` output in the shape `ed16aad27638` sanctions and `1.2.0`'s `LinkCard` uses (`dist/components/LinkCard.js:19-27`), since Chakra v3's `LinkOverlay` drops a `rel` prop (`node_modules/@chakra-ui/react/dist/esm/components/link/link-box.js:10`). [[7d1f5d0a8c9b]]: `<Badge css={{ backgroundColor: `${StoryCategoryColors[category]}.100`, fontSize: 'sm', fontWeight: 'normal' }}>{capitalize(category)}</Badge>`, `capitalize` from `lodash-es` (MIT, legacy's `apps/gridkit/package.json:61`; add `lodash-es` and `@types/lodash-es`, the bump named in the commit). [[2a6f442cbd7f]]: the date only when `showDate`, `toLocaleDateString('en-NZ', { day: 'numeric', month: 'numeric', year: 'numeric' })`. [[656dc6d730d7]]: `<Image type="cloudinary" {...image} className="stories-item-image" sizes={{ base: 'md' }} css={{ aspectRatio: '4 / 3', borderRadius: 'xl', boxShadow: 'md', marginBottom: 4, objectFit: 'cover', width: '100%' }} />` from `@villagekit/ui` (the story metadata's `image.src` is a Cloudinary public id, `content/stories/whats-a-grid-unit.mdx:15-20`); `md` is a size name `1.2.0` knows (`dist/hooks/useSizeWidths.js`), so no `full` is needed here. [[150c408aba5a]]: the focus-within shadow as `css={{ _focusWithin: { '& .stories-item-image': { boxShadow: 'outlineLarge' } } }}` on the `Container`, the nested selector form Chakra v3's `css` accepts, so the console no longer prints `Using kebab-case for css properties in objects is not supported` for a card.
- `app/_components/story/StoryImage.tsx` stays: the story pages' MDX components import it (`app/_components/story/index.ts`), and that use is the story pages record `56e6eb197e6c`'s (its item `09c3946b1912`).
- The same change closes items on `/stories` that describe the card: read [[81fe5de78974]] (the date format), [[2b12ca93bdd3]] (the badge shade) and [[1cfa347f2c9c]] (the external icon's color and size) and fix each whose text this change meets, the Outcome naming them; the ledger is the current state, whichever record's route an item sits on. The icon's exposure, [[2c38c9e6a5da]], is not this slice's to close: its Log asks the operator (rule 5) before legacy's unnamed image is restored, so `Item` renders the icon as legacy's `item.tsx:99` does, line for line, and the item gets a note saying so and stays `regression` for the stories index record's split or verdicts plan.
- Interfaces: consumes the mounted `MediaProvider` (`Shell: MediaProvider mounted for the home's ui Image and Video`); produces `Item` for the page re-port and the stories index record.
- Verify first: `grep -rn 'StoryCard' app` prints exactly the component and its three call sites; `grep -n 'HoverCardContainer' node_modules/@villagekit/ui/dist/index.d.ts` prints the export.
- Not this slice: the row the cards sit in on `/` (the page re-port's [[01bf9692dcea]]); the two fixed cards ([[774792ad37d6]], the page re-port's); the stories index's grid, filters and catalog shape.

## Seams under test

None pure; the proof is the aria tree, the console and the pairs on `/` and `/stories`.

## Done when

- `pnpm audit:dom --routes <a file naming / and /stories>` against a running `pnpm dev`: on `/` each story card is a `region` named by its title holding a `heading` and a `link` each named by the title, shaped as `legacy.aria.yaml`'s; on `/` no card shows a date and on `/stories` every card shows `d/m/yyyy`
- The browser console on `/stories` prints no `kebab-case` warning, and Tab onto a card shows the `outlineLarge` shadow on its image
- `pnpm audit:pages --routes <the same file>` at 375, 768 and 1280 looked at against legacy: the card's `md` width cap, the `primary.100` guide badge, the hover scale
- On `/stories` the external cards' overlay anchors render `target="_blank" rel="noopener noreferrer"` (`curl -s localhost:3000/stories | grep -o '<a[^>]*chakra-linkbox__overlay[^>]*>'`)
- `ls app/_components/stories/Item.tsx` succeeds and `ls app/_components/StoryCard.tsx` fails; `grep -rn 'from .*StoryImage' app/_components/stories` prints nothing
- The nine items are `fixed`, each `/stories` item the Outcome names is `fixed`, and `2c38c9e6a5da` carries its note, checked after the fixes
- `timeout 900 just check` is green

## Outcome

Shipped: `app/_components/stories/Item.tsx`, legacy's `components/stories/item.tsx` at `fce357d` again, exporting `Item` with `metadata` and `showDate = true`: `HoverCardContainer as="section" aria-label={title}` > `LinkBox` > `Container maxW="md"`, the cover the ui media `Image` inline (`type="cloudinary"`, the spread `image`, `className="stories-item-image"`, `sizes={{ base: 'md' }}`, legacy's `sx` as `css`), `Heading size="md"` beside an empty `LinkOverlay` named by the title (`as={NextLink}` for a hosted story; for an external one `LinkOverlay asChild` around a `chakra.a` with `target="_blank" rel="noopener noreferrer"`, the shape `ed16aad27638` sanctions and `1.2.0`'s `LinkCard` uses, since Chakra v3's `LinkOverlay` drops `rel`), the badge on `${StoryCategoryColors[category]}.100` with `capitalize` from `lodash-es` (already in `package.json`, nothing added), the date only when `showDate`, `new Date(publishedAt).toLocaleDateString('en-NZ', numeric day, month, year)`, the external icon `<Icon as={FaExternalLinkAlt} css={{ color: 'gray.300' }} boxSize="4" />`, and the focus-within image shadow as `_focusWithin: { '& .stories-item-image': { boxShadow: 'outlineLarge' } }` on the `Container`. `StoryCategoryColors` added to `app/_lib/stories.ts` as legacy's `stories.ts:27-32`; `app/_components/StoryCard.tsx` deleted with its `categoryLabels` and `categoryPalettes` maps; the three call sites render `Item` (`app/page.tsx` as `Item as StoryItem` with `showDate={false}`, legacy's `index.tsx:46,302-303`; `StoriesBrowser` and `StoriesStatic` bare). `StoryImage` stays for the story pages' MDX. The `landing` module and the featured-stories selection and grid on `/` are untouched, the page re-port's.

Translations, each the app router's or Chakra v3's and none a visible change: the file carries `'use client'` because `Icon as={FaExternalLinkAlt}` hands a plain function to Chakra's client `Icon`, which a server component (the `/stories` page's static grid, the home page) cannot do; `useTheme` and `theme.shadows.outlineLarge` become the token name `'outlineLarge'`; `spacing` becomes `gap`, `sx` becomes `css`; `isExternal` becomes `external != null`, `url` becomes `slug` or `external.url` (the site's catalog shape, `753b16d28130`); a Biome suppression (`lint/a11y/useAnchorContent`) sits on the empty overlay anchor, named by its `aria-label` as legacy's was. Legacy's `key={title}` on the `LinkBox` is kept.

Proof, against a running `pnpm dev` with a routes file naming `/` and `/stories`: `pnpm audit:dom` shows every card on both routes as `region "<title>"` holding `img`, `heading "<title>" [level=2]`, `paragraph`, `text: Guide`, and `link "<title>"`, the shape of `legacy.aria.yaml` (on `/stories` the hosted cards' blocks diff empty against legacy's, and the four external cards differ only by legacy's bare `img` for the icon, `2c38c9e6a5da`, and by their cover alt texts, the stories ledger's copy items; on `/` the cards differ only in which stories, the page re-port's `774792ad37d6`); on `/` no card shows a date, on `/stories` every card shows `d/m/yyyy` (`29/11/2024`, `17/09/2017`, the same text as legacy). A Playwright probe on both sides at 1280 and 375: hover scales the section to `matrix(1.05, ...)` with a pointer cursor, Tab onto the overlay gives the image a `0 0 0 4px` shadow (`outlineLarge`, the `outlineColor` literal being the palette's upstream item), `sizes="448.00px"` on every cover on both sides, every Cloudinary cover URL 200, the badge 14px at 400 on the `100` shade with the body's text color, the date `marginLeft` 8px, the external icon 16px on `gray.300`, `aria-hidden="true"` by Chakra v3's `Icon`, the overlay anchors on `/stories` `target="_blank" rel="noopener noreferrer"` (the curl in Done-when). `pnpm audit:pages` at 375, 768 and 1280 looked at: the `md` cap, the `primary.100` (pink) guide badge, the hover scale. `timeout 900 just check` green; `kipu verify --warnings-as-errors` green.

The console, the one Done-when line that did not hold: the `[dataStoryImage]` kebab-case error the old card logged is gone (`150c408aba5a` fixed, its Current text being the old selector), but the browser console on `/stories` still prints one kebab-case error and Next's dev badge still shows `1 Issue` on `/` and `/stories`: `Using kebab-case for css properties in objects is not supported. Did you mean .uiHoverCard?`, from `@villagekit/ui@1.2.0`'s own `HoverCardContainer`, whose `.ui-hover-card` keys Chakra v3 flattens (pre-existing on `/designs` through `ItemCard`), filed on `shell` as `7877c267268c` (regression, code), the fix a ui slice's, not this one's. `/stories` on dev also warns that the first cover is the LCP without `priority`, as any story cover has since the old card; nothing to note on `f9729ac7552a`, no `priority` image was added.

Differences: nine on `/` fixed (`342f8aa75017`, `df00ae405803`, `e24f5a2979c0`, `7bb1c0ef2970`, `c66129279f12`, `7d1f5d0a8c9b`, `2a6f442cbd7f`, `656dc6d730d7`, `150c408aba5a`); three on `/stories` fixed (`81fe5de78974` the date format, `2b12ca93bdd3` the badge shade and text color, `1cfa347f2c9c` the icon's color and size); `2c38c9e6a5da` noted and left `regression`. Three filed `regression` for what the re-port meets at 1.2.0, each the ui's to fix: `5c1af396cc2e` on `shell` (visual, refiled from `/` at the Spec review since a ui container recipe closes it for every bare `Container`: Chakra v3's recipe pads the card 32px at 1280 and 24px at 768 where legacy's pads 16px, and positions it, so the covers are narrower than legacy's at md and up on both routes), `7877c267268c` on `shell` (code: the HoverCardContainer console error above), `2363cb52f9d8` on `shell` (visual: Chakra v3's badge recipe box, `px 1.5`, `minH 5`, `inline-flex`, against v2's `px 1` inline). Expected residuals on the pairs, all `upstream` on `shell`: the heading at 18px semibold (`77cd1426ac65`), the palette and shadow literals (`72b776cb0d3f`, `1f11773f8e2b`).

What changed on `/stories`: the cards are legacy's again (region, heading and named overlay link, `md` container cap, the `100`-shade badge with `Guide`/`Newsletter`/`Inspiration` from `capitalize`, the numeric date, the `gray.300` icon at 16px, the hover scale from `HoverCardContainer`, the focus-within image shadow, the ui `Image` at `sizes="448.00px"` in place of the `next/image` wrapper's `(min-width: 1024px) 1024px, 100vw`); the grid, filters and the page's `Container maxW="4xl"` are untouched (the stories index record's).

For the page re-port `159c621d8a1a`: the import is `Item as StoryItem` with `showDate={false}`, as legacy's `index.tsx:46`; the row (`01bf9692dcea`) and the two fixed cards (`774792ad37d6`) are its. For the stories records: `publishedAt` is an ISO date string (`'2024-10-10'`), which `new Date` parses as UTC midnight where legacy's `new Date('2017/09/17')` parsed as local time, the code item `82762f6b27de` (on the story page template, `regression`); its visible side is the card date, the same on the server (UTC) and in any browser at or east of UTC, the previous day with a hydration text mismatch in a browser west of UTC, noted on that item at the Parity review. The old card had the same parse. The external overlay's `asChild` shape is noted on `ed16aad27638`, the sanctioned LinkCard item with the same Chakra v3 cause, rather than filed again. Review: Standards found the three new items' line cites into `Item.tsx` off by one to ten lines (corrected) and flagged the bare id cites in item prose and the missing TSDoc on `StoryCategoryColors` (dropped: the ledger's items cite ids bare throughout, and the export is legacy's line, its file's other exports carrying none); Spec found the unmet Done-when line above (recorded), the same cites, the container item's route (refiled), the aria claim above (corrected) and that the plan's `lodash-es` bump was already made by the design carousel slice (nothing added); Parity found nothing still open or dropped; it asked for the two notes above (written) and could not compare the covers on `/` at 375 and 768 because the legacy capture left its lazy story covers blank, the card structure matching (the 1280 pair and the probe cover them).

## Log
