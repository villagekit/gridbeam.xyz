---
title: "M1: the parity ledger"
status: doing
parent: 1783931160f2
---

## Goal

Every difference between the legacy site and this one is an item in the `difference` collection, cited on both sides and judged by rule, so that the copy grilling can start from a complete list and M2 can be planned from facts. Decision `ee86d68a`.

## Scope

- The audit tooling gains DOM extraction: for every route in `scripts/audit-routes.txt`, on both sides, the visible text in document order and the accessibility tree, written under `audit/<slug>/dom/` beside the screenshot pairs `pnpm audit:pages` already writes. `pnpm audit:dom` runs it with the same flags.
- The ledger for `shell` first (decision `bfa9a416` names the files on both sides), then for every route in `scripts/audit-routes.txt`, filled by the `parity` skill: Sonnet sub-agents per route per family, items minted and judged by the worker under decisions `2032533f` and `ad5363e4`, copy and `added` items left `open` (decision `ca677697`). `/suppliers` is an `added` route and is run in full, its copy one `open` item per visible text block; `/store`, `/legal/cookie-policy` and `/legal/return-policy` are legacy-only and get one `removed` item each, sanctioned under the matching rule, not a diff.
- The carried-forward findings in note `526d5330` are checked by the slice whose route they touch, and each one that still holds is a filed difference. The cross-cutting findings there (the dead-code sweep, metadata and config drift, `@villagekit/ui`'s Next peer claim, `LinkCard`'s accessible name, the media transforms) belong to the shell slice; `ImageCarousel` to the home slice for `/` and to the stories slice for the newsletters; the catalogue empty state and the sibling-repo findings on the 3D viewer (`detect-gpu`'s unpkg request, the sandbox's Chakra v3 CSS, `utility-workbench`'s 0 gu beams) to the designs slice; the `CutBeamSvg` swap and the planner copy to the cutting-planner slice. The note's "small visual and a11y nits" names no route and is superseded by the ledger.

## Seams under test

The DOM extraction's pure part: the function that turns a captured page's visible text into the normalized, line-per-block `.txt` form, tested with a fixture string. Nothing else here is pure.

## Exit demo

`pnpm audit:dom --routes scripts/audit-routes.txt` exits 0 against the legacy site and a local `pnpm dev`, writing both sides for every route that exists on both; `kipu list --collection difference --json` has items on `shell` and on every route the parity notes on this record do not report as identical; every `open` item is on the `copy` axis or is `kind: added`.

## Out of scope

Fixing anything. Judging copy.

## Outcome

## Log

- 2026-09-12: Shell ledger filled (plan b2ed8f4c). 104 items on `shell`. By state: open 42 (copy 29, added on visual 3, code 8, accessibility 2), regression 45 (code 24, visual 15, accessibility 4, interaction 2), sanctioned 17 (code 9, copy 5, visual 3), dismissed 0. By axis: code 41, copy 34, visual 21, accessibility 6, interaction 2. By kind: changed 49, removed 26, added 29. Sanctions cite rule: no e-commerce (6), no startup plumbing (3), upgrade (5), lock: header (1), lock: footer (1), operator (1, the hosting call in CLAUDE.md). Found identical by the sub-agents: `lang="en"`; the header's structure, sticky position, `accentB.50` background and dashed border; the `Main` and `Navigation` landmark labels and the toolbar roles; the footer's level-2 headings; the `Link`, `Button`, `Text` and `NavLink` recipes and the palette role mapping (pink, cyan, yellow); `useIsMobile` and `useTopNavHeight`; the brand SVG geometry; the Cloudinary image transform string and the ui `Video` component; the `ContentLayout` structure; the focus ring tokens; the hover and active styles of `Link`, `Button` and `NavLink`; escape-to-close on the mobile menu; the slogan and copyright. Note `526d5330` checked: `LinkCard`'s unnamed overlay anchor is the same on both sides (0.9.0 named a roleless div), so no accessibility item, and the dropped `aria-label` is a code item; no unreferenced file or export in `app/_components` or `app/_lib`, nor in the legacy shell files (the ui exports the site never imports have their own items); the metadata drift, the Next peer claim and the media transforms are filed. Evidence: the `audit/_root/` captures are regenerated by `pnpm audit:pages` and `pnpm audit:dom` with a routes file holding `/`; the legacy `@villagekit/ui@0.9.0` source the items cite is `npm pack @villagekit/ui@0.9.0` unpacked (its `src/` ships in the tarball). Left to the route ledgers: the `CatalogueLayout` and `StoriesLayout` removals (designs, stories), story images bypassing Cloudinary (stories), each route's own metadata override (the shell item covers the mechanism).

- 2026-09-12: Home and about ledgers filled (plan 3f28cb79). `/`: 115 items. By state: open 53 (copy 45, added on visual 4, code 2, interaction 1, accessibility 1), regression 52 (visual 24, code 14, accessibility 9, interaction 5), sanctioned 10 (code 5, copy 4, visual 1), dismissed 0. By axis: copy 49, visual 29, code 21, accessibility 10, interaction 6. By kind: changed 89, added 20, removed 6. Sanctions cite rule: upgrade (4), lock: home (3), rule: rebrand (1), rule: no e-commerce (1), rule: operator (1). `/about`: 42 items. By state: open 32 (copy 29, added on visual 3), regression 7 (visual 4, code 3), sanctioned 3 (code 2, copy 1), dismissed 0. By axis: copy 30, visual 7, code 5. By kind: changed 22, added 20. Sanctions cite rule: upgrade (1), rule: rebrand (1), rule: operator (1). Found identical by the sub-agents on `/`: the hero headline and the Mix testimonial's text; the testimonial names and order; the "Build a" prefix logic and the live-region wording; the "How to get started" heading, step count and order; the section order of the seven legacy sections; the alternating tint and row direction; the typing timing (100ms, 2500ms) and `pluralize` article logic; the design image mirror; the video attributes; the 4000ms carousel interval; the list's `ul`/`li` semantics with icons hidden on both sides; the `BlockSection` quote icon `role="presentation"`; the "What's a Grid Unit" card's title, description, badge, alt and href; the `accentA.500` typed label (legacy's `captitalize` typo is a no-op on both). On `/about`: the second caption and its bold span ("The primary building components are beams, panels, and fasteners."); the "Beam" and "Plywood panels" bold spans; the six-image order, count and dimensions; the centered captions; one `h1` and one `main` per side; no image inside a link; no `aria-describedby` on the description on either side; on the interaction family the legacy route has no interactive element, so the family's only entries are the added section's cards and buttons, filed as that section. Shell items checked and not repeated: the per-route `Main` and `SkipNavContent` ([[a73e9678cd57]], [[9c1d2ab08b15]]), the video sources and transforms ([[b397b0deb1cc]]), the route metadata override mechanism ([[f46533a8ae54]]), the Cloudinary loader ([[6c566c2715e0]]), the `LinkCard` unnamed anchor ([[2cbb4f4b8497]]), the 768 overflow ([[fb033121d164]], present on `/about` too: the current 768 captures are 1018px wide, so the 768 pairs of both routes compare the shell overflow and nothing else), the palette literals ([[72b776cb0d3f]]). Note [[526d5330ef4e]] checked on `/`: the home copy rewrites are filed (the kit heading and paragraph, "Have fun assembling your design." is a period only, the stories blurb, the community paragraph); the "Perfect for creators, inventors, and other innovative folk" line exists on neither side and gets no item; `ImageCarousel`'s dead half and its accessibility differences hold and are filed; the typing preload loss holds. `StoryCard` and `StoryImage` differences are filed on `/` where first met (eight items, logged on each); the stories ledger cites them rather than re-filing. `app/_lib/designs.ts` itself is left to the designs ledger; the home's use of it is filed. The re-hosted Cloudinary public IDs cite decision [[6b7a97f83ce7]] under rule 5, minted from CLAUDE.md's media layout as the shell did for hosting. Observations outside the ledger: no `// ported from` citation on `app/page.tsx`, `app/about/page.tsx` or the landing components except `useDesignTypingEffect.ts` (a Standards matter for the re-port slices, not a parity difference); the legacy 1280 capture of `/` shows two lazy photos blank and the current capture shows the Next dev overlay badge, both capture artifacts. Evidence: `audit/_root/` and `audit/about/` regenerated this run with `pnpm audit:pages` and `pnpm audit:dom` on a routes file holding `/` and `/about`; the legacy `@villagekit/ui@0.9.0` source is `npm pack` unpacked.

- 2026-09-12: Cutting planner and suppliers ledgers filled (plan 5c68a1cb). `/tools/cutting-planner`: 58 items. By state: open 23 (copy 19, added on code 2, interaction 2), regression 29 (visual 10, code 7, interaction 6, accessibility 6), sanctioned 6 (code 5, accessibility 1), dismissed 0. By axis: copy 19, code 14, visual 10, interaction 8, accessibility 7. By kind: changed 48, added 10. Sanctions cite rule: upgrade (4: the Chakra v3 compound primitives, `chakra.label` for `FormControl`, `'use client'`, the steppers v3 exposes), rule: operator (2: the in-app module for the private workspace package, and Vitest, both CLAUDE.md calls). `/suppliers`: 36 items. By state: open 33 (copy 22, added on accessibility 5, code 2, visual 2, interaction 2), regression 2 (interaction 1, code 1: the map decision [[8b5e51fcaf61]] names is not built, [[f7ea0bf1394d]] and [[36541273e623]]), sanctioned 1 (the route itself, rule: operator by [[8b5e51fcaf61]]), dismissed 0. By axis: copy 22, accessibility 5, code 4, interaction 3, visual 2. By kind: added 34, removed 2. The copy is one open item per visible text block, the unrendered empty state and the unused badge labels included. `/store`: 1 item, removed, sanctioned under rule: no e-commerce ([[8c78b2c12f47]]); not diffed. Found identical by the sub-agents on the planner: the h1; the "Beams you want" and "Beams you have" titles and their `section aria-label` regions; the "Controls", "Cut beams" and "Uncut beams" section labels; the "Quantity" and hidden "Delete" headers; the "Cutting plan" h2; the switch's accessible name "Display units as millimeters or grid units" and its native checkbox; the default rows (8 x 10 gu, 4 x 15 gu) and the empty stock table; the Plan button's variant and its disabled rule; the Add button's variant and size; the round `IconButton` and its `title`-to-`aria-label` mechanism; the Button recipe values and the `outline` shadow token; the single-column stacking at 375; the first-fit-decreasing loop, `beamQuotasToBeams`, `lengthRemaining`, the algorithm's default of 60 and every ported test fixture; the steppers out of the Tab order on both sides. On `/suppliers` nothing is identical by construction; the sub-agents found the heading outline clean (h1, h2, h3, h3, h2), the Tab order in DOM order with nothing unreachable, and the "Become a supplier" link internal and unambiguous. Note [[526d5330ef4e]] checked: the four planner labels are filed ([[7edc3d5964c1]], [[f97ec7a03444]], [[de7d971e590b]], [[378cedb39bc6]]); "its default rows and stock" holds for the top-up default ([[0db3cce2de84]]) and the Add-row defaults ([[7622f099a13f]]) and not for the rows, which are identical; the `CutBeamSvg` swap holds and is filed as [[7f2556a9ec9d]], still blocked on a `@villagekit/part-gridbeam` publish (the registry has 0.10.0 with no `./svg`, the sibling `../gridkit` holds `915085f`, `d4b3e7e`, `e58d700`, the branch `stream-07/09-cutgridbeamsvg` is unmerged). Shell items cited and not repeated: the route metadata mechanism [[f46533a8ae54]], the url-state helper [[43c1babc2051]], the nav and footer Suppliers links [[191e28561c6d]] and [[703c2d78a721]], the store redirects and plumbing [[206a60875a2b]] and [[62a5f2d31990]], the 768 overflow, the bold-span pattern [[01ae1e943907]]. Two shell items the shell run missed, found on the planner and filed on `shell`: the ui `Select` and `NumberInput` wrappers dropped `focusBorderColor` ([[dc60b9b9c1bf]]) and Chakra v3's gray focus-visible outline layers over the recipe's outline shadow ([[6a18067acb3b]]). Observations outside the ledger: `app/tools/cutting-planner/algorithm.ts:3` cites `packages/applet-cutting-planner/src/lib.ts`, a path that does not exist at fce357d (the sources are `shared.ts` and `algorithms/first-fit-decreasing.ts`; the test file cites them correctly), a Standards matter for the re-port slice; `app/suppliers/page.tsx` has no `// ported from` and nothing to cite; `content/suppliers.ts` sets `region` on both suppliers and nothing reads it; the suppliers page's non-breaking spaces and dashes read as plain text to assistive technology (no item); the Next dev badge in the current captures is a capture artifact. Evidence: `audit/tools__cutting-planner/` and `audit/suppliers/` regenerated this run with `pnpm audit:pages` and `pnpm audit:dom` on a routes file holding `/tools/cutting-planner` and `/suppliers current-only`; the result state after Plan, the unit toggle, the add-row and clamp probes and the Tab-order traces were captured by the sub-agents with Playwright on both sides (scratchpad only); the legacy `CutGridBeamSvg` was read from the sibling `../gridkit` at `e58d700` since the pinned 0.9.0 is not installed; the legacy `@villagekit/ui@0.9.0` source is `npm pack` unpacked; the 2023 `/order` page still responds at `https://gridkit-landing-8wj2weam2-villagekit.vercel.app/order` with the globe map, the "Locations" panel and two markers.

- 2026-09-12: Cutting planner and suppliers ledgers, after the code review (plan 5c68a1cb): the previous paragraph's planner counts are superseded. `/tools/cutting-planner`: 65 items. By state: open 23 (copy 19, added on code 2, interaction 2), regression 36 (visual 13, code 8, accessibility 8, interaction 7), sanctioned 6 (code 4, accessibility 2), dismissed 0. By axis: copy 19, code 14, visual 13, accessibility 10, interaction 9. By kind: changed 55, added 10. Sanctions cite rule: upgrade (4: the Chakra v3 compound primitives [[65c4396337e7]], `'use client'` [[06512316db56]], the steppers v3 exposes [[46efc6a1c050]], the switch's forced label element [[758e01571037]]) and rule: operator (2: [[fede2033572a]], [[e17abab6ea0e]], both CLAUDE.md calls). The Parity review added seven items the sub-agents missed: the section width [[6ec723376c9e]], the mm aria-label defect [[e72e07839c0b]], the result section on an all-infeasible plan [[6262e248fa04]], the caption size [[af37ba3b1195]], the toggle's group role [[91ab04d18791]], the icon glyphs [[2320a37e2a41]], the switch label [[758e01571037]]; it moved the `chakra.label` sanction [[6db1fb31e995]] to regression (Chakra v3 ships `Field.Root`, so rule 4 never covered it) and corrected [[c6792d6e6ec8]] to the select chevron alone. The Spec review corrected five line citations and the `/store` item's capture paths. `/suppliers` and `/store` are unchanged: 36 and 1. One clarification: the three engine fixes (`915085f`, `d4b3e7e`, `e58d700`) are on `main` of the sibling `../gridkit`, unpublished; the unmerged branch `stream-07/09-cutgridbeamsvg` is in this repo.

- 2026-09-12: Stories ledger filed (plan 843901f4): 82 items on the seven routes, plus 1 on `/` (the StoryCard focus-within selector) and 2 on `shell` (the ui `Link` dropping `isExternal`; the MDX blockquote). By route: `/stories` 27 (open 8, regression 18, sanctioned 1); `/stories/whats-a-grid-unit` 25 (open 7, regression 13, sanctioned 4, dismissed 1), which carries the story page template items shared by the six story routes; `/stories/how-to-cut-grid-beams` 2 (open 1, sanctioned 1); `/stories/how-to-furniture-bolts` 9 (open 4, regression 4, sanctioned 1); `/stories/building-with-grid-kit` 5 (open 4, regression 1); `/stories/2021-winter-newsletter` 7 (open 2, regression 5); `/stories/2022-newsletter` 7 (open 1, regression 6). By state: open 27, regression 47, sanctioned 7, dismissed 1. By axis: copy 20, visual 21, interaction 6, accessibility 15, code 20. By kind: changed 62, added 17, removed 3. Every open item is copy or `added`. The eight StoryCard items on `/` (342f8aa75017, 656dc6d730d7, 7bb1c0ef2970, 7d1f5d0a8c9b, c66129279f12, df00ae405803, e24f5a2979c0, 2a6f442cbd7f) were cited, not re-derived; three carry a new log line. Found identical: the MDX prose of all six stories word for word (note 48c33db3 holds) apart from the items filed; every heading text and level except the three furniture-bolts promotions; every image alt except the four inspiration cards and the how-to-cut hero; every link href; the section, row and column structure and indices; the card set and order on `/stories` (six internal, four external); the story titles, descriptions and categories; no video, grid or carousel on how-to-cut and building. Capture caveats: lazy-loaded images below the fold render blank in the current full-page screenshots (verified present in the DOM and on Cloudinary), and the Next dev badge appears in current captures; neither is filed.

- 2026-09-12: Correction: of the eight StoryCard items, two carry a new log line (342f8aa75017, 2a6f442cbd7f); the third log line is on 081bb0ea7651, the ImageCarousel item the newsletters cite. After review the stories ledger holds 88 items: 85 on the seven routes (open 27, regression 51, sanctioned 6, dismissed 0), 1 on `/`, 2 on `shell`.

- 2026-09-12: Final counts after two review rounds (plan 843901f4). The seven routes hold 88 items: `/stories` 29 (open 8, regression 20, sanctioned 1); `/stories/whats-a-grid-unit` 24 (open 7, regression 13, sanctioned 4); `/stories/how-to-cut-grid-beams` 4 (open 3, regression 1); `/stories/how-to-furniture-bolts` 10 (open 4, regression 5, sanctioned 1); `/stories/building-with-grid-kit` 5 (open 4, regression 1); `/stories/2021-winter-newsletter` 8 (open 2, regression 6); `/stories/2022-newsletter` 8 (open 1, regression 7). By state: open 29, regression 53, sanctioned 6, dismissed 0. By axis: copy 20, visual 23, interaction 8, accessibility 15, code 22. By kind: changed 66, added 18, removed 4. Beyond the seven routes the plan minted 1 item on `/` and 2 on `shell`, and appended a log line to three existing items on `/`. The earlier two count paragraphs above are superseded by this one.
