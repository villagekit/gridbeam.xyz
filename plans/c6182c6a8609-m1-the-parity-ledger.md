---
title: "M1: the parity ledger"
status: doing
parent: 1783931160f2
---

## Goal

Every difference between the legacy site and this one is an item in the `difference` collection, cited on both sides and judged by rule, so that the copy grilling can start from a complete list and M2 can be planned from facts. Decision `ee86d68a`.

## Scope

- The audit tooling gains DOM extraction: for every route in `scripts/audit-routes.txt`, on both sides, the visible text in document order and the accessibility tree, written under `audit/<slug>/dom/` beside the screenshot pairs `pnpm audit:pages` already writes. `pnpm audit:dom` runs it with the same flags.
- The ledger for `shell` first (decision `bfa9a416` names the files on both sides), then for every route in `scripts/audit-routes.txt`, filled by the `parity` skill: Sonnet sub-agents per route per family, items minted and judged by the worker under decisions `2032533f` and `ad5363e4`, copy and `added` items left `open` (decision `ca677697`). `/suppliers` is an `added` route and is run in full, its copy one `open` item per visible text block; `/store`, `/legal/cookie-policy` and `/legal/return-policy` are legacy-only and get one `removed` item each, sanctioned under the matching rule, not a diff.
- The carried-forward findings in note `526d5330` are checked by the slice whose route they touch, and each one that still holds is a filed difference. The cross-cutting findings there (the dead-code sweep, metadata and config drift, `@villagekit/ui`'s Next peer claim, `LinkCard`'s accessible name, the media transforms) belong to the shell slice; `ImageCarousel` to the home slice for `/` and to the stories slice for the newsletters; the catalogue empty state and the sibling-repo findings on the 3D viewer (`detect-gpu`'s unpkg request, the sandbox's Chakra v3 CSS, `utility-workbench`'s 0 gu beams) to the designs slice; the `CutBeamSvg` swap and the planner copy to the cutting-planner slice. The note's "small visual and a11y nits" names no route and is superseded by the ledger.

## Seams under test

The DOM extraction's pure part: the function that turns a captured page's visible text into the normalized, line-per-block `.txt` form, tested with a fixture string. Nothing else here is pure.

## Exit demo

`pnpm audit:dom --routes scripts/audit-routes.txt` exits 0 against the legacy site and a local `pnpm dev`, writing both sides for every route that exists on both; `kipu list --collection difference --json` has items on `shell` and on every route the parity notes on this record do not report as identical; every `open` item is on the `copy` axis or is `kind: added`.

## Out of scope

Fixing anything. Judging copy.

## Outcome

## Log

- 2026-09-12: Shell ledger filled (plan b2ed8f4c). 104 items on `shell`. By state: open 42 (copy 29, added on visual 3, code 8, accessibility 2), regression 45 (code 24, visual 15, accessibility 4, interaction 2), sanctioned 17 (code 9, copy 5, visual 3), dismissed 0. By axis: code 41, copy 34, visual 21, accessibility 6, interaction 2. By kind: changed 49, removed 26, added 29. Sanctions cite rule: no e-commerce (6), no startup plumbing (3), upgrade (5), lock: header (1), lock: footer (1), operator (1, the hosting call in CLAUDE.md). Found identical by the sub-agents: `lang="en"`; the header's structure, sticky position, `accentB.50` background and dashed border; the `Main` and `Navigation` landmark labels and the toolbar roles; the footer's level-2 headings; the `Link`, `Button`, `Text` and `NavLink` recipes and the palette role mapping (pink, cyan, yellow); `useIsMobile` and `useTopNavHeight`; the brand SVG geometry; the Cloudinary image transform string and the ui `Video` component; the `ContentLayout` structure; the focus ring tokens; the hover and active styles of `Link`, `Button` and `NavLink`; escape-to-close on the mobile menu; the slogan and copyright. Note `526d5330` checked: `LinkCard`'s unnamed overlay anchor is the same on both sides (0.9.0 named a roleless div), so no accessibility item, and the dropped `aria-label` is a code item; no unreferenced file or export in `app/_components` or `app/_lib`, nor in the legacy shell files (the ui exports the site never imports have their own items); the metadata drift, the Next peer claim and the media transforms are filed. Evidence: the `audit/_root/` captures are regenerated by `pnpm audit:pages` and `pnpm audit:dom` with a routes file holding `/`; the legacy `@villagekit/ui@0.9.0` source the items cite is `npm pack @villagekit/ui@0.9.0` unpacked (its `src/` ships in the tarball). Left to the route ledgers: the `CatalogueLayout` and `StoriesLayout` removals (designs, stories), story images bypassing Cloudinary (stories), each route's own metadata override (the shell item covers the mechanism).

- 2026-09-12: Home and about ledgers filled (plan 3f28cb79). `/`: 115 items. By state: open 53 (copy 45, added on visual 4, code 2, interaction 1, accessibility 1), regression 52 (visual 24, code 14, accessibility 9, interaction 5), sanctioned 10 (code 5, copy 4, visual 1), dismissed 0. By axis: copy 49, visual 29, code 21, accessibility 10, interaction 6. By kind: changed 89, added 20, removed 6. Sanctions cite rule: upgrade (4), lock: home (3), rule: rebrand (1), rule: no e-commerce (1), rule: operator (1). `/about`: 42 items. By state: open 32 (copy 29, added on visual 3), regression 7 (visual 4, code 3), sanctioned 3 (code 2, copy 1), dismissed 0. By axis: copy 30, visual 7, code 5. By kind: changed 22, added 20. Sanctions cite rule: upgrade (1), rule: rebrand (1), rule: operator (1). Found identical by the sub-agents on `/`: the hero headline and the Mix testimonial's text; the testimonial names and order; the "Build a" prefix logic and the live-region wording; the "How to get started" heading, step count and order; the section order of the seven legacy sections; the alternating tint and row direction; the typing timing (100ms, 2500ms) and `pluralize` article logic; the design image mirror; the video attributes; the 4000ms carousel interval; the list's `ul`/`li` semantics with icons hidden on both sides; the `BlockSection` quote icon `role="presentation"`; the "What's a Grid Unit" card's title, description, badge, alt and href; the `accentA.500` typed label (legacy's `captitalize` typo is a no-op on both). On `/about`: the second caption and its bold span ("The primary building components are beams, panels, and fasteners."); the "Beam" and "Plywood panels" bold spans; the six-image order, count and dimensions; the centered captions; one `h1` and one `main` per side; no image inside a link; no `aria-describedby` on the description on either side; on the interaction family the legacy route has no interactive element, so the family's only entries are the added section's cards and buttons, filed as that section. Shell items checked and not repeated: the per-route `Main` and `SkipNavContent` ([[a73e9678cd57]], [[9c1d2ab08b15]]), the video sources and transforms ([[b397b0deb1cc]]), the route metadata override mechanism ([[f46533a8ae54]]), the Cloudinary loader ([[6c566c2715e0]]), the `LinkCard` unnamed anchor ([[2cbb4f4b8497]]), the 768 overflow ([[fb033121d164]], present on `/about` too: the current 768 captures are 1018px wide, so the 768 pairs of both routes compare the shell overflow and nothing else), the palette literals ([[72b776cb0d3f]]). Note [[526d5330ef4e]] checked on `/`: the home copy rewrites are filed (the kit heading and paragraph, "Have fun assembling your design." is a period only, the stories blurb, the community paragraph); the "Perfect for creators, inventors, and other innovative folk" line exists on neither side and gets no item; `ImageCarousel`'s dead half and its accessibility differences hold and are filed; the typing preload loss holds. `StoryCard` and `StoryImage` differences are filed on `/` where first met (eight items, logged on each); the stories ledger cites them rather than re-filing. `app/_lib/designs.ts` itself is left to the designs ledger; the home's use of it is filed. The re-hosted Cloudinary public IDs cite decision [[6b7a97f83ce7]] under rule 5, minted from CLAUDE.md's media layout as the shell did for hosting. Observations outside the ledger: no `// ported from` citation on `app/page.tsx`, `app/about/page.tsx` or the landing components except `useDesignTypingEffect.ts` (a Standards matter for the re-port slices, not a parity difference); the legacy 1280 capture of `/` shows two lazy photos blank and the current capture shows the Next dev overlay badge, both capture artifacts. Evidence: `audit/_root/` and `audit/about/` regenerated this run with `pnpm audit:pages` and `pnpm audit:dom` on a routes file holding `/` and `/about`; the legacy `@villagekit/ui@0.9.0` source is `npm pack` unpacked.
