---
title: "ui Badge: selectable text and proportional numerals, Chakra v2's badge again"
status: done
parent: 337e35d86920
derived_from: a78b167170b8
tags:
  - "worker:fable"
priority: medium
---
A badge's text is selectable and its digits proportional again, as under `@villagekit/ui@0.9.0`: the package's `badgeRecipe` no longer keeps Chakra v3's `userSelect: none` and `fontVariantNumeric: tabular-nums`, which Chakra v2's badge never wrote. Closes [[a90ae9e6e022]] on `shell` for the shell record `a78b167170b8`, filed by the recipes slice [[2bd0169a6dda]] after the record and the shell's verdicts plan were written and owned by no slice until the about's finish found it (decision `40abdb2f222a`, a slice beside the record). A fix in `../ui`, so it follows decision `28c1a536`: committed in the sibling by pathspec, never pushed from here; seen on this site through the uncommitted `pnpm.overrides["@villagekit/ui"] = "file:../ui"` (`pnpm install` after each sibling edit) beside the uncommitted `transpilePackages: ['@villagekit/ui']` in `next.config.ts` that the `src/` copy needs (CLAUDE.md's ui row), both reverted by path before the commit; the item moved to `upstream` with a note citing the sibling commit; the bump plan `99f2fe62c62f` is `blocked_by` this slice (the edge written at the mint).

## Work

In `../ui/src/components/Badge.recipe.ts` `badgeRecipe` (ui commit `6603102`, the recipes slice) writes Chakra v2's box, type and 0.9.0's radius and text transform in its base and says in its doc comment that v3's `whiteSpace: nowrap`, `userSelect: none` and `fontVariantNumeric: tabular-nums` stay. Chakra v3's badge recipe base writes the three (`node_modules/@chakra-ui/react/dist/esm/theme/recipes/badge.js:6-15`), and `createSystem(defaultConfig, config)` (`../ui/src/theme/index.ts:104`) deep-merges the package's recipe over it (`node_modules/@chakra-ui/react/dist/esm/styled-system/system.js:25`, `mergeConfigs`; the recipes slice's Outcome read the same merge on the container recipe), so a declaration the package does not override stands. Chakra v2's badge theme (`@chakra-ui/theme@3.3.1` `components/badge.js:31-35`) and the v2 `Badge` component's own css (`@chakra-ui/layout@2.3.1` `dist/chunk-Z6RXEUPO.mjs:21-25`) wrote `whiteSpace: nowrap` and neither of the other two, the item's readings on the live legacy site (`user-select: auto`, `font-variant-numeric: normal`). Write `userSelect: 'auto'` and `fontVariantNumeric: 'normal'` in the base, the initial values, as overrides the merge keeps, and take the two names out of the comment's list of what stays; `whiteSpace: nowrap` stays. The emitted CSS is read once to confirm the two declarations land in the badge's class.
Verify first: on `pnpm dev` with the published 1.2.0, the story card's `Guide` badge on `/stories` at 1280 reads `user-select: none` and `font-variant-numeric: tabular-nums`; the live legacy site's reads `auto` and `normal` (the item's probe, repeated by a new one in the scratchpad).
Docs: `../ui/CHANGELOG.md`, an entry under Fixed, beside the recipes slice's badge entry.
Not this slice: the badge's box, type and radius ([[2363cb52f9d8]], `upstream` by [[2bd0169a6dda]]); the radius token scale ([[f40107b60034]], the operator's on `77cf83a1285a`); the story card's and the suppliers page's own badge props.

## Seams under test

None pure; the proof is a Playwright probe of the computed styles and a selection on `pnpm dev` under the override, against the live legacy site, and the screenshot pairs.

## Done when

- A Playwright probe at 1280 on `pnpm dev` under the override reads, on the first story card's badge on `/stories`, on the suppliers page's badges (`app/suppliers/page.tsx:168,177`) and on a filter badge on `/designs` (`app/_components/catalogue/Catalogue.tsx:341`, the `role="radio"` option, where `user-select` reaches a visitor), `user-select: auto` and `font-variant-numeric: normal`, the live legacy site's readings on `/stories` and `/designs`; a mouse drag across the `/stories` badge's text then reads that text from `window.getSelection().toString()`, as on legacy, saved beside the probe
- The badge's `display`, padding, font size and weight, radius, minimum height and palettes are unchanged (the recipes slice's readings: `inline-block`, 4px horizontal padding, 21px tall on a 21px line, no minimum height)
- `pnpm audit:pages` for `/`, `/stories`, `/designs` and `/suppliers` (every route with a `Badge`: `grep -rn "<Badge" app` prints four call sites in those routes' files) at 375, 768 and 1280 under the override, looked at: nothing moves, and every remaining difference on the pairs is ledgered
- In `../ui`: `pnpm lint`, `pnpm types`, `pnpm build:pkg` and `pnpm build:storybook` green; the change committed by pathspec on its `main`, not pushed
- [[a90ae9e6e022]] is `upstream` with a note citing the sibling commit; `99f2fe62c62f` is `blocked_by` this slice (checked at the finish) and carries a note that the bump makes no site edit for it and repeats the probe on the published package
- The override and the `transpilePackages` entry reverted by path (`git restore -- package.json pnpm-lock.yaml next.config.ts`, `pnpm install --frozen-lockfile`); `timeout 900 just check` is green

## Outcome

Shipped in `../ui` as commit f8b772f on its `main` over `5c5cd1e` (fifteen ahead of origin, not pushed; the push goes with the operator's publish, decision `28c1a536`), by pathspec: `src/components/Badge.recipe.ts` and `CHANGELOG.md` (one Fixed entry under Unreleased beside the recipes slice's badge entry, whose closing clause "and v3's `user-select: none` and tabular numerals stay" is dropped, since both entries describe the same unreleased version). The package version stays 1.2.0. This repo's commit carries kipu writes only: no site code changes.

What shipped: `badgeRecipe`'s base writes `userSelect: 'auto'` and `fontVariantNumeric: 'normal'`, the initial values, as overrides the theme merge keeps over Chakra v3's `userSelect: none` and `fontVariantNumeric: tabular-nums` (`node_modules/@chakra-ui/react/dist/esm/theme/recipes/badge.js:6-15`, deep-merged under the package's recipe by `createSystem(defaultConfig, config)` at `../ui/src/theme/index.ts:104`, `mergeConfigs` at `styled-system/system.js:25`, a `mergeWith` that cannot delete a key, so an override is the one lever), which Chakra v2's badge theme (`@chakra-ui/theme@3.3.1` `dist/components/badge.js:30-39`, the `baseStyle`) and the v2 `Badge` component's own css (`@chakra-ui/layout@2.3.1` `dist/chunk-Z6RXEUPO.mjs:21-25`, which alone wrote `whiteSpace: nowrap`) never wrote; both read in the recipes slice's packed copies, since the legacy checkout installs neither. The doc comment names the two as reset and keeps `whiteSpace: nowrap` as what stays. Nothing else in the recipe changes: the base's `display`, `verticalAlign`, `fontSize`, `fontWeight`, `borderRadius` and `textTransform`, and the `sm` size.

Verified. Before, on `pnpm dev` with the published 1.2.0 against the live legacy site (the scratchpad's `badge-probe.mjs`, `badge-before.json` and `badge-legacy.json`): the first `/stories` card badge (`Guide`) at 1280 read `user-select: none` and `font-variant-numeric: tabular-nums` against legacy's `auto` and `normal`; a drag from the page margin across the badge (the card's `LinkOverlay` covers the badge, so a drag that starts on it drags the link, on both sides) selected the card's title and description and excluded `Guide`, where legacy's selection included it. After, under the `file:../ui` override (`badge-after.json`): the card badge reads `auto` and `normal`, the drag selection includes `Guide`, and the emitted badge rule reads `font-variant-numeric: normal; white-space: nowrap; user-select: auto`; the `/suppliers` badges read `auto` and `normal`; the `/designs` option badges read `font-variant-numeric: normal` and `user-select: none`, and legacy's read the same `none`. That last reading deviates from the plan's Done when, which expected `auto` there: both sides' option components write `userSelect: 'none'` themselves (legacy `components/option.tsx:61` at `fce357d`, current `app/_components/catalogue/Catalogue.tsx:359`), so the option is at parity by the route's own prop and the recipe's change reaches only its numerals; no item is needed. The box readings are unchanged from the recipes slice's: the card badge `display: block` (its flex parent blockifies it; the rule reads `inline-block`), 47 by 21 on a 21px line, 4px horizontal padding, 14px at weight 400 (the card's own props), radius 8px, `min-height: auto`, `rgb(254, 215, 226)`, legacy's numbers exactly; the `/suppliers` badges 12px bold, 4px padding, `accentA` on `rgb(237, 253, 253)`. The screenshot pairs for `/`, `/stories`, `/designs` and `/suppliers` (current-only) at 375, 768 and 1280 under `audit/` (24 of 24 captures), captured under the override and looked at: no badge moves, and every remaining difference on the pairs is ledgered (the stories and designs indexes not yet re-ported, their route items, the `/stories` filter chips `73dd61e26135` among them; the `/designs` option rhythm `7362efc441a1` and radius `1d2677d8b187`; the home's verdicts plan; the shell's `upstream` items, `c9ea22823a23` and the wrapping wordmark `1d2562d6b3bc` at 768). In `../ui`: `pnpm lint`, `pnpm types`, `pnpm build:pkg` (the built recipe reads `userSelect: "auto"`, `fontVariantNumeric: "normal"`) and `pnpm build:storybook` green. The override and the `transpilePackages` entry reverted by `git restore -- package.json pnpm-lock.yaml next.config.ts` and `pnpm install --frozen-lockfile`, the dev server stopped by pid first; `timeout 900 just check` green against the published 1.2.0 (78 tests, exit 0).

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity) before the sibling commit, none critical or major. Standards, no hard violation; the mechanism held against `unset`, `initial` and deleting the keys (the deep merge cannot delete, and the literal values are the legacy computed readings), and the amended recipes entry held (same unreleased version). Spec, three minor notes taken into this Outcome: the v2 citation is `dist/components/badge.js:30-39`, the `baseStyle`, and the theme writes no `whiteSpace`, only the component does; `font-variant-numeric` is inherited where `user-select` is not, so `normal` equals v2's unwritten value only while no ancestor sets numerals, and none does (no `fontVariantNumeric` or `tabular` in `app` or `../ui/src` outside the recipe), where `unset` would be the exact v2 equivalent: the plan's values kept, since the readings match legacy and the plan states the diff, the inheritance point recorded here; the Done when lines that name the commit, the item and the gate written in after them. Parity: the closure holds on every badge the site renders at every width, no ui component or story renders a badge, no copy in the diff, nothing legacy did silently dropped (the option's hover, active and focus styles sit on the call site); legacy's cart-count and order-id badges belong to the dropped store (`2032533f`). Both Standards and Parity caught one citation in the draft (`Catalogue.tsx:356` for `:359`), fixed. Findings dropped: none.

Items: [[a90ae9e6e022]] moved to `upstream` with the ui commit in its note; `99f2fe62c62f` `blocked_by` this slice (the edge from the mint, checked) and carrying the note that the bump makes no site edit for it and what its repeat probe reads on the published package. No visitor-facing copy in the change. Beyond the ask: the recipes entry's stale clause in the ui CHANGELOG dropped.

## Log
