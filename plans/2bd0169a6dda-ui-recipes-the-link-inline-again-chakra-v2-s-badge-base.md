---
title: "ui recipes: the link inline again, Chakra v2's badge base, container and button sizes"
status: todo
parent: 337e35d86920
derived_from: a78b167170b8
tags:
  - "worker:fable"
priority: medium
---
Four recipes in `@villagekit/ui` render what Chakra v2's did under `@villagekit/ui@0.9.0`, so every inline link, badge, bare `Container` and sized button on the site matches the legacy site again: a link is `display: inline` and breaks across lines with its sentence; a badge is Chakra v2's `inline-block` box at `px: 1` with no minimum height; a `Container` pads `4` at every width, caps at `prose` and is not positioned; a button's `md` and `lg` sizes are 40px at 16px and 48px at 18px. Closes [[12a2882546e6]], [[2363cb52f9d8]], [[5c1af396cc2e]] and [[ebdb3183c0dd]] on `shell` for the shell record `a78b167170b8`, finished before the home's slices found them (decision `40abdb2f222a`, a slice beside the record). A fix in `../ui`, so it follows decision `28c1a536`: committed in the sibling by pathspec, never pushed from here; seen on this site through the uncommitted `pnpm.overrides["@villagekit/ui"] = "file:../ui"` (`pnpm install` after each sibling edit) beside the uncommitted `transpilePackages: ['@villagekit/ui']` in `next.config.ts` that the `src/` copy needs (CLAUDE.md's ui row), both reverted by path before the commit; the items moved to `upstream` with a note citing the sibling commit; the bump plan `99f2fe62c62f` is `blocked_by` this slice (the edge written at the mint).

## Work

In `../ui`, four recipes, each the smallest change that renders Chakra v2's base (`@chakra-ui/theme@3.3.1`, `npm pack` into the scratchpad, `dist/components/{link,badge,container,button}.js`) and keeps every other declaration as it renders today; read each item for the values measured on both sides.
- `src/components/Link.tsx:37-69` `linkRecipe`: Chakra v3's link recipe writes `display: inline-flex`, `alignItems: center` and `gap: 1.5` (`node_modules/@chakra-ui/react/dist/esm/theme/recipes/link.js:7-10`) and the package's recipe extends it with no display rule; v2's link theme wrote none, so a link was `inline`. `display: inline` in the base ([[12a2882546e6]]); whether `gap` and `alignItems` need an override once the link is inline is read in the emitted CSS.
- `src/components/Badge.recipe.ts` `badgeRecipe`: `borderRadius: lg` and `textTransform: none` alone over Chakra v3's badge recipe, whose base is `inline-flex` with `alignItems`, `gap` and `fontWeight: medium` and whose default `size: sm` writes `textStyle: xs`, `px: 1.5` and `minH: 5` (`recipes/badge.js:6-15,49-53`); v2's base was `px: 1`, `fontSize: xs`, `fontWeight: bold`, `borderRadius: sm`, `textTransform: uppercase` (`badge.js:31-35`), rendered `display: inline-block`, `vertical-align: middle` and `white-space: nowrap` by the v2 `Badge` component's own `__css` (`@chakra-ui/layout@2.3.1`, `dist/chunk-Z6RXEUPO.mjs:21-25`, `npm pack` into the scratchpad), no minimum height, and 0.9.0 set the radius `lg` and `textTransform: none` over it. Write v2's base under 0.9.0's two overrides ([[2363cb52f9d8]]); whether the v3 `size` variants stay reachable or are neutralized through `defaultVariants` is the worker's call, stated in the Outcome. The `100`-shade background, `fontSize: sm` and `fontWeight: normal` on the story card are the card's own props and stay; the suppliers page's badges (`app/suppliers/page.tsx:168,177`) are read before and after.
- `src/theme/index.ts:79-87` `recipes`: no `container` key, so Chakra v3's `Container` (`node_modules/@chakra-ui/react/dist/esm/components/container/container.js`, recipe key `container`) renders v3's recipe, `position: relative`, `maxWidth: 8xl`, `px: { base: 4, md: 6, lg: 8 }` (`recipes/container.js:6-12`); v2's container theme was `w: 100%`, `mx: auto`, `maxW: prose`, `px: 4`. A `containerRecipe` with v2's base, registered under `recipes.container` ([[5c1af396cc2e]]). Read first how the brand footer slice's ui commit `ae593d0` sized the footer's own `Container` ([[da830eb83c42]], `upstream`), so the recipe and that fix agree; every `Container` the package renders (`src/components/layouts/Section.tsx:99`, `Footer.tsx`, `CardsLayout.tsx`, `Title.tsx`, `Description.tsx`, `TableOfContents.tsx`) and every `Container` on the site takes the recipe (`grep -rln '<Container' app`: thirteen files, every one with a `maxW` except `app/_components/SiteFooter.tsx:123`, whose bare `Container` goes from `8xl` to `prose`, legacy's width per [[da830eb83c42]]), so each is read before and after.
- `src/components/Button.tsx:18-109` `buttonRecipe`: no `size` variants, so Chakra v3's apply (`recipes/button.js`: `md` is `h: 10`, `minW: 10`, `textStyle: sm`, `px: 4`; `lg` is `h: 11`, `minW: 11`, `textStyle: md`, `px: 5`, each with `gap` and `_icon` sizes); v2's were `lg` `h: 12`, `minW: 12`, `fontSize: lg`, `px: 6`; `md` `h: 10`, `minW: 10`, `fontSize: md`, `px: 4`; `sm` `h: 8`, `minW: 8`, `fontSize: sm`, `px: 3`; `xs` `h: 6`, `minW: 6`, `fontSize: xs`, `px: 2`. A `size` variant set with v2's four sizes on `buttonRecipe`, the shape `inputRecipe` carries for v2's input sizes (`src/components/Input.tsx:37-41`, [[6b5488415a06]]) ([[ebdb3183c0dd]]); `LinkButton`, `IconButton` and `LinkIconButton` render through the same recipe, so the header action, the mobile menu toggle and the catalog's `Clear search` button are read before and after.
Verify first: on `pnpm dev` with the published 1.2.0, the `with the community!` link in the home's steps list at 375 has one client rect on its own line where the live legacy site's has two; a `/stories` badge at 1280 measures 6px horizontal padding, `display: flex` and 20px tall here against 4px, `block` and 21px on legacy; the story card's `Container` at 1280 pads 32px here and 16px on legacy; a home CTA at 1280 is 44px tall at 16px here and 48px at 18px on legacy (the four items' probes, repeated).
Docs: `../ui/CHANGELOG.md`, one Fixed entry per recipe.
Not this slice: the `Section` band ([[9746fd6890c2]]) and the `HoverCardContainer` selectors ([[7877c267268c]]), the two slices minted beside this one; the radius token scale ([[f40107b60034]]), the toast regions ([[ee7575aea3ae]]) and the wordmark wrap at 768 ([[1d2562d6b3bc]]), the operator's on plan `77cf83a1285a`.

## Seams under test

None pure; the proof is a Playwright probe of the computed styles on `pnpm dev` under the override, against the live legacy site, and the screenshot pairs.

## Done when

- A Playwright probe on `pnpm dev` under the override reads, against the live legacy site's same reading: at 375 on `/` the `with the community!` link with two client rects, wrapping inside the link; at 1280 on `/stories` a badge with 4px horizontal padding, 21px tall on a 21px line (a flex parent blockifies it on both sides, so `display` is read on `/suppliers`), and the story card's `Container` with 16px padding and its cover 416px wide; on `/` the CTAs 48px tall at 18px at 1280 and 768 and 40px tall at 16px at 375
- The hover, press and focus colors and transforms of the four button variants, the header action and the mobile menu toggle are unchanged (the scratchpad's `press-probe.mjs` and `toggle-probe.mjs` from plans `8a869061bce7` and `61a42a0adbc8` read them), and the suppliers page's badges keep their palettes and read `display: inline-block`, 4px horizontal padding and no minimum height (the v2 component's rendering; legacy has no such page)
- `pnpm audit:pages --routes scripts/audit-routes.txt` (every route: the container recipe reaches every page and the footer) at 375, 768 and 1280 under the override, looked at: the containers, badges, buttons and inline links match legacy, and every remaining difference is ledgered (the `upstream` items' residuals, the shell's `c9ea22823a23` at 768, the routes' own open items)
- In `../ui`: `pnpm lint`, `pnpm types`, `pnpm build:pkg` and `pnpm build:storybook` green; the change committed by pathspec on its `main`, not pushed
- [[12a2882546e6]], [[2363cb52f9d8]], [[5c1af396cc2e]] and [[ebdb3183c0dd]] are `upstream` with a note each citing the sibling commit; `99f2fe62c62f` is `blocked_by` this slice (checked at the finish) and carries a note naming what the bump checks and any site edit it makes for these
- The override and the `transpilePackages` entry reverted by path (`git restore -- package.json pnpm-lock.yaml next.config.ts`, `pnpm install --frozen-lockfile`); `timeout 900 just check` is green

## Outcome

## Log
