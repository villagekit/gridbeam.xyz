---
title: "Route: stories index"
status: doing
parent: 337e35d86920
blocked_by:
  - a78b167170b8
  - target: 7f0b60d948c5
    strength: soft
    note: route order
---

## Goal

The route `/stories` is at parity with the legacy site: every difference on it is `fixed` or `sanctioned`, and the operator reviews it at the parity gate `f7a700a3e482`. Decision `ee86d68a`.

## Scope

The differences on `/stories` in the ledger (`kipu list --collection difference --filter route=/stories --json`): thirty-one items at the split, twenty-six `regression` (all found by M1, eight of them `open` until the stories grilling moved them to `regression`), three `fixed` (the card's, `1cfa347f2c9c`, `2b12ca93bdd3`, `81fe5de78974`), one `sanctioned` (`9e10796f13c5`), one `open` (`e3a2d4d66691`, filed at the split). The port rule of decision `ee86d68a` decides whether each slice re-ports from the legacy source or fixes in place; here the page and the catalog module are re-ported, the copy landing first on the current page.

The copy, six items, every verdict the operator's from the stories grilling (`3ef459e66cfb`): the meta description `39bf8c100615`; the title's description `cea94daa1b77`; the four inspiration alts `cef251754b6e`, `f50a60375633`, `46a9ea2b1eb2`, `045b9541f486`.

The catalog re-port, one item (`48c8cbdb206a`): the module's shape `753b16d28130`, its field names and the `[slug]` route's lookup left to the story pages record (`56e6eb197e6c`, its `82762f6b27de`, `c983ec56248e` and `9c48718ec6f8`).

The page re-port, eighteen items (`278fb531e229`): the width `1d5c23180ebf`, the spacing `1e0284dbf88d`, the animation `226f8b8127e6`, the filter row's alignment `33b40aded547`, the parameter `35529c01ddaf`, the history entry `10bea19307bb`, the grid gap `54f14be9aedd`, the markup `73dd61e26135`, `cc70e66936f8`, `ff07afa4ea2b`, the chips' shades `92dd755da0a4`, order `a1a6605dcec3`, palette `e524aaa90e2f`, size and gap `f3ff654e792d` and focus ring `f7905b047359`, the components `c4edacc9eba5`, the state `cbe45596b840`, the breakpoint hooks `d52710d109cb`; and the sanctioned `9e10796f13c5`, whose Suspense boundary the re-port removes, moving it to `fixed` below its verdict.

The operator's, on the attended verdicts plan beside this record (`239f17128896`): the icon's `aria-hidden` `2c38c9e6a5da` and the two-file page `e3a2d4d66691`.

Fixed already, not a slice's: `1cfa347f2c9c`, `2b12ca93bdd3`, `81fe5de78974`, the card's.

## Seams under test

Named when sliced; none pure unless the route carries logic.

## Exit demo

`kipu list --collection difference --filter route=/stories --json` shows no `open` or `regression` item, and `/finish-epic` finishes this record; the operator reviews the route at the parity gate `f7a700a3e482`.

## Out of scope

Other routes; the shell, except where a difference on this route is closed by a shell change, which then belongs to the shell record.

## Outcome

## Log

- 2026-09-26: Split into three slices (plan ca353de8b645), reviewed once on one Opus sub-agent. Against `@villagekit/ui@1.2.0`, in the order the edges give: `3ef459e66cfb` the per-page meta description removed and the copy verdicts applied on the title description and the four inspiration alts (Sonnet, six items, every verdict the operator's from the stories grilling); `48c8cbdb206a` the story catalog re-ported from `stories.ts` (Fable, one item), consuming the copy and the home's story card slice (`e332105c3b52`); `278fb531e229` the page re-ported from `pages/stories.tsx` with legacy's context, filters, option and list (Fable, eighteen items), last, consuming both, the shell's adapter (`531b810f2dbd`) and layout (`a7bf623f885c`) slices and the card. Every `regression` on `/stories` is closed by exactly one slice, twenty-five in all, or named by the verdicts plan `239f17128896` beside this record (one, `2c38c9e6a5da`, with the `open` item filed at the split, `e3a2d4d66691`); the three `fixed` items are the card's, and the one `sanctioned` item gets a note from the re-port. Calls made at the split, for the operator to read: (1) the page is re-ported, not fixed in place: the current route has a `Section`, an inner `4xl` container, a client browser on `useSearchParams` and a hand-written `url-state` helper with a native-button chip, no animation and no context, where legacy has a `Title`, an `8xl` container, a `constate` context over `use-query-params`, a `Badge` option and a `motion` list; the port rule says in doubt re-port, and the copy lands first on the current page, on Sonnet, so the re-ports carry settled text; (2) the catalog module is its own slice, before the page: `app/_lib/stories.ts` is read by three routes and the sitemap, and the card slice wrote `Item` against its current field names, so its shape is a call a reviewer could reject while approving the page; (3) that call's boundary with the story pages record `56e6eb197e6c`, whose items this record does not absorb: `753b16d28130` on this route is the module's structure (one `allStories` array, `linkedStories` inline, no wrapper, no accessors, no sort in the module), and the field names and date types (`82762f6b27de`), the editorial field (`c983ec56248e`) and the `[slug]` route's lookup of a story's `Content` (`9c48718ec6f8`) are the story page template's, filed there where first met, so the catalog slice keeps them (the type's names, `STORY_SLUGS` and `getStory` below legacy's exports) and closes only what is its, writing legacy's `updatedAt` on the four linked stories where no other item is entangled; `82762f6b27de` carries a note from the split extending it to the linked stories' `slug`, `external` and dropped `image.type`, so no part of the module's difference is left on no item, and the story pages record and `753b16d28130` carry a note each; (4) the accessibility markup returns as legacy wrote it, the `menubar` box owning a `listbox` of `option` badges with Enter-only keys and no `aria-pressed` or group name: the stories grilling's I3 verdicts on `cc70e66936f8` and `ff07afa4ea2b` say so, and `73dd61e26135` is `regression` with no verdict, so legacy's form ships (the faq split's call 6); the pass after M2 (note `eeba2a65cee4`) is where it is bettered; (5) the icon's `aria-hidden` (`2c38c9e6a5da`) is the operator's: the card is legacy's line, Chakra v3's `Icon` writes the attribute itself, and restoring legacy's exposed unnamed image would add a prop legacy never wrote; its twins `1ea1f9eda079` and `89301ca8a1fc` are on the home's and the shell's verdicts plans, so it goes on this record's verdicts plan, minted at the split rather than the finish (`40abdb2f222a` has the finisher mint it) so that a verdict given early shapes the re-port; an item a slice's review files later for the operator is added to that plan's list by the finish, which mints no second plan; (6) legacy's page calls `useBreakpointValue` and mounts a provider, so the re-port writes the home's two files (`091a47cb93e7`, `open` on the home's verdicts plan); filed at the split as `e3a2d4d66691` (`open`, code) and put to the operator on the same plan, not judged here; (7) the Suspense boundary and the static fallback (`9e10796f13c5`, sanctioned under rule 4 for `useSearchParams`) go with the re-port, since a consumer of `useQueryParams` under the shell's adapter needs none (the adapter slice's Outcome); the item moves to `fixed` with the re-port named, below the sanction's verdict, after a note saying why the force lapsed (the note alone if kipu refuses the move); (8) `constate` (4.0.0, MIT, peer React 19) is added by the re-port, legacy's context library; `use-query-params` is there already; (9) `Option` lands at `app/_components/Option.tsx` as a shared component, since legacy's catalog selector renders it too and the designs index record's slices will consume it; the context at `app/_lib/context/stories.tsx`, where legacy's `context/catalogue.ts` is ported beside it; (10) the translations: `spacing` to `gap`, `sx` to `css`, `theme.shadows.outline` to the token, `framer-motion` to `motion/react` with `motion.create`, `NextSeo` to `metadata`, `getLayout` to the root layout. Expected on the pairs until the bump, the shell's `upstream` items: the container padding (`5c1af396cc2e`), the badge box (`2363cb52f9d8`), the heading's size and weight (`77cd1426ac65`, `7124898563eb`), the palette and shadow literals (`72b776cb0d3f`, `1f11773f8e2b`), the `Title` container (`318456ddabc6`), the `HoverCardContainer` console line (`7877c267268c`); at 768 the footer (`c9ea22823a23`, the operator's). Items already the operator's: the two on the verdicts plan. Expected stops: none for the slices; at the finish the record finishes on `40abdb2f222a` with the verdicts plan standing, and no `upstream` item is expected from these slices. Review findings taken, all of them: the linked stories' `slug`, `external`, `image.type` and `updatedAt` had no item once `753b16d28130` closed (the note on `82762f6b27de` and legacy's `updatedAt` written); the sitemap check counts six `<loc>` lines, the index having no trailing slash; the icon question posed without a verdict, its twins' rule 4 case (an identical tree) not holding here, at `icon.js:22`; the verdicts plan's intro says `rule 4 candidate`, and names how later items join it; the `<QueryParamProvider` grep counts one; `motion.create` settled by the deprecation warning in `create-proxy.mjs`; the bailout check counts matches, not lines; `9e10796f13c5` to `fixed` rather than a note alone; a note on `e3a2d4d66691` citing the shipped files; the Scope's item history (all M1's, eight `open` until the grilling); this Log entry written at the mint so the slices' call numbers resolve; the catalog's baselines stated as counts and names, not as `the same as before`; legacy's `type` alias so its one cast compiles against `mdx.d.ts`; two nits, the option file's line count and two spellings. Dropped: none.
