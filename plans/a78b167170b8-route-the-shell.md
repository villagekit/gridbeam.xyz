---
title: "Route: the shell"
status: done
parent: 337e35d86920
blocked_by: e2805adefd47
---

## Goal

The route `shell` is at parity with the legacy site: every difference on it is `fixed` or `sanctioned`, and the operator reviews it at the parity gate `f7a700a3e482`. Decision `ee86d68a`.

## Scope

The differences on `shell` in the ledger, listed here by id when this record is sliced (`kipu list --collection difference --filter route=shell --json`). The port rule of decision `ee86d68a` decides whether each slice re-ports from the legacy source or fixes in place.

## Seams under test

Named when sliced; none pure unless the route carries logic.

## Exit demo

`kipu list --collection difference --filter route=shell --json` shows no `open` or `regression` item, and `/finish-epic` finishes this record; the operator reviews the route at the parity gate `f7a700a3e482`.

## Out of scope

Any single route's own differences; a route difference that a shell change closes is cited from here and fixed here.

## Outcome

Finished unattended by `/finish-epic` on 2026-09-26 (decisions `b9af27e0df50` and `40abdb2f222a`), the sixteen children `done`: the fifteen of the split plus `531b810f2dbd`, the Phase 0 slice the layout slice minted for the QueryParamProvider adapter. The shell's ledger at the finish: 149 items, 57 `sanctioned`, 35 `fixed`, 1 `dismissed`, 40 `upstream` (fixed in `../ui` by seven sibling commits, waiting on the operator's publish and the bump plan `99f2fe62c62f`, decision `28c1a536`), 9 `open` and 7 `regression`, every one of the last sixteen handed on below; two of the `open` ones, `e403eeab87ea` and `eca6acde1370`, were filed at this finish on the review's finding (below).

The exit demo, clause by clause. First, `kipu list --collection difference --filter route=shell --status open --status regression` prints sixteen items: `065852f6e813`, `13f1a23487f3`, `1eddda919812`, `89301ca8a1fc`, `920b8de82d4f`, `abb4c0b4f849`, `c9ea22823a23`, `e403eeab87ea`, `eca6acde1370` (`open`) and `1d2562d6b3bc`, `2f91880a9f4a`, `c440126f7064`, `e0824769af3d`, `ee7575aea3ae`, `f40107b60034`, `f85e98a67092` (`regression`). By `40abdb2f222a` the clause is met when each is `upstream`, named by the one attended verdicts plan beside the record, or named by a slice beside the record for a fix an agent can make: fourteen are on the verdicts plan `77cf83a1285a` and two on the slices `61a42a0adbc8` and `9e54dca30d48`, each item carrying a note naming where it went, and none was judged here (no state changed, no verdict written). Second, `/finish-epic` finishes this record: this finish, `kipu ready --json` having listed it as the one completable plan. Third, the operator reviews the route at the parity gate `f7a700a3e482`: the gate is `blocked_by` `77cf83a1285a` (written here), by the bump plan and by M2, so the operator's verdicts on the shell and the publish come before the review.

The items handed on. To the operator, on `77cf83a1285a` (attended, `--parent` M2, `derived_from` this record, a Wants line for the verdicts, one bullet per item with its state and the question with the alternatives the item names): `065852f6e813` the Open Graph site name, URL and manifest `start_url` under rule 1; `13f1a23487f3` the video alt's `made from grid beam`; `1eddda919812` the site-written query-param adapter under rule 4; `89301ca8a1fc` Chakra v3's `aria-hidden` beside `role="presentation"`; `920b8de82d4f` no CSP on the files the ASSETS binding serves; `abb4c0b4f849` the head metadata streamed into the body; `c9ea22823a23` the footer columns' 44 minimum between md and lg; `e403eeab87ea` the slide layer's `chakra-slide` class and `eca6acde1370` its `AnimatePresence` wrapper, Chakra v2 `Slide` internals the nav port left behind; `1d2562d6b3bc` the wordmark wrap at 768 (nowrap, a tighter gap or a narrower action); `2f91880a9f4a` the logo barrel without `gl` (the re-export or rule 5); `e0824769af3d` the bundler and the two webpack plugins (rule 5 or a webpack build); `ee7575aea3ae` one toast region for six (rule 4 or six toasters); `f40107b60034` the radius token scale (rule 4 on the residue or the scale rewritten). To an agent, two slices beside the record (`--parent` M2, `derived_from` this record, `worker:fable`, each following `28c1a536` and blocking the bump plan from the mint): `61a42a0adbc8` the mobile menu toggle's hover and press color, `c440126f7064`, a fix in `../ui` where the `color="gray.900"` style prop beats the `toolbar` variant's states under Chakra v3's cascade layer; `9e54dca30d48` the `rel` of `LinkButton`, `LinkIconButton`, `LinkCard`, `Footer` and `Social`, `f85e98a67092`, one line in each to render `noopener` as legacy did. Every shell regression is now named by a slice or by the verdicts plan; the record's Log rule that each is in exactly one slice's Closes line does not hold for the five on the verdicts plan, whose fix is the operator's call. A third slice beside the record, `a849fca1426c` (`worker:fable`), tests each `@villagekit/*` package's need for `transpilePackages` and corrects the comment in `next.config.ts`, minted on the review's finding.

The children, one line each, the site commit and, for a `../ui` slice, the sibling commit on its `main` (unpushed, ahead of origin by seven):
- `63e9c753ca55` top nav, header action and 404 heading: `4245dd1`.
- `ffe8e5d56f8e` document head: `9f45761`.
- `1c74a465996d` ui framework boundary: `59a81df`, ui `4e11d57`.
- `f8c93eaf4922` header brand by svgr: `e96cf61`.
- `5e4529a6aeac` site footer in place: `680a02f`.
- `39b1a28bb0cc` site theme: `4a7f3b6`.
- `93ef1234208c` footer WebGL cube: `18ea35d`.
- `1977c9af920c` ui brand footer: `a8c536d`, ui `ae593d0`.
- `1cc03cfabcf2` ui LinkCard and CardsLayout: `1840718`, ui `a4ef8ed`.
- `45d6f5634a11` ui recipes and provider: `09f9d75`, ui `540e9c3`.
- `4f6f086c5d77` next.config: `d03cd95`.
- `a7bf623f885c` layout composition: `c236c32`.
- `531b810f2dbd` QueryParamProvider adapter (Phase 0): `cb9eb5d`.
- `b0f69896e764` ui nav mobile menu: `6a2d35d`, ui `1cf88ed`.
- `bc0407533650` ui mdx and media: `6c30130`, ui `1c3e3e8`.
- `c14505b76b6a` ui palette literals: `93e9302`, ui `9642114`.

Deviations from Scope, each recorded in its child's Outcome: the layout slice built its layout half and verified rather than built the QueryParamProvider half, since `next-query-params`' app adapter fails the static prerender, and minted the Phase 0 slice `531b810f2dbd`, which shipped a site-written adapter and filed its own provenance as `1eddda919812`; the cube slice kept the brand slice's barrel without `gl` on a stated reason and filed `2f91880a9f4a` rather than re-export; the document head slice kept the story page's article `openGraph` (Next replaces a layout's `openGraph` whole) and lists `/icon.svg` in `metadata.icons`; the brand footer slice fit four columns at 768 with a narrower minimum between md and lg and filed `c9ea22823a23`, the expected stop the split named; the LinkCard slice found that 0.9.0's `icon: ComponentType` cannot cross from a server page into the client card, so the bump's site edit is a `'use client'` icons module (a helper the app router forces, for the bump to file as a code difference); the mdx and media slice found and fixed the size hooks (`252edab16c7a`) beyond its Closes line and filed `9affc679c88c` (legacy's own `Tip` over the mdx blockquote) on the first story route rather than on `shell`, the story pages record's; the palette slice closed `1f11773f8e2b` (the `fg` token) and `f45538f480af` (the shadow scale) beyond its Closes line, in the same shape; the recipes slice ported all four input sizes rather than `lg` alone; the nav slice took `react-focus-on` and `motion` where the plan named Chakra's `Presence`, on measured evidence; the next.config slice restored the two build flags with the legacy author's comment (`230ce4ee4848`), one operator sanction drops them. The site edits a published ui API needs at the bump are notes on `99f2fe62c62f`, one per ui slice.

Flags the children raised, together, for later iterations: the `transpilePackages` comment in `next.config.ts` carries the stale claim that the published packages ship TypeScript sources (every `@villagekit/*` package at the pinned versions resolves `.` to `dist/index.js`); CLAUDE.md's ui row is corrected in this commit and the slice `a849fca1426c` owns the test and the comment; `@mdx-js/react` sits in `package.json` with no importer; the live legacy site's `strict-transport-security` and `x-robots-tag` come from Vercel (noted on the release record `a4df2bf79395`); on `pnpm dev` React StrictMode's double mount returns the mobile menu's focus to the toggle, a development-only artifact noted on the bump plan; the story pages record `56e6eb197e6c` owns `9affc679c88c` and `7584e6e05aea` (the stacked bottom margins under the layout's main), the stories index record `ca353de8b645` owns `2b12ca93bdd3`, the cutting planner record `396c9af0cbd1` owns `df4fbe144cb3`, and the four LinkCard routes their description-clipping items (`2774d994ab92`, `e55422c77b65`, `2ea664bcbdd0`, `5ebdc4a4f50c`); the `file:../ui` override copies the sibling at install time, so a sibling edit needs a reinstall before `pnpm dev` sees it (now a clause in CLAUDE.md's ui row); running the gate beside a live `pnpm dev` corrupts the dev server, recorded under CLAUDE.md's Gotchas by the top nav slice. Three `sanctioned` items whose Current sections the top nav slice `63e9c753ca55` made stale (`9d1f2b7ae565`, `71dcfc2bdf8a`, `3a2b674677f9`) carry a note each from this finish.

Reviewed once on a fresh Opus sub-agent before the commit, nothing critical, every finding taken. Two major: the `chakra-slide` class and the `AnimatePresence` wrapper the nav slice's second review named and did not file are differences under `ee86d68a`, filed here as `e403eeab87ea` and `eca6acde1370` (`open`, code, removed, on the verdicts plan, no verdict written); and the stale `transpilePackages` comment had no owner once CLAUDE.md was corrected, so the slice `a849fca1426c` was minted. Minor, taken: the Closes-line claim above reworded; the Gotchas flag dropped as already recorded; the top nav slice named; the two stale-Current notes name the line (`app/_lib/nav.ts:6` and `:5`); the `dcd8df79` citation dropped from the video alt's bullet, since that decision covers the historical posts, not site metadata; `priority: medium` on the three slices; the reinstall clause in CLAUDE.md's ui row; `9affc679c88c`'s route named under the deviations; the two edited pages staged by hand. The review's borderline sorting notes stand as sorted: `2f91880a9f4a` has a one-line legacy-shape fix, but its cost makes the rule 5 question the operator's, and `abb4c0b4f849` has a setting, which is the alternative its bullet names. Findings dropped: none.

## Log

- 2026-09-25: Split into fifteen slices (plan a78b167170b8), reviewed once on two Opus sub-agents. Site-side, against `@villagekit/ui@1.2.0`: `63e9c753ca55` the top nav, header action and 404 heading (Sonnet); `ffe8e5d56f8e` the document head (Opus); `5e4529a6aeac` the site footer in place; `f8c93eaf4922` the header brand by svgr; `4f6f086c5d77` next.config; `39b1a28bb0cc` the site theme; `a7bf623f885c` the layout composition and QueryParamProvider; `93ef1234208c` the footer WebGL cube (Fable). In `../ui`, all Fable, each parking its items in `upstream` and blocking the bump plan `99f2fe62c62f` (decision `28c1a536`): `c14505b76b6a` the palette literals; `45d6f5634a11` the recipes and provider; `1c74a465996d` the framework boundary, which the nav `b0f69896e764`, brand footer `1977c9af920c`, mdx and media `bc0407533650` and LinkCard `1cc03cfabcf2` slices follow. Every `regression` on `shell` is named in exactly one slice's Closes line. Calls made at the split, for the operator to read: the top nav ships decision `c21b7e35f0c7`'s four items exactly, so Suppliers leaves the top nav (the header action `e48df8d1ce98` and the footer carry it) and `191e28561c6d`'s nav clause is overtaken; the footer's link labels stay legacy's (`Tools and resources`, `Newsletter`) where decision `9f344fbfde9a` lists pages without a sanctioned label change, its headings ship as it names them; `0efe45924dbe`'s verdict stands, so the ui media components take legacy's hard-coded cloud name as their default and the site's video swap (`b397b0deb1cc`) lands at the bump; `230ce4ee4848` restores the two build flags with the legacy author's comment, one operator sanction drops them; the OG video's alt applies rule 1 literally (`made from grid beam`). The site edits a published ui API needs (the nav provider's props, the brand footer, `Provider system`, LinkCard's icon type, the video components) are the bump commit's, each ui slice writing them as a note on `99f2fe62c62f`. Expected stop: with legacy's footer values four columns at `minWidth: 3xs` overflow 768 px, so the brand footer slice files one `open` visual item for the operator; the exit demo waits on that verdict. The footer signup box of `ac6579ec16d2` is the subscribe record's. Stale line found by review: CLAUDE.md says the published `@villagekit/ui` ships TypeScript sources at its top-level exports, but the tarball's `publishConfig.exports` point at `dist/`; only the sibling's top-level exports point at `src/`.
