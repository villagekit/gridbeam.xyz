---
title: "Story pages: the install video on react-youtube, the creations intro and the closing line as Descriptions, and the unused legacy imports restored in the MDX"
status: done
parent: 56e6eb197e6c
derived_from: 56e6eb197e6c
blocked_by: 373320c95e55
tags:
  - "worker:fable"
---
The lines legacy wrote in the story MDX with components outside the story set are legacy's again: the furniture bolts guide embeds its install video through `@u-wave/react-youtube`, legacy's dependency, full width with no box, cap, radius, shadow or title, in place of the boxed `youtube-nocookie` iframe; the 2022 newsletter's creations intro and the 2021 newsletter's closing line are a `Description` from `@villagekit/ui` (centered, `md` and `lg` from the `md` breakpoint, legacy's `ui-page` `Description`) in place of a body paragraph; and the unused imports and the two commented-out links legacy carried return where they resolve, the default for an item the operator has not judged. Five items on three story routes, plus the operator's [[8a6daf7fad72]] by default. The library is a class component the server layer cannot load, so the slice decides its one client boundary; Fable. Record `56e6eb197e6c`; decisions `ee86d68a`, `2032533f`, `ca677697`.

## Work

- Legacy at `fce357d`: `../node-modules/apps/gridkit/pages/stories/how-to-furniture-bolts.mdx:1,326`, `2021-winter-newsletter.mdx:1-5,212-218`, `2022-newsletter.mdx:1-2,268-270`, `how-to-cut-grid-beams.mdx:1-5`, `building-with-grid-kit.mdx:81,176`; `apps/gridkit/package.json:31`. Current: the same files under `content/stories/` as the components slice (`Story components re-ported from legacy's components/story, with the mdx overrides, the Tip and every story call site`) leaves them.
- [[d1fb9b4c176c]], [[fe647a9b88d8]], [[89d04ca61c84]]: `@u-wave/react-youtube` added at legacy's range `^0.7.4` (MIT; its peer range ends at React 18, so pnpm warns and installs: read the license and the peer line in `node_modules/@u-wave/react-youtube/package.json` after the install, and name the bump in the commit); legacy's line `<YouTube video="Xidi8xogSSQ" modestBranding showRelatedVideos={false} width='100%' style={{ aspectRatio: '16/9' }} />` in place of the `Box` and `iframe` block, the `Box` import going with it. The package is a class component (`dist/react-youtube.es.js` extends `React.Component`) with no `'use client'` directive, and the MDX modules are evaluated in the server layer too (the page renders them, `app/_lib/stories.ts` imports them), where React's server build exports no `Component`; so the MDX imports `YouTube` from `app/_components/YouTube.tsx`, a `'use client'` module of one line re-exporting the package's default, the same boundary the LinkCard bump note gives the icons (`app/_lib/icons.ts`): a rule 4 translation the Outcome names, not a workaround. If the component still does not render under React 19 (a blank frame, a console error from the library), the Outcome says what failed, the three items stay `regression` with a note each, the iframe stays, and a note on the verdicts plan `Story pages: the operator's verdicts` hands the shape to the operator.
- [[8661edb0bbc8]], [[ffe337ac3773]]: `import { Description } from '@villagekit/ui'` (1.2.0 exports it with `textAs`, `node_modules/@villagekit/ui/dist/components/layouts/Description.d.ts`) and legacy's `<Description textAs='div'>...</Description>` as a direct child of its `StorySection`: on 2021 the closing line (`:212-218`), replacing the added `StoryRow` and `StoryColumn` around the sentence; on 2022 the creations intro under `## Our latest creations with Grid Kit` (`:268-270`), replacing the bare paragraph; the sentences themselves as they are (legacy's text, the markdown links inside rendering through the mdx map).
- [[8a6daf7fad72]], the operator's on the verdicts plan: unless the operator has judged it by the time this runs, legacy's form ships where it resolves: `how-to-cut-grid-beams.mdx:1-5`'s `YouTube` (from the client module above), `Link`, `RasterImage`, `getCloudinaryUrl` (from `@/app/_lib/cloudinary`) and `StoryImageGrid` imports; 2021's `Box` (from `@villagekit/ui`) and `ImageCarousel` (from `@/app/_components/ImageCarousel`) and 2022's `ImageCarousel`; the two `{/* ... */}` comments in `building-with-grid-kit.mdx:81,176`, verbatim. The one line with no form that resolves, 2021's `import { VideoWrapper } from '@villagekit-private/ui-mdx'` (`:3`), names an export the package never had (`grep -rn VideoWrapper ../node-modules/packages ../node-modules/apps/gridkit` prints that import alone), so it is not written; the item closes with a note saying so, or takes the verdict's form. Biome reads no `.mdx` (`pnpm exec biome check content/stories/2021-winter-newsletter.mdx` reports no file processed), so an unused import costs nothing at the gate; `next build` is the check that each restored import resolves. Legacy's `withStoriesLayout` import (`:2` on most files) is the page re-port's, not this slice's.
- Verify first: `grep -c 'youtube-nocookie' content/stories/how-to-furniture-bolts.mdx` prints 1; `grep -c '"@u-wave/react-youtube"' package.json` prints 0; `grep -c '<Description' content/stories/2021-winter-newsletter.mdx content/stories/2022-newsletter.mdx` prints 0 for each; `grep -n 'Description' node_modules/@villagekit/ui/dist/index.d.ts` prints the export; `timeout 60 pnpm view @u-wave/react-youtube@0.7.4 peerDependencies license` prints the React range ending at `^18.0.0` and `MIT`; `grep -c 'exports.Component' node_modules/next/dist/compiled/react/cjs/react.react-server.production.js` prints 0.
- Docs: none.
- Not this slice: the story components and their call sites; the metadata fields; the page and its layout; the Description container's width ([[c325dd0181d9]], `upstream`: 768px at 1.2.0 where legacy's was 1024px, expected on the pairs).

## Seams under test

None pure; the proof is the DOM pairs, a probe of the embed and the two sentences on both sides, and the screenshot pairs.

## Done when

- Against a running `pnpm dev`, `pnpm audit:dom --routes <a file naming /stories/how-to-furniture-bolts, /stories/2021-winter-newsletter and /stories/2022-newsletter>`: `audit/stories__how-to-furniture-bolts/dom/current.aria.yaml` shows the embed as `legacy.aria.yaml` does (an `iframe` with no name); `audit/stories__2021-winter-newsletter/dom/current.txt` holds the closing sentence and `audit/stories__2022-newsletter/dom/current.txt` the creations intro where `legacy.txt` has each
- A Playwright probe on both sides at 1280 and 375, saved beside the pairs: the install video's `iframe` as wide as its column's content box, its parent with no `border-radius` and no `box-shadow`, its `src` on the host and with the parameters the live legacy page's iframe carries (read there first); the two `Description` sentences `text-align: center`, `font-size` 18px at 1280 and 16px at 375, inside a container whose `max-width` is 768px here and 1024px on legacy (the shell's `c325dd0181d9`); `/stories/how-to-cut-grid-beams` served 200 with the restored imports
- `pnpm audit:pages --routes <the same file>` at 375, 768 and 1280 looked at against legacy: the embed spanning the column, the two centered sentences; every residual a `shell` item in `upstream`, the operator's, an item another slice of this record owns, or filed
- `grep -c '"@u-wave/react-youtube"' package.json` prints 1 and `pnpm-lock.yaml` pins 0.7.4 with its integrity hash; `grep -c 'youtube-nocookie\|<iframe' content/stories/how-to-furniture-bolts.mdx` prints 0; `head -1 app/_components/YouTube.tsx` prints `'use client'`; the import block of each of the five MDX files, diffed against legacy's, differs by the import paths' translation, the `VideoWrapper` line and the `withStoriesLayout` line alone (or as the verdict says)
- The five items are `fixed` (`kipu fix <id> --outcome "plan <this prefix>"`); [[8a6daf7fad72]] `fixed` with its note, or as the verdict says; checked after the fixes
- `timeout 900 just check` is green

## Outcome

Shipped as scoped, on the five MDX files the plan names and one new module. `content/stories/how-to-furniture-bolts.mdx`: legacy's line `<YouTube video="Xidi8xogSSQ" modestBranding showRelatedVideos={false} width='100%' style={{ aspectRatio: '16/9' }} />` in place of the `Box` and `iframe` block, `Box` gone from the import, `YouTube` imported from `app/_components/YouTube.tsx`, a `'use client'` module whose one export re-exports the package's default under a TSDoc naming the constraint (the split's call 6; the same boundary the LinkCard bump note on `99f2fe62c62f` describes for the icons, a module the bump will add and that is not on disk yet, so the plan's pointer to `app/_lib/icons.ts` names a mechanism and not a file: `@u-wave/react-youtube` is a class component with no directive, `dist/react-youtube.es.js` extending `React.Component`, and the MDX modules are evaluated in the server layer, where `grep -c 'exports.Component' node_modules/next/dist/compiled/react/cjs/react.react-server.production.js` prints 0; a rule 4 translation, the one the app router forces). `@u-wave/react-youtube` is added at legacy's range `^0.7.4` (`apps/gridkit/package.json:31`), which resolves to 0.7.4, the `latest` dist-tag; MIT (`node_modules/@u-wave/react-youtube/package.json`), peer `react: ^0.14.0 || ^15.0.0 || ^16.0.0 || ^17.0.0 || ^18.0.0`, so pnpm warned and installed; `pnpm-lock.yaml` pins `@u-wave/react-youtube@0.7.4` with its integrity hash and its own `load-script2`, `prop-types` and `@types/youtube`. `content/stories/2021-winter-newsletter.mdx`: `<Description textAs='div'>` around the closing sentence as a direct child of `StorySection index={3}` (legacy `:212-218`), the `StoryRow` and `StoryColumn` the current file had added around it gone, so the team image is the section's direct child again and takes the section's sizes as legacy's (the exception the components slice's Outcome named closes with it). `content/stories/2022-newsletter.mdx`: the creations intro in `<Description textAs='div'>` under `## Our latest creations with Grid Kit` (legacy `:268-270`). `Description` comes from `@villagekit/ui` (1.2.0 exports it with `textAs`). The unused legacy imports, restored where they resolve, since no verdict had landed on [[8a6daf7fad72]]: `how-to-cut-grid-beams.mdx` imports `YouTube`, `Link`, `RasterImage`, `getCloudinaryUrl` and `StoryImageGrid`; `2021-winter-newsletter.mdx` `Box` and `ImageCarousel`; `2022-newsletter.mdx` `ImageCarousel`; `building-with-grid-kit.mdx` carries legacy's two commented-out `/todo` links verbatim (`:81` after the planning image, `:176` at the end of the nuts and bolts sentence). The `VideoWrapper` line is not written (the package never exported it; the item's note says so). No visitor-facing text changed: the two sentences and the comments are legacy's characters.

The import block of each of the five files, diffed against legacy's, differs by the paths' translation alone (`@u-wave/react-youtube` to the client module; `@villagekit-private/ui-page`'s `Description` and `ui-media`'s `RasterImage` merged into the `@villagekit/ui` import, the components slice's shape; `getCloudinaryUrl` from `@/app/_lib/cloudinary`; `@/components` to `@/app/_components/ImageCarousel`; `@/components/story` to `@/app/_components/story`, on the lines the earlier slices already wrap), the `VideoWrapper` line and the `withStoriesLayout` line. Two readings for the record, neither a change: the ui `Description` types `children: string` as legacy's `ui-page` one did, while MDX hands it a paragraph element (the mdx map's `p` with the links inside), which renders as it did on legacy and is never type-checked (tsc reads no `.mdx`); and the library renders a `div` the YouTube IFrame API replaces with the iframe, so legacy's `width='100%'` and `style` land on the iframe and the API writes `title="YouTube video player"` on both sides, which the DOM extraction reports as an unnamed `iframe` on both.

Proof, against `pnpm dev` at the published 1.2.0. Verify-first: 1, 0, 0 and 0, the export at `dist/index.d.ts:33`, the peer range ending at `^18.0.0` and `MIT`, 0. The six story routes, `/` and `/stories` serve 200 after the edits; `/stories/how-to-cut-grid-beams` serves 200 with the restored imports. `pnpm audit:dom` over the five routes (10 of 10): `audit/stories__how-to-furniture-bolts/dom/current.aria.yaml` shows `- iframe` with no name between the `How To Install` heading and the list, as `legacy.aria.yaml:136` does; `audit/stories__2021-winter-newsletter/dom/current.txt` holds the closing sentence and `audit/stories__2022-newsletter/dom/current.txt` the creations intro where `legacy.txt` has each; between the title block and the footer every `current.txt` differs from `legacy.txt` by the copy verdicts and the TOC list (the page re-port's) alone. The Playwright probe on both sides at 1280 and 375 (`audit/probe-eeeb3813939b/probe.json`, the script beside it, `probe-before.json` the reading before the edits): the iframe's `src` on `www.youtube.com/embed/Xidi8xogSSQ` with the parameters the live legacy page's carries (`autoplay=false`, `cc_load_policy=0`, `controls=1`, `disablekb=0`, `fs=1`, `iv_load_policy=1`, `modestbranding=1`, `playsinline=0`, `rel=0`, `enablejsapi=1`, `origin`, `widgetid=1`), `width="100%"`, `style="aspect-ratio: 16 / 9"`, the same `allow` list; the iframe as wide as its column's content box on both sides (992px in 992 on legacy and 960 in 960 here at 1280, the page's own container the page re-port owns; 343 in 343 at 375), its parent with `border-radius: 0px`, `box-shadow: none` and `max-width: none`; the two sentences `text-align: center`, `font-size` 18px at 1280 and 16px at 375 once hydrated (the SSR fallback `md` of `useBreakpointValue` renders 18px until the effect runs, on both sides' code; a first reading before hydration showed 18px at 375 and settled to 16px after five seconds), inside a container of `max-width` 768px here and 1024px on legacy ([[c325dd0181d9]], `upstream`). The browser console on the install page: no error from the library; YouTube's own `postMessage` origin warning on `localhost` and its `web-share` feature warning, and the phantom cell's `src` lines (the operator's [[0e4002cba0a5]]). `pnpm audit:pages` at 375, 768 and 1280 looked at against legacy: the embed spanning the column at every width (the cross-origin iframe paints blank in both captures, the space the same), the closing line and the creations intro centered at the same size on both sides (the 2021 line wrapping to two lines at 1280 here, in the 768px container); the residuals the shell's `upstream` items (the container padding `5c1af396cc2e`, the Section band `9746fd6890c2`, the headings `77cd1426ac65` and `7124898563eb`, the Description container), the TOC sidebar (`4331147cc118`), and at 768 the footer (`c9ea22823a23`). The Done-when greps: `"@u-wave/react-youtube"` 1 in `package.json`, `youtube-nocookie\|<iframe` 0, `head -1 app/_components/YouTube.tsx` prints `'use client'`, `<Description` 1 and 1.

The ledger: [[d1fb9b4c176c]], [[fe647a9b88d8]], [[89d04ca61c84]], [[8661edb0bbc8]] and [[ffe337ac3773]] are `fixed` with outcome `plan eeeb3813`; [[8a6daf7fad72]] `fixed` with its note (the imports restored where they resolve, `VideoWrapper` left out and why). No verdict written. No `../ui` or `../gridkit` change; nothing parked `upstream` by this slice. No Vitest test: nothing pure was written; the proof is the DOM pairs, the probe and the screenshot pairs. Gate: the dev server stopped by pid and port 3000 confirmed free, `timeout 900 just check` exited 0 (Biome clean over 107 files, `tsc` clean, 78 tests, `next build` prerendering the six story pages, the generated designs data unchanged); `kipu verify --warnings-as-errors` green. Not the last open child: the fields and the page re-port slices follow, the verdicts plan standing beside the record.

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity): nothing critical. Standards: no hard violation, no invariant decision to check; taken, the boundary module's comment as a TSDoc block (CLAUDE.md, Conventions: public items have TSDoc) and the peer-range mismatch named in the commit; noted and left, [[89d04ca61c84]]'s Current text citing the removed `title` line (a fixed item keeps its filing-time text, `differences/README.md`). Spec: every load-bearing upstream claim verified at source, the import blocks, the copy and the ledger moves as the plan says, and its own no-JS probe at 375 reading 18px on both sides, so the 16px line holds after hydration the same way on each; taken, a note on the verdicts plan [[14be8f377b34]] saying the slice closed [[8a6daf7fad72]] by default, and the Outcome's wording on the `app/_lib/icons.ts` precedent, which the bump note describes and no file yet holds. Parity looked at all three widths on the three visual routes and read the other two from the source and the DOM text: every claimed closure holds, the iframe's runtime (its `src`, `allow`, `fs=1`, `disablekb=0`, the placeholder `div` at 16/9 before the API loads) and the team image's `sizes` match legacy, no copy changed, nothing silently dropped; taken, one code difference the change introduces that no item recorded, the `'use client'` re-export module legacy did not have, filed as [[f7645a164ced]] (`open`, code, `added`, `/stories/how-to-furniture-bolts`, a rule 4 candidate the slice never sanctions itself, the shape of the home's [[1bbb3def5d97]]) and added to the verdicts plan by a note, since an `added` item is the operator's. Dropped: none. After the review's fixes the gate ran again, green, with the dev server stopped by pid.

## Log
