---
title: "ui mdx and media: the BlockSection blockquote, MediaContainer, the mdx Image and Video, legacy's default cloud name"
status: done
parent: a78b167170b8
derived_from: a78b167170b8
blocked_by: 1c74a465996d
tags:
  - "worker:fable"
priority: medium
---
The package's mdx module renders blockquotes as legacy's `BlockSection` stack (the shadow, padding 4 by 2, `Text variant="secondary"`) and exports `MediaContainer`, `Image` and `Video` for story MDX as `ui-mdx` did; the media components take legacy's cloud name as their default so a site need not mount `MediaProvider`, and the site's video components move onto the ui `Video` at the bump. Record `a78b167170b8`; decisions `ee86d68a`, `2032533f`, `28c1a536`.

## Work

- Legacy source: `../node-modules/packages/ui-mdx/src/{blockquote,MediaContainer,Image,Video,index}.tsx`, `packages/ui-page/src/components/BlockSection.tsx` and `packages/ui-media/src/video.tsx:73-88` (the cloud name `villagekit` hard-coded) at `fce357d`. Current: `../ui/src/mdx/{blockquote,index}.tsx`, `src/components/layouts/BlockSection.tsx` (already ported), `src/components/media/{Image,Video,context}.tsx`; on the site `app/_lib/cloudinary.ts:9,19-31`, `app/_components/story/StoryVideo.tsx`, `app/_components/landing/LandingVideo.tsx`.
- `f9b9ec72d771`: `MdxBlockquote` renders `<BlockSection Icon={FaQuoteRight}>`, an `HStack` exposed as a paragraph, with legacy's `boxShadow: sm`, padding and secondary text; the current `<blockquote>` element and its own padding go.
- `d750c0a47839`: `MediaContainer` (a `Center` at `maxW` md and lg by breakpoint), `Image` and `Video` wrapping the media components with the container's breakpoints as `sizes`, exported from `@villagekit/ui/mdx` beside `mdxComponents`, on the framework boundary slice's image mechanism; the stories' consumption is the story pages record's.
- Sibling steps (decision `28c1a536`): the change lands in `../ui` (`src/...`, and `stories/` where a story renders the component), verified there by `pnpm lint`, `pnpm types` and `pnpm build:pkg`, committed there by pathspec (`git -C ../ui add <paths>` then `git -C ../ui commit -- <paths>`, the message citing the legacy source by its SHA-pinned URL where code is ported) and not pushed; seen on this site through an uncommitted override (`pnpm.overrides` `"@villagekit/ui": "link:../ui"` in `package.json`, then `pnpm install`; the sibling's top-level `exports` point at `src/`, so no build is needed for the override), reverted by path before the commit here (`git restore -- package.json pnpm-lock.yaml`, then `pnpm install --frozen-lockfile`) so the commit never carries it; each item this slice closes moves to `upstream` (`kipu move <id> upstream --from regression`) with a note citing the sibling commit; then `kipu relate 99f2fe62c62f blocked_by <this slice>`. The package version stays: the publish is the operator's.
- `b397b0deb1cc` and `0efe45924dbe`: the media context's default cloud name becomes `villagekit`, legacy's hard-coded value, `MediaProvider` staying as the override for another cloud, so the site's sanctioned choice not to mount it (`0efe45924dbe`, the operator's confirmed verdict) holds while `Video` and `Image` work. The site's swap is the bump's: `kipu note 99f2fe62c62f` with the edits: `StoryVideo` and `LandingVideo` render the ui `Video` (`autoPlay loop muted playsInline`, the poster, webm, mp4 and ogv sources, click to toggle) with the props legacy's call sites passed (`title`, `sizes`, `aspectRatio`, `orientation`), and `getCloudinaryVideoUrl` with its header comment leaves `app/_lib/cloudinary.ts`; the wrapping boxes are the home and stories records' items. Probe the swap here through the override (the site edits made, verified, reverted with the override): every resulting webm, mp4, ogv and poster URL for the site's public ids (`gridbeam.xyz/...`, the builder prepending `v1/`) returns 200 from Cloudinary; a format Cloudinary does not serve for a re-hosted asset goes in the note, not around.
- Verify on this site through the override on `/stories/how-to-cut-grid-beams` (a tip blockquote) at 1280 against `audit/stories__how-to-cut-grid-beams/1280/legacy.png`, and the two probed video pages.
- Closes (to `upstream`) `f9b9ec72d771`, `d750c0a47839`, `b397b0deb1cc` (its site half at the bump; the note says so).
- Not this slice: the stories' own media call sites (the story pages record); the video boxes' radius, shadow and the stories' `controls` (the home and stories records' items).

## Seams under test

None pure.

## Done when

- with the override in place: the tip on `/stories/how-to-cut-grid-beams` is exposed as a paragraph (no `blockquote` node in the extracted tree) inside a shadowed dashed box matching the legacy capture; `grep -c "MediaContainer\|Image\|Video" ../ui/src/mdx/index.ts` is at least 3; with the video swap probed, `curl -s localhost:3000/ | grep -o '<video[^>]*>'` shows `autoplay`, `loop`, `muted`, `playsinline` and a `poster` with three `<source>` children whose URLs return 200, and no `MediaProvider` is mounted (`grep -rn MediaProvider app` empty); the probe and the override then reverted
- in `../ui`: `pnpm lint`, `pnpm types` and `pnpm build:pkg` are green and the commit there is by pathspec, its hash in each item's note
- the three items are `upstream`, `99f2fe62c62f` is `blocked_by` this slice and carries the bump note, checked after the moves
- `timeout 900 just check` is green on this site, without the override

## Outcome

Shipped in `../ui` as commit 1c3e3e8 on its `main` (not pushed; the push goes with the operator's publish, decision `28c1a536`), by pathspec: `CHANGELOG.md`, `src/mdx/{blockquote,link,index}.tsx|ts`, `src/mdx/{Image,MediaContainer,Video}.tsx` (new), `src/components/media/{context,Image,index,url}.tsx|ts`, `src/hooks/{useSizeWidths,useBreakpointWidth}.ts`, `src/framework.tsx`, `stories/mdx.stories.tsx`. The package version stays 1.2.0.

What shipped, each a port of the legacy `ui-mdx` or `ui-media` source at `fce357d` translated to Chakra v3: `MdxBlockquote` renders `<BlockSection Icon={FaQuoteRight}>` (`f9b9ec72d771`); `MediaContainer` (a `Center` bounded to `md`, `lg` from `md`, by `useBreakpointValue` over `useConst` as legacy), `useMediaMaxWidthBreakpoints`, and the mdx `Image` and `Video` wrapping the media components with those bounds as their default `sizes`, exported from `@villagekit/ui/mdx` (`d750c0a47839`); `MdxLink` is legacy's `MarkdownLink` again, a `#hash` href scrolling and every other href `<Link variant="paragraph" isExternal>`, so a story's internal link opens in a new tab with `rel="noopener"` as on the live legacy site (`63baa85427e0`, `d04a1827b838`); `MdxLink` no longer reads the framework context, and `framework.tsx`'s comment and the CHANGELOG's provider entry drop it from the list. The media context's cloud name defaults to `villagekit` (`defaultCloudinaryName`, exported), `MediaProvider` overriding it for another cloud; `assertCloudinaryName` and its throw are gone, `useCloudinaryName` returns the context's string, so `0efe45924dbe`'s sanctioned choice not to mount the provider holds while the ui `Image` and `Video` work (`b397b0deb1cc`, its site half at the bump).

Found and fixed in the same commit, filed as `252edab16c7a` (shell, code, regression, moved to `upstream`): a first probe of the video swap spread legacy's `story-image.tsx` column sizes (`{ base: 'full', md: ['container.lg', 2] }`) onto `StoryVideo` and 500ed on the two newsletter pages with `Unexpected size value: full`, since 1.2.0's `useSizeWidths` had dropped 0.9.0's `full` and `container.*` names, and with `full` restored `useBreakpointWidths`' `base: 0` (0.9.0 gives `base` the `sm` width) made a `%` size at `base` a `w_0` transformation Cloudinary answers with 400. Both hooks are 0.9.0's again on the v3 tokens (`sizes.full`, `sizes.breakpoint-*`, which v3 registers from the breakpoints; v2's `container.sm` is 640px and has no v3 token, so it is a literal with its reason); `useSizes` and `useVideoSizes` are untouched. The Parity and Spec reviews then read the legacy caller closer: `story-video.tsx` builds that `sizes` object and never spreads it, so legacy's video path never ran it and the spread was the probe's own mis-port; the item cites `story-image.tsx:24-34`, the caller that does, and the bump note ports `StoryVideo` without the spread (a note on `8df7a085e421`, whose Legacy section had the same misreading).

Verified: in `../ui`, `pnpm lint`, `pnpm types`, `pnpm build:pkg` and `pnpm build:storybook` green (the Storybook `"use client"` directive warnings are the baseline's plus the three new client files, 1077 against the nav slice's 1074). On this site through the `file:../ui` override with the video swap probed (`StoryVideo` as legacy's `story-video.tsx` on the mdx `Video`, `LandingVideo`'s inner element as the ui `Video` with legacy's `sizes`, `getCloudinaryVideoUrl` removed, and the LinkCard bump's `app/_lib/icons.ts` so `/` renders, all reverted): `/`, `/stories/how-to-cut-grid-beams`, `/stories/2021-winter-newsletter`, `/stories/2022-newsletter` and `/stories/whats-a-grid-unit` served 200; `curl localhost:3000/` shows `<video ... autoPlay loop muted playsInline poster=...>` with webm, mp4 and ogv sources; every rendered video and poster URL (16 for the site's public ids, `gridbeam.xyz/...` and the home page's `gridkit.nz/...`, the builder prepending `v1/`) returns 200 from Cloudinary, `f_auto` answering the three extensions with `video/mp4;codecs=avc1` as it does for the legacy site; `grep -rn MediaProvider app` empty; the tip on `/stories/how-to-cut-grid-beams` is exposed as a paragraph with no `blockquote` node (`audit/stories__how-to-cut-grid-beams/dom/current.aria.yaml`), and measures padding 8px 16px, a 2px dashed border, radius 16px on both sides; a story's internal link renders `<a target="_blank" rel="noopener" href="/designs">`. Screenshot pairs captured at 375, 768 and 1280 under `audit/{_root,stories__how-to-cut-grid-beams,stories__2021-winter-newsletter,stories__2022-newsletter,stories__whats-a-grid-unit}/`, and element crops of the tip and two story videos on both sides looked at. The override reverted by path and `pnpm install --frozen-lockfile` run before the gate, which ran green against the published 1.2.0.

Four differences the comparison and the reviews surfaced, filed and left: `9affc679c88c` (`/stories/how-to-cut-grid-beams`, visual, regression): the legacy site overrode the `ui-mdx` blockquote with its own `Tip` (`FaRegLightbulb`, `alignSelf: center`, `maxWidth: lg`) in `apps/gridkit/components/mdx/blockquote.tsx`, which `f9b9ec72d771` did not see; filed on the first story route that renders a blockquote, as `8df7a085e421` was, so the story pages record (`56e6eb197e6c`) owns it, and it needs no publish (`BlockSection` is in 1.2.0). `f45538f480af` (shell, visual, regression): the `sm` and `md` shadow tokens are Chakra v3's, the same shape as the radius scale item `f40107b60034`, the ui theme's to fix or rule 4's to judge. `f85e98a67092` (shell, code, regression): `LinkButton`, `LinkIconButton`, `LinkCard`, `Footer` and `Social` still write `rel="noopener noreferrer"`, the last clause of `d04a1827b838` split out so that item closes on `MdxLink`. `89301ca8a1fc` (shell, accessibility, added, `open`): Chakra v3's `Icon` adds `aria-hidden` beside `BlockSection`'s `role="presentation"`, the tree identical, a rule 4 candidate. The two shell regressions have no slice: this record's Log rule that every shell regression is named in one slice is broken by them, for the operator or a later ui slice, as `f40107b60034` already was.

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity). Taken: `container.sm` mapped to v2's 640px rather than v3's 480px `breakpoint-sm` (both reviews); the nav context's comment and the CHANGELOG's provider entry no longer name `MdxLink`, and the `MdxLink` entry sits under Breaking, since a consumer's internal links change tab; TSDoc on the four new public types; the `defaultCloudinaryName` wording; the item and bump-note corrections above; `imageComponent` answered in the bump note (no provider here, since the site renders no ui `Image`; a re-port of `StoryImage` onto the mdx `Image` needs `<MediaProvider imageComponent={NextImage}>` and supersedes `0efe45924dbe`, the story pages record's call); the boundary note's claim that MDX prose links route through `NextLink` corrected in the bump note. Dropped: `ReactNode | Array<ReactNode>` in `MdxBlockquoteProps` is legacy's type, kept. Parity found `/`, `/stories/how-to-cut-grid-beams`, `/stories/2021-winter-newsletter`, `/stories/2022-newsletter`, `/stories/whats-a-grid-unit` and `/stories/building-with-grid-kit` at parity at 375 and 1280 for this change's surface, every remaining visible difference mapping to an item (the palette `72b776cb0d3f`, the story column `16a68e71f5cd`, the four filed here). Scope beyond the Closes line, each by the plan's own Log or by evidence: `63baa85427e0` and `d04a1827b838` closed here, the hooks fix. The site's `NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME` (`app/_lib/cloudinary.ts:11`) does not reach the package's default; nothing sets it, and a site on another cloud mounts the provider. The mdx `isExternal` re-port is the legacy behavior by rule; if the operator prefers a story's internal link in the same tab, a note on `63baa85427e0` and a new state supersede it at the bump, never an edit here.

## Log

- 2026-09-26: From the framework boundary slice (plan 1c74a465996d): follow its mechanism, the framework context in ../ui/src/framework.tsx read through useFramework(), for MdxLink and the media components; the link component is the context's default for a route href. The Parity review of that slice filed 63baa85427e0 (MDX internal links open in the same tab where legacy opened every non-hash link in a new tab), on shell, axis interaction, regression; this slice re-ports MdxLink and is where it is closed or judged. The override that shows a sibling change on this site is pnpm.overrides["@villagekit/ui"] = "file:../ui" (link: fails under next dev --turbopack), with pnpm install after each sibling edit.

- 2026-09-26: From the recipes slice [[45d6f5634a11]]: `Link` takes `isExternal` again and renders `rel="noopener"`, what Chakra v2's `Link` rendered; `MdxLink` still writes `rel="noopener noreferrer"` itself, filed as [[d04a1827b838]] on shell for this slice beside [[63baa85427e0]].
