---
title: FAQ page re-ported from the legacy faq page
status: todo
parent: 7f0b60d948c5
derived_from: 7f0b60d948c5
blocked_by:
  - 241b65da8226
  - a7bf623f885c
tags:
  - "worker:fable"
priority: medium
---
The FAQ page is legacy's `pages/faq.tsx` again, translated for the app router in one server file: `app/faq/page.tsx` holding `metadata` and legacy's tree, a `Title` over a `Container` at the `md` container width holding a `VStack` of category sections, each a `Box as="section"` with a `Heading size="md"` and an accordion, and the closing line after the stack; legacy's `FaqEntries` object keyed by category label and iterated with lodash-es `map`; the trigger's label a `Text variant="tertiary"`; no `Section`, no slugs, no `paddingY`, no `variant` on the links. Copy is as the copy slice left it; this slice changes no visitor-facing text. Twelve items on `/faq`: eleven closed, one moved to `upstream` because the published package's `Link` takes no `isExternal`. A re-port that decides every translation, so Fable. Record `7f0b60d948c5`; decisions `ee86d68a`, `2032533f`, `ca677697`, `28c1a536`.

## Work

- Legacy: `../node-modules/apps/gridkit/pages/faq.tsx` at `fce357d` (393 lines; the tree at `:333-386`, the data shape at `:22-29`). Current: `app/faq/page.tsx` as the removals and copy slices leave it.
- One file, a server component: `app/faq/page.tsx` keeps `metadata` (`title: 'FAQ'`, the removals slice's) and exports the page, with the header `// ported from https://github.com/villagekit/node-modules/blob/fce357d/apps/gridkit/pages/faq.tsx`. No `'use client'` and no second file: legacy's page calls no hook of its own, every ui component it renders is a client module at `1.2.0` (`head -1 node_modules/@villagekit/ui/dist/components/{Text,Link,Heading}.js` and `dist/components/layouts/Title.js` print `"use client"`; `Accordion.js` re-exports Chakra's, itself client) and the data is static, so the home's two-file shape (`091a47cb93e7`) is not forced, as it was not for the about (`97e702d40df5`). If a reason for a client file surfaces, the Outcome says which and the slice files one `open` code item on `/faq` citing [[091a47cb93e7]], for a verdicts plan at the finish. Legacy's `getLayout` and `MainLayout` are the shell's layout (`a7bf623f885c`); `NextSeo` is the `metadata` (`65c21e08b3f1`, rule 4).
- [[eb2304414513]]: legacy's data shape, `interface FaqEntries { [category: string]: Array<{ question: string; answer: React.ReactNode | Array<React.ReactNode> }> }` and `const faq: FaqEntries = { Product: [...], Sustainability: [...], Support: [...] }` (`faq.tsx:22-29`), the entries as the copy slice wrote them, in its order; iterated with `map` from `lodash-es` (`faq.tsx:341,348`; the package is a dependency already, `package.json`, used by `app/_lib/useDesignTypingEffect.ts`), `map(faq, (entries, category) => ...)` and `map(entries, ({ question, answer }) => ...)`; the ids `faq-${category}` are `faq-Product`, `faq-Sustainability` and `faq-Support`, every one a valid id token, so legacy's `Returns & Support` defect (an ampersand and spaces in the id, an unnamed region) does not recur; the `FaqEntry` and `FaqCategory` interfaces and the slugs go.
- [[e253200bf486]], [[eda7795256a9]]: `<Title>Frequently asked questions</Title>` then `<Container maxW="breakpoint-md">` holding `<VStack alignItems="left" gap="12">` of the sections and, after the stack closes, `<Text marginTop="8">` with the closing line (`faq.tsx:337-384`, `sx` to a style prop); the `Section index={0} maxW="6xl"` and the `Container maxW="3xl"` go. Chakra v2's `container.md` is v3's `sizes.breakpoint-md` (768px, registered from the `md` breakpoint by `expandBreakpoints`, `node_modules/@chakra-ui/react/dist/esm/styled-system/token-dictionary.js:21-29`), the rename the about re-port made on its own container (rule 4, named in the Outcome). `spacing="12"` is `gap="12"` (`23f0413009ed`, sanctioned).
- [[63e2ef778789]]: `alignItems="left"`, legacy's value (`faq.tsx:340`): not an `align-items` keyword, so the browser drops the declaration on both sides and the flex default renders, the same layout as the current `stretch`; Chakra v3's `alignItems` prop takes any string (`node_modules/@chakra-ui/react/dist/types/styled-system/generated/system.gen.d.ts:45`, `AnyString`), so it typechecks. The split's call 6 (the record's Log): the item is `regression` with no verdict, so legacy's form ships; the operator may sanction the current form by a note on the item, and if the item is `sanctioned` when this slice runs, keep `stretch`.
- [[1205c31bebd0]], [[0bd8893128a4]]: each category is `<Box as="section" aria-labelledby={`faq-${category}`} key={category}>` holding `<Heading id={`faq-${category}`} size="md" marginBottom="4">{category}</Heading>` (`faq.tsx:342-345`) then the accordion; the `VStack as="section" gap="4"` and the `Heading as="h2" size="xl"` go. Chakra v3's `Heading` renders an `h2` by default as v2's did (`faq.tsx:343` wrote no `as`), which the aria tree's `level=2` checks. At the published `1.2.0`, `size="md"` renders 18px on a 21.6px line where legacy's rendered 20px on 24px (the shell's [[77cd1426ac65]], `upstream`, fixed in the sibling at ui commit `540e9c3`) and semibold where legacy's was normal ([[7124898563eb]], `upstream`): both expected on the pairs until the bump, neither this slice's.
- The accordion as the current page has it, the sanctioned `2ed11f164573`: `<Accordion.Root multiple collapsible>`, `<Accordion.Item key={question} value={question}>`, `<Accordion.ItemTrigger>`, `<Accordion.ItemIndicator />`, `<Accordion.ItemContent><Accordion.ItemBody>`.
- [[6c31193ec773]]: no `paddingY` on `ItemTrigger` or `ItemContent`, legacy's form (`faq.tsx:350,356` wrote none), which closes the item, the code being legacy's; the rendered 16px waits on the bump. At the published `1.2.0` the ui recipe's `paddingY: "4"` (`node_modules/@villagekit/ui/dist/components/Accordion.recipe.js`) loses to Chakra's `md` size variant, which writes the trigger's `py` at `spacing.2` after the base (`node_modules/@chakra-ui/react/dist/esm/theme/recipes/accordion.js:115-122`), and Chakra's base gives the trigger `fontWeight: "medium"` and pads the `itemBody` itself (`:23,33-35`): the shell's [[e1ad4ba984a5]] (`upstream`, fixed in the sibling at ui commit `540e9c3`), filed at the split, expected on the pairs until the bump and never fixed here.
- [[55b8453f7512]], [[bba676a97320]]: the trigger's label is `<Text variant="tertiary">{question}</Text>` (`faq.tsx:351`), a `<p>` inside the `<button>`, legacy's markup (the live legacy page: `<button ...><p class="chakra-text ...">`), so every `button` in the aria tree has a nested `paragraph` child as `legacy.aria.yaml` has; the `Text as="span" variant="secondary" fontWeight="bold" textAlign="left" flex="1"` goes, and the recipe's `justifyContent: "space-between"` keeps the indicator at the row's end, where legacy's screenshot has it. If React 19 warns on the nesting (`validateDOMNesting`, `cannot be a descendant`) in the dev console or the build, keep the `span` for that item only, leave [[bba676a97320]] `regression` with a note quoting the warning and name it in the Outcome for a verdicts plan at the finish (`40abdb2f222a`); [[55b8453f7512]] closes either way, the variant and the weight being the `Text`'s props; until the bump the label inherits the trigger's `medium` weight from Chakra's recipe ([[e1ad4ba984a5]]), so it reads at 500 on the pairs where legacy's reads 400. As with the alignment above, a `sanctioned` item at run time keeps the current form.
- [[753e30b25966]]: no `variant` on any FAQ `Link` (`faq.tsx:36-380` write none): the ui `Link`'s default `primary` at `1.2.0` (`node_modules/@villagekit/ui/dist/components/Link.js`: `accentA.600`, no underline, hover `primary.700`), the `0.9.0` default legacy rendered; `variant="paragraph"` goes from every link on the page.
- [[154bcd2abdfb]], [[226f1042adb3]]: the EPA link (`faq.tsx:185-191`) and the community forum link (`:380`) were `<Link href isExternal>`, rendering `target="_blank" rel="noopener"`. The published `1.2.0` `Link` takes no `isExternal` (`node_modules/@villagekit/ui/dist/components/Link.d.ts`) and the sibling's does again (ui commit `540e9c3`, `45d6f5634a11`), and no slice waits on a publish (`28c1a536`), so these two keep `target="_blank" rel="noopener noreferrer"` written in the JSX, the home's mechanism (`58252b32e362`, `159c621d8a1a`): `kipu move 154bcd2abdfb upstream --from regression` with a note citing that ui commit, and `kipu note 99f2fe62c62f` naming the two lines to become `<Link href={...} isExternal>` at the bump, with the bump's check `grep -c noreferrer app/faq/page.tsx` printing 0 and `curl -s localhost:3000/faq | grep -o '<a [^>]*noopener[^>]*>'` reading `rel="noopener"` on both anchors; the bump, not this slice, then runs `kipu fix 154bcd2abdfb`. The repository link (`faq.tsx:141`) and the Australia link (`:197-200`) wrote no `isExternal`, so they are `<Link key="..." href="...">` with no `target` and no `rel`, the same tab, as the live legacy page renders them; that closes [[226f1042adb3]]. Each `Link` inside an array answer carries legacy's `key`.
- The multi-paragraph branch stays as the copy slice wrote it (`58dc1b8f98fd`, fixed there): `VStack alignItems="flex-start"` of `Text key={i}` with the `biome-ignore` comment.
- Chakra v3 and app-router translations throughout, each sanctioned or upgrade-forced: `spacing` to `gap`, `sx` to a style prop, `container.md` to `breakpoint-md`, v2's compound accordion to v3's slots, `NextSeo` to `metadata`, `getLayout` to the root layout; a translation with more than one fitting form is named in the Outcome with the form chosen.
- Interfaces: consumes the shell's layout (`a7bf623f885c`) and the ui `Title`; produces nothing another slice consumes.
- Verify first: `grep -c 'Section\|slug\|paddingY\|variant="paragraph"\|fontWeight="bold"' app/faq/page.tsx` prints at least 5; `grep -c 'isExternal' node_modules/@villagekit/ui/dist/components/Link.d.ts` prints 0 (if it prints 1 the publish and the bump have landed first: write `isExternal` on the two links, close [[154bcd2abdfb]] as `fixed` and write no bump note); `grep -c "'lodash-es'" package.json` prints 1; `kipu show 63e2ef778789` and `kipu show bba676a97320` are `regression`.
- Docs: none; CLAUDE.md's Structure already names the route.
- Not this slice: the accordion trigger's padding and weight and the body's padding (`e1ad4ba984a5`), the heading's size and weight (`77cd1426ac65`, `7124898563eb`), the container's padding (`5c1af396cc2e`, 32px at 1280 against legacy's 16px), the accordion's separator and hover fill (`cb9dce13acba`, `cb528cbc05e6`), the inline links' display (`12a2882546e6`), the `Title` container (`318456ddabc6`), every one `shell` and `upstream`, each expected on the pairs; the shell's `c9ea22823a23` at 768.

## Seams under test

None pure; the proof is the DOM pair's copy diff and aria tree, the screenshot pairs with a computed-style probe on both sides, and the eye on `pnpm dev`.

## Done when

- `pnpm audit:dom --routes <a file naming /faq>` against a running `pnpm dev`: between the nav and the footer, `audit/faq/dom/current.txt` is the copy slice's twenty-line listing unchanged; `current.aria.yaml` holds one `heading` at `level=1`, three named `region`s `Product`, `Sustainability` and `Support`, each with its `level=2` heading and its buttons, every `button` with a nested `paragraph` child as `legacy.aria.yaml`'s have (or the `bba676a97320` clause above is taken and recorded), then a `paragraph` with the links `send us an email` and `ask on the community forum`
- `curl -s localhost:3000/faq | grep -o 'aria-labelledby="faq-[^"]*"' | sort -u` prints `faq-Product`, `faq-Sustainability` and `faq-Support`; `curl -s localhost:3000/faq | grep -o '<a [^>]*href="https://[^"]*"[^>]*>'` shows the EPA and forum anchors with `target="_blank"` and the repository and Australia anchors with neither `target` nor `rel`, as the same command against `https://gridkit-landing-villagekit.vercel.app/faq` shows for the corresponding four anchors (the Australia href there is legacy's `blog.handkrafted.com` one, which the verdict of `9da5502523b9` replaced)
- `pnpm audit:pages --routes <the same file>` at 375, 768 and 1280 looked at against legacy, with a computed-style probe on both sides saved beside it: each category heading with `margin-bottom` 16px; the stack's gap 48px; each trigger padded 8px above and below at weight 500 and each open panel's body 8px above and 16px below its content padding, the published recipe's residue ([[e1ad4ba984a5]], 16px at weight 400 after the bump); the question labels in the `tertiary` tone with no `bold`; the answer links with no underline in the `primary` tone; the closing line 32px below the last rule; the content bounded at 768px; every residual difference is a `shell` item in `upstream` or is filed
- `grep -c 'Section\|slug\|paddingY\|variant="paragraph"\|fontWeight\|as="span"\|FaqCategory' app/faq/page.tsx` prints 0, `grep -c 'noreferrer' app/faq/page.tsx` prints 2, `grep -c "from 'lodash-es'" app/faq/page.tsx` prints 1, and `head -1 app/faq/page.tsx` prints the `// ported from` line
- `next build` and the dev console show no DOM-nesting warning for `/faq`, or the `bba676a97320` clause above is taken and recorded
- The eleven items are `fixed`, `154bcd2abdfb` is `upstream` with its note, and the note on `99f2fe62c62f` is written, checked after the moves
- `timeout 900 just check` is green

## Outcome

## Log
