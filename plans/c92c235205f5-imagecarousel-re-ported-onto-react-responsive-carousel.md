---
title: ImageCarousel re-ported onto react-responsive-carousel
status: done
parent: fd9a92bd8abd
derived_from: fd9a92bd8abd
blocked_by:
  - 6f90e7e24ca6
  - 2de4b709bb36
tags:
  - "worker:fable"
priority: medium
---
The hero carousel is legacy's `components/image-carousel.tsx` again: `react-responsive-carousel`'s `Carousel` with its stylesheet, `infiniteLoop`, a 4000 ms `autoPlay`, the library's translate slide transition, `RasterImage` slides from the ui media package, and the arrows and indicators that only an interactive carousel (no autoplay, more than one slide) renders, in place of the hand-written `motion` carousel whose interactive half nothing calls. The hero then exposes what legacy's tree exposes (a list of slides with the loop's clones, no region, no live text), keeps one height across slides, has no backdrop, and animates whatever the visitor's motion preference. The file moves to `app/_components/ImageCarousel.tsx`, legacy's module location. A re-port on a library under React 19, so Fable. Seven items on `/`, plus the carousel half of the reduced-motion item and the first of the three `sizes` call sites. Record `fd9a92bd8abd`; decisions `ee86d68a`, `2032533f`, `28c1a536`.

## Work

- Legacy: `../node-modules/apps/gridkit/components/image-carousel.tsx` at `fce357d` (146 lines); the call site `pages/index.tsx:107-142`. Current: `app/_components/landing/ImageCarousel.tsx`, `app/page.tsx` (`HERO_SLIDES` and the `ImageCarousel` element).
- [[081bb0ea7651]]: `react-responsive-carousel` (MIT, `^3.2.23`, legacy's `apps/gridkit/package.json:74`; no `peerDependencies` declared, so it installs under React 19) added, the import of `react-responsive-carousel/lib/styles/carousel.min.css` at the top of the client module, `ImageCarouselProps` as legacy's (`ariaLabel`, `slides: Array<RasterImagePropsWithOptionalSizes>`, `autoPlayEnabled`, `slideSx` as `slideCss`, `sizes`, `priority`, the type names from `@villagekit/ui`, which `1.2.0` exports with `RasterImage`), `isInteractive`, the `Carousel` props line for line (`autoPlay`, `ariaLabel`, `emulateTouch`, `infiniteLoop`, `interval={4000}`, `renderArrowPrev`, `renderArrowNext`, `renderIndicator`, `showArrows`, `showStatus={false}`, `showThumbs={false}`, `showIndicators`, `swipeable`), the `Indicator` and `ArrowButton` helpers with `IconButton variant="tertiary"`; the wrapper's `sx` class selectors (`.carousel`, `.carousel-root, .carousel, .slider-wrapper, .slider, .slide`) written in Chakra v3's nested form (`'& .carousel'`). The `index.ts` exports of `CarouselSlide` and `ImageCarouselProps` go with the module.
- [[780e17e5fe2b]], [[d39c2d7084cc]], [[edccbd498f7f]], [[e87be1434e6b]], [[eb165877f8f1]], [[6f9b6d9162de]] close with the rewrite: no `AnimatePresence`, no `section`/`aria-roledescription`, no `VisuallyHidden` live region, no `aspectRatio` on the wrapper, no `bg`. [[c7d1f28fcea1]]'s carousel half: no `useReducedMotion`; the item closes in the design carousel slice once the typing section's half is gone too.
- The call site in `app/page.tsx`: `slides` as legacy's objects (`type: 'cloudinary'`, `src`, `alt`, `width`, `height`, `priority: true` on the first), the first slide's `src` staying `gridbeam.xyz/home/record-shelf-hero` (`bea4a6b2871b`, sanctioned), `priority`, `autoPlayEnabled`, and `sizes={{ base: '100%', md: ['1500px', 2] }}`: the record's Log says why `'100%'` stands where legacy wrote `'full'` (`index.tsx:109`) until the bump, and this slice files that difference: one item on `/` (code, `changed`) with the legacy line, the `1.2.0` hook that throws on `full` (`node_modules/@villagekit/ui/dist/components/media/hooks.js`, `Unexpected size value`) and the rendered `sizes` attribute being identical (`100.00vw`), moved to `upstream` (`kipu move <id> upstream --from regression`) with a note citing ui commit `1c3e3e8` (`252edab16c7a`), and a note on `99f2fe62c62f` naming the call site to swap back to `'full'` at the bump, which the later slices extend. The wrapping `Flex` and the `Box flex="1"` are the page re-port's.
- Interfaces: consumes the mounted `MediaProvider`; produces `ImageCarousel` with legacy's props for the page re-port, and for the story pages record's newsletters (`story-image-carousel.tsx`, the interactive mode).
- Verify first: `timeout 60 pnpm view react-responsive-carousel@3.2.23 peerDependencies` prints nothing; on `pnpm dev` the library renders under React 19 with no error (a deprecation warning for a legacy lifecycle, if any, is named in the Outcome, not fixed here); `next build` accepts the global stylesheet import from a client component under `app/` (Next 15 allows it anywhere under `app/`).
- Not this slice: the hero's copy (the copy slice's, which this slice is blocked by, so the four alts and the label are carried as found); the `Flex justifyContent="flex-end"` wrapper and the inlining of the slides into the JSX (the page re-port).

## Seams under test

None pure; the proof is the aria tree, the pairs and the eye on `pnpm dev`.

## Done when

- `pnpm audit:dom --routes <a file naming />` against a running `pnpm dev`: `audit/_root/dom/current.aria.yaml` shows the hero as `legacy.aria.yaml` does, a `list` of six `listitem` and `img` nodes for four slides, no `region` named by the label and no `Slide 1 of 4` text
- On `pnpm dev` at 1280 the slides advance every 4 s with the library's horizontal slide, the box keeps one height across the four slides, and the wrapper's computed `background-color` is transparent; the pairs at 375, 768 and 1280 looked at against legacy
- `grep -rn 'useReducedMotion\|AnimatePresence\|motion/react' app/_components/ImageCarousel.tsx` prints nothing; `ls app/_components/landing/ImageCarousel.tsx` fails
- The seven items are `fixed`; the `sizes` item is filed and `upstream` with its notes; the reduced-motion item carries a note that its carousel half is done, checked after the moves
- `timeout 900 just check` is green

## Outcome

Shipped: `app/_components/ImageCarousel.tsx`, legacy's `components/image-carousel.tsx` at `fce357d` line for line, translated: the ui-media imports to `@villagekit/ui@1.2.0` (`RasterImage`, `RasterImageProps`, `RasterImagePropsWithOptionalSizes`, which the published package exports), Chakra v2 `sx` to v3 `css`, the wrapper's class selectors in the nested form (`'& .carousel'`, `'& .carousel-root, & .carousel, & .slider-wrapper, & .slider, & .slide'`), `slideSx` to `slideCss`, and the arrow's icon as `IconButton` children where legacy used the `icon` prop the package now marks v2 compat (`node_modules/@villagekit/ui/dist/components/IconButton.d.ts`); the `Carousel` props, `isInteractive`, `Indicator` and `ArrowButton` are legacy's, `key={slide.key}` included. `react-responsive-carousel` added at legacy's range `^3.2.23` (MIT, `node_modules/react-responsive-carousel/LICENSE.md`; the lockfile pins 3.2.23; `pnpm view react-responsive-carousel@3.2.23 peerDependencies` prints nothing, and `pnpm add`'s unmet-peer lines were the pre-existing `react-spring` and `ansi-to-react` ones), its stylesheet imported at the top of the client module, which `next dev --turbopack` and `next build` both accept. `app/_components/landing/ImageCarousel.tsx` deleted with its `index.ts` line (the `CarouselSlide` type went with it). `app/page.tsx` imports the carousel from its new module, `HERO_SLIDES` typed `ImageCarouselProps['slides']` with `type: 'cloudinary'` on every slide and `priority: true` on the first (the first `src` stays `gridbeam.xyz/home/record-shelf-hero`, `bea4a6b2871b`), and the element takes `sizes={{ base: '100%', md: ['1500px', 2] }}`, `priority` and `autoPlayEnabled`; the `Box flex="1"` wrapper and the slides' inlining are the page re-port's.

Verified on `pnpm dev` against the published 1.2.0 (no override, no `transpilePackages`): a Playwright probe at 1280 sampled the hero four times 4.2 s apart: six `.slide` items for four slides, the `ul.slider` moving by `translate3d(-100%)`, `-200%`, `-400%` with `transition-duration: 350ms` (one sample caught mid-transition at -1771 px), the `.carousel-root` 512 by 384.33 px in every sample, the wrapper's and the root's `background-color` `rgba(0, 0, 0, 0)`, `.carousel` at `border-radius: 16px`, the `md` shadow and `user-select: none`, `aria-label="Showcase of things made with grid beam"` on `.carousel-root`; every slide `img` with a sixteen-entry Cloudinary `srcset` and `sizes="(min-width: 768px) 750.00px, 100.00vw"`, the attribute a curl of the legacy site renders for `base: 'full'`. No console error or warning from the library or React (the pre-existing `kebab-case` error from `StoryImage` only). `pnpm audit:dom` on `/`: `current.aria.yaml` shows the hero as `legacy.aria.yaml` does, a bare `list` of six `listitem`/`img` in the same order (the last slide's clone first), no `region`, no live text; `diff legacy.txt current.txt` is empty at the hero. `pnpm audit:pages` at 375, 768 and 1280 under `audit/_root/`, looked at: the hero box, its radius, shadow and first slide match legacy at every width; the current 768 capture is 928 px wide because the footer's four columns overflow (`c9ea22823a23`, the shell's), the carousel root being 720 px there. The Done-when greps: no `useReducedMotion`, `AnimatePresence` or `motion/react` in the file, and the landing module gone.

The ledger: `081bb0ea7651`, `780e17e5fe2b`, `d39c2d7084cc`, `edccbd498f7f`, `e87be1434e6b`, `eb165877f8f1` and `6f9b6d9162de` fixed. `152511f71ef4` filed on `/` (code, `changed`): `base: '100%'` where legacy wrote `'full'`, the 1.2.0 hook that throws on `full` and the identical rendered attribute; moved `regression` then `upstream` with a note citing ui commit `1c3e3e8` (`252edab16c7a`); the bump plan `99f2fe62c62f` carries a note naming the call site to swap back to `'full'` and the grep that checks it, which the design carousel and page slices extend. `c7d1f28fcea1` noted: its carousel half is done, the design carousel slice closes it. One more item, found by the probe and not introduced here: `f9729ac7552a` (`open`, `/`, code, `removed`), Next 15.5.18 writes no `fetchpriority="high"` on a `priority` image where Next 14.2.5 wrote it on all six hero images (`get-img-props.js` no longer derives it from `priority`; the preload links are still written); the replaced `motion` carousel had the same gap under the same Next, so it is filed for the rules (rule 4, or a `fetchPriority` the site writes and legacy did not) and goes to the home's verdicts plan at the finish. No verdict written.

Deviations from the plan's letter: none in what the visitor sees. Two shape calls, both the v3 translation: the arrow icon as children rather than the compat `icon` prop, and `slideCss` for `slideSx`, as the Work says. Not fixed here, as the plan says: nothing, the library emitted no deprecation warning under React 19.

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity). Standards found one critical: the lockfile's specifier was the exact `3.2.23` of the first `pnpm add` while `package.json` had been edited to legacy's `^3.2.23`, so `pnpm install --frozen-lockfile` (CI's step) failed; fixed by `pnpm install --lockfile-only`, and the frozen offline install then passed. Its judgment calls: TSDoc absent on the two exports, as on legacy's file and the other ported modules, the legacy author's pattern being the standard (`ee86d68a`), so not taken; `key={slide.key}` faithful, and `pnpm dev` logged no key warning since the library clones each child with its own key. Spec found every load-bearing claim verified, with two citation slips, both fixed: the legacy `priority` line is `pages/index.tsx:141`, not 139, in `f9729ac7552a`; and the later `'full'` call sites are `pages/index.tsx:334,365,444,454` (the hidden eager next-design image was missed), in `152511f71ef4` and the bump note; it also named the stale `LandingPhoto.tsx` header comment naming the carousel as a sibling, tidied to name `LandingVideo` only; its remark that `152511f71ef4` never passed through `regression` is not so (it was moved `open` to `regression` to `upstream`; `kipu move` writes no Log line). Parity found the carousel at parity at 375, 768 and 1280: the library markup, transform, duration, radius, `user-select`, `sizes`, `srcset`, `decoding` and `object-fit` the same on both sides, the shadow's value the shell's `f45538f480af` (`upstream`), the box sizes the page layout's; one addition taken, a note on `f9729ac7552a` that the hydrated legacy DOM holds six preload links against the current four, the same cause. Dropped: none.

Gate: `timeout 900 just check` green after the dev server was stopped; `kipu verify --warnings-as-errors` green.

## Log
