---
title: "Route: faq"
status: done
parent: 337e35d86920
blocked_by:
  - a78b167170b8
  - target: 40179ab9e779
    strength: soft
    note: route order
---

## Goal

The route `/faq` is at parity with the legacy site: every difference on it is `fixed` or `sanctioned`, and the operator reviews it at the parity gate `f7a700a3e482`. Decision `ee86d68a`.

## Scope

The differences on `/faq` in the ledger (`kipu list --collection difference --filter route=/faq --json`): forty-nine items at the split, forty `regression` (thirty-eight found by M1 and the shell's reviews, two filed at the split), nine `sanctioned`, none `open`. The port rule of decision `ee86d68a` decides whether each slice re-ports from the legacy source or fixes in place; here the page is re-ported, the removals and the copy landing first on the current page.

The removals, nine items, every verdict the operator's from the FAQ grilling (`b52b62e260f3`): the meta description `e91809d5069d`; the description line `886bec463e1b`; the `Suppliers` section `ae7dd01512fb`, `c8c20c62cde3`, `8c0dd813f0a2`, `d5226f4b1c1b`, `033b62f084cf`; the two questions added under `Other` `e8bfbfcee3b2`, `317e8711ff84`.

The copy verdicts, nineteen items (`241b65da8226`): the sections `8368a3c0a5cc`, `cf09a6601985`; the `Product` questions `887f3d253094`, `7416dab47f06`, `a344d9b33bca`, `2a940fa3e6fd`, `3e2d549b5ada`, `086eab31135d`, `96e9c9873dd1`, `39a41b3159df`, `79a184bb6000`; the `Sustainability` questions `8106f4775b6b`, `3e857451d7f2`, `b83540802426`, `9da5502523b9`; the `Support` questions `62ebc1fe6962`, `68a65ec89524`; the closing line `eceaf3fc05dd`; the array branch the multi-paragraph answers need `58dc1b8f98fd`.

The page re-port, twelve items (`bba2bb35f208`): the width `e253200bf486`, the closing line's spacing `eda7795256a9`, the section wrapper `1205c31bebd0`, the heading size `0bd8893128a4`, the data shape `eb2304414513`, the stack's alignment `63e2ef778789`, the trigger's label `55b8453f7512`, `bba676a97320`, the paddings `6c31193ec773`, the links' variant `753e30b25966`, the repository link's tab `226f1042adb3`, and the external links' `rel` `154bcd2abdfb`, which moves to `upstream` until the bump.

Sanctioned, not a slice's: `23f0413009ed` the stack's gap, `2ed11f164573` the accordion slot API, `63130f446c8d` the title, `a1336cb4a3ef` the rebrand swap in one question, and the e-commerce removals `73eda11cfbd1`, `805d395bccce`, `8994d21a11f5`, `adce8828614b`, `ba753f7499b9`.

## Seams under test

Named when sliced; none pure unless the route carries logic.

## Exit demo

`kipu list --collection difference --filter route=/faq --json` shows no `open` or `regression` item, and `/finish-epic` finishes this record; the operator reviews the route at the parity gate `f7a700a3e482`.

## Out of scope

Other routes; the shell, except where a difference on this route is closed by a shell change, which then belongs to the shell record.

## Outcome

Finished unattended by `/finish-epic` on 2026-09-26 (decisions `b9af27e0df50` and `40abdb2f222a`), the three children of the split `done`. The faq's ledger at the finish: 49 items on `/faq`, 39 `fixed`, 9 `sanctioned` (`23f0413009ed`, `2ed11f164573`, `63130f446c8d`, `a1336cb4a3ef`, `73eda11cfbd1`, `805d395bccce`, `8994d21a11f5`, `adce8828614b`, `ba753f7499b9`), none `dismissed`, 1 `upstream` (`154bcd2abdfb`, the EPA and forum anchors' `rel`, fixed in `../ui` at commit `540e9c3` by `Link`'s `isExternal` and waiting on the operator's publish and the bump plan `99f2fe62c62f`, decision `28c1a536`), none `open`, none `regression`; the forty-nine of the split, no item filed on the route since.

The exit demo, clause by clause. First, `kipu list --collection difference --filter route=/faq --json` holds 49 items, `fixed 39, sanctioned 9, upstream 1`, and a jq count of the items in `open` or `regression` prints 0; the one `upstream` item is what `40abdb2f222a` and `28c1a536` allow, waiting on the bump plan. No item was judged here (no state changed, no verdict written), and no item was left for the operator, so no verdicts plan stands beside this record: the split's Log foresaw it (none `open` at the split, no expected stop but the `upstream` rel item), the three rule-5 items the split's call 6 named (`63e2ef778789`, `bba676a97320`, `eb2304414513`) had no sanction when the re-port ran, so legacy's form shipped and closed them, and the re-port's conditional client-file item did not arise (its Outcome: one server file, React 19 raising no nesting warning for the `<p>` inside the `<button>`). Second, `/finish-epic` finishes this record: this finish, `kipu ready --json` having listed it as the one completable plan. Third, the operator reviews the route at the parity gate `f7a700a3e482`: the gate is `blocked_by` M2, the bump plan and the shell's and the home's verdicts plans, so the publish that carries the `rel` fix comes before the review; this finish adds no edge, having no verdicts plan to add.

The items handed on. None to the operator. To an agent, one slice beside the shell record for the two shell regressions the re-port's Parity review filed, both from Chakra v3's accordion recipe and hidden until the re-port removed the route's own props: `c7ffae735176`, the trigger's `textAlign: start` where legacy's button kept the browser's `center`, so the two questions that wrap at 375 left-align, and `2cc5b00fb582`, the indicator's `fg.subtle` 2px stroke chevron at 1.2em where legacy's was a 20px filled chevron in the text color. The slice is `aff1c5f9a5f2` (`--parent` M2, `derived_from` `a78b167170b8`, `worker:fable`, `priority: medium`, following `28c1a536` and blocking the bump plan from the mint): one slice for both items because each is a line in the one slot recipe, `../ui/src/components/Accordion.recipe.ts` (`itemTrigger.textAlign: center`; an `itemIndicator` slot with `color: inherit` and the icon at `1.25em`), read at ui commit `8f85a7c`; the glyph itself is the one part the recipe cannot reach, since Chakra's `ItemIndicator` renders its stroked `ChevronDownIcon` as the default child, so the slice decides whether the ui wraps `ItemIndicator` with legacy's filled path, with the wrapper as the default, which is why it runs on Fable. Each item carries a note naming the slice, and the shell record and the bump plan a note naming it. The third shell item the split filed, `e1ad4ba984a5` (the trigger's padding and weight and the body's padding), was fixed in `../ui` at `540e9c3` by the recipes slice already and is `upstream`, so no slice is minted for it. Every `open` or `regression` item on `shell` is named by the verdicts plan `77cf83a1285a` (eighteen: the fourteen in its body and the four its notes added) or by this slice (two), checked by grepping each id across `plans/`.

The children, one line each with the site commit:
- `b52b62e260f3` the added Suppliers section, the two added questions, the description line and the per-page metadata removed: `8250c73`.
- `241b65da8226` the copy verdicts applied on the sections, the questions, the answers and the closing line: `d11d9d9`.
- `bba2bb35f208` the page re-ported from `pages/faq.tsx`: `e9222ff`.

Deviations from Scope, each recorded in its child's Outcome. The removals slice shipped as scoped and wrote the bump note that `/faq` leaves the Description probe's list. The copy slice shipped as scoped: `58dc1b8f98fd` had no verdict Log line and was closed by the default rule (legacy's array branch restored), and the quoted link label was written as a JS string expression, which the re-port replaced with plain JSX text; the custom design question sits under `Support` by the letter of `cf09a6601985`'s verdict where legacy had it under `Other` (the split's call 3), and the item carries the note that lets the operator correct it. The re-port shipped as scoped with the translations its Work named, each with one fitting form (`spacing` to `gap`, `sx` to style props, `container.md` to `breakpoint-md` under rule 4, `allowMultiple` to `multiple collapsible` and the slot API, `NextSeo` to `metadata`, `getLayout` to the root layout); one server file, no client file; the EPA and forum anchors keep `target` and `rel` in the JSX until the publish, the home's mechanism, with `154bcd2abdfb` moved to `upstream` and the bump note naming the two lines to become `isExternal`; one slip in its plan's letter, the verify-first grep on `package.json` that read single quotes where the file has double. Nothing crossed Out of scope: no shell code changed; the two shell items were filed to the ledger and go to the slice above, never fixed in the route. Every `regression` on `/faq` at the split was closed by exactly one slice, forty in all (thirty-nine `fixed`, one `upstream`), as the split's Log set out.

Flags the children raised, together, for later iterations: the bump plan `99f2fe62c62f` carries the faq's site edit for the publish (`app/faq/page.tsx:113-118` and `:214` to `<Link href isExternal>`, checked by `grep -c noreferrer app/faq/page.tsx` printing 0 and the curl reading `rel="noopener"` on the two anchors), the accordion probe for `e1ad4ba984a5` (16px above and below at weight 400 on a trigger, 16px above and below on an open body) and now the slice's probe for the two accordion items; the residuals on the faq's pairs are the shell's `upstream` items (the heading at 18px on 21.6px at weight 600, `77cd1426ac65` and `7124898563eb`; the container padding 32px at 1280 and 24px at 768, `5c1af396cc2e`; the inline links' `inline-flex`, `12a2882546e6`; the trigger's 8px padding at weight 500, `e1ad4ba984a5`; the separator and the hover fill, `cb9dce13acba` and `cb528cbc05e6`; the `Title` container, `318456ddabc6`; the palette items) and at 768 the footer's four columns overflowing the viewport (`c9ea22823a23`, the operator's on `77cf83a1285a`) and the wrapping wordmark (`1d2562d6b3bc`, the operator's there too); the `<p>` inside the `<button>` (`bba676a97320`, `fixed`) holds while React 19 does not warn on the nesting, read in react-dom 19.2.6 by the split's review, so a React bump that changes `validateDOMNesting` reopens it; the custom design question sits under `Support` by the letter of `cf09a6601985`'s verdict where legacy had it under `Other`, the one faq item the operator may still correct, by a note on the item, before or at the parity gate `f7a700a3e482`.

Beyond the ask: `plans/README.md`'s Milestones paragraph names this finish and the slice; the bump plan's note. The orchestrator's brief expected the finish to mint one or two ui slices; one was minted, on the code's evidence that both items are the one recipe's.

Reviewed once on a fresh Opus sub-agent before the commit, nothing critical, nothing major; every claim above was verified by it (the counts and the nine sanctioned ids, the newest items on the route dated at the split, the one completable plan, the gate's `blocked_by` list, the shell coverage at eighteen and two, every file claim in the slice against `../ui` at `8f85a7c`, `@chakra-ui/react` in `node_modules` and a Chakra v2 `@chakra-ui/accordion@2.3.1` copy on disk outside the repo, which confirmed the item's record of the filled path at `1.25em` and no `textAlign`; the children's commits touching only `app/faq/page.tsx` outside the store). Minor, all taken: the slice's fallback for the glyph now says the state the item takes (`2cc5b00fb582` stays `regression` with a note, the filled-path and `upstream` lines then applying to `c7ffae735176` alone); the package's own `stories/Accordion.stories.tsx`, the second consumer of `ItemIndicator`, named in the glyph bullet; the recipe declares the `itemIndicator` slot and writes no styles for it, not "no slot at all". Nits taken: three possessives lost to quoting in the new notes restored by hand; the Done when line on the bump note reads as written at the mint; the custom design question's placement added to the flags above. The review's call that one slice is right (one component, one probe, one route, one CHANGELOG entry) and that `worker:fable` follows the rule stands. Findings dropped: none.

## Log

- 2026-09-26: Split into three slices (plan 7f0b60d948c5), reviewed once on one Opus sub-agent. Against `@villagekit/ui@1.2.0`, in the order the edges give: `b52b62e260f3` the added Suppliers section, the two added questions, the description line and the per-page metadata removed (Sonnet, nine items, every verdict the operator's from the FAQ grilling); `241b65da8226` the copy verdicts applied on the sections, the questions, the answers and the closing line (Sonnet, nineteen items); `bba2bb35f208` the page re-ported from `pages/faq.tsx` (Fable, twelve items), last, consuming both and the shell's layout slice (`a7bf623f885c`). Every `regression` on `/faq` is closed by exactly one slice, forty in all, one of them (`154bcd2abdfb`) moved to `upstream` rather than `fixed`; no item is `open`; the nine `sanctioned` items need no slice. Calls made at the split, for the operator to read: (1) the page is re-ported, not fixed in place: the current page has a `Section`, a slugged array, a span label, explicit paddings and a `variant` on every link where legacy has a `Title`, a `Container`, a label-keyed object and bare components, and the port rule says in doubt re-port; the removals and the copy land first on the current page, on Sonnet, so the re-port carries settled text and its review is structure only, the about's shape (`40179ab9e779`); (2) three verdicts ship answers of several paragraphs (`8106f4775b6b`, `3e857451d7f2`, `9da5502523b9`), so the copy slice restores legacy's array branch (`58dc1b8f98fd`, a code item closed there and not by the re-port); the add-ons answer, one paragraph after rule 2 removes its first, is a single node and not a one-element array; (3) the verdict of `cf09a6601985` names the custom design question among the two under `Support`, though legacy had it under `Other` (`faq.tsx:304-329`) and only the contact support question under `Returns & Support`; the copy slice follows the verdict's letter, which `62ebc1fe6962` agrees with, so no `Other` section remains, its other question being the sanctioned removal `805d395bccce`; the item carries a note saying so, and the operator may correct it there before the copy slice runs; (4) a verdict quoting legacy text ships legacy's characters, the typographic apostrophes of `can’t`, `We’d`, `isn’t` and `We’re` included, where the Log lines write them straight by their quoting (the grilling wrote `straight apostrophe` where it meant one, `8881ebddcf54`); (5) the published 1.2.0 `Link` takes no `isExternal`, so the EPA and forum anchors keep `target` and `rel` written in the JSX and `154bcd2abdfb` moves to `upstream` citing ui commit `540e9c3` with a bump note, the home's mechanism (`58252b32e362`); the repository and Australia links, which legacy wrote without `isExternal`, ship as plain `Link href` in the same tab; (6) three code items whose M1 note says the operator may weigh the current form under rule 5 (`63e2ef778789` `alignItems="left"`, `bba676a97320` the `<p>` inside the `<button>`, `eb2304414513` the label-keyed object) are `regression` with no verdict, so the re-port writes legacy's form and closes them, keeping the current form only where the operator has sanctioned the item by the time it runs; the id defect the slug fixed does not recur, every surviving label being one word; the `<p>` inside the `<button>` is conditional on React 19 not warning, which the review checked in react-dom 19.2.6 (`findInvalidAncestorForTag('p')` reads only `pTagInButtonScope`, which a `button` resets), so the clause is a guard and not an expected stop; (7) two items filed at the split, both the re-port's: `eda7795256a9` the closing line's spacing (32px below the stack in legacy, 48px as a child of it here) and `226f1042adb3` the repository link opening a new tab; (8) the translations: `container.md` to `breakpoint-md` (rule 4, the about's), `spacing` to `gap` (`23f0413009ed`), the accordion slot API (`2ed11f164573`), `sx` to style props, `NextSeo` to `metadata`, `getLayout` to the root layout; one server file, as the about; (9) the bump notes: the removals slice says `/faq` renders no `Description` after it, so the Description probe's `/faq` reading goes; the re-port names the two anchors for the `isExternal` swap. Expected on the pairs until the bump, the shell's `upstream` items: the accordion trigger's padding and weight and the body's padding (`e1ad4ba984a5`, filed at the split), the heading's size and weight (`77cd1426ac65`, `7124898563eb`), the container's padding (`5c1af396cc2e`), the accordion's separator and hover fill (`cb9dce13acba`, `cb528cbc05e6`), the inline links' display (`12a2882546e6`), the `Title` container (`318456ddabc6`); at 768 the footer (`c9ea22823a23`). Items already the operator's: none `open` on `/faq` at the split, so no verdicts plan is expected at the finish. Expected stops: none for the slices; at the finish the `upstream` rel item waits on the bump plan `99f2fe62c62f`, and the record finishes on `40abdb2f222a`. Review findings taken, all of them: the re-port's claim that removing the route's `paddingY` props renders legacy's 16px at the regular weight was false at the published 1.2.0, where Chakra's `md` size variant and base win over the ui recipe (the fix in the sibling's `540e9c3` had no item), so `e1ad4ba984a5` is filed on `shell`, moved to `upstream` on that commit, named in the re-port's bullets, its `Not this slice` and its probe line, and noted on the shell record and the bump plan; the copy slice's served-HTML count reads distinct strings (`sort -u`), since the RSC payload repeats each one, and its verify-first matches the closing `</p>` of an answer; the copy slice's slug grep matches the literal quote so the interface line does not count; this Log entry, which the slices cite by call number; call 3 states the verdict's misplaced premise and the item carries a note; the two filed items cite the page's current lines; the re-port's legacy-anchor line names the Australia href as the one the verdict changed; the bump note's `rel` check is a curl, not the DOM extraction, and the `kipu fix` is the bump's; three legacy line numbers in the copy slice (`:299`, `:40`, `:187`), the `key` on the `Link` inside a fragment, and the apostrophe rule extended to the restored contact support string. Dropped: none.
