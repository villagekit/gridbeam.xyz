---
title: "Route: about"
status: done
parent: 337e35d86920
blocked_by:
  - a78b167170b8
  - target: fd9a92bd8abd
    strength: soft
    note: route order
---

## Goal

The route `/about` is at parity with the legacy site: every difference on it is `fixed` or `sanctioned`, and the operator reviews it at the parity gate `f7a700a3e482`. Decision `ee86d68a`.

## Scope

The differences on `/about` in the ledger (`kipu list --collection difference --filter route=/about --json`): forty-two items at the split, thirty-nine `regression`, three `sanctioned`, none `open`. The port rule of decision `ee86d68a` decides whether each slice re-ports from the legacy source or fixes in place; here the page is re-ported, the removals and the copy landing first on the current page.

The removals, twenty-one items, every verdict the operator's from the copy grilling (`5ac30176aa70`): the title and the meta description `f6ebf1620bad`, `494cc9baa8bd`; the description line `fb23903a3c70`; the intro block `5d2c47f5c255`, `4613624745c4`, `54acd326d718`, `5ee924ad245e`; the `Where it came from` section `473fb083a0e7`, `e6071f319f08`, `88cde35fe693`, `24c7ab53652c`, `00b6bb04683c`, `49b374146247`; the `Start building` section `d9ced6913e2b`, `ca856b1fae92`, `f40c010ea9c4`, `1b8f98bd363b`, `d5bc3b1abc0f`, `d0caa4bf0339`, `cae5306ea71e`, `e66dbbbf6d1f`.

The copy verdicts, eleven items (`5176646603be`): the captions `b60740a84adf`, `45283323e3ed`, `547b0a4b92ce`, `0e029af68390`, `d19bc2fe8044`; the alts `fde584087061`, `da3845c72595`, `a8cb80eee630`, `ea001d8f3c25`, `12f205a40fb6`, `1be8e5c18f77`.

The page re-port, seven items (`97e702d40df5`): the shape `bfe876779ed0`, the heading size `5f618057b23d`, the gap `2b1dce44fd7a`, the images `878fefa617c4`, `13bdc7d5ca36`, the text helper `61dedaa19b75`, `c52a29c7ef8c`.

Sanctioned, not a slice's: `01ae1e943907` the bold spans, `1f742640f27f` the heading's text, `3741ad0e8bbf` the re-hosted assets.

## Seams under test

Named when sliced; none pure unless the route carries logic.

## Exit demo

`kipu list --collection difference --filter route=/about --json` shows no `open` or `regression` item, and `/finish-epic` finishes this record; the operator reviews the route at the parity gate `f7a700a3e482`.

## Out of scope

Other routes; the shell, except where a difference on this route is closed by a shell change, which then belongs to the shell record.

## Outcome

Finished unattended by `/finish-epic` on 2026-09-26 (decisions `b9af27e0df50` and `40abdb2f222a`), the three children of the split `done`. The about's ledger at the finish: 43 items on `/about`, 39 `fixed`, 3 `sanctioned` (`01ae1e943907`, `1f742640f27f`, `3741ad0e8bbf`), none `dismissed`, 1 `upstream` (`36dc54eb5955`, the six images' `sizes` value, fixed in `../ui` at commit `1c3e3e8` and waiting on the operator's publish and the bump plan `99f2fe62c62f`, decision `28c1a536`), none `open`, none `regression`; one more than the forty-two at the split, the `sizes` item the re-port filed.

The exit demo, clause by clause. First, `kipu list --collection difference --filter route=/about --json` holds 43 items, `fixed 39, sanctioned 3, upstream 1`, and a jq count of the items in `open` or `regression` prints 0; the one `upstream` item is what `40abdb2f222a` and `28c1a536` allow, waiting on the bump plan. No item was judged here (no state changed, no verdict written), and no item was left for the operator, so no verdicts plan stands beside this record: the split's Log foresaw it (none `open` at the split, no expected stop), and the re-port's conditional client-file item did not arise (its Outcome: one server file). Second, `/finish-epic` finishes this record: this finish, `kipu ready --json` having listed it as the one completable plan. Third, the operator reviews the route at the parity gate `f7a700a3e482`: the gate is `blocked_by` M2, the bump plan and the shell's and the home's verdicts plans, so the publish that carries the `sizes` fix comes before the review; this finish adds no edge, having no verdicts plan to add.

The items handed on. None to the operator. To an agent, one slice beside the shell record for the one shell regression this record's split filed, `318456ddabc6`, the ui `Title`'s heading container at 672px where legacy's `ui-page` `Title` wrapped at 768px, which the item's Log sent to the about's finish: `728a36aedc8c` (`--parent` M2, `derived_from` `a78b167170b8`, `worker:fable`, `priority: medium`, following `28c1a536` and blocking the bump plan from the mint), one line in `../ui/src/components/layouts/Title.tsx` to `breakpoint-md`, the name `CardsLayout`'s container took at ui commit `a4ef8ed`, read on every route that renders `Title`. The item carries a note naming its slice, and the shell record a note naming it.

The children, one line each with the site commit:
- `5ac30176aa70` the added intro, the two added sections and the per-page metadata removed: `bba3661`.
- `5176646603be` the copy verdicts applied on the captions and the alts: `14004b6`.
- `97e702d40df5` the page re-ported from `pages/about.tsx`: `71ad308`.

Deviations from Scope, each recorded in its child's Outcome. The removals and copy slices shipped as scoped; the removals slice also added a note to the bump plan correcting three stale claims (`/about` renders no `LinkCard` and leaves the `isExternal` and gray-band lists) and noted `bfe876779ed0`'s stale Current text. The re-port shipped as scoped with the translations its Work named, each with one fitting form: `spacing` to `gap`, `sx` to a style prop, `container.md` on the page's own container to `breakpoint-md` (rule 4), `NextSeo` to `metadata`, `getLayout` to the root layout; one server file, no client file; it wrote `'768px'` for legacy's `'container.md'` on the six `sizes` objects, since the published 1.2.0 size hooks throw on the name, and filed that as `36dc54eb5955`, `regression` at the mint and moved to `upstream` citing ui commit `1c3e3e8`, with the bump note naming the six lines to swap back (the split's call 3). Nothing crossed Out of scope: no shell code changed; the one shell item, `318456ddabc6`, was filed at the split and goes to the slice above, never to the route. Every `regression` on `/about` at the split was closed by exactly one slice, thirty-nine in all, as the split's Log set out.

Flags the children raised, together, for later iterations: the bump plan `99f2fe62c62f` carries the about's site edit for the publish (the six `sizes` lines in `app/about/page.tsx` back to `'container.md'`, checked by `grep -n "base: '768px'" app/about/page.tsx` printing nothing) and the note that `/about` renders no `Section` and its images take the `imageComponent` note; the residuals on the about's pairs are the shell's `upstream` items, the container's padding (`5c1af396cc2e`, 24px at 768 and 32px at 1280 against legacy's 16px, fixed in `../ui` by `2bd0169a6dda`) and at 768 the footer's clipped column (`fb033121d164`) and the wrapping wordmark (`1d2562d6b3bc`, the operator's on `77cf83a1285a`); the `Title` container (`318456ddabc6`) is invisible on `/about` and reads on every route that renders `Title`.

Beyond the ask: `plans/README.md`'s Milestones paragraph names this finish and the two slices; a second `../ui` slice, `a4f938a27428`, minted beside the shell record for `a90ae9e6e022` (the badge's `user-select: none` and tabular numerals kept over Chakra v2's), a shell `regression` the recipes slice `2bd0169a6dda` filed after the shell's verdicts plan and the home's finish were written, owned by no slice and no verdicts plan until this finish listed the shell's regressions; the item carries a note naming its slice. It goes to a slice and not onto the shell's verdicts plan because the recipes slice filed it `regression` (the default of `2032533f`) although its kind is `added`, the two declarations being ones legacy never wrote, and the fix restores legacy's rendering with no verdict to write, as `1ef32a0328e6` was `added` and `fixed`; the operator may still move it to `77cf83a1285a`. The orchestrator's brief expected an attended verdicts plan for the about's `sanctioned` and copy items; none was minted, since those items are judged already (the copy grilling's verdicts, applied by the copy slice) and `40abdb2f222a` gives a verdicts plan only the items an agent may not judge, of which `/about` has none.

Reviewed once on a fresh Opus sub-agent before the commit, nothing critical, nothing major; every claim above was verified by it (the counts, the commits, the gate's `blocked_by` list, the shell coverage, the two slices' citations against the files, the no-verdicts-plan call). Minor, all taken: a residual named here that no child recorded (the heading recipe's sizes, `0369f14e4df4`; the re-port's probe read the heading at legacy's size on both sides) dropped; the badge slice's probe and pairs widened to `/designs`, whose filter badges (`app/_components/catalogue/Catalogue.tsx:341`) are where `user-select` reaches a visitor; the Title slice's legacy list gains the story pages and `/subscribe` (`apps/gridkit/components/layouts/stories.tsx:56`, `packages/applet-subscribe/src/page.tsx:32` at `fce357d`) and its probe a story page; the sentence above on why the badge item goes to a slice. Nits taken: the removals slice's bump note described as a note, not an edit of the stale lines; a flag with no reader (the old line numbers on the closed `/about` items) dropped; a confusing parenthetical in the Title slice's first Done when line reworded. Dropped: none.

## Log

- 2026-09-26: Split into three slices (plan 40179ab9e779), reviewed once on one Opus sub-agent. Against `@villagekit/ui@1.2.0`, in the order the edges give: `5ac30176aa70` the added intro, the two added sections and the per-page metadata removed (Sonnet, twenty-one items, every verdict the operator's from the copy grilling); `5176646603be` the copy verdicts applied on the captions and the alts (Sonnet, eleven items); `97e702d40df5` the page re-ported from `pages/about.tsx` (Fable, seven items), last, consuming both and the shell's `MediaProvider` (`6f90e7e24ca6`) and layout (`a7bf623f885c`) slices. Every `regression` on `/about` is closed by exactly one slice, thirty-nine in all; no item is `open`; the three `sanctioned` items (`01ae1e943907`, `1f742640f27f`, `3741ad0e8bbf`) need no slice. Calls made at the split, for the operator to read: (1) the page is re-ported, not fixed in place: the current page has three `Section`s, a route-local `next/image` wrapper and a children-only text helper where legacy has a `Title`, a `Container` and a `VStack` of captions and ui `Image`s, and the port rule says in doubt re-port; the removals and the copy land first on the current page, on Sonnet, so the re-port carries settled text and its review is structure only, the home's shape (`fd9a92bd8abd`); (2) the removals slice blocks on the document head slice `ffe8e5d56f8e`, whose title template renders `Grid Beam: About`, and on the home's removals slice `4faefea81c76`, whose bump note it corrects (`/about` renders no `LinkCard` after it), both done, so the slice is ready at the split; (3) as on the home, the published `1.2.0` size hooks throw on legacy's `sizes={{ base: 'container.md' }}` (`dist/components/media/hooks.js:52`, the name restored only in the sibling, `252edab16c7a`, `upstream`), and no slice waits on a publish (`28c1a536`), so the re-port writes `'768px'`, the width `0.9.0` resolved `container.md` to, which renders the identical `sizes="768.00px"` attribute, and files that as one code item on `/about` moved to `upstream` citing ui commit `1c3e3e8`, with a bump note naming the six call sites; (4) the ui `Title` at `1.2.0` wraps its heading in `Container maxW="2xl"` (672px) where legacy's `ui-page` `Title` used `container.md` (768px); no shell item named it (`8a3babf21c3c` is `CardsLayout`'s container, the same rename), so it is filed at the split as `318456ddabc6` (`shell`, code, `regression`), the ui's to fix by a slice beside the shell record at the about's finish (decision `40abdb2f222a`), never in the route; (5) Chakra v2's `container.md` on the page's own `Container` becomes v3's `breakpoint-md` (768px, the same width), rule 4, named in the re-port's Outcome; (6) the page stays one server file, since legacy's page calls no hook of its own and every ui component it renders is a client module, so the home's two-file mechanism (`091a47cb93e7`) is not forced; the Outcome says so if it turns out to be. Items already the operator's: none `open` on `/about` at the split. Expected stops: none for the slices; at the finish, the `upstream` sizes item waits on the bump plan `99f2fe62c62f` as the home's does, and the record finishes on `40abdb2f222a`. Review findings taken, all eleven: the caption count is six, not seven, in both DOM checks and the re-port's paragraph; the copy slice's `&nbsp;` count before is 4 on the page the removals leave; a client file, if one is forced, files an `open` code item citing `091a47cb93e7` rather than treating it as settled; the `Title` container item filed now rather than on a condition the about heading never meets; the removals' bump note also takes `/about` out of the links note's `isExternal` list and the `Section` note's band list, and drops an untested claim about the sibling override; the verify-first grep's output described as it is; `65c21e08b3f1` cited for `NextSeo` to `metadata`; the fasteners image at its intrinsic 685px on the pairs; the `sizes` item filed `regression` then moved to `upstream`, with the publish-first case; two `HomePage.tsx` lines. Dropped: none.
