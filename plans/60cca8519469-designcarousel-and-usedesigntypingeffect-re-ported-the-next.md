---
title: DesignCarousel and useDesignTypingEffect re-ported, the next design preloaded
status: done
parent: fd9a92bd8abd
derived_from: fd9a92bd8abd
blocked_by:
  - 6f90e7e24ca6
  - c92c235205f5
  - 2de4b709bb36
tags:
  - "worker:fable"
priority: medium
---
The typing section's picture is legacy's `components/carousel.tsx` again, `DesignCarousel` on the Web Animations API: the old image fades out over 100 ms, and when the new one loads it fades in over 1500 ms while scaling from 0.8 over 2000 ms, mirrored by `shouldMirror`, the ui media `Image` inside; the hook is legacy's `hooks/useDesignTypingEffect.tsx` again (lodash `shuffle`, the timer effect on `[designs, pause]`, the three-tuple), and the section reads all three elements so the next design's image is loaded hidden before the swap. No `motion`, no reduced-motion branch. The modules move to `app/_components/DesignCarousel.tsx` and `app/_lib/useDesignTypingEffect.ts` (CLAUDE.md, Structure: helpers under `app/_lib/`). A re-port, so Fable. Five items on `/` and the second `sizes` call site. Record `fd9a92bd8abd`; decisions `ee86d68a`, `2032533f`, `83b6c8ceb5d0`.

## Work

- Legacy: `../node-modules/apps/gridkit/components/carousel.tsx` at `fce357d` (99 lines), `apps/gridkit/hooks/useDesignTypingEffect.tsx` (89 lines), the section's use at `pages/index.tsx:398-404,440-457`. Current: `app/_components/landing/TypingDesignSection.tsx` (`DesignCarouselImage`, the two-element destructure, the reduced-motion branch), `app/_components/landing/useDesignTypingEffect.ts`.
- [[97fd4a76d9f1]], [[6a093c343640]]: `DesignCarousel` with `fadeBoxRef`, `zoomBoxRef`, the `useEffect` on `design.image` running `fadeBox.animate({ opacity: [1, 0] }, { duration: 100, easing: 'ease-in', fill: 'forwards' })` and setting the image when it finishes, `handleImageLoaded` running the 1500 ms `ease-out` fade-in and the 2000 ms `cubic-bezier(0.33, 1, 0.68, 1)` scale from 0.8, `fill: 'forwards'` on each; the outer `Box` `display: flex, justifyContent: center, height: 100%`, the inner `Box` `aspectRatio: '4 / 3'`; `<Image type="local" priority src={image} alt={...} sizes={sizes} css={shouldMirror ? { transform: 'scaleX(-1)' } : {}} onLoad={handleImageLoaded} />` from `@villagekit/ui`. The site's `DesignIndexEntry.image` is a `StaticImageData | null` where legacy's `DesignIndex.image` was a `LocalImageProps` object with `alt` (`packages/designs/src/index.ts:10-15` and `getDesignImage`, which the worker reads for the alt legacy gave a design); the props type takes a non-null image, the page's `image !== null` filter staying with [[272613135119]] (left for the designs record, the record's Log).
- [[2c82682942d8]]: the hook with `shuffle` from `lodash-es` (the story card slice adds the dependency; if this slice lands first, it adds it), the effect's deps `[designs, pause]` under legacy's `biome-ignore` line, the port citation at the top; the current file's null guards inside `emulateKeyStroke` stay only where `strict` needs them, named in the Outcome.
- [[fbd34de5c07d]]: the section renders `nextDesign`'s image hidden, `<Image priority type="local" src={nextDesign.image} alt sizes css={{ display: 'none' }} />` (`index.tsx:449-457`). [[c7d1f28fcea1]]: `useReducedMotion`, `prefersReducedMotion` and `displayLabel` gone from the section; with the carousel slice's half done the item closes here.
- The `sizes` on both images: `{ base: '100%', md: ['1500px', 2] }`, the record's Log rule; add the two call sites to the note on the `sizes` item the carousel slice filed and to its bump note. The static string goes with them (the third call site and [[1501ee97ed24]]'s closure are the page re-port's). `unoptimized` stays on both local images: at `1.2.0` the ui `Image` passes its own loader for `type="cloudinary"` only (`dist/components/media/Image.js:65-72`), so a `local` image falls through to `next.config.ts`'s `images.loaderFile`, which rewrites the `/_next/static/media/` path into a Cloudinary URL that answers 404 (`app/_lib/design-images.ts:1-7`); legacy's images went through Next's default optimizer. That is a consequence of the sanctioned global loader (`6c566c2715e0`), so this slice files it as one code item on `/` (`changed`, `open`, for rule 4), notes on `1501ee97ed24` that its `unoptimized` half moved there, and writes a bump note on `99f2fe62c62f` that a local ui `Image` keeps `unoptimized` under `imageComponent={NextImage}` too.
- Until the page re-port inlines the section, `TypingDesignSection.tsx` stays a module and consumes the two new modules; its heading, paragraph and button are untouched here (the heading's exposure and size are the page re-port's [[878e1bbba4a9]] and [[ce0ba1ecc47a]]).
- Interfaces: consumes the mounted `MediaProvider`; produces `DesignCarousel` (`design`, `sizes`, `shouldMirror`) and `useDesignTypingEffect` (`designs`, `pause`, `loop`, `playbackRate`) for the page re-port.
- Verify first: `grep -n 'nextDesign\|useReducedMotion' app/_components/landing/TypingDesignSection.tsx` shows the dropped element and the branch; `motion/react`'s `useInView` stays (sanctioned `83b6c8ceb5d0`).
- Not this slice: the section's markup and its inlining into the page (the page re-port); the label sort and the nullable image of `app/_lib/designs.ts`.

## Seams under test

None pure: the hook is timers and state, which legacy left untested, and the animation is the eye's.

## Done when

- On `pnpm dev` at 1280 the design swap fades out, then fades in and zooms from 0.8 after the new image loads, timed as the live legacy site's (`https://gridkit-landing-villagekit.vercel.app/`), and the network panel shows the next design's image requested before its swap
- On `pnpm dev`, the design image and the hidden preload request `/_next/static/media/` URLs that answer 200 (the network panel or `curl -sI` on one)
- `grep -rn 'useReducedMotion\|prefersReducedMotion' app` prints nothing; `grep -rn 'motion.div' app/_components/DesignCarousel.tsx app/_components/landing` prints nothing
- `ls app/_components/DesignCarousel.tsx app/_lib/useDesignTypingEffect.ts` succeeds and `ls app/_components/landing/useDesignTypingEffect.ts` fails
- The five items are `fixed`; the `sizes` item's note and the bump note name the two call sites; the `unoptimized` item is filed `open` with its notes; checked after the fixes
- `timeout 900 just check` is green

## Outcome

Shipped: `app/_components/DesignCarousel.tsx`, legacy's `components/carousel.tsx` at `fce357d` line for line on the Web Animations API (`fadeBoxRef`, `zoomBoxRef`, the effect on `design.image` running the 100 ms `ease-in` fade-out and setting the image when it finishes, `handleImageLoaded` running the 1500 ms `ease-out` fade-in and the 2000 ms `cubic-bezier(0.33, 1, 0.68, 1)` scale from 0.8, `fill: 'forwards'` on each, the outer flex `Box` and the `4 / 3` inner `Box`), translated: the ui-media imports to `@villagekit/ui@1.2.0` (`Image`, `LocalImageProps`), Chakra v2 `sx` to v3 `css`, and the legacy image object (a `LocalImageProps` holding `alt`, `src` and `type`, built by `packages/designs/src/index.ts:71-78`'s `getDesignImage` with the design's label as alt) to a local `getDesignImage(design)` returning the `{ alt, src }` pair, held in state and spread onto the `Image` with `type="local"` as legacy spread its object, since the site's `DesignIndexEntry.image` is a bare `StaticImageData`; the effect keys on `[design]` where legacy keyed on `[design.image]`, the design being what carries both fields here; `unoptimized` on the image, item `a704be5b8765` below. `app/_lib/useDesignTypingEffect.ts`, legacy's `hooks/useDesignTypingEffect.tsx` line for line with `shuffle` from `lodash-es`, the timer effect on `[designs, pause]` under legacy's bare `biome-ignore` line (Biome 1.9.4 accepts it), the three-tuple, `nonBreakingSpace` and the two comments; `app/_components/landing/useDesignTypingEffect.ts` deleted. The guards `strict` with `noUncheckedIndexedAccess` needs and legacy did not write: `const nextDesign = designs[nextDesignIndex]; if (nextDesign === undefined) return` inside `emulateKeyStroke` in place of the bare `designs[nextDesignIndex].label` reads, and `?? null` on `designs[designIndex]` and `designs[(designIndex + 1) % designs.length]`. `app/_components/landing/TypingDesignSection.tsx` reads all three tuple elements, passes `pause: !isInView`, renders `DesignCarousel` with legacy's `sizes={{ base: '100%', md: ['1500px', 2] }}` and `shouldMirror` behind a `hasImage` type guard (the nullable image, `272613135119`), and the hidden next-design `Image` (`type="local" priority unoptimized`, `alt={nextDesign.label}`, the same `sizes`, `css={{ display: 'none' }}`) as the last child of `LandingSection`, where legacy put it (`pages/index.tsx:449-457`); `useReducedMotion`, `prefersReducedMotion`, `displayLabel`, `motion` and the private `DesignCarouselImage` are gone; the heading, paragraph, button and live region are untouched. `lodash-es` added at legacy's range `^4.17.21` (MIT; the lockfile pins 4.18.1, already there as a transitive dependency) and `@types/lodash-es` `^4.17.6` as a dev dependency (MIT, 4.17.12), since `lodash-es` ships no types; `pnpm install --frozen-lockfile --offline` passes.

Verified on `pnpm dev` against the published 1.2.0 (no override, no `transpilePackages`): a Playwright probe at 1280 sampled the section every 250 ms over ten seconds and read `getAnimations()` on the two boxes. On each swap the fade box runs `opacity 1 to 0`, 100 ms, `ease-in`, `forwards`; when the new image loads it runs `opacity 0 to 1`, 1500 ms, `ease-out`, and the zoom box `scale 0.8 to 1`, 2000 ms, `cubic-bezier(0.33, 1, 0.68, 1)`, both `forwards`; the same probe on the live legacy site reads the same four animations with the same timings. The image's `alt` and `src` switch together after the fade-out (the second probe, after the review), the design image's computed transform is `matrix(-1, 0, 0, 1, 0, 0)`, the zoom box's aspect ratio `4 / 3`, the fade box `display: flex; justify-content: center`. The hidden image is in the DOM at `display: none` with the next design's src, a `<link rel="preload" as="image">` is written for it, and the network log shows each design's PNG requested one cycle before its swap (`garage-workbench` at 0.96 s beside the first design, its swap at 20.4 s; `chair` at 20.4 s, its swap at 24.5 s); every request is `/_next/static/media/<design>.<hash>.png` and answers 200. Under `reducedMotion: 'reduce'` the probe logs no hydration error: the `<span>` mismatch the carousel slice's Spec reviewer saw came from `displayLabel`, whose reduced-motion branch is gone, so it is moot. The only console error is the pre-existing kebab-case selector of `StoryCard.tsx:57`, the story card slice's. `pnpm audit:dom` on `/`: `current.aria.yaml` shows `img "<label>"` where `legacy.aria.yaml` does, the hidden image on neither side; the heading's exposure (`text:` here, `heading` there) is the page re-port's `878e1bbba4a9`. `pnpm audit:pages` at 375, 768 and 1280 under `audit/_root/`, looked at: the design renders mirrored in the section at every width on both sides, the picture different per capture by design; the heading size, band tint and column widths are the page re-port's items. The Done-when greps: `useReducedMotion` and `prefersReducedMotion` nowhere under `app`, no `motion.div` in the two files, the two new modules present and the old hook gone.

The ledger: `97fd4a76d9f1`, `6a093c343640`, `2c82682942d8`, `fbd34de5c07d` and `c7d1f28fcea1` fixed. `a704be5b8765` filed on `/` (code, `changed`, `open`): `unoptimized` on both local design images, because at 1.2.0 a `local` ui `Image` falls through to the site's Cloudinary `loaderFile` (a consequence of `6c566c2715e0`), where legacy's went through Next's optimizer with a sixteen-entry `srcset` and a `sizes` attribute; the item's note names the rule-4 question and that the two `sizes` objects render no attribute while `unoptimized` stays. `0903eceefbaf` filed on `/` (visual, `changed`, `regression`), found by the Parity review in code this slice did not touch: before the shuffle the server HTML reads `Build ` here and `Build a ` on legacy, since `getArticle('')` returns `''` where legacy's null `currentDesignIsPlural` falls through to `'a '` (a curl of both sides); the page re-port closes it with legacy's heading logic, and a note on `159c621d8a1a` says so. Notes on `1501ee97ed24` (its `unoptimized` half moved to the new item, the static string left for the page re-port), on `152511f71ef4` (the two new call sites, `TypingDesignSection.tsx:72,87`), on the bump plan `99f2fe62c62f` (the swap back to `'full'` at those lines, and that `unoptimized` stays under `imageComponent={NextImage}` too since the sibling's `Image` at `46eab3c` also passes a loader for `cloudinary` only), and on `f9729ac7552a` (the two new `priority` images without `fetchpriority`; the preload links accumulating one per design shown under React 19; the second fetch per swap on `pnpm dev` from its `no-store` header). No verdict written.

Deviations from the plan's letter: none in what the visitor sees. Two shape calls forced by the site's data, both gone when the designs record makes the image non-null (`272613135119`): the `getDesignImage` pair-builder in `DesignCarousel.tsx`, and the `hasImage` type guard in the section, since the props type takes a non-null image and TypeScript does not narrow an object by a property check. The hook's placement under `app/_lib/` is the plan's (CLAUDE.md, Structure). For the page re-port: the hidden image and the `DesignCarousel` call site move into `app/page.tsx` as they are, the guard with them; `useDesignTypingEffect` returns `DesignIndexEntry | null` elements.

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity), none critical. Standards: a literal U+00A0 byte where legacy wrote the `'\u00A0'` escape, and `DesignCarouselProps` exported where legacy's was not, both fixed; TSDoc absent on the two exports, dropped, as on legacy's files and the other re-ports (the audit slice `b209e5941cdd` owns TSDoc on every export); the narrowing spread named as a smell, replaced by the guard. Spec: every Work bullet and Done-when line met and every upstream claim verified (`Image.js:65-74`, `get-img-props.js:98-102`, `useImageSizes`); the `f9729ac7552a` note and the `46eab3c` pin on the `../ui` citation are its. Parity: the section at parity at 375, 768 and 1280 apart from the recorded items; its two findings are the alt switching 100 ms before the picture (fixed, the pair in state) and the article before the shuffle (`0903eceefbaf`); the current 768 capture's sideways scroll is the shell's `c9ea22823a23`. Dropped: nothing else.

## Log
