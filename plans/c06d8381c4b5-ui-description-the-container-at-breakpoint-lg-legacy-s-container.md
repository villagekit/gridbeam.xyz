---
title: "ui Description: the container at breakpoint-lg, legacy's container.lg width"
status: done
parent: 337e35d86920
derived_from: a78b167170b8
tags:
  - "worker:fable"
priority: medium
---
The ui `Description` bounds its sentence at legacy's width again: its `Container` is `maxW="breakpoint-lg"` (1024px, Chakra v3's size token for the `lg` breakpoint), where `@villagekit/ui@1.2.0` and the sibling write `3xl` (768px) and legacy's `ui-page` `Description` wrote `container.lg`. Closes [[c325dd0181d9]] on `shell` for the shell record `a78b167170b8`, filed by the Parity review of the Title slice `728a36aedc8c` (decision `40abdb2f222a`, a slice beside the record). A fix in `../ui`, so it follows decision `28c1a536`: committed in the sibling by pathspec, never pushed from here; seen on this site through the uncommitted `pnpm.overrides["@villagekit/ui"] = "file:../ui"` (`pnpm install` after each sibling edit) beside the uncommitted `transpilePackages: ['@villagekit/ui']` in `next.config.ts` that the `src/` copy needs (CLAUDE.md's ui row), both reverted by path before the commit; the item moved to `upstream` with a note citing the sibling commit; the bump plan `99f2fe62c62f` is `blocked_by` this slice (the edge written at the mint).

## Work

In `../ui/src/components/layouts/Description.tsx:18` `Description` writes `<Container maxW="3xl">` around its `Text` (the published `@villagekit/ui@1.2.0 dist/components/layouts/Description.js` the same), where legacy's `packages/ui-page/src/components/Description.tsx:23` at `fce357d` wrote `<Container maxW="container.lg">`, Chakra v2's 1024px token. Chakra v3 has no `container.*` size: `expandBreakpoints` registers `sizes.breakpoint-lg` from the `lg` breakpoint (`node_modules/@chakra-ui/react/dist/esm/styled-system/token-dictionary.js:21-29`), 1024px, and `sizes.3xl` is 48rem, 768px (`node_modules/@chakra-ui/react/dist/esm/theme/tokens/sizes.js:14`). Write `maxW="breakpoint-lg"`, the name `../ui/src/hooks/useSizeWidths.ts:50` maps `container.lg` to and the form the Title slice's ui commit `5c5cd1e` wrote on `Title`'s container; nothing else in `Description` changes (the `fontSize` breakpoint value, the centered `Text`, `textAs`). The TSDoc on `Title` (`../ui/src/components/layouts/Title.tsx`, written by `5c5cd1e`) names the description's bound as `3xl`; it says `lg` breakpoint's width after this. Every route that passes `description` to `Title`, or renders `Description` itself, takes it: on this site `/faq`, `/contact`, `/legal`, `/legal/privacy-policy`, `/tools-and-resources`, `/tools/cutting-planner`, `/subscribe` and `/stories/[slug]` (`grep -rln 'description=' app` with `Title`), several of them added descriptions their route records remove; on legacy `/stories`, the story pages and `/tools/cutting-planner`, and the two newsletters' closing `Description`. A description narrower than 768px does not move; a wider one (the story page's, at 1280) is one line again, as on legacy. The reading is the container's computed `max-width`, on every route.
Verify first: on `pnpm dev` with the published 1.2.0, a probe at 1280 on `/stories/how-to-furniture-bolts` reads the description paragraph's parent `max-width` as `768px` and the paragraph two lines tall; the live legacy site's reads `1024px` and one line (Chakra v2 emits `var(--chakra-sizes-container-lg)`).
Docs: `../ui/CHANGELOG.md`, an entry under Fixed.
Not this slice: the heading recipe's sizes ([[0369f14e4df4]], [[77cd1426ac65]], `upstream`); the story page's own `3xl` container ([[69e3e2447813]], the story pages' record), which caps the description at 736px there until that record re-ports the page; the routes' own `Title` descriptions (added copy, each route record's).

## Seams under test

None pure; the proof is a Playwright probe of the computed style on `pnpm dev` under the override, against the live legacy site, and the screenshot pairs.

## Done when

- A Playwright probe at 1280 on `pnpm dev` under the override reads the description's `Container` `max-width` as `1024px` on `/tools/cutting-planner`, `/stories/how-to-furniture-bolts` and `/faq`, the live legacy site's reading on the routes it renders a description on; at 375 and 768 the same element reads `1024px` on both sides too (the declared maximum does not change with the viewport), saved beside the probe
- `pnpm audit:pages --routes <a file naming the routes that render a description>` at 375, 768 and 1280 under the override, looked at: no description narrower than 768px moves, a wider one is one line as legacy's is, and every remaining difference on the pairs is ledgered (the `upstream` items' residuals, the shell's `c9ea22823a23` at 768, the routes' own open and regression items)
- In `../ui`: `pnpm lint`, `pnpm types`, `pnpm build:pkg` and `pnpm build:storybook` green; the change committed by pathspec on its `main`, not pushed
- [[c325dd0181d9]] is `upstream` with a note citing the sibling commit; `99f2fe62c62f` is `blocked_by` this slice (checked at the finish) and carries a note that the bump makes no site edit for it and that the bump repeats the probe on the published package
- The override and the `transpilePackages` entry reverted by path (`git restore -- package.json pnpm-lock.yaml next.config.ts`, `pnpm install --frozen-lockfile`); `timeout 900 just check` is green

## Outcome

Shipped in `../ui` as commit 8f85a7c on its `main` over `f8b772f` (sixteen ahead of origin, not pushed; the push goes with the operator's publish, decision `28c1a536`), by pathspec: `src/components/layouts/Description.tsx`, `src/components/layouts/Title.tsx` and `CHANGELOG.md` (one Fixed entry under Unreleased beside the Title slice's entry, whose closing clause "`Description`'s own `3xl` container is unchanged" is dropped, since both entries describe the same unreleased version). The package version stays 1.2.0. This repo's commit carries kipu writes only: no site code changes.

What shipped: `Description`'s `Container` is `maxW="breakpoint-lg"`, the size token Chakra v3 generates from the `lg` breakpoint (`@chakra-ui/react@3.35.0` `styled-system/token-dictionary.js:21-29`, `expandBreakpoints`; `theme/breakpoints.js:5`, 1024px), the width legacy's `ui-page` `Description` wrote as Chakra v2's `container.lg` (`packages/ui-page/src/components/Description.tsx:23` at `fce357d`) and the name `../ui/src/hooks/useSizeWidths.ts:50` already maps `container.lg` to; 1.2.0 and the sibling wrote `3xl` (`theme/tokens/sizes.js:14`, 48rem, 768px). Nothing else in `Description` changes: the `fontSize` breakpoint value (`md`, `lg` from `md`), the centered `Text` and `textAs`. The constraint went into a one-paragraph TSDoc on `Description` with a line on each prop, the shape the Title slice's review gave `Title`; `Title`'s TSDoc now says the description is bounded as `Description` bounds it, in place of naming the width twice (the review's call, one fact, one home).

Verified. Before, on `pnpm dev` with the published 1.2.0 against the live legacy site (the scratchpad's `desc-probe.mjs`, `desc-before.json` and `desc-legacy.json`): on `/tools/cutting-planner`, `/stories/how-to-furniture-bolts`, `/faq` and `/stories` at 1280, 768 and 375 the description's container read `max-width: 768px` against legacy's `1024px` on the three routes legacy renders a description on (legacy's `/faq` and `/tools-and-resources` have none: the probe reads `description: null` there, so those routes' descriptions are the route records' added copy), the container padded 32px at 1280 and 24px at 768 (the container recipe, `5c1af396cc2e`) against legacy's 16px; at 1280 the story page's description ran to two lines, 640 by 54px, against legacy's one line, 992 by 27px. After, under the `file:../ui` override (`desc-after.json`): every route at every width reads `1024px`, legacy's reading, on the four routes and on `/contact` and `/legal/privacy-policy`; the container is padded 16px at every width, the recipes slice's fix (`2bd0169a6dda`, `upstream`) rendering with this one, so the bump's repeat probe needs both in the publish. On `/tools/cutting-planner` at 1280 the container is 1024 wide and the paragraph 992 by 27, legacy's sizes (the words differ, the route's copy item `6cb10026c904`); at 768 the paragraph is 704 by 27, one line as legacy's 736 by 27, where before it was 672 by 54, two lines: that one moved, toward legacy, and the cause is the padding fix rendering with this one, the declared maximum not binding at 768 on either side; at 375 it is 311 by 48 against legacy's 343 by 48, two lines on both sides (the 32px is the route's own outer `Section`, `69bc376ee08f`). `/stories` at 1280 is one line on both sides, the paragraph 960 against legacy's 992 under the index's own container (`1d5c23180ebf`), and two lines on both sides at 375. The story page at 1280 is still two lines, 704 by 54: its own `3xl` container (`69e3e2447813`, the story pages' record) caps the description at 736px there, as the plan said, until that record re-ports the page; at 768 it is two lines and at 375 three lines on both sides (54 and 72px). The screenshot pairs for the nine routes that render a description on either side at 375, 768 and 1280 under `audit/` (54 of 54 captures), captured under the override and looked at (the page heads cropped in `desc-crops/`): no description below 768px wide moves except the planner's at 768, above; the cutting planner's sits where legacy's does at 1280; `/legal`, `/tools-and-resources` and `/subscribe` are 500 under the override (the `LinkCard` icon boundary in the bump plan's note), so their descriptions are unseen until the bump; every remaining difference on the pairs is ledgered (the `upstream` items, `c9ea22823a23` and the wrapping wordmark `1d2562d6b3bc` at 768, the routes' own items, the added descriptions and the story TOC among them; the two newsletters' closing `Description`, which the current MDX does not render, is `8661edb0bbc8` and `ffe337ac3773`). In `../ui`: `pnpm lint`, `pnpm types`, `pnpm build:pkg` (the built `Description.js` reads `maxW: "breakpoint-lg"`) and `pnpm build:storybook` green, twice (again after the review's doc edits, which change no emitted CSS). The override and the `transpilePackages` entry reverted by `git restore -- package.json pnpm-lock.yaml next.config.ts` and `pnpm install --frozen-lockfile`, the dev server stopped by pid first; `timeout 900 just check` green against the published 1.2.0.

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity) before the sibling commit, none critical or major on the code. Standards, no hard violation; three judgment calls taken: `Title`'s TSDoc no longer repeats the description's bound (the clause that went stale in the first place), `Description`'s TSDoc says font size rather than type, and the CHANGELOG entry no longer narrows the fix to a description under a `Title`, since `Description` is exported on its own; a fourth taken beyond the ask, one doc line on each `DescriptionProps` prop. Spec, every upstream citation verified in `token-dictionary.js`, `breakpoints.js`, `sizes.js`, the legacy file and `useSizeWidths.ts`; three factual slips in the draft Outcome fixed above (`/stories` is two lines at 375 on both sides; the planner's description at 768 does move, toward legacy, from the padding fix; the controls item `6ec723376c9e` is not the Title's wrapper, so it is not cited, and the Title slice's Outcome carries the same mis-citation, left as it stands). Parity: the closure holds in the code and on screen on every probed route and width, the two components compared attribute by attribute (the `fallback`, the centered text, `textAs`), no copy in the diff, nothing legacy did silently dropped, no unrecorded difference beside the change. Findings dropped: none.

Items: [[c325dd0181d9]] moved to `upstream` with the ui commit in its note; `99f2fe62c62f` `blocked_by` this slice (the edge from the mint, checked) and carrying the note that the bump makes no site edit for it and what its repeat probe reads on the published package, the three routes that are 500 under the override included. No visitor-facing copy in the change. This was the last of the ui slices minted at the about record's finish and by the Title slice's review; the bump plan now waits on the publish alone. Beyond the ask: the Title entry's stale clause in the ui CHANGELOG dropped, the prop docs on `DescriptionProps`.

## Log
