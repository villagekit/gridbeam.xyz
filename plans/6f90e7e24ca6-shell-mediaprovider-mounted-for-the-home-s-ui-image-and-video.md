---
title: "Shell: MediaProvider mounted for the home's ui Image and Video"
status: done
parent: fd9a92bd8abd
derived_from: fd9a92bd8abd
blocked_by:
  - bc0407533650
  - 531b810f2dbd
tags:
  - "worker:fable"
priority: medium
---
The site mounts `@villagekit/ui`'s `MediaProvider` once, in `SiteProvider` inside `ChakraProvider`, so the home's re-ports can render the ui `Image` and `Video` (legacy's `@villagekit-private/ui-media` components, `../node-modules/packages/ui-media/src/` at `fce357d`) against the published `1.2.0`, whose media context throws without a cloud name (`node_modules/@villagekit/ui/dist/components/media/context.js`, `assertCloudinaryName`). A shell change, so the difference it makes is filed on `shell`; the site-side edit the bump needs is a note on the bump plan. A prefactor for the story card, carousel, design carousel and page slices, which consume it. Record `fd9a92bd8abd`; decisions `ee86d68a`, `2032533f`, `28c1a536`.

## Work

- `app/_components/SiteProvider.tsx`: `<MediaProvider cloudinaryName={CLOUDINARY_NAME}>` around the children, inside `ChakraProvider` (legacy's `_app.tsx` at `fce357d` nested no media provider, since `ui-media` hard-coded the cloud; the order inside the Chakra provider is this slice's call, stated in the Outcome), `CLOUDINARY_NAME` imported from `app/_lib/cloudinary.ts` so the cloud name keeps one home. `MediaProvider` is exported by `1.2.0` (`dist/index.d.ts`).
- The difference: one item on `shell` (code, `added`, `open`: an addition is the operator's to judge, `2032533f`) with Legacy (`packages/ui-media/src/image.tsx:211-222`, a constant, no context) and Current (the mount and its reason: the published media components need the context, and the home renders them); `kipu relate <new item> supersedes 0efe45924dbe`, and [[0efe45924dbe]] gets a note saying its verdict (not mounted, the loader file being the one call site) is overtaken now that the home renders the ui `Image` and `Video`, and a new state, `kipu move 0efe45924dbe open --from sanctioned` (`differences/README.md`: a wrong verdict is superseded by a note and a new state). Both items are the operator's: `kipu note 77cf83a1285a` names them for the shell's verdicts plan. No verdict written here.
- The bump note: `kipu note 99f2fe62c62f` saying that after the publish `MediaProvider` takes `imageComponent={NextImage}` (`next/image` imported in `SiteProvider.tsx`, a client module, so the component crosses no server boundary) or every ui `Image` renders a plain `<img>` on one URL (the mdx and media slice `bc0407533650`'s Outcome and the ui `CHANGELOG.md` Unreleased entry), and that `cloudinaryName` may stay. The note supersedes the mdx and media slice's clause (4) on the bump plan, which assumed the site renders no ui `Image`.
- Interfaces: produces the mounted context; the ui `Image` (`type: 'cloudinary' | 'local'`, `sizes` object) and `Video` (`src`, `posterSrc`, `title`, `sizes`, `css`) are then renderable from any component.
- Verify first: `grep -rn MediaProvider app` prints nothing; `node_modules/@villagekit/ui/dist/components/media/context.js` throws when `cloudinaryName` is null. Next's `images.loaderFile` (`6c566c2715e0`) applies to a `next/image` without a `loader` prop; the ui `Image` passes its own Cloudinary loader for `type="cloudinary"` only (`dist/components/media/Image.js:65-72`), so a `type="local"` image falls through to the site's loader and needs `unoptimized`, the design carousel slice's concern.
- Docs: none; CLAUDE.md's Structure names the shared components under `app/_components/` already.
- Not this slice: any consumer (the four slices blocked by this one); the size hooks' `full` name, which the sibling restores (`252edab16c7a`, `upstream`) and the consumers write around as the record's Log says.

## Seams under test

None pure; the proof is a rendered image and video URL.

## Done when

- `grep -c '<MediaProvider cloudinaryName={CLOUDINARY_NAME}>' app/_components/SiteProvider.tsx` prints 1
- A throwaway route under `app/` (not committed, removed by path) renders `<Image type="cloudinary" src="v1/gridkit.nz/douglas-fir-forest_etzvle" alt="probe" width={5120} height={3840} sizes={{ base: 'md' }} />` and `<Video src="gridkit.nz/gridkit-coffee-table-website_bqmjpv" posterSrc="gridkit.nz/gridkit-coffee-table-website_gyr39u" title="probe" sizes={{ base: '356px', md: '622px' }} />` from `@villagekit/ui` on `pnpm dev` at 200, every `res.cloudinary.com/villagekit/` URL in its HTML answering 200; the Outcome quotes the URLs
- `/`, `/stories` and a story page serve 200 with no new console error, and `pnpm audit:pages --routes <a file naming / and /stories>` shows no change against a capture of the same routes taken before the edit (no consumer yet)
- The shell item is filed `open` and supersedes `0efe45924dbe`, which is `open` with its note; the note on `77cf83a1285a` and the bump note are written; checked after the filing, before the commit
- `timeout 900 just check` is green

## Outcome

Shipped: `app/_components/SiteProvider.tsx` mounts `<MediaProvider cloudinaryName={CLOUDINARY_NAME}>` from the published `@villagekit/ui@1.2.0` inside `ChakraProvider` and around `QueryParamProvider`, the name imported from `app/_lib/cloudinary.ts`; the doc comment says why the provider exists where legacy's `_app.tsx` had none. The order is this slice's call: inside Chakra because the ui media components are Chakra components and every provider of the ui belongs under its system, outside the query-param provider because the media context has no dependency on it and legacy's `_app.tsx` put its own contexts (cart, cookies) inside Chakra and outside the query-param provider too. No consumer changed in behavior; the one edit beyond the plan's Work is the header comment of `app/_components/landing/LandingVideo.tsx`, which said the site does not wire the context and became false with this mount, reworded to the constraint it guards (all three reviews named it).

Verified on `pnpm dev` against the published 1.2.0 (no override, no `transpilePackages`): before the edit `grep -rn MediaProvider app` matched only the `LandingVideo` comment, and `node_modules/@villagekit/ui/dist/components/media/context.js` throws `Cloudinary cloud name is not configured` when the name is null. A throwaway client route `app/zz-media-probe/page.tsx` (deleted by path before the commit) rendering the plan's `<Image type="cloudinary" ...>` and `<Video ...>` from `@villagekit/ui` served 200; its HTML held 22 distinct `res.cloudinary.com/villagekit/` URLs, every one answering 200: the image's sixteen-entry `srcSet` from `.../c_limit,dpr_auto,f_auto,fl_alpha,fl_lossy,w_16,q_75/v1/gridkit.nz/douglas-fir-forest_etzvle` to `w_3840`, its `src` the `w_3840` entry with `sizes="448.00px"`; the video's poster `https://res.cloudinary.com/villagekit/image/upload/c_limit,dpr_auto,f_auto,fl_alpha,fl_lossy,w_356,q_60/v1/gridkit.nz/gridkit-coffee-table-website_gyr39u` and its three sources `https://res.cloudinary.com/villagekit/video/upload/c_limit,dpr_auto,f_auto,w_356,q_60/v1/gridkit.nz/gridkit-coffee-table-website_bqmjpv.{webm,mp4,ogv}`, the `<video>` carrying `autoPlay loop muted playsInline`; the other two are the layout's Open Graph image and video. `/`, `/stories` and `/stories/how-to-cut-grid-beams` served 200 before and after, and the dev log holds no new error or warning (the pre-existing `kebab-case` warning only). Screenshot pairs of `/` and `/stories` at 375, 768 and 1280 under `audit/_root/` and `audit/stories/`, captured before the edit (kept aside in the scratchpad) and twice after: the only bands that differ between the baseline and the after capture are the home's design typing section (its design is random on every load: `Build a Wall Shelf` against `Build a Bab`) and the animated footer cube, the same bands that differ between the two after captures of the same code; everything else is pixel-identical.

The ledger: `afe50ab5c7aa` filed on `shell` (code, `added`, `open`), Legacy citing `packages/ui-media/src/image.tsx:211-222`, `video.tsx:73-76` and `pages/_app.tsx:45`, Current the mount and its reason; `kipu relate afe50ab5c7aa supersedes 0efe45924dbe`; `0efe45924dbe` noted and moved `sanctioned` to `open`. Both are named for the operator in a note on the shell's verdicts plan `77cf83a1285a`. The bump note on `99f2fe62c62f` says the provider takes `imageComponent={NextImage}` after the publish, that `cloudinaryName` may stay, and that it supersedes the mdx and media slice's clause (4) there. No verdict written.

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity): nothing critical. Taken: the item's `_app.tsx` citation widened to `45-104` naming the two `QueryClientProvider`s, its `SiteProvider.tsx` and `Image.js` line ranges corrected, the sentence about the provider order moved out of the item into this Outcome; the ids in this slice's notes cited as `[[id]]`; the bump note's `imageComponent` attributed to ui commit 4e11d57 (the framework boundary slice) with the default cloud name to 1c3e3e8, and the note now names the three sentences on that plan it supersedes (the boundary note's last sentence, and clauses (3) and (4) of the mdx and media note), not clause (4) alone; the `LandingVideo` comment above. Parity found `/` and `/stories` at 375, 768 and 1280 at parity for this change's surface: the provider renders no element, `SiteProvider` was already a client component so the RSC payload and every route's static-ness are unchanged, and the one `MediaProvider` string in the dev HTML of `/` is the dev-only component stack of the footer cube's `BailoutToCSR`. Dropped: none. Nothing filed beyond the plan's item.

Gate: `timeout 900 just check` green after the dev server was stopped; `kipu verify --warnings-as-errors` green.

## Log
