---
title: "ui Accordion: the trigger's text centered and the indicator a 20px filled chevron in the text color, the 0.9.0 accordion's"
status: done
parent: 337e35d86920
derived_from: a78b167170b8
tags:
  - "worker:fable"
priority: medium
---
The ui accordion's trigger centers its text and its indicator is the 0.9.0 glyph again: a wrapped question reads centered as the browser's `<button>` default gave it under Chakra v2, and the chevron at the row's end is a 20px filled chevron in the trigger's own color, where `@villagekit/ui@1.2.0` and the sibling leave Chakra v3's `textAlign: start` on the trigger and its `fg.subtle` 2px stroke chevron at 1.2em on the indicator. Closes [[c7ffae735176]] and [[2cc5b00fb582]] on `shell` for the shell record `a78b167170b8`, both filed by the Parity review of the faq page re-port `bba2bb35f208` after the shell's verdicts plan was written and owned by no slice until the faq's finish (decision `40abdb2f222a`, a slice beside the record); one slice for both because each is a line in the one slot recipe, `../ui/src/components/Accordion.recipe.ts`. A fix in `../ui`, so it follows decision `28c1a536`: committed in the sibling by pathspec, never pushed from here; seen on this site through the uncommitted `pnpm.overrides["@villagekit/ui"] = "file:../ui"` (`pnpm install` after each sibling edit) beside the uncommitted `transpilePackages: ['@villagekit/ui']` in `next.config.ts` that the `src/` copy needs (CLAUDE.md's ui row), both reverted by path before the commit; each item moved to `upstream` with a note citing the sibling commit; the bump plan `99f2fe62c62f` is `blocked_by` this slice (the edge written at the mint).

## Work

`../ui/src/components/Accordion.recipe.ts` at ui commit `8f85a7c` (`accordionRecipe`, written by the recipes slice `45d6f5634a11` at `540e9c3`): its `itemTrigger` base writes `display`, `justifyContent`, `paddingX`, `fontWeight: inherit`, the transition, `_hover` and `_focusVisible`, and no `textAlign`; it declares the `itemIndicator` slot (`:13`) and writes no styles for it. `createSystem(defaultConfig, config)` (`../ui/src/theme/index.ts:104`; the site's `app/theme.ts:12` the same) deep-merges the package's recipe over Chakra v3's accordion recipe and cannot delete a key (the badge slice `a4f938a27428` read the merge), so every v3 declaration the package does not override stands: `itemTrigger.textAlign: "start"` (`node_modules/@chakra-ui/react/dist/esm/theme/recipes/accordion.js:19`) and `itemIndicator.color: "fg.subtle"` with the icon at `1.2em` under `_icon` (`:49-57`; `_icon` is `& :where(svg)`, `preset-base.js:172`). Legacy, `@villagekit/ui@0.9.0 src/components/Accordion.tsx:12-17` (the sibling's first commit `a5cbe36` carries it): the button's base style writes `display`, `justifyContent` and the paddings and no `textAlign`, so the button keeps the browser's `center`, which its `<p>` label inherits; `AccordionIcon` is Chakra v2's, re-exported untouched, a filled chevron (`<path fill="currentColor" d="M16.59 8.59L12 13.17 7.41 8.59 6 10l6 6 6-6z">`) at `fontSize: 1.25em` in the trigger's color, rotated 180deg when open. The live legacy `/faq` readings are on the two items: the trigger and its label `text-align: center`, the icon 20px by 20px in `rgb(26, 32, 44)`.

- [[c7ffae735176]]: `itemTrigger.textAlign: 'center'` in the recipe's base, the button default written out because the merge keeps v3's `start` otherwise. A one-line question is unmoved (`justifyContent: space-between` places it and the indicator as before); a question that wraps, the two at 375 on `/faq`, centers its lines as legacy's did.
- [[2cc5b00fb582]], the color and the size: an `itemIndicator` slot in the recipe's base with `color: 'inherit'` (the trigger's own color, v2's `currentColor` through the label's text color) and `_icon: { width: '1.25em', height: '1.25em' }` (20px at the trigger's 16px, v2's `fontSize: 1.25em` on an icon sized `1em`); v3's `transition`, `transformOrigin` and `_open` rotate stay, the rotation legacy's icon had.
- [[2cc5b00fb582]], the glyph: the recipe cannot change the path. Chakra's `AccordionItemIndicator` renders `ChevronDownIcon` as its default child (`node_modules/@chakra-ui/react/dist/esm/components/accordion/accordion.js:29-34`), a stroked path at `stroke-width` 2 with `fill: none` (`components/icons.js:47-60`), where legacy's was the filled path above. The ui's `Accordion` (`../ui/src/components/Accordion.tsx`) is a bare re-export of Chakra's namespace today. The shape this slice decides: the ui wraps `ItemIndicator` so that, with no children, it renders legacy's filled chevron (an svg on the 24-unit viewBox with the path above, `fill="currentColor"`), the rest of the namespace re-exported as it is, so a consumer keeps the slot API (`2ed11f164573`, sanctioned; `app/faq/page.tsx` writes `<Accordion.ItemIndicator />` with no children) and a child passed in still wins, the way Chakra's wrapper sets `defaultProps.children`. The one consumer on either site is `/faq` (`grep -rln Accordion app` prints `app/faq/page.tsx`; legacy `apps/gridkit/pages/faq.tsx` at `fce357d`), so the readings are taken there; the package's own story, `../ui/stories/Accordion.stories.tsx:21,31`, writes `<Accordion.ItemIndicator />` too and takes the wrapper, which `pnpm build:storybook` builds. If the wrapper proves to cost more than it buys (a second file, a type to re-export), the Outcome says so, [[2cc5b00fb582]] stays `regression` with a note saying the color and the size are in the sibling and the glyph is not, and the Done when lines below on the filled path and on `upstream` apply to [[c7ffae735176]] alone; the default is the wrapper.
- Verify first: on `pnpm dev` with the published 1.2.0, a probe at 375 on `/faq` reads a trigger's computed `text-align` as `start` and the `.chakra-accordion__itemIndicator` svg as 19.19px in `rgb(161, 161, 170)` with `fill: none` and `stroke` the same gray; the live legacy site's reads `center`, 20px, `rgb(26, 32, 44)` and a filled path (the re-port's probes, `audit/faq/{375,1280}/probe.json`, `wrappedLabel` and `indicator`, until a new capture overwrites them).
- Docs: `../ui/CHANGELOG.md`, one entry under Fixed beside the recipes slice's accordion entry, which says the accordion is the 0.9.0 accordion again and names the rule, the hover fill, the corners and the paddings; this entry adds the centered trigger text and the indicator, or extends that entry, both describing the same unreleased version (the badge slice's call).
- Not this slice: the trigger's padding and weight and the body's padding ([[e1ad4ba984a5]], `upstream` at `540e9c3`); the separator and the hover fill ([[cb9dce13acba]], [[cb528cbc05e6]], `upstream`); the route's own tree, closed by the re-port; the toast regions ([[ee7575aea3ae]]) and the radius scale ([[f40107b60034]]), the operator's on `77cf83a1285a`.

## Seams under test

None pure; the proof is a Playwright probe of the computed styles on `pnpm dev` under the override, against the live legacy site, and the screenshot pairs.

## Done when

- A Playwright probe on `pnpm dev` under the override reads, on `/faq` at 375 and 1280, every trigger's computed `text-align` as `center` and, at 375, the `<p>` of a wrapped question (`Do I need any special tools to assemble it?`) as `center` with its lines centered in the row (the label's `getClientRects` left edges differ, the first line's wider than the second's); every `.chakra-accordion__itemIndicator` svg 20px by 20px with `color` `rgb(26, 32, 44)`, its path's computed `fill` `rgb(26, 32, 44)` and `stroke` `none`; an open item's indicator rotated 180deg; the live legacy site's readings on the same route (`text-align: center`, 20px, `rgb(26, 32, 44)`, the filled path), saved beside the probe
- The trigger's other readings are unchanged from the sibling's: `display: flex`, `justify-content: space-between`, `padding` 16px above and below and 8px at the sides (the padding fix of `540e9c3` rendering with this one), the `blackAlpha.50` hover fill and the `outline` shadow on keyboard focus; no one-line question moves
- `pnpm audit:pages --routes <a file naming /faq>` at 375, 768 and 1280 under the override, looked at: the two wrapped questions at 375 read centered as in `legacy.png`, the chevrons are dark and filled at every width, and every remaining difference on the pairs is ledgered (the `upstream` items' residuals, the shell's `c9ea22823a23` and the wordmark `1d2562d6b3bc` at 768)
- In `../ui`: `pnpm lint`, `pnpm types`, `pnpm build:pkg` (the built recipe reads `textAlign: "center"` on the trigger and the `itemIndicator` slot) and `pnpm build:storybook` green; the change committed by pathspec on its `main`, not pushed
- [[c7ffae735176]] and [[2cc5b00fb582]] are `upstream`, each with a note citing the sibling commit; `99f2fe62c62f` is `blocked_by` this slice (checked at the finish) and carries the note written at the mint that the bump makes no site edit for it and repeats the probe on the published package, extended if the wrapper is dropped
- The override and the `transpilePackages` entry reverted by path (`git restore -- package.json pnpm-lock.yaml next.config.ts`, `pnpm install --frozen-lockfile`); `timeout 900 just check` is green

## Outcome

Shipped in `../ui` as commit 8a3b1a2 on its `main` over `8f85a7c` (seventeen ahead of origin, not pushed; the push goes with the operator's publish, decision `28c1a536`), by pathspec: `src/components/Accordion.recipe.ts`, `src/components/Accordion.tsx` and `CHANGELOG.md` (the recipes slice's accordion entry under Unreleased extended with the centered text and the indicator, both describing the same unreleased version, the badge slice's call). The package version stays 1.2.0. This repo's commit carries kipu writes only: no site code changes.

What shipped, the recipe: `accordionRecipe`'s `itemTrigger` base writes `textAlign: 'center'`, the browser's button default that Chakra v2 left standing (`@chakra-ui/theme@3.3.1` `dist/components/accordion.js`'s `baseStyleButton` writes no `textAlign`, nor does 0.9.0's `accordionTheme.button`, `../ui` at `a5cbe36`), as an override the theme merge keeps over Chakra v3's `textAlign: "start"` (`node_modules/@chakra-ui/react/dist/esm/theme/recipes/accordion.js:19`; `createSystem(defaultConfig, config)` at `../ui/src/theme/index.ts:104` deep-merges and cannot delete a key); and an `itemIndicator` slot with `color: 'inherit'` and `_icon: { width: '1.25em', height: '1.25em' }` (`_icon` is `& :where(svg)`, `preset-base.js:172`), over v3's `color: "fg.subtle"` and `1.2em` (`accordion.js:49-57`), where v2's `AccordionIcon` was an `Icon` at `fontSize: 1.25em` (the v2 theme's `baseStyleIcon`, `dist/components/accordion.js:58-60`) in `currentColor` (`@chakra-ui/icon` `Icon`'s `color = "currentColor"` default over `w: 1em, h: 1em`). v3's `transition: rotate 0.2s`, `transformOrigin: center` and `_open: { rotate: 180deg }` stay: legacy's icon turned by `transform: rotate(-180deg)` over `transform 0.2s` (`@chakra-ui/accordion@2.3.1 dist/chunk-YI3ICT2T.mjs:19-24`), the same half turn.

The glyph, the wrapper (the plan's default, taken): `Accordion.tsx` builds the `Accordion` namespace as an object of Chakra v3's ten members (`namespace.js`) with `ItemIndicator` swapped for a `forwardRef` wrapper that renders `BaseAccordion.ItemIndicator` with legacy's filled chevron as the default child: `<svg viewBox="0 0 24 24" focusable="false" aria-hidden="true"><path fill="currentColor" d="M16.59 8.59L12 13.17 7.41 8.59 6 10l6 6 6-6z"/></svg>`, the path and the two attributes v2's `AccordionIcon` rendered through `Icon` (`chunk-YI3ICT2T.mjs:26-42`, `focusable = false` in `@chakra-ui/icon`'s `Icon`), read in the packed v2 copies under `~/.nvm/versions/node/v22.3.0/lib/node_modules/@villagekit/screenshot/node_modules/@chakra-ui/`, since the legacy checkout installs neither; the path's source is cited in the chevron's doc comment as the SHA-pinned URL of Chakra's `accordion-icon.tsx` at the `@chakra-ui/accordion@2.3.1` tag (`d911122`). A child passed in wins by the destructuring default; Chakra's own `defaultProps.children`, the stroked `ChevronDownIcon` (`components/accordion/accordion.js:29-34`, merged under the props by the `chakra` factory, `styled-system/factory.js:114`), never renders because the wrapper always passes a child. Two constraints the code imposed, each a doc comment: the module carries no `'use client'`, because the site's server page reads `Accordion.Root` by property and Next refuses that on a client module's export ("You cannot dot into a client module from a server component", `node_modules/next/dist/compiled/react-server-dom-turbopack/cjs/react-server-dom-turbopack-server.node.development.js:5396-5399`), and a `forwardRef` in a shared module renders under Flight as a function component (`:1796-1803`), which is how `/faq`, one server file, renders it; and the `Accordion` const carries an explicit type (`Omit<typeof BaseAccordion, 'ItemIndicator'> & { ItemIndicator: typeof AccordionItemIndicator }`) because `tsc` refused the inferred one (TS2742, naming Ark's pnpm path), which also makes the compiler check that every member of Chakra's namespace is listed. The svg is a plain element, not `chakra.svg`, a property read on Chakra's client `chakra` factory that meets the same refusal from a server component; v3's `Icon` would have worked too (its recipe, `theme/recipes/icon.js`, writes display, line height, `flexShrink: 0`, color and vertical alignment and no size), the same one element plus a client import, so not simpler; the one computed residual of the plain svg is `flex-shrink: 1` where legacy's `Icon` read 0, with no effect since the flex item is now v3's indicator div. A `Note(cc)` beside the chevron records the two v2 behaviors neither 1.2.0 nor this change carries, the 0.4 opacity on a disabled item and the transition dropped under `reduceMotion`, which no consumer uses. Cost against the plan's fallback: no second file, the type annotation one line; the wrapper stands and [[2cc5b00fb582]] goes `upstream` whole. The one story, `stories/Accordion.stories.tsx`, takes it unchanged; `app/faq/page.tsx` is unchanged.

Verified. Before, on `pnpm dev` with the published 1.2.0 against the live legacy site (the scratchpad's `accordion-probe.mjs`, `accordion-before.json`, `accordion-legacy.json`): on `/faq` at 1280 and 375 every trigger read `text-align: start` against legacy's `center`; at 375 the wrapped question (`Do I need any special tools to assemble it?`) ran two lines both starting at 24px against legacy's at 32.5 and 131.5; every indicator svg 19.19px in `rgb(161, 161, 170)` with `fill: none` and `stroke` the same gray, against legacy's 20x20 in `rgb(26, 32, 44)`, `fill` `rgb(26, 32, 44)`, `stroke: none`; the trigger's own color `rgb(9, 9, 11)`, v3's `fg`, against legacy's `rgb(26, 32, 44)` (the palette fix of ui `9642114`, `upstream`, rendering with this one under the override). After, under the `file:../ui` override (`accordion-after.json`): every trigger `center` at both widths; the wrapped question two lines at 375 with left edges 26.5 and 162.5 (290 and 18 wide: the second line is `it?`, the copy verdict's shorter question, where legacy's `my Grid Kit?` is 92 wide at 131.5), one line at 1280 starting at 280 as legacy's; fifteen indicators 20x20 in `rgb(26, 32, 44)`, path `fill` `rgb(26, 32, 44)`, `stroke: none`, the markup the filled path with `focusable="false"` and `aria-hidden="true"`; the opened item's indicator `rotate: 180deg` (legacy's `transform: matrix(-1, 0, 0, -1, 0, 0)`, `rotate(-180deg)`, the same half turn). Unchanged from the sibling's: `display: flex`, `justify-content: space-between`, padding `16px 8px 16px 8px` (the padding fix of `540e9c3` rendering with this one), the hover fill `rgba(0, 0, 0, 0.04)` (`blackAlpha.50`) and the keyboard-focus shadow `0 0 0 2px` in `rgba(0, 163, 196, 0.5)` (the `outline` token) on both sides (`accordion-states-{after,legacy}.json`; legacy's `outline-style: solid` beside it is Chakra v2's `2px solid transparent` reset, invisible); no one-line question moved. The browser console is clean on the current side. The screenshot pairs for `/faq` at 375, 768 and 1280 (6 of 6) and section crops with the first item opened (`acc-crops/{current,legacy}-{375,1280}.png`), looked at: the two wrapped questions at 375 read centered as in `legacy.png`, the chevrons dark and filled at every width; every remaining difference on the pairs is ledgered: the questions and sections themselves (the faq record's copy verdicts and sanctioned removals), the heading at 18px at weight 600 (`77cd1426ac65`, `7124898563eb`), the container padding (`5c1af396cc2e`), the inline links (`12a2882546e6`), the separator and hover fill (`cb9dce13acba`, `cb528cbc05e6`), the palette items, the `Title` container (`318456ddabc6`), all `upstream` and all rendering fixed under the override; at 768 the footer (`c9ea22823a23`) and the wordmark (`1d2562d6b3bc`), the operator's; legacy's cookie banner, dropped startup plumbing (`2032533f`); and the footer credit shown twice on the current side, the override rendering the package's brand-footer credit beside the site's own `FooterSlogan` until the bump's `SiteFooter` edit (the bump plan's note from the footer slice `1977c9af920c`), absent on the published package. In `../ui`: `pnpm lint`, `pnpm types`, `pnpm build:pkg` (the built recipe reads `textAlign: "center"` on the trigger and the `itemIndicator` slot at `1.25em`; `dist/components/Accordion.js` carries the filled path and no `'use client'` banner; the `.d.ts` the annotated type) and `pnpm build:storybook` green, twice (again after the review's comment edits, which change no emitted code). The override and the `transpilePackages` entry reverted by `git restore -- package.json pnpm-lock.yaml next.config.ts` and `pnpm install --frozen-lockfile`, the dev server stopped by pid first (its `next-server` child survives the parent's kill and is found on port 3000 by `ss -ltnp` and killed by its own pid); `timeout 900 just check` green against the published 1.2.0 (78 tests, exit 0).

Reviews on three fresh Opus sub-agents (Standards, Spec, Parity) before the sibling commit, none critical or major. Standards, no hard violation; four judgment calls read and left as they are: the namespace built as an explicit ten-member object rather than a spread (the `Tabs` house shape, and the annotated type makes the compiler check every member), the public export last because the object literal needs the wrapper defined first (`Tabs.tsx` the same), the doc comments' register, and the CHANGELOG entry extended rather than split. Spec, every upstream citation verified (the refusal at `:5399`, the `forwardRef` branch at `:1796-1803`, `factory.js:114`, `preset-base.js:172`, the v2 chunks, `a5cbe36`); one false reason in the draft Outcome corrected above: v3's `Icon` recipe writes no size, so `Icon` was a working option that is not simpler, not a broken one; the plain svg's `flex-shrink: 1` recorded. Parity: the accordion at parity at 375, 768 and 1280 by its own probe (the svg 20x20, 8px from the trigger's right edge, 18px from the top of a 56px row on both sides, unmoved by the rotation); no copy in the diff; the two v2 behaviors above taken as the `Note(cc)` rather than items, since no route or consumer renders a disabled item or sets `reduceMotion`; the doubled footer credit, which it attributed to `c9ea22823a23`, explained above. Findings dropped: none.

Items: [[c7ffae735176]] and [[2cc5b00fb582]] moved to `upstream` with the ui commit in their notes; `99f2fe62c62f` `blocked_by` this slice (the edge from the mint, checked) and carrying the mint's note, extended here with what the bump's repeat probe reads on the published package. No visitor-facing copy in the change. Beyond the ask: none.

## Log
